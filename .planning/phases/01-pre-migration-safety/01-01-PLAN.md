---
phase: 01-pre-migration-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sync.sh
autonomous: true
requirements:
  - PMS-05
  - PMS-06
must_haves:
  truths:
    - "Running `./scripts/sync.sh --wargov-only` (or any path that reaches the war.gov stage) does not fail with `python3: can't open file '.../download.py'`."
    - "CLAUDE.md §5.1 reflects the actual remote `hectorchanht/gov-ufo-archive`; the historical local folder name `war-gov-ufo-release` is noted as historical only."
    - "A reviewer can `grep` CLAUDE.md and find zero stale `hectorchanht/war-gov-ufo-release` GitHub URLs."
  artifacts:
    - path: scripts/sync.sh
      provides: "Working war.gov download invocation"
      contains: "scripts/download-war.gov.py"
    - path: CLAUDE.md
      provides: "Correct §5.1 release-URL pattern referencing gov-ufo-archive"
      contains: "hectorchanht/gov-ufo-archive"
  key_links:
    - from: scripts/sync.sh
      to: scripts/download-war.gov.py
      via: "python3 invocation in the war.gov branch"
      pattern: "scripts/download-war\\.gov\\.py"
---

<objective>
Close the two latent FIX bugs that Phase 1 inherits from `.planning/codebase/CONCERNS.md`:

1. PMS-05 — `scripts/sync.sh:144` invokes `$ROOT/download.py`, which does not exist. The correct target is `scripts/download-war.gov.py`. The wargov download stage is broken at the entrypoint and must be made invokable on a fresh clone.
2. PMS-06 — Per `.planning/phases/01-pre-migration-safety/01-CONTEXT.md` decision D-14.2, `CLAUDE.md §5.1` was supposed to be patched to point at `hectorchanht/gov-ufo-archive` (the canonical remote) and away from the historical `hectorchanht/war-gov-ufo-release`. The patch appears to have already landed during the discuss-phase — this plan verifies the current state and locks it down with a grep-based contract.

Purpose: Both fixes unblock downstream phases. The broken `sync.sh:144` path means PMS-01 (URL snapshot) cannot validate against a freshly synced site; the CLAUDE.md drift means new docs in §13 (SSG migration in progress) would reference the wrong remote.

Output: A one-line edit to `scripts/sync.sh` and a verification grep that CLAUDE.md §5.1 is correct, both committed to `main`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-pre-migration-safety/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix scripts/sync.sh:144 broken download.py path</name>
  <files>scripts/sync.sh</files>
  <read_first>
    - scripts/sync.sh (lines 130-160 — the war.gov branch where `$ROOT/download.py` is invoked)
    - scripts/download-war.gov.py (confirm this is the correct target file; it exists at `scripts/download-war.gov.py`)
    - .planning/codebase/CONCERNS.md §"`download.py` Path Mismatch in `sync.sh`" (lines 132-140) — documents the bug and the fix
    - .planning/phases/01-pre-migration-safety/01-CONTEXT.md D-16 — confirms the one-line edit; planner verified target is `scripts/download-war.gov.py`
  </read_first>
  <action>
    Open `scripts/sync.sh` and locate the war.gov download branch around line 144 — currently reads `python3 "$ROOT/download.py"`. Change the path to `python3 "$ROOT/scripts/download-war.gov.py"`. Do NOT change anything else in the script — no reformatting, no shellcheck cleanup, no flag additions. The intent is a minimal, reviewable one-line fix per D-16. Preserve the surrounding `if ! python3 -c "import curl_cffi" 2>/dev/null` guard and the warning prose verbatim.

    The repo root variable `$ROOT` is already defined at the top of sync.sh; the fix relies on it, not on cwd assumptions.
  </action>
  <verify>
    <automated>grep -n 'scripts/download-war\.gov\.py' scripts/sync.sh | grep -v '^#' && ! grep -nE 'python3 +"\$ROOT/download\.py"' scripts/sync.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'scripts/download-war\.gov\.py' scripts/sync.sh` returns at least 1.
    - `grep -nE 'python3 +"\$ROOT/download\.py"' scripts/sync.sh` returns no matches (exit 1).
    - `bash -n scripts/sync.sh` passes (no syntax regressions from the edit).
    - `git diff scripts/sync.sh` shows exactly one line changed.
  </acceptance_criteria>
  <done>The war.gov download invocation in scripts/sync.sh resolves to a file that exists. A fresh-clone `./scripts/sync.sh --wargov-only` reaches the curl_cffi import-check guard without the `can't open file` error.</done>
</task>

<task type="auto">
  <name>Task 2: Verify CLAUDE.md §5.1 already reflects gov-ufo-archive remote (PMS-06)</name>
  <files>(none — verification only)</files>
  <read_first>
    - CLAUDE.md §5.1 (around line 236, the "URL pattern" line)
    - .planning/phases/01-pre-migration-safety/01-CONTEXT.md D-14.2 — declares this fix was applied during discuss-phase
    - .planning/codebase/CONCERNS.md §"CLAUDE.md Spec vs Reality Drift" — original drift documentation
  </read_first>
  <action>
    This is a verification-only task closing PMS-06 per D-14.2. The CLAUDE.md §5.1 fix was applied during the discuss-phase. Confirm:

    1. CLAUDE.md contains the substring `hectorchanht/gov-ufo-archive` (the canonical remote per .planning/STATE.md Project Reference).
    2. CLAUDE.md contains a parenthetical noting `war-gov-ufo-release` is the historical local folder name only.
    3. CLAUDE.md does NOT contain any GitHub release URL of the form `hectorchanht/war-gov-ufo-release/releases/download/`. (The phrase `war-gov-ufo-release` may appear in the historical note context, but never as part of a live `github.com/hectorchanht/war-gov-ufo-release/...` URL.)

    If all three pass, no file edit is required — write the result to the plan summary. If any fail, edit CLAUDE.md §5.1 minimally to restore the contract described in D-14.2 (one URL pattern line + the historical-name parenthetical) and do not touch any other section.

    Do NOT regenerate CLAUDE.md via `gsd-sdk query generate-claude-md` — D-13 explicitly rejects that. Per D-15, `.planning/PROJECT.md` is the higher-precedence source for any decision that contradicts a CLAUDE.md section — this verification gates only the §5.1 URL pattern surface, not the meta-precedence question.
  </action>
  <verify>
    <automated>grep -c 'hectorchanht/gov-ufo-archive' CLAUDE.md | awk '$1 >= 1' && ! grep -nE 'github\.com/hectorchanht/war-gov-ufo-release/releases/download/' CLAUDE.md && grep -c 'war-gov-ufo-release.*historical\|historical.*war-gov-ufo-release\|local folder name `war-gov-ufo-release`' CLAUDE.md | awk '$1 >= 1'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'hectorchanht/gov-ufo-archive' CLAUDE.md` returns at least 1.
    - `grep -nE 'github\.com/hectorchanht/war-gov-ufo-release/releases/download/' CLAUDE.md` returns no matches.
    - CLAUDE.md mentions `war-gov-ufo-release` only in a historical-context phrase (e.g. "local folder name `war-gov-ufo-release` is historical").
  </acceptance_criteria>
  <done>CLAUDE.md §5.1 release-URL pattern is locked to `hectorchanht/gov-ufo-archive` with a single historical note about the local folder name. PMS-06 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→sync.sh | Build-time only; no user-supplied input crosses this boundary. |
| developer→CLAUDE.md | Documentation file; no executable surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | scripts/sync.sh edit | accept | One-line edit reviewed in git diff; `bash -n` syntax check in verification. No external input; build-time only. |
| T-01-02 | Information Disclosure | CLAUDE.md remote URL | accept | The remote URL is already public (live `realufo.org` serves the same GitHub-hosted assets); no secret exposure. |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |
</threat_model>

<verification>
- `grep -c 'scripts/download-war\.gov\.py' scripts/sync.sh` ≥ 1
- `bash -n scripts/sync.sh` passes
- `grep -c 'hectorchanht/gov-ufo-archive' CLAUDE.md` ≥ 1
- `grep -nE 'github\.com/hectorchanht/war-gov-ufo-release/releases/download/' CLAUDE.md` empty
</verification>

<success_criteria>
1. `scripts/sync.sh:144` (or thereabouts after the edit) invokes `scripts/download-war.gov.py`, a file that exists.
2. CLAUDE.md §5.1 URL pattern references `hectorchanht/gov-ufo-archive`; historical note about `war-gov-ufo-release` is present exactly once and only in historical context.
3. Git diff of the entire plan touches only `scripts/sync.sh` (Task 1). Task 2 is verification-only; if it triggers an edit, that edit is scoped to §5.1 alone.
</success_criteria>

<output>
Create `.planning/phases/01-pre-migration-safety/01-01-SUMMARY.md` when done — summarize the one-line fix, the §5.1 verification result, and any deltas to the assumption that CLAUDE.md was already patched.
</output>

---
phase: 03-ssg-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - tsconfig.json
  - package.json
  - .gitignore
  - .planning/decisions/astro-version-pin.md
autonomous: true
requirements: [SSG-01]

must_haves:
  truths:
    - "Astro 5.18.x is pinned as `~5.18.0` in package.json devDependencies (NOT `^5`, NOT `5.x`) per D-29"
    - "`@astrojs/cloudflare` adapter is installed at the compatible 5.x version range per D-30"
    - "`pnpm install --frozen-lockfile && pnpm build` succeeds locally and emits `dist/index.html` (placeholder index page in this plan; replaced in Plan 03-05) per D-31..D-33"
    - "`dist/` is gitignored per D-15 (verified — Phase 2 02-01 already added it; this plan asserts it persists)"
    - "ADR `.planning/decisions/astro-version-pin.md` documents the chosen Astro + adapter versions, re-verifies astro#15684 fix status (closed via PR #15711), and records the defensive `~5.18.0` choice"
    - "Phase 2 CI gates (quality-gates.yml) continue to trigger and pass — no regression to _headers, _redirects, baselines, fidelity, tone-colours, js-off (predicted FAIL for hydrating archives per 02-08-SUMMARY remains the expected signal, not a regression introduced by this plan)"
    - "Python build scripts under `scripts/build-*.py` are NOT touched — coexistence invariant per D-12..D-14 + D-39"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Astro 5.18 root config — output: 'static', adapter: @astrojs/cloudflare, markdown.smartypants: false (D-28), markdown.remarkPlugins: [], integrations: []"
      contains: "import.*@astrojs/cloudflare"
    - path: "tsconfig.json"
      provides: "TypeScript strict config extending astro/tsconfigs/strict"
      contains: "astro/tsconfigs/strict"
    - path: "package.json"
      provides: "Extended with astro@~5.18.0 + @astrojs/cloudflare + zod@^3 + papaparse + scripts.dev/build/preview/prebuild (placeholder echo until 03-03 wires CSV normaliser)"
      contains: "\"astro\": \"~5.18"
    - path: ".planning/decisions/astro-version-pin.md"
      provides: "ADR documenting Astro version + adapter version + astro#15684 re-verification + Phase 4 upgrade window discussion"
      contains: "astro#15684"
  key_links:
    - from: "package.json"
      to: ".nvmrc + engines.node='>=22 <23'"
      via: "packageManager + engines pins preserved from Phase 2 02-01"
      pattern: "\"node\": \">=22 <23\""
    - from: "astro.config.mjs"
      to: "@astrojs/cloudflare adapter"
      via: "import + adapter() in config"
      pattern: "import cloudflare from '@astrojs/cloudflare'"
    - from: "package.json scripts.build"
      to: "Astro CLI"
      via: "pnpm build → astro build → dist/"
      pattern: "\"build\": \"astro build\""
---

<objective>
Scaffold Astro 5.18.x in the repo alongside Phase 2 Node skeleton, install the `@astrojs/cloudflare` adapter compatible with Astro 5, and commit an ADR documenting the defensive `~5.18.0` pin + adapter version + astro#15684 re-verification result. Add Zod + Papaparse as build dependencies for Plan 03-02 + 03-03.

Purpose: SSG-01 unlocks every subsequent Phase 3 plan. Defensive pin (`~5.18.0` not `^5`) prevents accidental Astro 6 upgrade per D-29 / research/PITFALLS.md. ADR captures the re-verification of astro#15684 (closed via PR #15711 upstream) so the next planner has a paper trail for tightening or relaxing the pin. Coexistence invariant (D-12..D-14) — Python build scripts untouched.

Output: `astro.config.mjs`, `tsconfig.json`, extended `package.json` + `pnpm-lock.yaml`, ADR at `.planning/decisions/astro-version-pin.md`. Phase 3 Plans 03-02..03-06 unblocked.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ssg-foundation/03-CONTEXT.md
@.planning/phases/02-infrastructure-ci-scaffolding/02-01-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/decisions/cf-pages-project.md
@package.json
@.gitignore
@CLAUDE.md

<interfaces>
<!-- Existing Phase 2 package.json (verbatim from 02-01-SUMMARY) — EXTEND, do not rewrite -->
<!-- {
       "name": "realufo",
       "private": true,
       "version": "0.0.0",
       "engines": { "node": ">=22 <23" },
       "packageManager": "pnpm@9.15.9",
       "scripts": {},
       "devDependencies": {
         "@lhci/cli": "0.14.0",
         "@playwright/test": "1.49.0",
         "playwright": "1.49.0"
       }
     } -->
<!-- Expected post-plan shape (script values + dep versions are illustrative — see <action> for exact values) -->
<!-- {
       ...,
       "scripts": {
         "dev": "astro dev",
         "build": "astro build",
         "preview": "astro preview",
         "prebuild": "echo 'prebuild placeholder — wired to scripts/normalize-csv.py in Plan 03-03'"
       },
       "dependencies": {
         "@astrojs/cloudflare": "<compatible-5.x-version>",
         "astro": "~5.18.0",
         "papaparse": "^5",
         "zod": "^3"
       },
       "devDependencies": {
         "@lhci/cli": "0.14.0",
         "@playwright/test": "1.49.0",
         "@types/papaparse": "^5",
         "playwright": "1.49.0",
         "typescript": "~5"
       }
     } -->

<!-- Astro 5 strict tsconfig extension target (Astro docs) -->
<!-- {
       "extends": "astro/tsconfigs/strict",
       "include": [".astro/types.d.ts", "**/*"],
       "exclude": ["dist"]
     } -->

<!-- astro.config.mjs target shape -->
<!-- import { defineConfig } from 'astro/config';
     import cloudflare from '@astrojs/cloudflare';
     export default defineConfig({
       output: 'static',           // D-12..D-15: CF Pages serves dist/
       adapter: cloudflare(),      // D-30 — adapter pinned alongside Astro
       markdown: {
         smartypants: false,       // D-28 BANNED for archive cards
         remarkPlugins: [],        // no typographer
         rehypePlugins: [],
       },
       integrations: [],           // no PWA, no UI framework — D-23
       site: 'https://realufo.org',
       trailingSlash: 'ignore',
     }); -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pnpm registry → local node_modules | Supply-chain ingress: every dep arrives over HTTPS from registry.npmjs.org via pnpm-lock.yaml |
| package.json → CF Pages build | CI runs `pnpm install --frozen-lockfile && pnpm build` server-side; package.json + pnpm-lock.yaml determine what executes |

## Package Legitimacy Audit (npm)

| Package | Source | Maintainer signal | Verdict |
|---------|--------|-------------------|---------|
| `astro` (~5.18.0) | npmjs.com/package/astro | Official `@withastro` org; weekly downloads >1M; pinned in research/STACK.md as recommended SSG | [LEGITIMATE] |
| `@astrojs/cloudflare` | npmjs.com/package/@astrojs/cloudflare | Official `@astrojs` scope; first-party adapter | [LEGITIMATE] |
| `zod` (^3) | npmjs.com/package/zod | colinhacks; >50M weekly downloads; research/STACK.md recommended | [LEGITIMATE] |
| `papaparse` (^5) | npmjs.com/package/papaparse | mholt; de-facto JS CSV parser; research/STACK.md recommended | [LEGITIMATE] |
| `@types/papaparse` | npmjs.com/package/@types/papaparse | DefinitelyTyped community | [LEGITIMATE] |
| `typescript` (~5) | npmjs.com/package/typescript | Microsoft official | [LEGITIMATE] |

All packages are LEGITIMATE per research/STACK.md headline recommendation. No `[ASSUMED]` or `[SUS]` packages — no human-verify checkpoint required for these installs.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `pnpm-lock.yaml` integrity | mitigate | `--frozen-lockfile` in CF Pages build (per D-31); lockfile committed; CI fails if lockfile drifts from package.json |
| T-03-02 | Tampering | Astro version drift via transitive `^5` semver creep | mitigate | Defensive pin `~5.18.0` (D-29) — only patch updates allowed; ADR records re-evaluation cadence |
| T-03-03 | Elevation of Privilege | Cloudflare adapter could expose Worker bindings to build code | accept | Adapter is first-party; `output: 'static'` means no runtime Worker code emitted in this phase (D-12) |
| T-03-SC | Tampering | npm installs (transitive deps) | mitigate | All direct deps audited above as [LEGITIMATE]; `pnpm-lock.yaml` integrity hashes verified by pnpm install |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — re-verify astro#15684 + draft astro-version-pin.md ADR</name>
  <files>.planning/decisions/astro-version-pin.md</files>
  <read_first>
    - `.planning/research/STACK.md` lines 1-200 — find the Astro section that calls out astro#15684 + recommends `~5.x` pin.
    - `.planning/research/PITFALLS.md` — find Pitfall #6 (markdown typographer drift) which intersects D-28.
    - `.planning/decisions/cf-pages-project.md` — ADR-style precedent (## Status / ## Project identity / ## Branch strategy / ## Build configuration / ## Phase N invariants honored / ## Future-phase hooks).
    - `.planning/decisions/dns-ttl.md` — second ADR precedent for structure.
    - https://github.com/withastro/astro/issues/15684 — re-fetch issue state. Confirm CLOSED (resolved via PR #15711). Record the fix's release version range in the ADR.
    - Current Astro 5.x latest version on npmjs.com/package/astro — use `pnpm view astro versions --json` (or `npm view astro versions --json`) and pick the latest tagged release in the 5.x.x line (currently 5.18.x per CONTEXT D-29).
  </read_first>
  <action>
    Write `.planning/decisions/astro-version-pin.md` as a new ADR matching the precedent of `.planning/decisions/cf-pages-project.md`. Sections (verbatim heading names):

    1. `## Status` — `accepted` with state-machine line `proposed → accepted (2026-05-25) → revisit at Phase 4 close`.
    2. `## Decision` — One sentence: pin `astro` to `~5.18.0` (tilde patch-only) and `@astrojs/cloudflare` to the compatible 5.x version range, deferring any move to Astro 6.x to Phase 4 close.
    3. `## Context` — astro#15684 (Cloudflare adapter prerender regression in Astro 6) WAS the original reason for the defensive pin. Re-verified during Phase 3 kickoff: issue is CLOSED upstream, resolved via PR #15711. The fix exists in Astro 6.x but at the time of Phase 3 kickoff the broader 6.x line still introduces unrelated breaking changes (Zod 4 jump, content layer API shifts). Solo maintainer + brownfield migration risk profile says: keep the defensive pin; revisit at Phase 4 close once wargov is stable and the 14-archive port hasn't surfaced framework-level issues.
    4. `## Alternatives considered` — three options with one-paragraph each:
       (a) `astro@^5` (caret) — REJECTED: invites accidental 5.x → 5.99.x drift if Astro renumbers, weak guard against the original astro#15684 class of regression.
       (b) `astro@~5.x` (latest tilde) — REJECTED for now: same risk-class as (a) until execution-time `pnpm view astro versions` confirms what "latest tilde" resolves to.
       (c) `astro@~6.x.y` (move now) — REJECTED: Astro 6 also bumped Zod 3 → Zod 4 (per research/STACK.md), invalidates any Zod 3-coded Plan 03-02 schema work; defers complexity to a single Phase 4 close transition instead of compounding it in Phase 3.
    5. `## Implementation` — Exact package.json entries:
       - `"astro": "~5.18.0"` (replace 5.18.0 with the actual latest 5.18.x patch when `pnpm view astro versions --json | grep 5.18` is run; if no 5.18.x exists at execution time, pick the highest 5.18.x available OR fall back to `~5.17.0` and document the patch chosen).
       - `"@astrojs/cloudflare": "<version>"` (use `pnpm view @astrojs/cloudflare versions --json` to find the latest compatible with Astro 5; per their changelog, this is typically the v11.x or v12.x line as of mid-2026).
       - Both installed via `pnpm add astro@~5.18.0 @astrojs/cloudflare@<v>` (NOT `--save-exact`; the tilde IS the pin).
    6. `## Phase 3 invariants honored` — explicit list:
       - D-29 (tilde pin, NOT caret)
       - D-30 (adapter pinned alongside; documented in this ADR)
       - D-31 (CF Pages build command unchanged: `pnpm install --frozen-lockfile && pnpm prebuild && pnpm build`)
       - D-37 (no DNS swap)
       - D-39 (no Python build script deletion)
    7. `## Future-phase hooks` — Phase 4 close decision: revisit moving to Astro 6.x once 14-archive port + Pagefind + injectManifest SW have shipped; document migration cost (Zod 3→4 schema rewrites; content layer API check) at that point.
    8. `## References` — Links to astro#15684, PR #15711, research/STACK.md, research/PITFALLS.md §Pitfall #6.

    Word count: aim for 600-1200 words (the precedent ADRs are 1500-2500 words; this is more focused so shorter is fine). NO fenced JSON blobs for package.json — describe in prose. NO speculation about Astro 7.x.
  </action>
  <verify>
    <automated>
      test -f .planning/decisions/astro-version-pin.md \
        && grep -q '^## Status' .planning/decisions/astro-version-pin.md \
        && grep -q '^## Decision' .planning/decisions/astro-version-pin.md \
        && grep -q '^## Context' .planning/decisions/astro-version-pin.md \
        && grep -q '^## Alternatives considered' .planning/decisions/astro-version-pin.md \
        && grep -q '^## Implementation' .planning/decisions/astro-version-pin.md \
        && grep -q '^## Phase 3 invariants honored' .planning/decisions/astro-version-pin.md \
        && grep -q '^## Future-phase hooks' .planning/decisions/astro-version-pin.md \
        && grep -q '^## References' .planning/decisions/astro-version-pin.md \
        && grep -q '15684' .planning/decisions/astro-version-pin.md \
        && grep -q 'D-29' .planning/decisions/astro-version-pin.md \
        && grep -q 'D-30' .planning/decisions/astro-version-pin.md \
        && grep -qE '~5\.[0-9]+\.[0-9]+' .planning/decisions/astro-version-pin.md
    </automated>
  </verify>
  <done>
    `.planning/decisions/astro-version-pin.md` exists, has all 8 sections, cites astro#15684 + PR #15711, documents the chosen `~5.18.x` pin (or the actual chosen patch version), references D-29 / D-30, and identifies the Phase 4 close revisit point.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install Astro 5.18.x + @astrojs/cloudflare + zod + papaparse + write astro.config.mjs + tsconfig.json</name>
  <files>astro.config.mjs, tsconfig.json, package.json, .gitignore</files>
  <read_first>
    - `package.json` (current Phase 2 shape — 11 lines, 3 devDeps already pinned exact per CONVENTIONS.md anti-drift posture from 02-03-SUMMARY).
    - `.gitignore` — confirm `dist/` is already listed (Phase 2 02-01 SUMMARY says it is; this task asserts).
    - `.planning/decisions/astro-version-pin.md` from Task 1 — read the chosen exact pin string.
    - `.planning/research/STACK.md` §"Installation" — pnpm command examples.
    - Astro docs `https://docs.astro.build/en/guides/integrations-guide/cloudflare/` — adapter installation incantation + supported version matrix.
    - `.planning/codebase/CONVENTIONS.md` — anti-drift exact-pin posture for dev deps (Playwright + LHCI are exact-pinned; runtime Astro is tilde-pinned per D-29 because Astro's own patch releases are the value we want to track).
  </read_first>
  <action>
    Step 1 — Run `pnpm view astro versions --json | python3 -c "import json,sys; vs=json.load(sys.stdin); print(max(v for v in vs if v.startswith('5.18.')))"` (or `pnpm view astro versions --json` and manually pick the highest `5.18.x`). Capture this version string as `$ASTRO_VER`. If no 5.18.x exists at execution time, pick the highest 5.17.x and update the ADR from Task 1 to reflect the actual patch line chosen.

    Step 2 — Run `pnpm view @astrojs/cloudflare versions --json` and pick the latest version whose changelog/peerDeps list `astro@^5`. Capture as `$CF_VER`. Compatible range is typically v11.x or v12.x as of mid-2026 (verify against the package's peerDependencies field at install time).

    Step 3 — Install with tilde pin for Astro, plain caret/version for adapter:
    `pnpm add astro@~$ASTRO_VER @astrojs/cloudflare@$CF_VER zod@^3 papaparse@^5`
    `pnpm add -D @types/papaparse@^5 typescript@~5`

    DO NOT use `--save-exact` for `astro` (the tilde is the pin; D-29). DO use the patch version that pnpm view resolves for the EXACT first patch (the tilde lives in package.json; pnpm-lock.yaml records the resolved patch).

    Step 4 — Write `astro.config.mjs` with the interfaces block target shape from <context>. Required keys:
      - `output: 'static'` (D-12..D-15 — CF Pages serves dist/ as static files; no SSR in Phase 3)
      - `adapter: cloudflare()` (D-30)
      - `markdown.smartypants: false` (D-28 BAN — defends Pitfall #6 typographer drift)
      - `markdown.remarkPlugins: []`
      - `markdown.rehypePlugins: []`
      - `integrations: []` (D-23 — no React/Vue/Svelte; no @vite-pwa/astro until Phase 4 SW-01)
      - `site: 'https://realufo.org'`
      - `trailingSlash: 'ignore'` (matches current GH Pages behaviour; _redirects already handles trailing-slash 301s per Phase 2 02-05)

    Step 5 — Write `tsconfig.json` extending `astro/tsconfigs/strict`. Required keys: `extends`, `include: [".astro/types.d.ts", "**/*"]`, `exclude: ["dist"]`. NO `compilerOptions` overrides — strict preset is the contract.

    Step 6 — Edit `package.json` to add scripts. Required script entries (preserve existing scripts unchanged — there are none yet, but be defensive):
      - `"dev": "astro dev"`
      - `"build": "astro build"`
      - `"preview": "astro preview"`
      - `"prebuild": "echo 'prebuild placeholder — Plan 03-03 wires scripts/normalize-csv.py'"` — placeholder; Plan 03-03 replaces with the actual Python invocation.

    Step 7 — Verify `.gitignore` already has `dist/` (Phase 2 02-01 added it). If somehow absent, append (in the SSG-migration fenced section).

    Step 8 — Run `pnpm install --frozen-lockfile` (validates the lockfile matches package.json). Then `pnpm build`. Expect: `dist/` directory created. Because no `src/pages/` exists yet (added in Plan 03-04 + 03-05), Astro will emit zero pages but exit 0 — verify exit code is 0 and `dist/` directory exists.

    Step 9 — Smoke-curl: `pnpm preview &` then `curl -sI http://localhost:4321/` should return 200 or 404. Kill preview.

    NO Python scripts touched. NO src/ files created (deferred to Plan 03-02 + 03-04 + 03-05). NO _headers / _redirects edits.
  </action>
  <verify>
    <automated>
      test -f astro.config.mjs \
        && test -f tsconfig.json \
        && grep -q "import cloudflare from '@astrojs/cloudflare'" astro.config.mjs \
        && grep -qE "output:[[:space:]]*'static'" astro.config.mjs \
        && grep -qE "smartypants:[[:space:]]*false" astro.config.mjs \
        && grep -qE "adapter:[[:space:]]*cloudflare\(\)" astro.config.mjs \
        && grep -q "\"astro/tsconfigs/strict\"" tsconfig.json \
        && python3 -c "import json; d=json.load(open('package.json')); assert d['dependencies']['astro'].startswith('~5.'), 'astro must be tilde-pinned per D-29'; assert d['dependencies']['astro'][0] == '~', 'astro pin must START with tilde'; assert '@astrojs/cloudflare' in d['dependencies']; assert 'zod' in d['dependencies']; assert 'papaparse' in d['dependencies']; assert d['scripts']['build'] == 'astro build'; assert 'prebuild' in d['scripts']" \
        && grep -q 'dist/' .gitignore \
        && pnpm install --frozen-lockfile \
        && pnpm build \
        && test -d dist
    </automated>
  </verify>
  <done>
    `pnpm install --frozen-lockfile && pnpm build` exits 0 against a clean checkout; `astro.config.mjs` declares static output + cloudflare adapter + smartypants:false; `tsconfig.json` extends strict; `package.json` pins `astro` to `~5.18.x` (or actual chosen 5.x.y) with tilde; `dist/` is created (empty pages OK — wargov index.astro comes in Plan 03-05); `dist/` remains gitignored.
  </done>
</task>

</tasks>

<verification>
- `astro.config.mjs` parses (verified by `pnpm build` exit 0).
- `package.json` `astro` entry matches regex `^~5\.[0-9]+\.[0-9]+$` (tilde + patch, NOT caret, NOT bare).
- `.planning/decisions/astro-version-pin.md` contains all 8 required sections + cites astro#15684 + PR #15711 + D-29 + D-30.
- `dist/` is gitignored AND created on build (proves Phase 2 invariant + Phase 3 build wiring).
- `scripts/build-*.py` files are unchanged (`git diff --stat scripts/` shows no Python script touched per D-39).
- `_headers`, `_redirects`, `tests/`, `.github/workflows/` are unchanged (Phase 2 outputs preserved).
- `pnpm-lock.yaml` is committed and matches package.json (`pnpm install --frozen-lockfile` exits 0).
</verification>

<success_criteria>
- SSG-01 satisfied: Astro 5.18.x scaffold exists alongside Python build scripts (both build trees coexist on `ssg-migration`).
- D-29..D-31 honored: tilde pin, adapter compatible with 5.x, CF Pages build command structure preserved.
- D-15 honored: `dist/` gitignored, `_headers`/`_redirects` not gitignored.
- D-39 honored: zero Python scripts modified.
- ADR at `.planning/decisions/astro-version-pin.md` documents the choice + re-verification of astro#15684 closure.
- Plans 03-02..03-06 unblocked: src/ can now be created against Astro 5 conventions; Zod schema can import from `astro:content` and `zod`.
</success_criteria>

<output>
Create `.planning/phases/03-ssg-foundation/03-01-SUMMARY.md` capturing: actual `$ASTRO_VER` chosen (e.g. `5.18.3`), `$CF_VER` chosen (e.g. `12.0.1`), `pnpm install` resolution time, `pnpm build` duration on empty-pages tree, and any deviation from the ADR's planned versions.
</output>

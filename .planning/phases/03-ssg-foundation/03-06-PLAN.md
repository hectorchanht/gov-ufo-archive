---
phase: 03-ssg-foundation
plan: 06
type: execute
wave: 4
depends_on: [03-05]
files_modified:
  - .planning/phases/03-ssg-foundation/03-06-SUMMARY.md
autonomous: false
requirements: [SSG-05]

must_haves:
  truths:
    - "wargov port (Plan 03-05 `dist/index.html`) passes ALL 7 jobs in `.github/workflows/quality-gates.yml` on the CF Pages preview URL per D-35 + D-36"
    - "Visual regression: 4 viewports (360 / 768 / 1024 / 1440) against `tests/visual-baselines/wargov-*.png` — pixel-diff ≤ 0.001 per D-35 + Phase 2 INF-04"
    - "Fidelity: 115/115 samples in `tests/fidelity-samples.json` match byte-equivalent per D-26..D-27 + Phase 2 INF-05"
    - "Tone colour: wargov `--caution` resolves to `#d4a017` per CLAUDE.md §3.1 + Phase 2 INF-06 + Plan 03-04 TONE map"
    - "JS-off rendering: wargov row in `tests/js-off.spec.ts` flips FAIL → PASS per D-25 + Phase 2 INF-07 + Plan 03-05 pre-rendered 50 cards"
    - "Redirects + URL contract: every URL-CONTRACT.txt entry under `/` and `#card-*` returns 200 from the preview origin per D-40 + Phase 1 PMS-01 + Phase 2 INF-02 (`scripts/verify-redirects.sh`)"
    - "Lighthouse mobile budgets: LCP ≤ 2.5s + transfer ≤ 500KB per D-35 + Phase 2 INF-08 (SOFT gate per D-28 — warnings acceptable, hard-fail at Phase 4 close)"
    - "Phase 1 + Phase 2 invariants preserved — Python build scripts unchanged, CSV unchanged, `_headers` unchanged, no DNS swap, SW kill-switch invariant intact (`updateViaCache: 'none'` on registration)"
    - "If any HARD gate (visual / fidelity / tone / js-off / redirects) fails, the FIX path is template-correction in src/ NOT baseline regeneration per D-17 (operator-only baselines invariant — Phase 2 02-03 + 02-04)"
  artifacts:
    - path: ".planning/phases/03-ssg-foundation/03-06-SUMMARY.md"
      provides: "Validation record — preview URL evaluated, all 7 job statuses with screenshots/excerpts, any template fixes required to reach green, sign-off statement for SSG-05 completion"
      contains: "quality-gates.yml"
      min_lines: 80
  key_links:
    - from: "ssg-migration branch HEAD"
      to: "CF Pages preview URL"
      via: "Cloudflare Pages GitHub App git-integration → deployment_status event"
      pattern: "<sha>.realufo.pages.dev"
    - from: "quality-gates.yml"
      to: "preview URL"
      via: "deployment_status trigger → preflight job → 6 data jobs read preflight.outputs.preview_url"
      pattern: "deployment_status"
---

<objective>
Validate the Plan 03-05 wargov port against the full Phase 2 quality-gates.yml CI matrix on a real CF Pages preview URL. This is the GATE for SSG-05 / Phase 3 close — the wargov archive must pass every Phase 2 invariant (visual regression at 4 viewports, fidelity byte-match, tone colour, JS-off rendering, redirects, Lighthouse soft budget) before Phase 4 starts the 14-archive port.

Purpose: ROADMAP §Phase 3 SC#5 + D-35..D-36 hang on this — "wargov port passes all Phase 2 gates." A green CI run is the objective signal Phase 3 has succeeded; a red CI run identifies WHICH template change in `src/` needs correction. Baseline regeneration is FORBIDDEN by D-17 (operator-only invariant — fix the template, not the baseline).

Output: `.planning/phases/03-ssg-foundation/03-06-SUMMARY.md` — the validation record. SSG-05 sign-off. If a fix-loop is required, the fixes happen via amendments to Plan 03-05's `src/pages/index.astro` / components / CSS — this plan is the orchestrator + recorder, not the implementer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ssg-foundation/03-CONTEXT.md
@.planning/phases/03-ssg-foundation/03-05-PLAN.md
@.planning/phases/02-infrastructure-ci-scaffolding/02-08-SUMMARY.md
@.planning/phases/02-infrastructure-ci-scaffolding/02-03-SUMMARY.md
@.planning/phases/02-infrastructure-ci-scaffolding/02-04-SUMMARY.md
@.planning/phases/02-infrastructure-ci-scaffolding/02-06-SUMMARY.md
@.planning/phases/02-infrastructure-ci-scaffolding/02-07-SUMMARY.md
@.planning/decisions/cf-pages-project.md
@.github/workflows/quality-gates.yml
@scripts/verify-redirects.sh
@scripts/verify-fidelity.py
@scripts/verify-lighthouse-budgets.py
@tests/visual-baselines/README.md
@tests/playwright.config.ts
@tests/visual-regression.spec.ts
@tests/fidelity-samples.json
@tests/tone-colours.spec.ts
@tests/js-off.spec.ts
@URL-CONTRACT.txt
@CLAUDE.md

<interfaces>
<!-- Phase 2 quality-gates.yml (from 02-08-SUMMARY) — 7 jobs:
       1. preflight (coordinator, reads deployment_status.environment_url)
       2. visual-regression (hard-fail; 60 PNG baselines × 0.1% pixel diff)
       3. fidelity (hard-fail; 115 samples byte-equivalent)
       4. tone-colours (hard-fail; per-archive --caution + seal gradient)
       5. js-off (hard-fail; ≥1 card + ≥1 heading per archive without JS)
       6. lighthouse (SOFT per D-28; LCP + transfer-byte budgets)
       7. redirects (hard-fail; verify-redirects.sh curl harness against URL-CONTRACT.txt)
-->

<!-- CF Pages preview URL pattern (from cf-pages-project.md):
       Per-commit:  https://<commit-sha>.realufo.pages.dev/
       Production:  https://realufo.pages.dev/ (production-branch = ssg-migration; Pages calls this "Production")
       Per-PR:      https://<branch-name>.realufo.pages.dev/ (if git-integration is connected)

     OPEN QUESTION from cf-pages-project.md: git-integration may still be pending operator UI click.
     If not connected at Plan 03-06 execution time, the preview URL only updates via Direct Upload
     (`wrangler pages deploy dist --project-name=realufo --branch=ssg-migration` from the operator's
     machine with the API token at /tmp/cf-realufo-token). Document the state at execution time
     in 03-06-SUMMARY.md.
-->

<!-- D-17 Operator-only baseline invariant (from Phase 2 02-03-SUMMARY + tests/visual-baselines/README.md):
       Baselines under tests/visual-baselines/ are FROZEN.
       If visual regression fails, the FIX is:
         (1) FIRST CHOICE — adjust src/pages/index.astro / Card.astro / wargov.css to produce
             pixel-equivalent output to the baseline.
         (2) LAST CHOICE — operator runs scripts/capture-baselines.py against the new SSG output
             ONLY if they accept that the visual contract is intentionally changing (e.g. a CSS
             improvement that supersedes the legacy visual). This is a CONSCIOUS decision logged
             in 03-06-SUMMARY.md, NOT a CI-auto-recapture loop.
       Same rule applies to:
         - tests/fidelity-samples.json (operator-only via scripts/extract-fidelity-samples.py)
         - tests/tone-colours-fixture.json (operator-only)
-->

<!-- Phase 2 D-25 + B3 invariant: js-off has NO continue-on-error in quality-gates.yml.
     Phase 2/3 wargov-only window: js-off will PASS for wargov (Plan 03-05 pre-rendered 50 cards)
     and FAIL for the other 14 archives that still render via legacy Python build (current main
     hydrating cards client-side per CLAUDE.md §7 invariants — pre-SSG).
     Those 14 FAILs are the EXPECTED SIGNAL of Phase 3+ port work pending; NOT a gate to soften.
     The js-off job currently runs against `realufo.pages.dev` which serves the PYTHON-BUILT
     output from ssg-migration (because CF Pages build command in Phase 2 was empty — Python
     output committed to ssg-migration was the build product).
     IN PHASE 3: after Plan 03-01 wires `pnpm build` as the CF Pages build command (per D-31),
     dist/ becomes the served product — Python output IS NO LONGER SERVED from ssg-migration.
     So js-off against the post-Plan-03-05 preview will:
       - wargov row PASS (dist/index.html pre-rendered)
       - other 14 rows FAIL because Plan 03-05 doesn't ship src/pages/[archive]/index.astro
         for them yet → CF Pages 404s these paths → js-off can't fetch them → test fails differently
     Plan 03-06 MUST surface this state correctly:
       - wargov passing is the SSG-05 success signal.
       - other 14 archives failing is Phase 4 SSG-06 runway — acceptable.
       - However, if the 14 archives 404 instead of fail-meaningfully, the js-off SPEC may report
         a different error mode than "no cards visible". DECISION: planner orchestration —
         either (a) Plan 03-05 includes a fallback that copies Python-built <slug>/index.html
         into dist/<slug>/ via a build hook (D-14 planner discretion) so the 14 archives serve
         the OLD content until Phase 4, OR (b) accept 404s for the 14 archives in Phase 3 close
         and surface in Plan 03-06 SUMMARY as a documented gap.
       OPTION (a) is recommended because it preserves URL-CONTRACT.txt without forcing Phase 4
       to deliver on day one. If Plan 03-05 didn't ship the build hook, decide here whether to
       (i) loop back to Plan 03-05 with a revision, OR (ii) accept the 404s as a Phase 4 entry
       condition. Document in 03-06-SUMMARY.md.
-->

<!-- Triggering the gates:
     Once a commit on ssg-migration produces a CF Pages deploy AND the GH App emits
     deployment_status with state=success + environment_url=<sha>.realufo.pages.dev,
     quality-gates.yml fires automatically. The orchestrator does not need to manually
     trigger anything — but a workflow_dispatch fallback is available
     (`gh workflow run quality-gates.yml -f preview_url=https://<sha>.realufo.pages.dev`)
     if the deployment_status path doesn't fire (e.g. git-integration still pending).
-->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CF Pages preview URL → quality-gates.yml jobs | Read-only inspection; jobs `curl` and Playwright-eval the preview |
| Operator-only baseline edit path | D-17 invariant — baselines are FROZEN unless operator consciously recaptures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-20 | Tampering | Auto-regeneration of `tests/visual-baselines/wargov-*.png` to make a failing visual test pass | mitigate | D-17 + 02-03-SUMMARY's README forbids; Plan 03-06 acceptance criteria EXPLICITLY require fix-in-template OR operator-conscious-recapture-with-rationale-in-SUMMARY |
| T-03-21 | Tampering | Soften js-off gate by adding `continue-on-error: true` to quality-gates.yml because 14 archives fail | mitigate | D-25 + B3 + 02-08-SUMMARY explicitly forbid; Plan 03-06 acceptance criteria check `! grep -A2 "name: js-off" .github/workflows/quality-gates.yml | grep continue-on-error` (the negation grep) |
| T-03-22 | Information Disclosure | Preview URL exposes a draft state to the public web | accept | All content is public-domain government doc archive (CLAUDE.md §9); preview is intended for testing by the maintainer + automated CI |
| T-03-23 | Tampering | URL-CONTRACT.txt update to make a redirects-gate failure pass | mitigate | URL-CONTRACT.txt is the snapshot from `main` at Phase 1 (PMS-01); modifying it = silently dropping the URL invariant. Plan 03-06 forbids; fix is in src/ slugify alignment with scripts/snapshot-urls.py |
| T-03-24 | DoS | A Plan 03-05 inadvertent giant inline JSON blob exceeds CF Pages 25 MiB/file limit | mitigate | Shard scheme (D-08..D-10) keeps `dist/index.html` ≤ ~500KB target (Lighthouse soft); CF Pages 25 MiB hard cap is unreachable |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Trigger preview deploy + record preview URL + run all 7 quality-gates.yml jobs</name>
  <files>(no files modified — read-only validation)</files>
  <read_first>
    - `.planning/decisions/cf-pages-project.md` §"Soft followup — operator finishes git-integration" — check if git-integration is connected at execution time. If not, fall back to Direct Upload via `wrangler pages deploy dist --project-name=realufo --branch=ssg-migration`.
    - `.github/workflows/quality-gates.yml` — the 7-job workflow. Note the manual trigger fallback (`workflow_dispatch` with `preview_url` input — verify the input is wired; if not, document and use `gh workflow run quality-gates.yml`).
    - `scripts/verify-redirects.sh` — confirms it accepts a preview URL argument and exits 0 only if every URL-CONTRACT.txt route returns 200.
    - `tests/visual-baselines/README.md` — D-17 operator-only invariant.
  </read_first>
  <what-built>
    Plan 03-05 has produced `dist/index.html` from Astro + the wargov data envelope. This validation step:
    1. Triggers a CF Pages preview deployment from the current `ssg-migration` HEAD (either via push or via Direct Upload).
    2. Captures the resulting preview URL (e.g. `https://abc123.realufo.pages.dev/`).
    3. Triggers `.github/workflows/quality-gates.yml` against that preview URL (via deployment_status event auto-fire OR manual `workflow_dispatch`).
    4. Records the status of each of the 7 jobs (preflight, visual-regression, fidelity, tone-colours, js-off, lighthouse, redirects).
  </what-built>
  <how-to-verify>
    Step 1 — Confirm CF Pages git-integration state:
    - Run `wrangler pages project list` and check if `realufo` row's Git-Provider column reads "GitHub" (connected) or "No" (not connected).
    - If "No": operator finishes the dashboard UI step per `.planning/decisions/cf-pages-project.md` §"Soft followup" — visit Cloudflare dashboard → Workers & Pages → realufo project → Settings → Builds & deployments → Connect a Git repository → install/configure GitHub App scoped to `hectorchanht/gov-ufo-archive`. OR fall back to Direct Upload (next bullet).
    - Fallback (Direct Upload):
      ```
      CLOUDFLARE_API_TOKEN=$(cat /tmp/cf-realufo-token) \
      CLOUDFLARE_ACCOUNT_ID=f1868a071996e836eae6da2b65f37929 \
      npx -y wrangler@latest pages deploy dist --project-name=realufo --branch=ssg-migration --commit-dirty=true
      ```
      Capture the resulting preview URL printed by wrangler.

    Step 2 — Confirm preview is live:
    ```
    curl -sI https://<preview-url>/ | head -1     # expect HTTP/2 200
    curl -sI https://<preview-url>/sw.js | grep -i 'cache-control'   # expect no-cache, no-store, must-revalidate (Phase 2 _headers)
    ```

    Step 3 — Trigger or wait for quality-gates.yml:
    - If git-integration connected: the workflow auto-fires on the deployment_status event; visit `https://github.com/hectorchanht/gov-ufo-archive/actions/workflows/quality-gates.yml` to watch the run.
    - If using Direct Upload: manual trigger via `gh workflow run quality-gates.yml -f preview_url=https://<preview-url>/` if the workflow exposes a `preview_url` input. If not, the workflow only fires on deployment_status — operator needs to add a temporary `workflow_dispatch.inputs.preview_url` block to `.github/workflows/quality-gates.yml`, manually trigger, then revert the temp change. Document the route taken.

    Step 4 — Record each of 7 job outcomes in `.planning/phases/03-ssg-foundation/03-06-SUMMARY.md`:
    | Job | Status (PASS / FAIL / SKIPPED) | Detail |
    |-----|-------------------------------|--------|
    | preflight | (expected PASS) | preview_url extracted = ... |
    | visual-regression | (target PASS) | 60 baseline tests; wargov × 4 viewports key; report any pixel-diff |
    | fidelity | (target PASS) | 115 samples; wargov subset must be byte-equivalent |
    | tone-colours | (target PASS) | wargov `--caution: #d4a017` + seal gradient asserted |
    | js-off | (target PASS for wargov; FAIL acceptable for other 14) | wargov pre-rendered 50 cards |
    | lighthouse | (target PASS; SOFT) | LCP + transfer warnings logged but not blocking |
    | redirects | (target PASS) | `verify-redirects.sh` runs URL-CONTRACT.txt's 95 canonical routes + 223 wargov anchors |

    Step 5 — For any HARD-gate FAIL (visual / fidelity / tone / js-off-wargov / redirects), capture:
    - Job name + failed assertion (Playwright trace screenshot OR Python diff output).
    - Root cause hypothesis: which Plan 03-05 artifact needs adjustment (likely Card.astro markup OR wargov.css selector OR slugify alignment with snapshot-urls.py).
    - Whether the fix is loop-to-Plan-03-05 (revision mode) OR template-tweak-in-place.

    Step 6 — If 14-archive js-off fails because dist/<slug>/ paths 404 (not because cards are hydrating client-side), document the state. Decision tree:
      (a) If the executor on Plan 03-05 elected option (a) above (copy Python-built `<slug>/index.html` into dist/<slug>/ via prebuild hook), 404s should not happen — the legacy HTML is served from dist/. Verify by `curl -s https://<preview>/aaro/ | head -5`.
      (b) If option (b) was chosen, 404s are expected. Update quality-gates.yml js-off baseline expectations in 03-06-SUMMARY accompanying note (NOT in the YAML — never soften the gate).

    Step 7 — Resume signal: type "approved — all hard gates green; lighthouse SOFT warnings noted" OR list specific failures with remediation plan.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved — all hard gates green` (proceed to Task 2)
    - `fail: <gate-name> — fix via <plan-03-05 file>` (loop back to Plan 03-05 via /gsd:plan-phase revision OR direct edit)
    - `blocked: git-integration pending` (operator UI step required before this plan can complete)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 03-06-SUMMARY.md sign-off + invariant final check</name>
  <files>.planning/phases/03-ssg-foundation/03-06-SUMMARY.md</files>
  <read_first>
    - Task 1 captured outcomes — all 7 job statuses.
    - `.planning/phases/03-ssg-foundation/03-CONTEXT.md` §"Phase 3 wargov port acceptance" D-35..D-36 — the exact acceptance criteria to sign off against.
    - All five Plan 03-0[1-5]-SUMMARY.md files for cross-reference of what was actually shipped vs. what was planned.
  </read_first>
  <action>
    Write `.planning/phases/03-ssg-foundation/03-06-SUMMARY.md` covering:

    1. Header — phase/plan, completion timestamp, status (`PASS` / `PASS-WITH-SOFT-WARNINGS` / `FAILED-REGRESSION-NOTE`), one-sentence summary.

    2. `## Preview URL` — the actual preview URL deployed against. Whether via git-integration auto-deploy or Direct Upload + wrangler.

    3. `## Quality Gates Results` — verbatim table from Task 1 Step 4 + a column for "decision-locked status" referencing D-35..D-36.

    4. `## Hard-gate failures (if any)` — for each: gate name, assertion text, screenshot/diff excerpt, root cause, fix applied (which file + which commit). If zero hard-gate failures, this section is one line: "None — all 5 hard gates green."

    5. `## Soft-gate warnings (lighthouse)` — LCP measured (target ≤ 2.5s), transfer-byte measured (target ≤ 500 KB). Note D-28 SOFT — warnings acceptable; Phase 4 close converts to HARD per 02-08-SUMMARY Phase 4 close plan.

    6. `## 14-archive js-off state` — for each of the 14 non-wargov archives, record the js-off row outcome. Document whether 404s (no dist/<slug>/) or "no cards visible without JS" (Python output served, cards hydrating). Reference Plan 03-05's option (a) vs (b) decision.

    7. `## URL contract preservation` — confirm `bash scripts/verify-redirects.sh https://<preview>/` exit 0; spot-check 5 random `/#card-*` anchors from URL-CONTRACT.txt resolve to a card in the rendered DOM.

    8. `## Invariant preservation check (D-37..D-40)` —
       - D-37: `dig +noall +answer realufo.org | awk '{print $4}' | head -1` returns `A` or `AAAA` of the GitHub Pages IP range (NOT Cloudflare — DNS untouched in Phase 3).
       - D-38: `_headers` referenced from CF Pages preview returns identical Cache-Control / HSTS as Phase 2 02-01 wrote. `curl -sI <preview>/sw.js | grep -i cache-control` returns `no-cache, no-store, must-revalidate`.
       - D-39: `git diff main -- scripts/build-*.py scripts/sync-*.py uap-release001.csv uap-data.csv` is empty.
       - D-40: URL-CONTRACT.txt unchanged on `main` since Phase 1 (`git log main -- URL-CONTRACT.txt` shows only the Phase 1 commit + Phase 2 02-05 _redirects-related-update; nothing in Phase 3).

    9. `## SSG-05 Sign-off Statement` — One paragraph. Either:
       - "SSG-05 SATISFIED. wargov archive renders at / from Astro 5.18.x + Content Collections + `<script is:inline>` JS invariants. All 5 hard Phase 2 gates green on preview `<url>`. Lighthouse soft budget warnings: [list, or 'none']. Phase 3 closes per ROADMAP §Phase 3 SC#5; Phase 4 can begin SSG-06 (14-archive port)."
       OR
       - "SSG-05 FAILED. Hard gate(s): [list]. Remediation loop required via /gsd:plan-phase 03 --gaps. Plan 03-06 reports red until subsequent revisions land green."

    10. `## Open questions for Phase 4`
        - Lazy-load shard pre-rendering: pre-rendered HTML strings in shards vs client-side data templating (option (a) vs (b) from Plan 03-05).
        - GEIPAN 3.3MB sharding strategy (PERF-01 — informed by wargov sharding measurement).
        - Lighthouse HARD-flip prerequisites (which budgets need lift to hit `error` level).
        - sync-nav.py / sync-footer.py retirement timing (SSG-10 — depends on all 15 archives porting first).
        - dist/<slug>/ fallback strategy for the 13 archives that haven't ported yet (option (a) decision from this plan).
  </action>
  <verify>
    <automated>
      test -f .planning/phases/03-ssg-foundation/03-06-SUMMARY.md \
        && grep -q "Preview URL" .planning/phases/03-ssg-foundation/03-06-SUMMARY.md \
        && grep -q "Quality Gates Results" .planning/phases/03-ssg-foundation/03-06-SUMMARY.md \
        && grep -qE "SSG-05 (SATISFIED|FAILED)" .planning/phases/03-ssg-foundation/03-06-SUMMARY.md \
        && grep -q "D-35\|D-36" .planning/phases/03-ssg-foundation/03-06-SUMMARY.md \
        && grep -q "URL-CONTRACT.txt\|verify-redirects.sh" .planning/phases/03-ssg-foundation/03-06-SUMMARY.md \
        && grep -q "scripts/build-\*\.py\|scripts/build-" .planning/phases/03-ssg-foundation/03-06-SUMMARY.md \
        && git diff --quiet main -- uap-release001.csv \
        && git diff --quiet main -- scripts/build-wargov.py
    </automated>
  </verify>
  <done>
    `03-06-SUMMARY.md` exists with all 10 sections, signs off SSG-05 EITHER as SATISFIED (with green-gate evidence) OR as FAILED (with remediation plan). Invariant check (D-37..D-40) confirmed in the SUMMARY. Phase 3 closeout state captured for Phase 4 entry.
  </done>
</task>

</tasks>

<verification>
- All 7 quality-gates.yml jobs evaluated on the preview URL; status table verbatim in SUMMARY.
- 5 HARD gates (visual / fidelity / tone-colours / js-off-wargov / redirects) GREEN OR failure documented with remediation plan.
- Lighthouse SOFT gate run regardless; warnings logged.
- `_headers`, `_redirects`, `URL-CONTRACT.txt` unchanged (`git diff main -- _headers _redirects URL-CONTRACT.txt` empty) — Phase 1+2 outputs preserved.
- Python build scripts unchanged (`git diff main -- scripts/build-*.py scripts/sync-*.py` empty per D-39).
- CSV unchanged (`git diff main -- uap-release001.csv uap-data.csv` empty per CLAUDE.md §11).
- SUMMARY contains the SSG-05 SATISFIED or FAILED sign-off statement.
</verification>

<success_criteria>
- SSG-05 EVALUATED: either signed off as SATISFIED (Phase 4 unblocked) or signed off as FAILED with documented remediation loop (Plan 03-05 revision queued).
- ROADMAP §Phase 3 SC#5 conclusively closed (green) or surfaced (red) — no ambiguity.
- D-17 operator-only baseline invariant honored: any visual/fidelity/tone failures are addressed via src/ template fixes, NOT baseline regeneration (unless operator explicitly chooses recapture in the SUMMARY's rationale section).
- D-25 + B3 js-off-hard-fail invariant honored: no `continue-on-error` added to js-off job; 14-archive expected-fail rows documented in SUMMARY as Phase 4 runway.
- D-37..D-40 invariants preserved end-to-end through Phase 3.
- 03-06-SUMMARY.md becomes the input artifact for Phase 4 planning kickoff.
</success_criteria>

<output>
This plan's primary output IS the `.planning/phases/03-ssg-foundation/03-06-SUMMARY.md` — there is no separate SUMMARY artifact. Treat it as the final Phase 3 sign-off document. The orchestrator's `git commit` step covers the SUMMARY commit; the user's `/gsd:next` (or `/gsd:plan-phase 4`) follows.
</output>

---
phase: 03-ssg-foundation
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - scripts/normalize-csv.py
  - data/wargov.json
  - data/wargov-shard-2.json
  - data/wargov-shard-3.json
  - data/wargov-shard-4.json
  - data/wargov-shard-5.json
  - data/wargov-shard-6.json
  - package.json
autonomous: true
requirements: [SSG-02]

must_haves:
  truths:
    - "`scripts/normalize-csv.py` reads the wargov source-of-truth CSV (`uap-data.csv` if present else `uap-release001.csv`, matching the precedence in `scripts/build-wargov.py`) and writes `data/wargov.json` + N shard files per D-04, D-08..D-10, D-32"
    - "First 50 rows land in `data/wargov.json` (D-08 — pre-rendered initial render); remaining rows (~211) are split into shards of 50 (D-08 starting point per D-discretion); shard count = ceil((total-50)/50). For ~261 rows that's 5 shards of 50 plus a tail (e.g. shard-2..shard-6)."
    - "`pnpm prebuild` script in package.json invokes `python3 scripts/normalize-csv.py` (replaces the placeholder echo from Plan 03-01)"
    - "Zod schema from `src/content.config.ts` (Plan 03-02) validates the emitted `data/wargov.json` AND each shard — `pnpm build` exits 0"
    - "Output JSON is byte-deterministic — same CSV input + same git state produces a byte-identical `data/wargov.json` per D-04 idempotency (sort by stable key, no timestamps, fixed indent=2)"
    - "CSV is UNTOUCHED per CLAUDE.md §11 — the normaliser READS uap-release001.csv (and uap-data.csv) but never writes back; `git diff uap-release001.csv uap-data.csv` is empty after running"
    - "Verbatim text fidelity preserved per D-26..D-28 — no smart-quote rewriting, no em-dash normalisation, no accent stripping; reuses the `csv.DictReader` semantics from `scripts/build-wargov.py` which already round-trips through to the embedded JSON in current `index.html`"
    - "Shard envelope shape matches `wargovEnvelopeSchema` from Plan 03-02 — the same Zod schema validates wargov.json AND every shard (planner notes if loader API requires a different collection-name for shards; D-discretion)"
  artifacts:
    - path: "scripts/normalize-csv.py"
      provides: "CSV → data/wargov.json + shards normaliser; argparse CLI (--check / --base-dir); idempotent; deterministic JSON"
      contains: "uap-release001.csv"
      min_lines: 120
    - path: "data/wargov.json"
      provides: "Initial 50 rows + manifest listing of shards (shards: [{ index, file }, ...])"
      contains: "\"rows\""
    - path: "data/wargov-shard-2.json"
      provides: "Rows 51-100 (skeleton if total &lt; 100 — empty rows[])"
      contains: "\"rows\""
    - path: "package.json"
      provides: "prebuild script invokes scripts/normalize-csv.py (replaces 03-01 placeholder)"
      contains: "scripts/normalize-csv.py"
  key_links:
    - from: "package.json scripts.prebuild"
      to: "scripts/normalize-csv.py"
      via: "python3 invocation"
      pattern: "python3 scripts/normalize-csv\\.py"
    - from: "scripts/normalize-csv.py"
      to: "uap-release001.csv + uap-data.csv"
      via: "csv.DictReader read-only"
      pattern: "csv\\.DictReader"
    - from: "scripts/normalize-csv.py"
      to: "data/wargov.json + data/wargov-shard-*.json"
      via: "json.dump with sort_keys=True, indent=2"
      pattern: "sort_keys=True"
---

<objective>
Stand up the CSV-to-JSON normaliser that turns the wargov source-of-truth CSVs (`uap-release001.csv` always, `uap-data.csv` if present) into `data/wargov.json` plus per-50-row shard files, then wire `pnpm prebuild` to invoke it. The normaliser is the single point of truth for the wargov data contract — Plan 03-05's `src/pages/index.astro` reads from `data/wargov.json` via the file() loader committed in Plan 03-02. CSV remains untouchable per CLAUDE.md §11.

Purpose: Closes the SSG-02 wargov data-side gap (Plan 03-02 set up the schema for all 15 archives; this plan provides the wargov data file the schema validates). Establishes the prebuild step at D-32 (CSV → JSON before `astro build`). The shard scheme (D-08..D-10) is the seam Phase 4 PERF-01 will rely on for GEIPAN's 3.3MB inline JSON refactor — Phase 3 proves the shape on the smaller wargov surface.

Output: `scripts/normalize-csv.py`, `data/wargov.json` + ~5 shards, updated `package.json#scripts.prebuild`. `pnpm build` continues to exit 0; wargov collection now has data; Plan 03-05 can render against it.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ssg-foundation/03-CONTEXT.md
@.planning/phases/03-ssg-foundation/03-01-PLAN.md
@.planning/phases/03-ssg-foundation/03-02-PLAN.md
@.planning/research/PITFALLS.md
@scripts/build-wargov.py
@scripts/snapshot-urls.py
@CLAUDE.md
@data/README.md

<interfaces>
<!-- CSV column headers verbatim (from uap-release001.csv line 1) — order matters; preserve every column per CLAUDE.md §11: -->
<!-- Redaction,Release Date,Title,Type,Video Pairing,PDF Pairing,Description Blurb,DVIDS Video ID,Video Title,Agency,Incident Date,Incident Location,PDF | Image Link,Modal Image,Image Alt Text,Image VIRIN -->
<!-- (trailing empty columns from spreadsheet export are ignored — see scripts/build-wargov.py for the precedent) -->

<!-- Existing scripts/build-wargov.py pattern (verbatim from lines 22-44) — REUSE THIS LOGIC: -->
<!--   CSV_COMBINED = os.path.join(REPO, 'uap-data.csv')
        CSV_LEGACY   = os.path.join(REPO, 'uap-release001.csv')
        CSV          = CSV_COMBINED if os.path.exists(CSV_COMBINED) else CSV_LEGACY -->

<!-- Existing scripts/snapshot-urls.py argparse pattern (Phase 1 PMS-01 precedent for CLI shape) — argparse with --check / --base-dir, exit code 0 success / 1 gap / 2 missing dep. Same pattern used by scripts/capture-baselines.py (Phase 2 02-03). -->

<!-- Shard naming convention to commit: data/wargov.json (first 50 rows) + data/wargov-shard-2.json (rows 51-100) + data/wargov-shard-3.json (rows 101-150) + ... -->
<!-- The first file is NOT data/wargov-shard-1.json — the unsharded name is the manifest-bearing primary, the only one mandatory per the wargov collection schema. Shards 2..N are siblings the runtime JS lazy-loads (see Plan 03-05 IntersectionObserver). -->

<!-- data/wargov.json output target shape (matching wargovEnvelopeSchema from Plan 03-02 + the file() loader form used in the 14 skeletons): -->
<!-- {
       "v1": {
         "schemaVersion": 1,
         "slug": "wargov",
         "rows": [
           { "Redaction": "", "Release Date": "5/8/26", "Title": "18_100754_ General 1946-7_Vol_2", "Type": "PDF", "Video Pairing": "", "PDF Pairing": "", "Description Blurb": "This file contains memorandums...", "DVIDS Video ID": "", "Video Title": "", "Agency": "Department of War", "Incident Date": "12/30/47", "Incident Location": "N/A", "PDF | Image Link": "https://www.war.gov/medialink/ufo/release_1/18_100754_...", "Modal Image": "https://...", "Image Alt Text": "Declassified Secret document...", "Image VIRIN": "260508-D-D0360-1001" },
           ... 49 more rows ...
         ],
         "shards": [
           { "index": 2, "file": "data/wargov-shard-2.json" },
           { "index": 3, "file": "data/wargov-shard-3.json" },
           ...
         ]
       }
     } -->

<!-- Shard file shape (data/wargov-shard-2.json etc.) — note: shards live OUTSIDE the wargov collection (they are not registered in src/content.config.ts collections). They are static JSON assets that the runtime lazy-loader fetches. -->
<!-- {
       "schemaVersion": 1,
       "slug": "wargov",
       "shardIndex": 2,
       "rows": [ ... rows 51-100 ... ]
     } -->

<!-- Pitfall #6 (research/PITFALLS.md) — the typographer drift surface lives at "any markdown-aware processor". csv.DictReader doesn't rewrite text; json.dump with ensure_ascii=False preserves Unicode verbatim. Both safe. The danger is downstream — Plan 03-05's Astro template must NOT use {markdown.render} on these fields. This plan's job is to never INTRODUCE a transform: no .strip() on Title (preserve leading/trailing spaces per CLAUDE.md §11 verbatim rule); no quote normalisation; no encoding conversion beyond what Python's csv module already does to read the file. -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `uap-release001.csv` / `uap-data.csv` → normaliser | Source-of-truth ingestion; CSV is committed in repo, edited only by operator (CLAUDE.md §11) — trusted relative to local fs |
| Normaliser → `data/wargov.json` + shards | Build-time output; consumed by Astro content layer + runtime lazy-loader |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | Normaliser writes back to CSV (would silently break source-of-truth invariant) | mitigate | Open CSVs read-only (`open(..., 'r')`); add a `git diff --quiet uap-release001.csv uap-data.csv` post-step in the normaliser exit path — non-zero diff fails the script with a clear error message |
| T-03-08 | Tampering | Smart-quote / em-dash rewrite via accidental `.translate()` or `unicodedata.normalize()` | mitigate | No `.translate()`, no `unicodedata.normalize()`, no `.strip()` on text fields. Acceptance criteria includes byte-equivalent round-trip test on a fixture row known to contain Unicode (em-dash + curly quote) |
| T-03-09 | Information Disclosure | Embed of secrets if CSV contains an internal URL not meant for public mirror | accept | wargov CSV is curated by operator + already published in `index.html` inline JSON today (no new disclosure surface introduced by this plan) |
| T-03-10 | DoS | Normaliser run-time on large CSV blows CF Pages 20-min build cap | accept | wargov is ~261 rows; Python csv read is microseconds; no concern. Phase 4 will scale-test with GEIPAN (3.3MB inline JSON ⇒ thousands of rows) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/normalize-csv.py — CSV → data/wargov.json + shards</name>
  <files>scripts/normalize-csv.py</files>
  <read_first>
    - `scripts/build-wargov.py` lines 1-100 — the existing CSV-read precedence + `csv.DictReader` pattern + DOW DVIDS map handling. THE READ-LOGIC FROM HERE IS THE STARTING POINT — port to a function that ONLY reads + emits JSON (no HTML emission; no in-place edit of index.html).
    - `scripts/snapshot-urls.py` — CLI conventions (argparse, --check mode, exit codes 0/1/2).
    - `scripts/capture-baselines.py` — Phase 2 precedent for the same CLI shape.
    - `.planning/codebase/CONVENTIONS.md` — Python conventions (stdlib only; PEP 8; type hints if natural; `from __future__ import annotations`).
    - `uap-release001.csv` line 1 — exact column header order. The normaliser MUST preserve every column key verbatim (with literal spaces).
    - `src/content.config.ts` from Plan 03-02 — confirm the `wargovEnvelopeSchema` shape and the file() loader's expected envelope (entries-object vs array form).
  </read_first>
  <action>
    Write `scripts/normalize-csv.py` as a stdlib-only Python script.

    Header docstring: cite CLAUDE.md §11 (CSV untouchable invariant), D-04 (committed, idempotent), D-26..D-28 (fidelity guards, no transform), D-32 (runs as pnpm prebuild before astro build), D-39 (does NOT replace scripts/build-wargov.py — that script keeps running on `main` for GH Pages per D-12..D-14 coexistence; this is a parallel reader feeding Astro).

    Module imports (stdlib only): `from __future__ import annotations`; `import argparse`, `csv`, `json`, `math`, `os`, `subprocess`, `sys`; `from pathlib import Path`; `from typing import Any`.

    Constants at top:
    - `REPO = Path(__file__).resolve().parent.parent` (mirrors scripts/build-wargov.py)
    - `CSV_COMBINED = REPO / 'uap-data.csv'`
    - `CSV_LEGACY = REPO / 'uap-release001.csv'`
    - `DATA_DIR = REPO / 'data'`
    - `OUT_PRIMARY = DATA_DIR / 'wargov.json'`
    - `SHARD_TEMPLATE = 'data/wargov-shard-{n}.json'`
    - `PAGE_SIZE = 50` (D-08; tunable via --page-size CLI flag)

    Functions (in dependency order):

    1. `_pick_csv() -> Path` — returns `CSV_COMBINED` if `CSV_COMBINED.exists()` else `CSV_LEGACY`. Logs which one was chosen. If neither exists, exits 2 with a CSV-missing error message.

    2. `_read_rows(csv_path: Path) -> list[dict[str, str]]` — opens the file with `open(csv_path, 'r', encoding='utf-8-sig', newline='')` (utf-8-sig handles the BOM the CSV has — see line 1 of uap-release001.csv). Uses `csv.DictReader`. Iterates rows; discards rows where `row.get('Title', '').strip() == ''` (a data-quality filter that matches `scripts/build-wargov.py`'s implicit filtering — verify by reading the existing build script's row-filtering logic). DO NOT `.strip()` other fields; only Title is used as a truthiness test, and only the test result is used — the row value is preserved verbatim. Return rows in CSV order (preserves operator-meaningful sequencing).

    3. `_shard(rows: list[dict[str, str]], page_size: int) -> tuple[list[dict], list[list[dict]]]` — returns `(first_page, shards)` where `first_page` is `rows[:page_size]` and `shards` is `[rows[page_size:page_size+page_size], rows[2*page_size:3*page_size], ...]`. For 261 rows + page_size=50 → first_page has 50, shards has 5 lists with sizes 50/50/50/50/11.

    4. `_assert_csv_unchanged() -> None` — runs `subprocess.run(['git', '-C', str(REPO), 'diff', '--quiet', '--', 'uap-release001.csv', 'uap-data.csv'], check=False, capture_output=True)`. If returncode != 0, prints an error "CSV was modified by normaliser — CLAUDE.md §11 violation" and exits 1. Run this as the LAST step before declaring success.

    5. `_write_json(path: Path, data: Any) -> None` — `path.parent.mkdir(parents=True, exist_ok=True)`. `path.write_text(json.dumps(data, ensure_ascii=False, indent=2, sort_keys=True) + '\n', encoding='utf-8')`. Trailing newline for POSIX-clean diffs. `sort_keys=True` for determinism (D-04). `ensure_ascii=False` to preserve Unicode (Pitfall #6 — DO NOT escape em-dashes to `—`).

    6. `main()` — argparse with flags:
       - `--check` (default False): runs the full pipeline against a TempDirectory and compares result to current `data/wargov.json` + shards. Exits 0 if byte-identical (idempotency proof); exits 1 if any drift detected — prints `git diff`-style unified diff of first 50 differing lines. Used by CI per D-04 idempotency invariant.
       - `--page-size INT` (default 50): D-08 tunable.
       Default mode (no flags): writes `data/wargov.json` + N shards in-place.

    7. `main()` flow:
       a. Pick CSV (`_pick_csv`).
       b. Read rows (`_read_rows`).
       c. Shard (`_shard`).
       d. Build primary envelope matching whichever file() loader form Plan 03-02's `src/content.config.ts` chose. If entries-object form: `{"v1": {"schemaVersion": 1, "slug": "wargov", "rows": first_page, "shards": [{"index": i+2, "file": SHARD_TEMPLATE.format(n=i+2)} for i in range(len(shard_lists))]}}`. If array form: `[{"id": "v1", ...same inner...}]`. INSPECT the data/aaro.json skeleton from Plan 03-02 at runtime — its shape is the contract. Match it.
       e. Write primary (`_write_json(OUT_PRIMARY, primary)`).
       f. For each shard list, write `data/wargov-shard-{n}.json` with shape `{"schemaVersion": 1, "slug": "wargov", "shardIndex": n, "rows": shard_rows}` (NOT envelope form — shards are runtime-fetched plain JSON, not collection entries).
       g. Run `_assert_csv_unchanged()`.
       h. Print summary: `[ok] wargov: N rows, K shards written` (matches existing scripts/ bracket-status convention from 02-08 SUMMARY).

    8. Exit codes: 0 success, 1 gap/failure (CSV modified, --check drift, malformed rows), 2 missing dep (CSV not found).

    NO `if row[key]: row[key] = row[key].strip()` anywhere — fidelity rule per Pitfall #6.
    NO `unicodedata.normalize` — would silently rewrite accented chars (research/PITFALLS.md Pitfall #4 footnote).
    NO `re.sub` against text fields — same reason.
    NO writes to `uap-release001.csv` or `uap-data.csv` — only reads.

    Script chmod 0755; first line `#!/usr/bin/env python3`.
  </action>
  <verify>
    <automated>
      test -f scripts/normalize-csv.py \
        && test -x scripts/normalize-csv.py \
        && head -1 scripts/normalize-csv.py | grep -q '#!/usr/bin/env python3' \
        && python3 -c "import ast; ast.parse(open('scripts/normalize-csv.py').read())" \
        && grep -q 'uap-release001.csv' scripts/normalize-csv.py \
        && grep -q 'CSV_COMBINED' scripts/normalize-csv.py \
        && grep -qE 'PAGE_SIZE[[:space:]]*=[[:space:]]*50' scripts/normalize-csv.py \
        && grep -q 'sort_keys=True' scripts/normalize-csv.py \
        && grep -q 'ensure_ascii=False' scripts/normalize-csv.py \
        && ! grep -nE '\.strip\(\)' scripts/normalize-csv.py | grep -v "Title.*strip" \
        && ! grep -q 'unicodedata' scripts/normalize-csv.py \
        && ! grep -q "open(CSV_COMBINED, 'w'" scripts/normalize-csv.py \
        && ! grep -q "open(CSV_LEGACY, 'w'" scripts/normalize-csv.py \
        && python3 scripts/normalize-csv.py --help | grep -q 'check'
    </automated>
  </verify>
  <done>
    Stdlib-only Python script exists, parses cleanly, declares `PAGE_SIZE = 50`, never opens CSVs in write mode, never `unicodedata.normalize`s, has `--check` and `--page-size` CLI flags, prints `[ok]` summary on success.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run normaliser, commit data/wargov.json + shards, wire pnpm prebuild, validate via pnpm build</name>
  <files>data/wargov.json, data/wargov-shard-2.json, data/wargov-shard-3.json, data/wargov-shard-4.json, data/wargov-shard-5.json, data/wargov-shard-6.json, package.json</files>
  <read_first>
    - `scripts/normalize-csv.py` from Task 1 — confirm `--page-size 50` default.
    - `package.json` from Plan 03-01 — confirm `scripts.prebuild` is the placeholder `echo` to replace.
    - `uap-release001.csv` line count via `wc -l` (~537 lines header + rows). After dropping the header + filtering empty Title rows, the actual row count is between 200 and 300 (likely 261 per existing build-wargov.py output). The exact shard count depends on this number.
    - `src/content.config.ts` + `data/aaro.json` (from Plan 03-02) — confirms the envelope shape the primary file must match.
  </read_first>
  <action>
    Step 1 — Run `python3 scripts/normalize-csv.py` from repo root. Observe the `[ok] wargov: N rows, K shards written` output. Confirm:
    - `data/wargov.json` was created.
    - `data/wargov-shard-2.json` through `data/wargov-shard-{N}.json` were created (N = ceil((total_rows - 50) / 50) + 1). For 261 rows, expect 5 shards (shard-2 through shard-6). The exact count depends on actual CSV row count; capture in SUMMARY.

    Step 2 — Open `data/wargov.json` and verify:
    - Envelope shape matches the file() loader form chosen in Plan 03-02 (entries-object OR array).
    - First row's `Title` field matches the first non-empty-Title row of the CSV verbatim (no .strip; no quote rewriting).
    - `shards` array lists all generated shard filenames + their indices.
    - JSON is sort_keys-ordered (deterministic).
    - File ends with `\n`.

    Step 3 — Idempotency check: re-run `python3 scripts/normalize-csv.py`, then `git diff data/wargov.json data/wargov-shard-*.json` — output MUST be empty (zero-byte diff). If non-empty, debug the determinism (most likely culprit: missing `sort_keys=True` or unstable shard-list ordering).

    Step 4 — `--check` mode: run `python3 scripts/normalize-csv.py --check`. Exit 0 expected. Now mutate one shard file by hand (`echo '[]' > data/wargov-shard-2.json`), re-run `--check`, exit 1 expected. Revert (`python3 scripts/normalize-csv.py` to rebuild). Re-run `--check`, exit 0 expected.

    Step 5 — Edit `package.json`: replace `"prebuild": "echo 'prebuild placeholder — ...'"` with `"prebuild": "python3 scripts/normalize-csv.py"`. Verify with `python3 -c "import json; d=json.load(open('package.json')); assert d['scripts']['prebuild'] == 'python3 scripts/normalize-csv.py'"`.

    Step 6 — Validate against Zod: run `pnpm build`. The build runs `pnpm prebuild` → `python3 scripts/normalize-csv.py` → writes/refreshes `data/wargov.json` + shards → then `astro build` validates `data/wargov.json` against `wargovEnvelopeSchema` from Plan 03-02. Expected: build exits 0. If Zod throws on any wargov row, the build prints the row index + field that failed validation — fix by widening the schema in Plan 03-02 (return there and edit) OR fix the normaliser if it's producing a non-canonical shape.

    Step 7 — Smoke test: `pnpm preview &`, then `curl -sI http://localhost:4321/` returns 200 or 404 (still no index.astro). Kill preview.

    Step 8 — Confirm CSV untouched: `git diff --stat uap-release001.csv uap-data.csv` shows no changes.

    Step 9 — Confirm scripts/build-*.py untouched: `git diff --stat scripts/build-*.py scripts/sync-*.py` shows zero modifications (D-39 invariant; coexistence).
  </action>
  <verify>
    <automated>
      python3 scripts/normalize-csv.py \
        && test -f data/wargov.json \
        && python3 -c "import json; d=json.load(open('data/wargov.json')); items = list(d.values()) if isinstance(d, dict) else d; rec = items[0] if items else None; assert rec is not None, 'wargov.json empty'; rows = rec.get('rows') if isinstance(rec, dict) else None; assert isinstance(rows, list) and len(rows) > 0, 'rows must be non-empty list'; assert len(rows) <= 50, 'first page must have <= 50 rows per D-08'; shards = rec.get('shards', []); assert isinstance(shards, list), 'shards must be a list'" \
        && ls data/wargov-shard-*.json | wc -l | grep -qE '^[[:space:]]*[1-9][0-9]?[[:space:]]*$' \
        && python3 scripts/normalize-csv.py --check \
        && python3 -c "import json; d=json.load(open('package.json')); assert d['scripts']['prebuild'] == 'python3 scripts/normalize-csv.py'" \
        && pnpm build \
        && git diff --quiet uap-release001.csv \
        && (test -f uap-data.csv && git diff --quiet uap-data.csv || true) \
        && git diff --quiet scripts/build-wargov.py scripts/build-aaro.py scripts/build-nasa.py
    </automated>
  </verify>
  <done>
    `python3 scripts/normalize-csv.py` produces `data/wargov.json` + shards deterministically (re-run = zero diff). `data/wargov.json` first page has ≤50 rows, shards manifest references all sibling shard files. `pnpm build` validates the output against Plan 03-02's Zod schema. `package.json#scripts.prebuild` invokes the normaliser. CSV + Python build scripts untouched.
  </done>
</task>

</tasks>

<verification>
- `scripts/normalize-csv.py` parses (`python3 -c "import ast; ast.parse(open(...).read())"`).
- Idempotency: re-running the normaliser produces zero-byte git diff on `data/wargov.json` + all shards.
- `pnpm prebuild` + `pnpm build` pipeline exits 0 — Zod schema accepts the normaliser output.
- `git diff uap-release001.csv uap-data.csv` is empty (CSV untouchable per CLAUDE.md §11; T-03-07 mitigated).
- `git diff scripts/build-*.py scripts/sync-*.py` is empty (D-39 coexistence; Python builds untouched).
- First-page row count ≤ 50 (D-08 — pre-render initial 50; lazy-load rest).
</verification>

<success_criteria>
- SSG-02 (data side, wargov) satisfied: `data/wargov.json` exists, is committed, is schema-valid against Plan 03-02's `wargovEnvelopeSchema`.
- D-04 idempotency proven: `--check` returns 0 on clean tree; deterministic JSON output (`sort_keys=True`, fixed indent, trailing newline).
- D-08..D-10 shard-pattern established: first 50 rows in primary, rest in `data/wargov-shard-{n}.json` files; primary lists shard manifest for Plan 03-05's lazy-loader.
- D-26..D-28 fidelity guards live: no `.strip()` on text fields except as truthiness filter on Title; no `unicodedata.normalize`; no regex rewrites; `ensure_ascii=False` preserves Unicode bytes.
- D-32 build pipeline wired: `pnpm prebuild` runs before `pnpm build`.
- D-39 coexistence: Python build scripts unmodified; CSV unmodified.
- Plan 03-05 unblocked: `src/pages/index.astro` can call `getCollection('wargov')` and receive validated rows.
</success_criteria>

<output>
Create `.planning/phases/03-ssg-foundation/03-03-SUMMARY.md` capturing: actual row count after empty-Title filtering, exact shard count produced (e.g. 5 shards), total wall-time of normalisation (microseconds expected), exact bytes-on-disk for `data/wargov.json` (input to Phase 4 PERF budget planning), any fidelity edge cases observed (rows with embedded newlines, smart quotes, accented characters), and confirmation that `pnpm build` validated all rows against Plan 03-02's schema without error.
</output>

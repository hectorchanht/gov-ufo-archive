---
phase: 02-infrastructure-ci-scaffolding
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tests/playwright.config.ts
  - tests/visual-regression.spec.ts
  - tests/visual-baselines/.gitkeep
  - tests/visual-baselines/README.md
  - tests/visual-baselines/AAA-aaro-360.png
  - scripts/capture-baselines.py
autonomous: true
requirements: [INF-04]

must_haves:
  truths:
    - "Playwright is installed as a dev dep on ssg-migration with version pinned (no caret/tilde drift)"
    - "tests/playwright.config.ts uses chromium-only (D-15 — no firefox/webkit matrix this phase)"
    - "scripts/capture-baselines.py captures 60 PNGs against live https://realufo.org (D-12) — NOT against local server, NOT against CF Pages preview"
    - "Captures cover all 15 archives × 4 viewports (360/768/1024/1440) — D-13, D-14"
    - "Each baseline PNG lives at tests/visual-baselines/{archive}-{viewport}.png (raw PNG, not LFS — D-13)"
    - "Pixel-diff threshold in playwright.config.ts is 0.1% (D-16)"
    - "Baselines NEVER regenerate automatically — operator must explicitly delete + recapture (D-17)"
    - "tests/visual-regression.spec.ts compares the CF Pages preview URL against the captured PNGs"
    - "tests/visual-baselines/README.md documents the D-17 invariant + the explicit recapture procedure"
    - "No SSG code, no Astro install (D-38)"
  artifacts:
    - path: "scripts/capture-baselines.py"
      provides: "One-shot operator-run script that captures 60 PNGs against live realufo.org via Playwright headless chromium"
      contains: "javaScriptEnabled"
    - path: "tests/playwright.config.ts"
      provides: "Playwright config with chromium-only project + 0.1% pixel threshold + baseURL placeholder for CI override"
      contains: "0.1"
    - path: "tests/visual-regression.spec.ts"
      provides: "Per-archive × per-viewport screenshot comparison test that runs against process.env.PREVIEW_URL"
      contains: "toHaveScreenshot"
    - path: "tests/visual-baselines/README.md"
      provides: "Operator runbook for the D-17 'explicit recapture' rule"
      contains: "D-17"
  key_links:
    - from: "scripts/capture-baselines.py"
      to: "https://realufo.org/<archive>/"
      via: "playwright chromium headless"
      pattern: "https://realufo.org"
    - from: "tests/visual-regression.spec.ts"
      to: "tests/visual-baselines/<archive>-<viewport>.png"
      via: "expect(page).toHaveScreenshot()"
      pattern: "toHaveScreenshot"
---

<objective>
Install Playwright as a dev dep on `ssg-migration`, write `scripts/capture-baselines.py` that captures 60 PNG fixtures (15 archives × 4 viewports) against the LIVE `https://realufo.org` production GH Pages origin (D-12 — pixel-true to what real users see today), commit the PNGs as raw bytes under `tests/visual-baselines/`, and ship the spec file that future plans (02-08 quality-gates.yml) will execute against the CF Pages preview URL.

Purpose: INF-04. Per ROADMAP §Phase 2 SC#4, "baselines exist on disk for all 15 archives × 4 viewports, captured against the current `main` deploy". Per D-17, these baselines are FROZEN — operator-only regen. Per D-12, capture against live realufo.org, not the local dev server (otherwise CSS-fonts that the browser fetches from Google Fonts at runtime would render differently in CI than on the production origin).

Output: `tests/playwright.config.ts`, `tests/visual-regression.spec.ts`, 60 PNG baselines at `tests/visual-baselines/`, `scripts/capture-baselines.py` (Python-driven Playwright capture, mirrors `scripts/snapshot-urls.py` style per CONVENTIONS.md), `tests/visual-baselines/README.md` documenting D-17.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-infrastructure-ci-scaffolding/02-CONTEXT.md
@.planning/phases/02-infrastructure-ci-scaffolding/02-01-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@CLAUDE.md
@scripts/snapshot-urls.py
@package.json

<interfaces>
<!-- Playwright Python (https://playwright.dev/python/) — chosen over Playwright TS for D-15 (chromium-only) -->
<!-- so the capture script follows existing CONVENTIONS.md §Python conventions (stdlib + targeted libs only). -->
<!-- The .spec.ts file remains TS because that is the Playwright test runner's native shape. -->

Capture script API (scripts/capture-baselines.py):
  CLI:
    python3 scripts/capture-baselines.py                       # capture all 60
    python3 scripts/capture-baselines.py --archive aaro        # one archive × 4 viewports
    python3 scripts/capture-baselines.py --viewport 360        # all 15 archives at 360 px
    python3 scripts/capture-baselines.py --check               # exit 1 if any PNG missing
    python3 scripts/capture-baselines.py --base-url https://realufo.org   # override (default = live)

Viewport list (D-14): 360 × 800, 768 × 1024, 1024 × 768, 1440 × 900

Archive slug list (per CLAUDE.md §2): wargov, aaro, nasa, nara, geipan, uk, brazil, chile, argentina,
canada, italy, nz, peru, spain, uruguay. wargov page lives at "/" not "/wargov/"; all others at "/<slug>/".

Output filename pattern: tests/visual-baselines/{archive}-{viewport_width}.png

Playwright test runner config (tests/playwright.config.ts):
  - projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }]   (D-15)
  - expect.toHaveScreenshot.maxDiffPixelRatio: 0.001                           (0.1% per D-16)
  - testDir: './tests'
  - baseURL: process.env.PREVIEW_URL || 'https://realufo.pages.dev'
  - workers: 4                                                                  (one per viewport in parallel)
  - reporter: 'list'                                                            (concise CI output)

Spec file (tests/visual-regression.spec.ts):
  - test.describe.parallel('visual regression — 15 archives × 4 viewports', ...)
  - One test per archive × viewport (60 total)
  - Each test: page.setViewportSize({ width, height }); await page.goto(<archive_path>);
              await expect(page).toHaveScreenshot('<archive>-<width>.png', { maxDiffPixelRatio: 0.001 });
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright dev dep + write playwright.config.ts + capture-baselines.py</name>
  <read_first>
    - .planning/phases/02-infrastructure-ci-scaffolding/02-CONTEXT.md §decisions D-12, D-13, D-14, D-15, D-16, D-17
    - .planning/phases/02-infrastructure-ci-scaffolding/02-01-SUMMARY.md (package.json + .nvmrc skeleton; preview URL)
    - .planning/codebase/CONVENTIONS.md §"Python Conventions (Build Scripts)" — argparse + stdlib pattern; §"Naming Patterns" — slug list
    - scripts/snapshot-urls.py — reference Python style (argparse, REPO root resolution, exit codes 0/1/2, stdlib-only structure)
    - CLAUDE.md §2 (archive slug table — the canonical 15 slugs)
    - CLAUDE.md §8 (mobile-first; 360 px is the baseline viewport — first capture)
    - .planning/research/PITFALLS.md §Pitfall #11 (mobile-first regression — visual regression at 360 px is the first viewport, not the last)
    - .planning/research/PITFALLS.md §Pitfall #7 (cross-archive consistency drift — 15-way matrix is the gate)
    - existing .github/workflows/lighthouse.yml — npm-install pattern reference (does not install pnpm; we will install pnpm via npm + use it to add Playwright)
  </read_first>
  <files>package.json, pnpm-lock.yaml, tests/playwright.config.ts, scripts/capture-baselines.py, tests/visual-baselines/README.md</files>
  <action>
    On ssg-migration branch.

    1. Install pnpm + Playwright. From repo root:
       ```
       npm install -g pnpm@9
       pnpm add -D @playwright/test@1.49.0 playwright@1.49.0
       pnpm add -D -E         # ensure --save-exact lockfile behavior
       pnpm exec playwright install chromium   # download chromium browser binary (~150 MB; cached in ~/.cache/ms-playwright)
       ```
       Pin Playwright to `1.49.0` exactly (no caret/tilde) — D-CONTEXT does not specify but our `--save-exact` invocation per CONVENTIONS.md anti-drift posture matches the Astro `~5.x.y` pinning ethos from research/STACK.md §"Single biggest version risk".

       Note: `pnpm add` will create `pnpm-lock.yaml`. Commit it. Do NOT add `pnpm-lock.yaml` to .gitignore (already excluded by Plan 02-01's `.gitignore` section if mistakenly added — verify and remove if so).

    2. Create `tests/playwright.config.ts` with:
       - Top comment header: "Playwright config — Phase 2 INF-04. Chromium-only per D-15. 0.1% pixel threshold per D-16. baseURL is the CF Pages preview URL — CI passes PREVIEW_URL env var per D-31; local default is the production-branch URL."
       - `import { defineConfig, devices } from '@playwright/test'`
       - `export default defineConfig({ ... })` with:
         - `testDir: './tests'`
         - `testMatch: '**/*.spec.ts'`
         - `fullyParallel: true`
         - `workers: 4`
         - `reporter: 'list'`
         - `use: { baseURL: process.env.PREVIEW_URL || 'https://realufo.pages.dev', trace: 'off', screenshot: 'off', video: 'off' }`
         - `projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }]`
         - `expect: { toHaveScreenshot: { maxDiffPixelRatio: 0.001, threshold: 0.1, animations: 'disabled' } }`
         - `snapshotPathTemplate: 'tests/visual-baselines/{arg}{ext}'`  (overrides default `*.spec.ts-snapshots/` subfolder so PNGs land directly under `tests/visual-baselines/`)

    3. Create `scripts/capture-baselines.py` following CONVENTIONS.md §Python style:
       - Module docstring describing: purpose (INF-04 baseline freeze per D-12), inputs (live https://realufo.org), outputs (60 PNGs at `tests/visual-baselines/`), invariant (D-17 — operator-only regen; running this script overwrites existing baselines; CI MUST NEVER call it).
       - `from __future__ import annotations`
       - Stdlib imports: `argparse`, `os`, `subprocess`, `sys`, `pathlib.Path`
       - Third-party: `from playwright.sync_api import sync_playwright` (matches research/STACK.md §"Astro 5 builds 91k pages in ~18 min" environment — Python Playwright bindings live in the same chromium install pnpm just made; if Python's `playwright` module is not installed, capture script exits 2 with `pip install playwright` instruction)
       - Constants:
         - `REPO = pathlib.Path(__file__).resolve().parent.parent`
         - `ARCHIVES = [('wargov', '/'), ('aaro', '/aaro/'), ('nasa', '/nasa/'), ('nara', '/nara/'), ('geipan', '/geipan/'), ('uk', '/uk/'), ('brazil', '/brazil/'), ('chile', '/chile/'), ('argentina', '/argentina/'), ('canada', '/canada/'), ('italy', '/italy/'), ('nz', '/nz/'), ('peru', '/peru/'), ('spain', '/spain/'), ('uruguay', '/uruguay/')]`
         - `VIEWPORTS = [(360, 800), (768, 1024), (1024, 768), (1440, 900)]`  (per D-14)
         - `DEFAULT_BASE = 'https://realufo.org'`  (per D-12 — explicitly NOT the preview URL, NOT localhost)
         - `OUTPUT_DIR = REPO / 'tests' / 'visual-baselines'`
       - CLI flags (use argparse — match snapshot-urls.py pattern):
         - `--archive <slug>` — capture one archive only (all 4 viewports)
         - `--viewport <width>` — capture all archives at one viewport
         - `--check` — verify every expected PNG exists; exit 1 on missing; do not capture
         - `--base-url <url>` — default `https://realufo.org`
         - `--full-page` — default false; baselines are above-the-fold only to avoid 3+ MB PNGs on geipan (3334 records inline). Above-the-fold = the configured viewport height.
       - `def main()` flow:
         - Parse args
         - If `--check`: walk OUTPUT_DIR, assert every `{archive}-{width}.png` exists with size > 0, print summary, return 0/1
         - Else: launch chromium headless, iterate archives × viewports, for each:
           - `page.set_viewport_size({'width': w, 'height': h})`
           - `page.goto(base_url + path, wait_until='networkidle', timeout=30000)`
           - Wait for body visible: `page.wait_for_selector('body', state='visible')`
           - Sleep 500 ms to let carousel autoplay land on slide 1 (CLAUDE.md §3 hero carousel has `setInterval(goTo, 6500)` — first interval doesn't fire until 6.5 s; 500 ms ensures slide 1 is stable)
           - Capture: `page.screenshot(path=OUTPUT_DIR / f'{slug}-{w}.png', full_page=args.full_page)`
           - Print: `f'  [ok]  {slug}-{w}.png ({size_bytes} B)'` (CONVENTIONS.md §"Shell Conventions" — indented status with `[ok]` bracket, used here in Python too for consistency with snapshot-urls.py)
         - Print summary count
       - Exit codes: 0 success, 1 `--check` found gaps, 2 dependency missing (no playwright module installed) — matches snapshot-urls.py
       - Make executable: `chmod +x scripts/capture-baselines.py`

    4. Create `tests/visual-baselines/README.md` (the D-17 runbook — committed alongside the PNGs):
       - `# Visual baselines — Phase 2 INF-04`
       - `## Purpose` — these 60 PNGs are the frozen contract; the CI gate in `.github/workflows/quality-gates.yml` (Plan 02-08) compares the CF Pages preview against them; >0.1% pixel diff hard-fails the PR (D-16).
       - `## D-17 invariant — NEVER auto-regen`
         - Re-running `scripts/capture-baselines.py` overwrites these PNGs — that is intentional, ONLY for operator-initiated visual-change acknowledgments. CI workflows MUST NEVER invoke this script. PR reviewers MUST NEVER suggest "just regen the baseline" without operator approval.
         - The explicit recapture procedure (when an intentional visual change ships in Phase 4):
           1. Operator deletes the affected PNG: `rm tests/visual-baselines/<archive>-<viewport>.png`
           2. Operator runs: `python3 scripts/capture-baselines.py --archive <slug>`  (or --viewport for a global change)
           3. Operator inspects the new PNG visually before staging
           4. Operator commits with message: `test(visual): intentional baseline recapture for <archive> — <reason>`
           5. PR description must reference the source-of-truth change (CLAUDE.md §3.1 update, Phase 4 PR that introduced the change, etc.)
       - `## Capture source` — D-12: live https://realufo.org production GH Pages origin. NOT the CF Pages preview. NOT local dev server. Rationale: pixel-true to what users see today; CSS fonts from Google Fonts must render identically.
       - `## File naming` — `<archive_slug>-<viewport_width>.png`. 15 archives × 4 widths = 60 files (D-13).
       - `## Size budget` — D-13: raw PNG, not LFS, ~50-200 KB each, ~6 MB total. If any single PNG exceeds 500 KB, operator should use `--full-page=false` (default) — only above-the-fold capture is required for INF-04.

    5. Add `tests/visual-baselines/.gitkeep` so the directory is tracked even before Task 2 captures fill it (avoids "directory not found" failures in the test runner).

    Commit before Task 2:
    ```
    git add package.json pnpm-lock.yaml tests/playwright.config.ts scripts/capture-baselines.py tests/visual-baselines/.gitkeep tests/visual-baselines/README.md
    git commit -m "infra(02-03a): install Playwright + capture-baselines.py + visual-baselines/ scaffold"
    git push origin ssg-migration
    ```
  </action>
  <verify>
    <automated>
      test -f package.json \
        && python3 -c "import json; d=json.load(open('package.json')); assert '@playwright/test' in d.get('devDependencies', {}), d.get('devDependencies'); assert d['devDependencies']['@playwright/test']=='1.49.0', d['devDependencies']['@playwright/test']" \
        && test -f pnpm-lock.yaml \
        && test -f tests/playwright.config.ts \
        && grep -q "maxDiffPixelRatio: 0.001" tests/playwright.config.ts \
        && grep -q "name: 'chromium'" tests/playwright.config.ts \
        && grep -q "process.env.PREVIEW_URL" tests/playwright.config.ts \
        && test -x scripts/capture-baselines.py \
        && grep -q 'DEFAULT_BASE.*realufo.org' scripts/capture-baselines.py \
        && grep -q "ARCHIVES = " scripts/capture-baselines.py \
        && grep -q '(360, 800)' scripts/capture-baselines.py \
        && grep -q '(1440, 900)' scripts/capture-baselines.py \
        && test -f tests/visual-baselines/README.md \
        && grep -q 'D-17' tests/visual-baselines/README.md \
        && grep -q 'NEVER auto-regen' tests/visual-baselines/README.md
    </automated>
  </verify>
  <done>
    - Playwright 1.49.0 installed as a pinned dev dep
    - `tests/playwright.config.ts` enforces 0.1% threshold + chromium-only + PREVIEW_URL env override
    - `scripts/capture-baselines.py` is executable and follows CONVENTIONS.md §Python style (argparse, exit codes 0/1/2, indented status output)
    - `tests/visual-baselines/README.md` documents D-17 verbatim
    - `pnpm-lock.yaml` committed
    - No PNGs yet — Task 2 captures them
  </done>
</task>

<task type="auto">
  <name>Task 2: Capture 60 baseline PNGs + write visual-regression.spec.ts</name>
  <read_first>
    - tests/playwright.config.ts (just created — confirm baseURL handling and snapshotPathTemplate)
    - scripts/capture-baselines.py (just created — this is what we'll run)
    - .planning/phases/02-infrastructure-ci-scaffolding/02-CONTEXT.md §decisions D-12, D-13, D-14, D-16, D-17
    - CLAUDE.md §2 (archive slug list — used by spec.ts iteration)
    - CLAUDE.md §3.1 (tone colours per archive — informs Plan 02-06's spec, not this one, but reading the slug list together prevents drift)
  </read_first>
  <files>tests/visual-baselines/*.png, tests/visual-regression.spec.ts</files>
  <action>
    1. Install Python Playwright bindings (the capture script's only third-party dep):
       ```
       pip install playwright==1.49.0
       playwright install chromium       # may be a no-op if pnpm install already cached it
       ```

    2. Capture all 60 baselines against LIVE realufo.org (D-12):
       ```
       python3 scripts/capture-baselines.py
       ```
       Expected output: 60 lines of `  [ok]  <slug>-<width>.png (<bytes> B)` followed by a summary `wrote 60 baselines`. Total runtime ~3-5 min (15 archives × 4 viewports × ~2-4 s per capture with networkidle wait).

       If any archive fails (e.g. network blip mid-capture), retry just that archive:
       ```
       python3 scripts/capture-baselines.py --archive <slug>
       ```

       Verify all 60 PNGs exist and are non-zero:
       ```
       python3 scripts/capture-baselines.py --check
       ```
       Must print `60 / 60 baselines present` and exit 0.

       Sanity-check file sizes (each PNG should be 30-500 KB; above 1 MB suggests `--full-page` was accidentally enabled):
       ```
       find tests/visual-baselines -name '*.png' -size +1M
       ```
       Must return empty. If not, re-capture without --full-page.

    3. Write `tests/visual-regression.spec.ts` (TypeScript — Playwright test runner native shape):
       - Top comment block: "Visual regression spec — Phase 2 INF-04. Runs against process.env.PREVIEW_URL (CF Pages preview per D-31). Compares against tests/visual-baselines/*.png frozen per D-17. 0.1% pixel-diff hard-fails per D-16. 60 tests (15 archives × 4 viewports) per D-13."
       - `import { test, expect } from '@playwright/test'`
       - Const `const ARCHIVES: Array<[string, string]> = [ ['wargov', '/'], ['aaro', '/aaro/'], ... 15 total ... ]`  (same list as capture-baselines.py for parity)
       - Const `const VIEWPORTS: Array<[number, number]> = [[360, 800], [768, 1024], [1024, 768], [1440, 900]]`  (same as D-14)
       - For each archive × viewport: `test('${slug} @ ${w}x${h}', async ({ page }) => { await page.setViewportSize({ width: w, height: h }); await page.goto('${path}', { waitUntil: 'networkidle' }); await page.waitForTimeout(500); await expect(page).toHaveScreenshot('${slug}-${w}.png'); });`
       - The screenshot name `${slug}-${w}.png` MUST match the capture filename so the snapshotPathTemplate config wire-up resolves to the same file.

    4. Run the spec locally pointing at production GH Pages (NOT preview yet — that's wired in Plan 02-08; this is a sanity test that the spec evaluates correctly against the same source it was captured from):
       ```
       PREVIEW_URL=https://realufo.org pnpm exec playwright test --config=tests/playwright.config.ts
       ```
       Expected: 60 tests pass, 0 fail. If failures occur, the baselines vs the production site have drifted within the capture window (CDN refresh, hero carousel slide rotation, etc.); diagnose by viewing the diff PNGs in `test-results/` and re-capture only the affected archive(s).

       Note: This is a development-time sanity check. The CI gate in Plan 02-08 will set PREVIEW_URL to the CF Pages preview, where the test SHOULD pass too (D-37: ssg-migration is the bare repo, no SSG changes yet — Phase 2 preview content equals main content).

    5. Commit:
       ```
       git add tests/visual-baselines/*.png tests/visual-regression.spec.ts
       git commit -m "infra(02-03b): capture 60 baselines from live realufo.org + visual-regression spec"
       git push origin ssg-migration
       ```
  </action>
  <verify>
    <automated>
      test "$(find tests/visual-baselines -name '*.png' -type f | wc -l | tr -d ' ')" = "60" \
        && test -z "$(find tests/visual-baselines -name '*.png' -size 0)" \
        && test -z "$(find tests/visual-baselines -name '*.png' -size +1M)" \
        && python3 scripts/capture-baselines.py --check \
        && test -f tests/visual-regression.spec.ts \
        && grep -q 'toHaveScreenshot' tests/visual-regression.spec.ts \
        && grep -q "VIEWPORTS" tests/visual-regression.spec.ts \
        && test "$(grep -cE '\[.{3,12}.,.\s.\/' tests/visual-regression.spec.ts)" -ge "15"
    </automated>
  </verify>
  <done>
    - 60 PNGs exist under `tests/visual-baselines/` (15 archives × 4 viewports per D-13/D-14)
    - Every PNG is non-zero and ≤ 1 MB
    - `python3 scripts/capture-baselines.py --check` exits 0 with "60 / 60 baselines present"
    - `tests/visual-regression.spec.ts` defines 60 tests (15 × 4)
    - Local run `PREVIEW_URL=https://realufo.org pnpm exec playwright test` returns 60 / 60 pass
    - Commit on ssg-migration includes all PNGs (raw, not LFS, per D-13)
    - INF-04 fully satisfied: baselines exist on disk + spec compares against them + threshold 0.1% wired
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Capture script ↔ live realufo.org | Read-only HTTP traffic to public site — same trust surface as any user browser. |
| Playwright runtime ↔ chromium browser | Local headless chromium; no remote code execution surface. |
| Spec runtime ↔ CF Pages preview URL | Read-only, traverses same trust boundary as a user. |
| Repo ↔ committed baseline PNGs | PNGs are public — no PII captured (above-the-fold archive pages only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | Auto-regen of baselines silently legitimizing a visual regression | mitigate | D-17 invariant codified in `tests/visual-baselines/README.md`; quality-gates.yml (Plan 02-08) will NOT invoke `capture-baselines.py`; reviewer checklist gate. |
| T-02-10 | Tampering | Supply chain — Playwright npm package | mitigate | Pinned to exact version `1.49.0` (no caret/tilde). `pnpm-lock.yaml` committed for reproducible installs. Per research/STACK.md HIGH-confidence Playwright source. |
| T-02-11 | Information disclosure | Baseline PNGs leaking authenticated content | accept | Capture script visits only public anonymous archive pages; no session/auth involved. Site is public-domain archive (CLAUDE.md §9). |
| T-02-SC | Tampering | npm/pnpm installs (Playwright + chromium binary) | mitigate | Playwright is widely-trusted Microsoft project (LEGITIMATE per package-legitimacy gate — not [ASSUMED]/[SUS]). No blocking-human checkpoint required. |
</threat_model>

<verification>
1. `pnpm list @playwright/test` shows version `1.49.0` (exact pin)
2. `find tests/visual-baselines -name '*.png' | wc -l` returns 60
3. `python3 scripts/capture-baselines.py --check` exits 0
4. `PREVIEW_URL=https://realufo.org pnpm exec playwright test --config=tests/playwright.config.ts` reports 60 passed
5. `git log --oneline ssg-migration -- tests/visual-baselines/` shows commits authored by Claude (not auto-generated from CI)
6. `grep -r "tests/visual-baselines" .github/workflows/` returns NO match (no workflow yet invokes the capture script — D-17 honored)
</verification>

<success_criteria>
- INF-04 satisfied: 60 PNG baselines committed; Playwright spec evaluates them against PREVIEW_URL with 0.1% threshold; D-17 invariant documented + structurally enforced
- D-12, D-13, D-14, D-15, D-16, D-17 all honored
- No SSG code introduced (D-38)
- No DNS or domain changes (D-37)
- Future plans: 02-06 reuses `tests/playwright.config.ts`; 02-08 wires the spec into `.github/workflows/quality-gates.yml` with `PREVIEW_URL` set from PR `deployment_status`
</success_criteria>

<output>
Create `.planning/phases/02-infrastructure-ci-scaffolding/02-03-SUMMARY.md` recording:
- Playwright version + pnpm-lock hash
- Capture timestamp (when the 60 PNGs were taken from realufo.org)
- Total size of `tests/visual-baselines/` (committed bytes — should be 3-10 MB)
- The D-17 recapture runbook URL: `tests/visual-baselines/README.md`
- Note for Plan 02-06 (tone-colour + JS-off specs): config + chromium browser are already installed; just `import { test, expect } from '@playwright/test'`
- Note for Plan 02-08 (quality-gates.yml): the `visual-regression` job runs `pnpm exec playwright test tests/visual-regression.spec.ts --config=tests/playwright.config.ts` with `PREVIEW_URL` from PR deployment_status payload
</output>

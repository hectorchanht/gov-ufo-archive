---
phase: 02-infrastructure-ci-scaffolding
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - .planning/decisions/workers-paid.md
autonomous: false
requirements: [INF-03]
user_setup:
  - service: cloudflare-workers
    why: "Activate Workers Paid plan ($5/mo) so Phase 5's 15-min CPU cron budget is available. Free plan's 10 ms CPU cap is unusable for HTML parsing (research/STACK.md §'Critical: Don't try this on the Free plan')."
    env_vars: []
    dashboard_config:
      - task: "Upgrade Workers plan from Free → Paid in CF dashboard"
        location: "Cloudflare dashboard → Workers & Pages → Plans → Paid → Subscribe"

must_haves:
  truths:
    - "Cloudflare Workers Paid plan is active on account f1868a071996e836eae6da2b65f37929 (D-05)"
    - "A $5/mo line item is visible in CF billing (ROADMAP Phase 2 SC#3)"
    - "Phase 2 verification that Paid CPU cap (15 min cron) is live happens before Phase 5 architecture commits (D-05)"
    - "Activation gated Phase 2 completion — no API workaround exists (D-06, D-36)"
    - "No Workers code or wrangler.toml lands in this phase — Phase 5 owns that (D-04)"
    - "No DNS or domain config changes — D-37 invariant honored"
  artifacts:
    - path: ".planning/decisions/workers-paid.md"
      provides: "Operator-recorded billing activation date, plan tier, projected monthly cost ceiling, and confirmation of Phase 5 CPU/subrequest budgets"
      contains: "Paid plan"
  key_links:
    - from: ".planning/decisions/workers-paid.md"
      to: "Phase 5 scrape architecture decisions"
      via: "documented Paid-plan limits feed PMS-03 spike outcome → Phase 5 plan"
      pattern: "15.min|10000"
---

<objective>
Operator-only checkpoint plan: activate Cloudflare Workers Paid plan ($5/mo) and record the activation in `.planning/decisions/workers-paid.md`. No Workers code lands — Phase 5 owns Worker authorship.

Purpose: INF-03. Per D-05 + D-06, billing requires UI interaction (no public API allows plan activation without a session token), so this is a pure `checkpoint:human-action`. Activating now (not deferring to Phase 5 start) lets the Phase 2 ssg-migration branch verify CPU/subrequest limits are live before Phase 5 architecture commits.

Output: `.planning/decisions/workers-paid.md` capturing activation date, plan tier, billing line-item confirmation, and the Paid-plan limit reference table from research/STACK.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-infrastructure-ci-scaffolding/02-CONTEXT.md
@.planning/research/STACK.md
@.planning/phases/01-pre-migration-safety/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Operator activates Workers Paid plan</name>
  <read_first>
    - .planning/phases/02-infrastructure-ci-scaffolding/02-CONTEXT.md §decisions D-05, D-06, D-36
    - .planning/research/STACK.md §"Cloudflare Workers" + §"Limits to design within (Paid plan, $5/mo)" + §"Critical: Don't try this on the Free plan"
    - .planning/phases/01-pre-migration-safety/01-03-SUMMARY.md (Akamai spike results — informs Phase 5 hybrid architecture; Workers Paid is the precondition)
  </read_first>
  <what-built>
    Nothing automated. Plan activation in Cloudflare requires a logged-in browser session against the operator's billing account. No public CLI / API allows a third party (or Claude) to subscribe to a paid Workers plan. CONTEXT.md §deferred explicitly lists "Workers Paid plan auto-activation via API — not possible; billing requires UI interaction".
  </what-built>
  <how-to-verify>
    Operator performs these steps in the CF dashboard:

    1. Visit https://dash.cloudflare.com/f1868a071996e836eae6da2b65f37929/workers-and-pages
    2. Click "Plans" in the left nav (under Workers & Pages)
    3. Confirm current plan shows "Free"
    4. Click "Upgrade" or "Subscribe" next to "Workers Paid — $5/month"
    5. Complete billing flow (payment method on file required; Cloudflare bills monthly)
    6. After completion, verify:
       - The Plans page now shows "Workers Paid" as the active plan
       - Billing → Subscriptions shows a new "Workers Paid" line item with the activation date
       - Workers limits page (https://dash.cloudflare.com/<account>/workers-and-pages/limits or the per-Worker Settings → Usage panel) reflects the Paid quotas (15-min CPU on cron, 10k subrequests/invocation, 128 MB memory, 10 MB script size — verify against research/STACK.md table)
    7. Copy the activation date (UTC) and the next-billing-date from the Subscriptions page.

    Operator does NOT create any Workers in this plan — Phase 5 plan 05-NN will be the first Worker. D-04 + D-38 (no premature commits) hold; this checkpoint is purely the billing activation.
  </how-to-verify>
  <resume-signal>
    Paste output:
    ```
    Plan: Workers Paid
    Activation date (UTC): YYYY-MM-DDTHH:MMZ
    Next billing date: YYYY-MM-DD
    Confirmed limits visible in dashboard:
      - CPU time per cron invocation: 15 min
      - Subrequests per invocation: 10,000
      - Memory: 128 MB
      - Worker script size: 10 MB (compressed)
    ```
    Or describe the blocker (no payment method on file, billing region restrictions, account-not-eligible error).
  </resume-signal>
  <acceptance_criteria>
    - Operator-supplied activation timestamp + next-billing-date are recorded
    - Operator confirms the four Paid-plan limits visible in dashboard match research/STACK.md table
    - No new Workers exist on the account (Phase 5 territory)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Record Workers Paid activation in decision doc</name>
  <read_first>
    - .planning/phases/02-infrastructure-ci-scaffolding/02-CONTEXT.md §decisions D-05, D-06, D-36
    - .planning/research/STACK.md §"Limits to design within (Paid plan, $5/mo)" (the exact limit table is the fixture)
    - .planning/decisions/dns-ttl.md and .planning/decisions/akamai-spike.md if they exist (existing decision-doc style references; recent Phase 1 work used this folder)
  </read_first>
  <files>.planning/decisions/workers-paid.md</files>
  <action>
    On ssg-migration branch (D-33 — no main commits this phase, per D-37).

    Create `.planning/decisions/workers-paid.md` with these sections, using operator values from Task 1's resume signal:

    1. `# Workers Paid activation — Phase 2 INF-03`
    2. `## Activation` — timestamp (UTC), next billing date, account id (`f1868a071996e836eae6da2b65f37929`), plan name (`Workers Paid`)
    3. `## Why now (D-05, D-06)` — paraphrase from CONTEXT.md: Free's 10 ms CPU cap unusable; activate at Phase 2 to verify Paid CPU/subrequest limits live before Phase 5 architecture commits; billing requires UI interaction so this is a non-skippable operator checkpoint; without it Phase 5 cannot start.
    4. `## Confirmed Paid-plan limits` — table verbatim from research/STACK.md §"Limits to design within (Paid plan, $5/mo)":
       | Limit | Value | Phase 5 headroom |
       |---|---|---|
       | CPU time per cron invocation | 15 min | Plenty (HTML parse + diff is sub-second per source) |
       | Wall clock | 15 min | Plenty |
       | Subrequests per invocation | 10,000 (configurable to 10M) | 15 sources × ~50 fetches = 750. Comfortable |
       | Memory | 128 MB | Plenty — stream binaries, don't buffer |
       | Worker size (compressed) | 10 MB gzip | Octokit + papaparse ~1 MB. Comfortable |
       | Cron triggers per account | 250 paid | More than needed |
    5. `## Cost ceiling` — projected $5/mo flat (no metered overage projected for Phase 5 scope per research/STACK.md). Operator commits to budget alert at $20/mo (per research/PITFALLS.md Pitfall #10 §"Budget guardrails").
    6. `## Phase 2 invariants honored` — verbatim:
       - D-05: Operator activates ASAP; not deferred to Phase 5
       - D-06: Surfaced as checkpoint:human-action (this plan)
       - D-04: No wrangler-based GH-Actions deploy step — Wrangler reserved for Workers cron in Phase 5
       - D-36: Workers Paid activation is the ONLY operator action gating Phase 2 completion (alongside 02-01's CF Pages creation)
       - D-37: No DNS or domain config touched — confirmed
       - D-38: No SSG code committed
    7. `## Future-phase hooks` — Phase 5 references this doc when scaffolding the first cron Worker; the activation date pins the "≥ 14 days kill-switch counter" runs in parallel (Phase 1 PMS-02 dependency for Phase 6 cutover).
    8. `## Out of scope for this plan` — Workers code, wrangler.toml, KV namespaces, R2 buckets. All deferred to Phase 5 plans.

    Commit on ssg-migration:
    ```
    git add .planning/decisions/workers-paid.md
    git commit -m "infra(02-02): record Workers Paid activation (INF-03)"
    git push origin ssg-migration
    ```
  </action>
  <verify>
    <automated>
      test -f .planning/decisions/workers-paid.md \
        && grep -q 'Workers Paid' .planning/decisions/workers-paid.md \
        && grep -q 'D-05' .planning/decisions/workers-paid.md \
        && grep -q 'D-06' .planning/decisions/workers-paid.md \
        && grep -q 'D-36' .planning/decisions/workers-paid.md \
        && grep -q '15 min' .planning/decisions/workers-paid.md \
        && grep -q '10,000' .planning/decisions/workers-paid.md \
        && grep -q 'f1868a071996e836eae6da2b65f37929' .planning/decisions/workers-paid.md \
        && git log -1 --format=%s ssg-migration | grep -q 'infra(02-02)' \
        && ! git log main..ssg-migration --format=%H -- main 2>/dev/null | grep -q .
    </automated>
  </verify>
  <done>
    - `.planning/decisions/workers-paid.md` exists on ssg-migration with all 8 sections
    - Activation timestamp matches operator's CF dashboard
    - Limit table mirrors research/STACK.md §"Limits to design within (Paid plan, $5/mo)" verbatim
    - Commit landed on ssg-migration only (D-33 + D-37)
    - INF-03 is satisfied: a $5/mo Workers Paid line item is verifiably visible in CF billing
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser ↔ Cloudflare billing portal | Operator authenticates via CF SSO; payment-method-on-file is required. No public API path. |
| Account billing ↔ Workers runtime | After activation, every Worker invocation under this account is governed by Paid quotas. No code lands this plan — only the billing state changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Denial of service | Workers Paid metered overage if a Phase 5 Worker misbehaves | accept | Phase 5 plans will implement budget alerts + KV cron-lock per research/PITFALLS.md Pitfall #10. Phase 2 only activates the plan; no Workers exist yet to misbehave. |
| T-02-07 | Information disclosure | Billing state visible to operator only | accept | CF dashboard requires authentication; activation log lives in private `.planning/decisions/` doc (committed but inside the public repo — no PII recorded, only plan tier + dates). |
| T-02-08 | Tampering | Plan downgrade reverting Phase 5 capacity | mitigate | Operator's responsibility (monthly billing decision). Phase 5 plans will codify "Workers Paid must be active" as a precondition check. |
</threat_model>

<verification>
1. CF dashboard → Workers & Pages → Plans shows "Workers Paid" as active
2. CF billing → Subscriptions shows a $5/mo Workers Paid line item with activation date
3. `.planning/decisions/workers-paid.md` exists with all required sections
4. No Workers exist on the account yet (Phase 5 territory)
5. No commit on main (D-37)
</verification>

<success_criteria>
- INF-03 satisfied: Workers Paid plan active, $5/mo billing line confirmed
- D-05, D-06, D-36 invariants documented
- No SSG code, no Workers code, no DNS changes (D-37 + D-38 honored)
- `.planning/decisions/workers-paid.md` is the canonical reference for Phase 5 plans to cite
</success_criteria>

<output>
Create `.planning/phases/02-infrastructure-ci-scaffolding/02-02-SUMMARY.md` recording:
- Activation timestamp + next billing date (verbatim from operator)
- Confirmed limit table (one-line per limit)
- Note for Phase 5 planner: precondition check should `cat .planning/decisions/workers-paid.md | grep "Plan: Workers Paid"` before scaffolding the first Worker
- Note that no Workers / wrangler.toml exist yet — Phase 5 owns the first wrangler.toml commit
</output>

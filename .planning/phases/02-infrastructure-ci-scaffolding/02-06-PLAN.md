---
phase: 02-infrastructure-ci-scaffolding
plan: 06
type: execute
wave: 3
depends_on: [02-01, 02-03]
files_modified:
  - tests/tone-colours.spec.ts
  - tests/js-off.spec.ts
  - tests/tone-colours-fixture.json
autonomous: true
requirements: [INF-06, INF-07]

must_haves:
  truths:
    - "tone-colours.spec.ts asserts each archive's getComputedStyle(document.documentElement).getPropertyValue('--caution') matches CLAUDE.md §3.1 table verbatim (D-22)"
    - "Tone colour fixture (tests/tone-colours-fixture.json) is sourced verbatim from CLAUDE.md §3.1 — 15 entries"
    - "tone-colours test runs in Playwright chromium (re-uses 02-03 config) — one assertion per archive (D-23) — hard fail"
    - "js-off.spec.ts uses browserContext with javaScriptEnabled: false (D-24) — runs against each archive page"
    - "Each archive page must show >=1 card visible AND >=1 heading visible AND no 'Enable JavaScript' / 'loading' placeholder dominates (D-24)"
    - "JS-off test is hard fail (D-25) — required because Phase 3 SSG must pre-render cards not hydrate"
    - "Both specs read baseURL via process.env.PREVIEW_URL (set by quality-gates.yml from PR deployment_status per D-31)"
    - "No SSG code (D-38); no DNS/domain changes (D-37)"
  artifacts:
    - path: "tests/tone-colours.spec.ts"
      provides: "15 assertions: each archive's --caution CSS variable === CLAUDE.md §3.1 value"
      contains: "getComputedStyle"
    - path: "tests/js-off.spec.ts"
      provides: "15 assertions: each archive renders meaningful cards + headings with JS disabled"
      contains: "javaScriptEnabled"
    - path: "tests/tone-colours-fixture.json"
      provides: "Locked CLAUDE.md §3.1 tone-colour table — fixture for tone-colours.spec.ts"
      contains: "wargov"
  key_links:
    - from: "tests/tone-colours.spec.ts"
      to: "CLAUDE.md §3.1"
      via: "tests/tone-colours-fixture.json"
      pattern: "tone-colours-fixture.json"
    - from: "tests/js-off.spec.ts"
      to: "javaScriptEnabled: false"
      via: "Playwright browser.newContext({ javaScriptEnabled: false })"
      pattern: "javaScriptEnabled"
---

<objective>
Write two Playwright specs that run against any base URL (CF Pages preview by default):
1. `tone-colours.spec.ts` — asserts each of the 15 archives serves the correct `--caution` CSS variable per CLAUDE.md §3.1 (D-22, D-23)
2. `js-off.spec.ts` — asserts each archive renders meaningful card + heading content with JavaScript disabled (D-24, D-25)

Both specs are HARD-FAIL gates per D-23 + D-25. Per D-25, the JS-off test is non-negotiable because Phase 3's SSG output must pre-render cards (not hydrate from JSON) — failing this gate in Phase 4 blocks the migration PR per PROJECT.md "Pre-rendered cards, no hydration" and research/PITFALLS.md §Pitfall #9.

Purpose: INF-06 (tone) + INF-07 (JS-off). Per research/PITFALLS.md §Pitfall #7 (cross-archive drift), one `--caution` typo in archive 14 of 15 produces a 95%-right page (e.g. Spain blue instead of gold) that visual-regression alone may miss. The tone-colour smoke test catches it in <100 ms per archive.

Output: 2 spec files + 1 locked fixture file. No SSG code (D-38). No new Playwright config — reuses Plan 02-03's `tests/playwright.config.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-infrastructure-ci-scaffolding/02-CONTEXT.md
@.planning/phases/02-infrastructure-ci-scaffolding/02-01-SUMMARY.md
@.planning/phases/02-infrastructure-ci-scaffolding/02-03-SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md
@tests/playwright.config.ts

<interfaces>
<!-- Tone-colour fixture (tests/tone-colours-fixture.json) — verbatim from CLAUDE.md §3.1 -->
<!-- NOTE on TS typing (W3 revision): the fixture object has a `_metadata` key + 15 archive slug keys. -->
<!-- TS infers a narrow object literal type from the JSON import; indexing as `fixture[slug]` with -->
<!-- `slug: string` fails strict type-check. Two acceptable patterns: -->
<!--   (a) cast at import:  `const fixture = (rawFixture as Record<string, string>)` -->
<!--   (b) cast at access:  `(fixture as Record<string, string>)[slug]` -->
<!-- The spec below uses (b). The `_metadata` key remains in the JSON; iteration is driven by the -->
<!-- hard-coded ARCHIVES list, not by Object.keys(fixture), so the metadata never collides. -->
{
  "wargov":    "#d4a017",
  "aaro":      "#4a9eff",
  "nasa":      "#fc3d21",
  "nara":      "#cbd5e1",
  "geipan":    "#0055a4",
  "uk":        "#012169",
  "brazil":    "#009c3b",
  "chile":     "#d52b1e",
  "nz":        "#5b8def",
  "canada":    "#ff6b6b",
  "argentina": "#74acdf",
  "uruguay":   "#3ba0d8",
  "peru":      "#ff6b6b",
  "spain":     "#f4c542",
  "italy":     "#5cb85c"
}

<!-- Spec shape: tone-colours.spec.ts -->
import { test, expect } from '@playwright/test';
import * as fixture from './tone-colours-fixture.json';

const ARCHIVES: Array<[string, string]> = [
  ['wargov', '/'], ['aaro', '/aaro/'], /* …15 total… */
];

for (const [slug, path] of ARCHIVES) {
  test(`tone colour for ${slug}`, async ({ page }) => {
    await page.goto(path);
    const caution = await page.evaluate(() =>
      getComputedStyle(document.documentElement).getPropertyValue('--caution').trim()
    );
    // W3 revision: cast at access site so `_metadata` key + slug keys coexist without TS narrowing fail
    const expected = ((fixture as unknown) as Record<string, string>)[slug];
    expect(caution.toLowerCase()).toBe(expected.toLowerCase());
  });
}

<!-- Spec shape: js-off.spec.ts (uses fresh context per test for isolation) -->
import { test, expect } from '@playwright/test';

test.describe('JS-off rendering — each archive renders cards with JavaScript disabled', () => {
  const ARCHIVES: Array<[string, string]> = [ /* …15… */ ];
  for (const [slug, path] of ARCHIVES) {
    test(`${slug} renders without JS`, async ({ browser }) => {
      const context = await browser.newContext({ javaScriptEnabled: false });
      const page = await context.newPage();
      await page.goto(path, { waitUntil: 'domcontentloaded' });
      // D-24 assertions
      const cards = await page.locator('article, .card, .arch-grid > *').count();
      expect(cards).toBeGreaterThan(0);
      const headings = await page.locator('h1, h2').count();
      expect(headings).toBeGreaterThan(0);
      const body = (await page.textContent('body')) || '';
      const bodyTrim = body.trim();
      expect(bodyTrim).not.toMatch(/^(Enable JavaScript|Loading|Please wait)/i);
      await context.close();
    });
  }
});
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/tone-colours-fixture.json + tests/tone-colours.spec.ts</name>
  <read_first>
    - CLAUDE.md §3.1 (the 15-row tone-colour table — fixture source of truth)
    - .planning/phases/02-infrastructure-ci-scaffolding/02-CONTEXT.md §decisions D-22, D-23
    - .planning/research/PITFALLS.md §Pitfall #7 (cross-archive drift — Spain shows blue not gold)
    - tests/playwright.config.ts (just created in 02-03 — confirm baseURL + chromium-only project + workers count)
    - 1 archive index.html (any — e.g. aaro/index.html) to confirm the CSS variable structure: `--caution: <hex>` lives in :root or :root scope
    - .planning/codebase/CONVENTIONS.md §"Shared Design Tokens" (the :root block structure)
  </read_first>
  <files>tests/tone-colours-fixture.json, tests/tone-colours.spec.ts</files>
  <action>
    On ssg-migration branch.

    1. Create `tests/tone-colours-fixture.json` — verbatim from CLAUDE.md §3.1:
       - Top-level JSON object, one key per archive slug, value = lowercase hex string
       - Order matches CLAUDE.md §3.1 table (wargov first, italy last — but JSON object key order is irrelevant; just write in the table order for readability)
       - Include a header comment: NOT POSSIBLE in pure JSON — instead, add a `_metadata` key:
         ```json
         {
           "_metadata": {
             "source": "CLAUDE.md §3.1 tone-colour table",
             "locked_per": "D-22",
             "purpose": "Fixture for tests/tone-colours.spec.ts — must match :root --caution per archive"
           },
           "wargov":    "#d4a017",
           "aaro":      "#4a9eff",
           ...
         }
         ```
       - All 15 archives present. Values verbatim from CLAUDE.md §3.1 (re-read the §3.1 table inside CLAUDE.md to copy each hex precisely — do not transcribe from memory).

    2. Write `tests/tone-colours.spec.ts`:
       - Header comment: "Tone-colour smoke test — Phase 2 INF-06. Asserts :root --caution per archive matches CLAUDE.md §3.1 (via tests/tone-colours-fixture.json) per D-22. Hard-fail per D-23. Runs against process.env.PREVIEW_URL (Plan 02-08 wires this from PR deployment_status per D-31)."
       - `import { test, expect } from '@playwright/test'`
       - `import fixture from './tone-colours-fixture.json' assert { type: 'json' }`  (TS 5+ JSON import assertions; if `pnpm exec playwright test` chokes on `assert { type: 'json' }` because of older TS target, fall back to `import fixtureJson from './tone-colours-fixture.json'` after enabling `resolveJsonModule: true` + `esModuleInterop: true` in tsconfig — but Plan 02-03 didn't ship a tsconfig; Playwright runs `.spec.ts` files with its own ts-node bundling. Test the simple import first; if it fails, switch to `const fixture = JSON.parse(readFileSync(...))` pattern using `fs/promises`.)
       - **TS typing (W3 revision):** the fixture object has a `_metadata` key plus 15 archive slug keys. TS infers a narrow object-literal type from the JSON import; indexing as `fixture[slug]` with a `string`-typed slug variable fails strict type-check. Use one of:
         - Cast at import: `const fixture = (rawFixture as unknown) as Record<string, string>;` (if using namespaced/default import)
         - Cast at access site (recommended for this spec): `const expected = ((fixture as unknown) as Record<string, string>)[slug];`
         - The `_metadata` key is never touched because iteration is driven by the hard-coded ARCHIVES list (not `Object.keys(fixture)`), so the cast is safe.
       - Const `ARCHIVES: Array<[string, string]>` — same 15 entries as 02-03's visual-regression.spec.ts (wargov → `/`, aaro → `/aaro/`, etc.)
       - For each entry: emit a `test('tone colour for ${slug}', async ({ page }) => { ... })` block:
         - `await page.goto(path)`
         - `const actual = (await page.evaluate(() => getComputedStyle(document.documentElement).getPropertyValue('--caution'))).trim().toLowerCase()`
         - `const expected = (((fixture as unknown) as Record<string, string>)[slug]).toLowerCase()`  (per W3 cast pattern above)
         - `expect(actual, \`Archive ${slug} (${path}) — expected --caution=${expected}, got ${actual}. Check CLAUDE.md §3.1 vs the archive's :root CSS.\`).toBe(expected)`
       - Skip the `_metadata` key when iterating fixture: the iteration is driven by `ARCHIVES`, NOT by `Object.keys(fixture)`.

    3. Smoke test against live realufo.org (the same source the fixture was extracted from — should be 100% match):
       ```
       PREVIEW_URL=https://realufo.org pnpm exec playwright test tests/tone-colours.spec.ts --config=tests/playwright.config.ts
       ```
       Expected: 15 / 15 pass. If any fail, the archive's :root --caution drifted from CLAUDE.md §3.1 — diagnose by visiting the archive page in DevTools and inspecting `getComputedStyle(document.documentElement).getPropertyValue('--caution')`. If drift exists on main, that's a Phase 4 SSG fix (CLAUDE.md is the spec; the page must obey it); for Phase 2 purposes, the test correctly identifies the drift and the operator decides whether to (a) fix the page on main or (b) update the fixture to match observed reality. Default: fix the page; the fixture is locked to CLAUDE.md §3.1 per D-22.

    4. Smoke test against CF Pages preview:
       ```
       PREVIEW_URL=https://realufo.pages.dev pnpm exec playwright test tests/tone-colours.spec.ts --config=tests/playwright.config.ts
       ```
       Expected: 15 / 15 pass (Phase 2 preview content == main content per D-38).

    5. Commit (Task 2 also commits in this same plan; we can commit them together at the end):
       Leave staged for Task 2's combined commit.
  </action>
  <verify>
    <automated>
      test -f tests/tone-colours-fixture.json \
        && python3 -c "import json; d=json.load(open('tests/tone-colours-fixture.json')); assert d.get('wargov')=='#d4a017', d.get('wargov'); assert d.get('aaro')=='#4a9eff', d.get('aaro'); assert d.get('nasa')=='#fc3d21', d.get('nasa'); assert d.get('geipan')=='#0055a4', d.get('geipan'); assert d.get('uk')=='#012169', d.get('uk'); assert d.get('brazil')=='#009c3b', d.get('brazil'); assert d.get('spain')=='#f4c542', d.get('spain'); assert d.get('italy')=='#5cb85c', d.get('italy'); assert len([k for k in d if k != '_metadata'])==15, 'expected 15 archive entries'" \
        && test -f tests/tone-colours.spec.ts \
        && grep -q 'getComputedStyle' tests/tone-colours.spec.ts \
        && grep -q '\-\-caution' tests/tone-colours.spec.ts \
        && grep -q 'tone-colours-fixture' tests/tone-colours.spec.ts \
        && grep -qE 'Record<string,\s*string>' tests/tone-colours.spec.ts \
        && PREVIEW_URL=https://realufo.org pnpm exec playwright test tests/tone-colours.spec.ts --config=tests/playwright.config.ts > /tmp/tone-realufo.log 2>&1 \
        && grep -qE '15 passed' /tmp/tone-realufo.log
    </automated>
  </verify>
  <done>
    - `tests/tone-colours-fixture.json` exists with 15 archive entries + `_metadata` key, all hex values verbatim from CLAUDE.md §3.1
    - `tests/tone-colours.spec.ts` exists with 15 tests (one per archive) using `getComputedStyle().getPropertyValue('--caution')`
    - Fixture access uses `Record<string, string>` cast (at import OR at access site per W3) to coexist with the `_metadata` key without TS narrowing failure
    - All 15 tests pass against live realufo.org
    - All 15 tests pass against CF Pages preview
    - D-22, D-23 honored: one assertion per archive, hard-fail, CLAUDE.md §3.1 is the fixture
  </done>
</task>

<task type="auto">
  <name>Task 2: Write tests/js-off.spec.ts + commit both specs</name>
  <read_first>
    - .planning/phases/02-infrastructure-ci-scaffolding/02-CONTEXT.md §decisions D-24, D-25
    - .planning/research/PITFALLS.md §Pitfall #9 (Offline-first regression — JS-off renders empty <main>; PROJECT.md "Pre-rendered cards, no hydration" constraint)
    - .planning/research/ARCHITECTURE.md (pre-rendered cards architecture — Phase 3 will preserve; this gate catches regressions)
    - CLAUDE.md §4 (page architecture — `.arch-grid` contains cards; cards are server-rendered today)
    - tests/playwright.config.ts (the chromium-only project supports `browser.newContext({ javaScriptEnabled: false })`)
    - 1 sample archive (e.g. aaro/index.html) viewed in DevTools with JS disabled to confirm the expected DOM contains visible `<article>` or `.arch-grid > *` elements pre-hydration
  </read_first>
  <files>tests/js-off.spec.ts</files>
  <action>
    1. Write `tests/js-off.spec.ts`:
       - Header comment: "JS-off rendering test — Phase 2 INF-07. Asserts each archive renders cards + headings + no 'loading' placeholder with JavaScript disabled (D-24). Hard-fail per D-25 (UNCONDITIONAL — Plan 02-08 wires this WITHOUT continue-on-error). Phase 3 SSG MUST pre-render cards not hydrate from JSON (PROJECT.md core constraint; research/PITFALLS.md §Pitfall #9). Runs against process.env.PREVIEW_URL (Plan 02-08 wires from PR deployment_status per D-31)."
       - `import { test, expect } from '@playwright/test'`
       - Const `ARCHIVES: Array<[string, string]>` — same 15 entries as 02-03 + 02-06-task1
       - `test.describe('JS-off rendering — 15 archives', () => { ... })` block
       - For each entry: emit a `test('${slug} renders meaningfully without JS', async ({ browser }) => { ... })`:
         - `const context = await browser.newContext({ javaScriptEnabled: false })`
         - `const page = await context.newPage()`
         - `await page.goto(path, { waitUntil: 'domcontentloaded' })`  (NOT networkidle — with JS off, networkidle never fires reliably)
         - Card-count check (D-24 first assertion): card selector matches the current main HTML structure. Inspect aaro/index.html for the actual selector:
           - Today's archive HTML renders cards from inline JSON via `cardHtml(a)` (CONVENTIONS.md §"JavaScript invariants" — cards are CLIENT-rendered today). This is the regression the Phase 3 SSG fixes.
           - **D-25 is unconditional hard-fail.** The spec asserts the Phase 3+ invariant from day one. During Phase 2/3 the gate WILL fail for archives that still hydrate client-side — that failure is the signal that Phase 3 work for that archive is pending. It is NOT a reason to soften the gate. Plan 02-08 ships the js-off job WITHOUT `continue-on-error: true`.
           - Phase-3+ archive-port PRs make the gate green one archive at a time. The gate IS hard-fail from day one; the expected-fail count during Phase 2/3 simply reflects "N archives still hydrate; Phase 3 has N archives of work left."
         - Implementation of the assertions:
           - `const cards = await page.locator('article, .arch-grid > *, .card, .head-card').count()`
           - `expect(cards, \`${slug}: expected >=1 card visible with JS off (D-24); got ${cards}. Page must pre-render cards per PROJECT.md.\`).toBeGreaterThan(0)`
           - `const headings = await page.locator('h1, h2').count()`
           - `expect(headings, \`${slug}: expected >=1 heading visible with JS off (D-24); got ${headings}\`).toBeGreaterThan(0)`
           - `const bodyText = ((await page.textContent('body')) || '').trim()`
           - `expect(bodyText.length, \`${slug}: empty body with JS off — page failed to render any text\`).toBeGreaterThan(50)`
           - Check for placeholder dominance (D-24 third assertion):
             - `const firstChars = bodyText.slice(0, 100).toLowerCase()`
             - `expect(firstChars, \`${slug}: first 100 chars of body match a JS-off placeholder — page is hydration-blocking\`).not.toMatch(/^(enable javascript|loading|please wait|loading\.\.\.|please enable)/i)`
         - `await context.close()`

    2. Smoke test against live realufo.org (the current main with hydrated cards — this will tell us EXACTLY which archives pre-render today vs which hydrate, which informs Phase 3+ ordering):
       ```
       PREVIEW_URL=https://realufo.org pnpm exec playwright test tests/js-off.spec.ts --config=tests/playwright.config.ts || echo "JS-off failures expected during Phase 2/3 wargov-only window — gate is hard-fail per D-25, not softened"
       ```
       Capture output to `/tmp/js-off-baseline.txt`. The number of failing archives here is the Phase 3+ work-in-flight number for SSG-05 + SSG-06 (one archive at a time gets pre-rendered).

    3. Commit both specs from this plan together:
       ```
       git add tests/tone-colours-fixture.json tests/tone-colours.spec.ts tests/js-off.spec.ts
       git commit -m "infra(02-06): tone-colour + JS-off Playwright specs (INF-06 + INF-07)"
       git push origin ssg-migration
       ```

       Note in commit body: "JS-off spec is hard-fail from day one per D-25 (Plan 02-08 ships the js-off job WITHOUT continue-on-error). Phase 2/3 wargov-only window will produce expected failures for archives still hydrating client-side — those failures signal Phase 3 work pending, NOT a gate-mode softening."
  </action>
  <verify>
    <automated>
      test -f tests/js-off.spec.ts \
        && grep -q 'javaScriptEnabled: false' tests/js-off.spec.ts \
        && grep -q 'newContext' tests/js-off.spec.ts \
        && test "$(grep -cE \"test\\('\" tests/js-off.spec.ts)" -ge "15" \
        && grep -q 'D-24' tests/js-off.spec.ts \
        && grep -q 'D-25' tests/js-off.spec.ts \
        && pnpm exec playwright test --list --config=tests/playwright.config.ts 2>&1 | grep -cE 'tone colour for|renders meaningfully without JS' | head -1 | tr -d ' \n' | grep -qE '^[3-9][0-9]$'
    </automated>
  </verify>
  <done>
    - `tests/js-off.spec.ts` exists with 15 tests using `browser.newContext({ javaScriptEnabled: false })`
    - Each test asserts: ≥1 card selector match, ≥1 heading, body length >50, no placeholder dominance
    - Spec evaluates against any `PREVIEW_URL`
    - Combined commit on ssg-migration includes both Plan 02-06 spec files + fixture
    - INF-06 fully satisfied (tone-colours hard-fail gate live)
    - INF-07 spec committed and wired as hard-fail per D-25; expected failures during Phase 2/3 wargov-only window signal Phase 3 work pending, NOT a gate-mode softening. The gate IS hard-fail from day one (no continue-on-error in 02-08's js-off job).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spec runtime ↔ browser context | Playwright manages browser context lifecycle; no cross-test leakage with explicit `context.close()`. |
| Fixture file ↔ CLAUDE.md §3.1 | Fixture is the source of truth proxy; CLAUDE.md remains authoritative. Drift requires explicit fixture update + code review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Tampering | Fixture edit to mask a tone-colour regression | mitigate | `tests/tone-colours-fixture.json` is in `_metadata.locked_per: D-22`; any edit requires CLAUDE.md §3.1 change first (reviewer checklist). |
| T-02-21 | Spoofing | JS-off context leaking JS state from parent | mitigate | `browser.newContext({ javaScriptEnabled: false })` creates an isolated context; explicit `context.close()` per test. |
| T-02-22 | Information disclosure | JS-off test logging private page content | accept | Pages tested are public archive content; no PII surface. |
| T-02-23 | Tampering | Hydration-only future archive sneaks past gate | mitigate | D-25 hard-fail; Plan 02-08 wires both specs as required CI checks. Any archive that hydrates client-side fails the gate at PR time. |
</threat_model>

<verification>
1. `PREVIEW_URL=https://realufo.org pnpm exec playwright test tests/tone-colours.spec.ts --config=tests/playwright.config.ts` → 15 passed
2. `PREVIEW_URL=https://realufo.pages.dev pnpm exec playwright test tests/tone-colours.spec.ts --config=tests/playwright.config.ts` → 15 passed
3. `pnpm exec playwright test --list --config=tests/playwright.config.ts` shows 90 total tests (60 visual + 15 tone + 15 JS-off)
4. JS-off spec baseline against current main captured — informs Phase 3+ archive port ordering
5. INF-06 satisfied; INF-07 spec committed (gates Phase 3+ archive ports)
6. No DNS/domain changes (D-37); no SSG code (D-38)
</verification>

<success_criteria>
- INF-06 satisfied: tone-colour spec runs + passes against any base URL; D-22 + D-23 honored
- INF-07 spec committed: `tests/js-off.spec.ts` exists and Plan 02-08 wires it as a hard-fail CI gate (no continue-on-error) per D-25; D-24 assertions encoded; Phase 2/3 expected-fail status documented in commit body as Phase 3 work-pending signal
- CLAUDE.md §3.1 is the single source of truth for tone colours
- 90 total Playwright tests now exist across all Phase 2 specs (60 visual + 15 tone + 15 JS-off)
- No DNS / SSG changes
</success_criteria>

<output>
Create `.planning/phases/02-infrastructure-ci-scaffolding/02-06-SUMMARY.md` recording:
- Tone-colour spec results against live + preview (should be 15/15 both)
- JS-off baseline against live realufo.org — table of which archives currently pass (pre-rendered already) vs which fail (hydrate client-side). This table seeds Phase 3+ archive port ordering: archives that already pass JS-off naturally are lower-risk Phase 3 candidates; archives that fail require deeper Phase 4 work.
- Note for Plan 02-08: `tone-colours` job runs `pnpm exec playwright test tests/tone-colours.spec.ts`; `js-off` job runs `pnpm exec playwright test tests/js-off.spec.ts`. Both required for Phase 4+ merges; the JS-off failure-mode in Phase 2/3 is "expected, indicates Phase 3 work not yet done for archive X" — Plan 02-08 documents the gate's interpretation but does NOT soften via continue-on-error.
- Open question: whether to add Firefox/WebKit projects for these specific specs (deferred per D-15; revisit if Chromium-only proves insufficient in Phase 4)
</output>
</output>

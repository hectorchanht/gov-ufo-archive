---
phase: 02-infrastructure-ci-scaffolding
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - scripts/extract-fidelity-samples.py
  - scripts/verify-fidelity.py
  - tests/fidelity-samples.json
autonomous: true
requirements: [INF-05]

must_haves:
  truths:
    - "tests/fidelity-samples.json is the frozen contract — hand-curated samples extracted ONCE from current main per D-19"
    - "Samples cover: hero ledes (h1.hero-title + p.hero-sub) for all 15 archives; FAQ accordion answers where present; license footers per archive; first 5 official card titles per archive (D-18)"
    - "scripts/verify-fidelity.py strips ONLY leading/trailing whitespace — NO smart-quote/em-dash/accent normalization (D-20). Round-trips smart quotes, em-dashes, accents byte-equivalent."
    - "verify-fidelity.py fails loudly with a unified diff per mismatched sample (D-21)"
    - "extract-fidelity-samples.py is the ONE-SHOT extractor that produces fidelity-samples.json from current main — re-running it is operator-only, equivalent to D-17's baseline invariant"
    - "verify-fidelity.py runs against any HTTP base URL (CF Pages preview by default; configurable via --base-url)"
    - "Sample text is verbatim CLAUDE.md §9 contract (no markdown smart-typography drift — per research/PITFALLS.md Pitfall #4)"
  artifacts:
    - path: "scripts/extract-fidelity-samples.py"
      provides: "Walk tracked HTML in current main, extract D-18 sample list as JSON"
      contains: "fidelity-samples.json"
    - path: "scripts/verify-fidelity.py"
      provides: "Fetch each sample's location from --base-url, compare byte-equivalent against fidelity-samples.json, emit unified diff on mismatch"
      contains: "difflib.unified_diff"
    - path: "tests/fidelity-samples.json"
      provides: "Locked sample list — array of {archive, kind, selector, expected_text, source_url}"
      contains: "hero-lede"
  key_links:
    - from: "scripts/extract-fidelity-samples.py"
      to: "tests/fidelity-samples.json"
      via: "one-shot extraction from current main"
      pattern: "fidelity-samples.json"
    - from: "scripts/verify-fidelity.py"
      to: "https://*.realufo.pages.dev/<path>"
      via: "HTTP GET + HTML text-node extraction + byte-equivalent compare"
      pattern: "base-url"
---

<objective>
Hand-curate the locked fidelity-sample list (D-18), write `scripts/extract-fidelity-samples.py` that extracts those samples from the current `main` HTML and serializes them to `tests/fidelity-samples.json`, and write `scripts/verify-fidelity.py` that fetches the same samples from any base URL (CF Pages preview by default) and asserts byte-equivalent text — stripping ONLY leading/trailing whitespace per D-20.

Purpose: INF-05 + ROADMAP §Phase 2 SC#5. The fidelity gate is the verbatim-text contract from CLAUDE.md §9 made executable: smart quotes, em-dashes, French/Spanish/Portuguese accents must round-trip exactly through any future SSG output. Per research/PITFALLS.md §Pitfall #4 (typographer drift), this is the gate that catches a markdown processor "improving" `"` → `"` or `é` → `Ã©` mojibake. Per D-19, samples are extracted ONCE from main and frozen — equivalent invariant to baselines.

Output: `scripts/extract-fidelity-samples.py`, `scripts/verify-fidelity.py`, `tests/fidelity-samples.json` (the locked contract).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-infrastructure-ci-scaffolding/02-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@CLAUDE.md
@scripts/snapshot-urls.py
@URL-CONTRACT.txt

<interfaces>
<!-- Sample shape — one record per locked sample (D-18). -->

tests/fidelity-samples.json (top-level array; ~80-120 entries total):
[
  {
    "archive": "aaro",
    "kind": "hero-lede",
    "selector": "h1.hero-title",
    "source_path": "/aaro/",
    "expected_text": "All-domain Anomaly Resolution Office",
    "source_file": "aaro/index.html"
  },
  {
    "archive": "aaro",
    "kind": "hero-sub",
    "selector": "p.hero-sub",
    "source_path": "/aaro/",
    "expected_text": "<verbatim lede paragraph from current main>",
    "source_file": "aaro/index.html"
  },
  ...
]

Sample taxonomy (D-18):
  - kind: "hero-lede"        — h1.hero-title per archive (15 entries, one per archive — wargov uses root /)
  - kind: "hero-sub"         — p.hero-sub per archive (15 entries)
  - kind: "license-footer"   — footer license text per archive (15 entries, content per CLAUDE.md §9)
  - kind: "faq-answer"       — root /about.html FAQ answers (1-5 entries — only where present; D-18 says "at minimum root index.html FAQ" but the actual FAQ lives at /about.html per STRUCTURE.md)
  - kind: "card-title"       — first 5 inline-JSON card titles per archive (15 × 5 = 75 entries)

Total: ~120 entries (15 + 15 + 15 + ~5 + 75)

extract-fidelity-samples.py (one-shot, run against local HTML):
  CLI:
    python3 scripts/extract-fidelity-samples.py                 # write tests/fidelity-samples.json
    python3 scripts/extract-fidelity-samples.py --stdout        # print to stdout, no write
    python3 scripts/extract-fidelity-samples.py --check         # exit 1 if file missing or schema invalid

verify-fidelity.py (CI gate, runs against any base URL):
  CLI:
    python3 scripts/verify-fidelity.py                                 # default base URL = https://realufo.pages.dev
    python3 scripts/verify-fidelity.py --base-url https://X.pages.dev  # CI passes PR preview URL
    python3 scripts/verify-fidelity.py --color                         # ANSI-colored diff (D-CONTEXT specifics)
  Exit codes: 0 all match, 1 mismatch (print unified diff per failure), 2 fetch error
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write extract-fidelity-samples.py + generate tests/fidelity-samples.json</name>
  <read_first>
    - .planning/phases/02-infrastructure-ci-scaffolding/02-CONTEXT.md §decisions D-18, D-19, D-20, D-21
    - .planning/research/PITFALLS.md §Pitfall #4 (content fidelity drift — smart quotes, em-dashes, accents, mojibake — this is what the gate catches)
    - .planning/codebase/CONVENTIONS.md §"Python Conventions" + §"Manifest record keys" (short-key shapes ti/de — used by inline arch-data JSON)
    - .planning/codebase/STRUCTURE.md (locations of about.html FAQ + per-archive index.html)
    - CLAUDE.md §3.1 (per-archive seal text — minor) + §4 (hero structure with h1.hero-title + p.hero-sub) + §9 (license-footer texts per jurisdiction — the contract)
    - scripts/snapshot-urls.py (style template — argparse, exit codes 0/1/2, REPO root resolution, MANIFEST_RE regex for inline JSON extraction)
    - 5 sample archive index.html files for hero + footer structure (one read each — pick aaro/index.html for AARO blue, nasa/index.html, geipan/index.html for French accents, brazil/index.html for Portuguese, spain/index.html for Spanish — covers ASCII + accented + smart-quoted text spans)
    - about.html for FAQ structure
  </read_first>
  <files>scripts/extract-fidelity-samples.py, tests/fidelity-samples.json</files>
  <action>
    On ssg-migration branch.

    1. Write `scripts/extract-fidelity-samples.py` matching scripts/snapshot-urls.py shape (CONVENTIONS.md):
       - Module docstring describing purpose (INF-05 + D-19 one-shot extraction), inputs (tracked HTML in current main), output (tests/fidelity-samples.json), invariant (run once; future regeneration is operator-only and equivalent to D-17 baseline regen).
       - `from __future__ import annotations`
       - Stdlib only: `argparse`, `json`, `os`, `re`, `subprocess`, `sys`, `pathlib.Path`, `html.parser.HTMLParser` (for text extraction without BeautifulSoup — CONVENTIONS.md §"Python Conventions" mandates stdlib-only)
       - Constants:
         - `REPO = pathlib.Path(__file__).resolve().parent.parent`
         - `OUTPUT = REPO / 'tests' / 'fidelity-samples.json'`
         - `ARCHIVES_AND_PATHS = [('wargov', 'index.html', '/'), ('aaro', 'aaro/index.html', '/aaro/'), ('nasa', 'nasa/index.html', '/nasa/'), ...]`  (15 entries; matches capture-baselines.py)
         - `MANIFEST_RE` borrowed from snapshot-urls.py (`<script id="(?:arch-data|archive-manifest)" type="application/json">(.*?)</script>` with `re.DOTALL`)
       - Functions:
         - `def extract_inner_text(html: str, selector: str) -> str | None`:
           - Stdlib-only selector — accept only the exact selectors the D-18 contract needs: `h1.hero-title`, `p.hero-sub`, `footer .license`, `.faq-answer`, etc.
           - Implement as a tiny `HTMLParser` subclass that tracks open tags + class attribute; matches when current tag matches `selector_tag` AND its classes include `selector_class`; collects character data until close.
           - NO smart-quote / em-dash normalization. NO HTML-entity decoding (those land in the JSON verbatim).
           - Return the first match's text content (whitespace untouched).
         - `def first_card_titles(html: str, n: int = 5) -> list[str]`:
           - Find `<script id="arch-data" type="application/json">…</script>` block (or `archive-manifest` for root); parse JSON; return first N records' `ti` or `Title` field verbatim.
         - `def extract_for_archive(archive: str, html_path: pathlib.Path, public_path: str) -> list[dict]`:
           - Read HTML; build 5 + 1 + 5 = ~11 records: 1 hero-lede + 1 hero-sub + 1 license-footer + (root only) up-to-5 FAQ answers + 5 card-title.
           - For license-footer: extract the verbatim license string per CLAUDE.md §9 — the source text is in the archive's footer. The extractor pulls whatever is between the first matching `<footer>` and its license `<small>` / `<p class="license">` element; if no class matches, fall back to selecting text inside `<footer>` between known marker phrases (e.g. "Public domain under" or "Loi n°" or "17 U.S.C."). If extraction returns nothing, emit a warning and skip that archive's footer sample — Task 2's QA step will validate the count matches expectations.
         - `def main()`:
           - Parse args (`--stdout`, `--check` matching snapshot-urls.py)
           - Iterate ARCHIVES_AND_PATHS, accumulate records, write JSON
           - On `--check`: load file, validate schema (every record has `archive`, `kind`, `selector`, `source_path`, `expected_text`, `source_file`), exit 1 on schema mismatch
       - Exit codes: 0 wrote/matched, 1 schema mismatch or `--check` gap, 2 file read error
       - Indented status: `[ok]    aaro: 11 samples`

       Make executable: `chmod +x scripts/extract-fidelity-samples.py`

    2. Run the extractor once to produce `tests/fidelity-samples.json`:
       ```
       python3 scripts/extract-fidelity-samples.py
       ```
       Expected: emits `tests/fidelity-samples.json` of ~100-150 KB with ~120 records (15 archives × ~7 samples + FAQ on root).

    3. QA the output by spot-checking:
       - Smart-quoted text preserved: `python3 -c "import json; d=json.load(open('tests/fidelity-samples.json')); print([s['expected_text'] for s in d if '"' in s['expected_text']][:3])"` — must show actual `"…"` characters, NOT `&quot;` or `"…"`.
       - French accents preserved: `python3 -c "import json; d=json.load(open('tests/fidelity-samples.json')); print([s for s in d if s['archive']=='geipan' and s['kind']=='hero-sub'])"` — must show `é`, `è`, etc. as native UTF-8, NOT as `Ã©` mojibake.
       - License text per jurisdiction matches CLAUDE.md §9:
         - aaro/nasa/nara → contains `17 U.S.C.` or `Public domain`
         - geipan → contains `Loi n° 78-753`
         - uk → contains `Open Government Licence`
         - brazil → contains `Lei nº 12.527`
         - chile → contains `Ley nº 20.285`
         - italy → contains `D.lgs. 33/2013`
         - spain → contains `Ley 19/2013`
         - uruguay → contains `Ley nº 18.381`
         If any archive's license-footer sample is missing or wrong, manually edit `tests/fidelity-samples.json` to insert the correct verbatim text from CLAUDE.md §9 (D-CONTEXT.md §"Claude's Discretion" allows hand-curation of locked samples — once written, they freeze per D-19).
       - At least 60 card-title entries (15 × ~4-5).

    4. Commit:
       ```
       git add scripts/extract-fidelity-samples.py tests/fidelity-samples.json
       git commit -m "infra(02-04a): extract-fidelity-samples.py + locked tests/fidelity-samples.json (INF-05 step 1)"
       git push origin ssg-migration
       ```
  </action>
  <verify>
    <automated>
      test -x scripts/extract-fidelity-samples.py \
        && test -f tests/fidelity-samples.json \
        && python3 -c "import json; d=json.load(open('tests/fidelity-samples.json')); assert isinstance(d, list), type(d); assert len(d) >= 75, f'expected >=75 samples got {len(d)}'; required={'archive','kind','selector','source_path','expected_text','source_file'}; missing=[s for s in d if not required.issubset(set(s.keys()))]; assert not missing, missing[:1]; assert any(s['kind']=='hero-lede' for s in d); assert any(s['kind']=='license-footer' for s in d); kinds={s['kind'] for s in d}; assert 'card-title' in kinds; archives={s['archive'] for s in d}; expected={'wargov','aaro','nasa','nara','geipan','uk','brazil','chile','argentina','canada','italy','nz','peru','spain','uruguay'}; assert archives>=expected, expected-archives" \
        && python3 -c "import json; d=json.load(open('tests/fidelity-samples.json')); g=[s for s in d if s['archive']=='geipan']; assert any('é' in s['expected_text'] or 'è' in s['expected_text'] or 'à' in s['expected_text'] for s in g), 'no accented French text in geipan samples — possible mojibake'"
    </automated>
  </verify>
  <done>
    - `scripts/extract-fidelity-samples.py` exists, executable, stdlib-only
    - `tests/fidelity-samples.json` exists with ≥75 samples covering all 15 archives + all D-18 kinds
    - French/Spanish/Portuguese accented characters round-trip as native UTF-8 (no `Ã©` mojibake)
    - Smart quotes preserved verbatim
    - License-footer texts match CLAUDE.md §9 jurisdiction table
    - D-19 honored: file is committed and frozen — future regen requires operator action
  </done>
</task>

<task type="auto">
  <name>Task 2: Write verify-fidelity.py CI gate</name>
  <read_first>
    - tests/fidelity-samples.json (just created)
    - scripts/extract-fidelity-samples.py (reuse the HTMLParser-based text extractor from Task 1)
    - .planning/phases/02-infrastructure-ci-scaffolding/02-CONTEXT.md §decisions D-20, D-21
    - .planning/phases/02-infrastructure-ci-scaffolding/02-01-SUMMARY.md (preview URL)
    - .planning/codebase/CONVENTIONS.md §"Python Conventions" §"Logging"
    - .planning/research/PITFALLS.md §Pitfall #4 (warning signs section — what failure modes the gate must catch)
  </read_first>
  <files>scripts/verify-fidelity.py</files>
  <action>
    Write `scripts/verify-fidelity.py` matching CONVENTIONS.md §Python style:

    - Module docstring describing: purpose (INF-05 CI gate; runs in quality-gates.yml from Plan 02-08), input (tests/fidelity-samples.json + --base-url), output (exit code + unified diff on mismatch per D-21), failure mode (any byte mismatch on smart quotes, em-dashes, accents — research/PITFALLS.md Pitfall #4 warning signs).
    - `from __future__ import annotations`
    - Stdlib only: `argparse`, `difflib`, `json`, `pathlib.Path`, `sys`, `urllib.request`, `urllib.error`. Import the HTMLParser-text extractor from `extract-fidelity-samples.py` via `sys.path.insert(0, str(pathlib.Path(__file__).parent))` then `from extract_fidelity_samples import extract_inner_text, first_card_titles` (rename file's importable name to use underscores — Python module name `extract_fidelity_samples`, file name kept as `extract-fidelity-samples.py` is NOT importable; either symlink as `extract_fidelity_samples.py` OR copy the helper functions into verify-fidelity.py). Pick: **copy the helper functions** (duplication acceptable; this is two scripts not a package).

       Rationale: CONVENTIONS.md private-helper modules use underscore prefix (`_site_template.py`). Promoting the extractor to a shared module would require renaming; cleaner to keep both as standalone scripts that share the function definitions verbatim. Add a comment at top of both copies: `# Shared with scripts/extract-fidelity-samples.py — keep in sync.`
    - Constants:
       - `REPO = pathlib.Path(__file__).resolve().parent.parent`
       - `SAMPLES = REPO / 'tests' / 'fidelity-samples.json'`
       - `DEFAULT_BASE = 'https://realufo.pages.dev'`  (CF Pages preview default per Plan 02-01's preview URL)
       - `USER_AGENT = 'realufo-fidelity-gate/1.0 (https://github.com/hectorchanht/gov-ufo-archive)'`
    - CLI:
       - `--base-url <url>` (default `DEFAULT_BASE`)
       - `--color` (enables ANSI in diff output — D-CONTEXT specifics)
       - `--archive <slug>` (run only one archive's samples; for local debug)
       - `--kind <kind>` (run only one sample kind: hero-lede / hero-sub / license-footer / faq-answer / card-title)
       - `--strip-leading-trailing-whitespace-only` (default ENABLED — per D-20; flag exists only to document the rule, never disabled)
    - Flow:
       1. Load `tests/fidelity-samples.json` (exit 2 if missing)
       2. Filter by `--archive`/`--kind` if given
       3. Group samples by `source_path` (to fetch each page once)
       4. For each unique source_path:
          - Fetch `args.base_url + path` with User-Agent header, 30 s timeout
          - On fetch error: print `[FETCH-FAIL] <url>: <error>` and exit 2
          - For each sample at this path: extract text via `extract_inner_text(html, selector)` or `first_card_titles(html, n)` (matching kind)
          - For each extracted text:
            - Strip leading/trailing whitespace from BOTH expected and actual (D-20: ONLY whitespace, NO other normalization)
            - Compare byte-equivalent
            - On match: increment counter, print `  [ok]    <archive>/<kind>` (CONVENTIONS.md §Shell Conventions style)
            - On mismatch: collect into `failures` list with `difflib.unified_diff(expected, actual, fromfile=f'{archive}/{kind}/expected', tofile=f'{archive}/{kind}/actual', n=1)` — emit ANSI-colored if `--color`
       5. After all samples:
          - Print summary: `f'{passed}/{total} samples matched.'`
          - If failures: print each unified diff (D-21: fail loudly with unified diff per mismatched sample) and exit 1
          - Else exit 0

    Make executable: `chmod +x scripts/verify-fidelity.py`

    QA — run locally against live realufo.org (the same origin the samples were extracted from — should be 100% match):
    ```
    python3 scripts/verify-fidelity.py --base-url https://realufo.org
    ```
    Expected: `{N}/{N} samples matched` and exit 0. If anything fails, the extractor and the verifier disagree on text-extraction logic — diagnose and fix (likely the HTMLParser duplicated copies have drifted; re-sync).

    Also run against the CF Pages preview (Phase 2 = bare repo = same content as main):
    ```
    python3 scripts/verify-fidelity.py --base-url https://realufo.pages.dev
    ```
    Expected: `{N}/{N} samples matched` and exit 0 — because ssg-migration is a fast-forward of main right now (no SSG code yet per D-38).

    Commit:
    ```
    git add scripts/verify-fidelity.py
    git commit -m "infra(02-04b): verify-fidelity.py CI gate (INF-05 step 2)"
    git push origin ssg-migration
    ```
  </action>
  <verify>
    <automated>
      test -x scripts/verify-fidelity.py \
        && python3 scripts/verify-fidelity.py --base-url https://realufo.org > /tmp/verify-realufo.log 2>&1 \
        && grep -qE '[0-9]+/[0-9]+ samples matched' /tmp/verify-realufo.log \
        && python3 scripts/verify-fidelity.py --base-url https://realufo.pages.dev > /tmp/verify-preview.log 2>&1 \
        && grep -qE '[0-9]+/[0-9]+ samples matched' /tmp/verify-preview.log \
        && grep -q 'D-20' scripts/verify-fidelity.py \
        && grep -q 'difflib.unified_diff' scripts/verify-fidelity.py \
        && grep -qE 'strip\(\)' scripts/verify-fidelity.py
    </automated>
  </verify>
  <done>
    - `scripts/verify-fidelity.py` exists, executable, stdlib-only
    - Runs against live realufo.org with 100% match (exit 0)
    - Runs against CF Pages preview with 100% match (exit 0, since ssg-migration content == main content per D-38)
    - Emits unified diff per mismatch per D-21
    - Whitespace handling is leading/trailing ONLY per D-20
    - Future plans: Plan 02-08 wires this script into `.github/workflows/quality-gates.yml` `fidelity` job
    - INF-05 fully satisfied
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Extractor ↔ local git-tracked HTML | Read-only filesystem access; no network. |
| Verifier ↔ remote --base-url | Read-only HTTP GET with user-agent identifier. |
| Repo ↔ tests/fidelity-samples.json | Committed as locked contract (D-19); future regen is operator-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering | Auto-regen of fidelity samples masking a verbatim-contract break | mitigate | D-19 invariant; samples extracted once + frozen. extract-fidelity-samples.py overwrites — CI never invokes it. Mirror of D-17 (visual baselines). |
| T-02-13 | Spoofing | Wrong --base-url passed by attacker → false pass | accept | This script is run in CI only against `PREVIEW_URL` from PR `deployment_status` event payload (Plan 02-08); not user-facing. |
| T-02-14 | Tampering | Markdown processor silently normalizing smart quotes / accents (research/PITFALLS.md Pitfall #4) | mitigate | D-20 strips ONLY leading/trailing whitespace; any other character change fails the gate with unified diff. |
| T-02-15 | Information disclosure | Sample text leaking PII | accept | All sampled text is public-domain CLAUDE.md §9 verbatim content. Zero PII surface. |
</threat_model>

<verification>
1. `python3 scripts/verify-fidelity.py --base-url https://realufo.org` → exit 0, all samples match
2. `python3 scripts/verify-fidelity.py --base-url https://realufo.pages.dev` → exit 0, all samples match
3. `tests/fidelity-samples.json` size ≥ 50 KB; ≥ 75 records
4. French accents present in geipan samples (no mojibake)
5. License-footer per archive matches CLAUDE.md §9 jurisdiction
6. INF-05 satisfied
</verification>

<success_criteria>
- INF-05 satisfied: `verify-fidelity.py` runs against any URL; emits unified diff on byte mismatch; D-20 + D-21 honored
- D-18 sample taxonomy implemented
- D-19 lock-once invariant codified
- No SSG code (D-38)
- No DNS / domain changes (D-37)
</success_criteria>

<output>
Create `.planning/phases/02-infrastructure-ci-scaffolding/02-04-SUMMARY.md` recording:
- Total samples extracted (number)
- Per-archive sample counts (table)
- Confirmation that French / Spanish / Portuguese accents round-trip
- Confirmation that smart quotes round-trip
- Note for Plan 02-08: `fidelity` job invokes `python3 scripts/verify-fidelity.py --base-url $PREVIEW_URL --color`; fail on exit != 0
- Note for any future plan touching CLAUDE.md §9: this file's regen invalidates samples; coordinate with operator
</output>

---
phase: 05-scrape-automation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/r2-sync.yml
  - .planning/phases/05-scrape-automation/05-01-SUMMARY.md
autonomous: false
requirements: [SCRP-03, SCRP-05, SCRP-09]
must_haves:
  truths:
    - "Operator gate confirms Hector pushed Phase 4 local commits to hectorchanht/gov-ufo-archive remote (D-21, D-22) — `gh workflow list` reflects current main"
    - "Operator gate confirms `gh auth status` reports active account `hectorchanht` (memory file: realufo-github-account)"
    - "Rewritten r2-sync.yml triggers via `workflow_dispatch` with `full_sync` input AND `repository_dispatch` event_type `scrape-promote` (forward-compat for Plan 05-05)"
    - "After operator runs `gh workflow run r2-sync.yml -f full_sync=true --repo hectorchanht/gov-ufo-archive`, R2 bucket `realufo` is seeded with the 4-active corpus (wargov + aaro + nasa + nara PDFs + videos under `pdfs/<slug>/` and `videos/<slug>/`)"
    - "Spot-check curl returns HTTP/2 200 on 3 sample R2 URLs (one per asset type per archive coverage)"
  artifacts:
    - path: ".github/workflows/r2-sync.yml"
      provides: "Rewritten R2 sync workflow with idempotent SHA-based skip + workflow_dispatch + repository_dispatch triggers"
      contains: "repository_dispatch"
    - path: ".planning/phases/05-scrape-automation/05-01-SUMMARY.md"
      provides: "Plan 05-01 close summary documenting operator-gate state + bulk-seed results"
  key_links:
    - from: ".github/workflows/r2-sync.yml"
      to: "r2://realufo (S3-compat endpoint via rclone)"
      via: "CLOUDFLARE_R2_ACCESS_KEY + CLOUDFLARE_R2_SECRET_KEY GH secrets"
      pattern: "r2-realufo:realufo/(pdfs|videos)/(wargov|aaro|nasa|nara)/"
---

<objective>
Land the rewritten `.github/workflows/r2-sync.yml` on the `hectorchanht/gov-ufo-archive` remote, then bulk-seed R2 with the 4-active-archive corpus. This is the Phase 5 entry plan — without it `gh workflow run` fails with "workflow not found" (D-21, D-22) and the Workers cron landing in Plans 05-03..05-05 has nowhere to promote staging objects to.

Purpose: Seed R2 canonical layout BEFORE Workers cron starts diffing — otherwise cards 404 while back-fill catches up. Also lay the `repository_dispatch` trigger surface that Plan 05-05's GH Action ingest pipeline will reuse.

Output: Pushed `r2-sync.yml` on remote main + populated R2 bucket + `05-01-SUMMARY.md` documenting operator-gate state.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scrape-automation/05-CONTEXT.md
@.planning/decisions/r2-setup.md
@.planning/phases/04-full-migration-search-offline-performance/04-02-SUMMARY.md
@.github/workflows/r2-sync.yml

<interfaces>
<!-- Key elements the executor needs (no codebase exploration). -->

From `.github/workflows/r2-sync.yml` (current LOCAL version, Phase 4 carryover — D-14 says rewrite):
- Triggers: `push` (paths filter on binary globs) + `workflow_dispatch` (input `full_sync` boolean)
- Concurrency group: `r2-sync` with `cancel-in-progress: false` (T-04-07)
- Job: `sync` → installs rclone via `https://rclone.org/install.sh | sudo bash` (T-04-SC, upstream-blessed path)
- Secrets used: `CLOUDFLARE_R2_ACCESS_KEY`, `CLOUDFLARE_R2_SECRET_KEY`, `CLOUDFLARE_ACCOUNT_ID`
- Sync targets: `bundles/Release_1/` → `pdfs/wargov/`, `bundles/videos/` → `videos/wargov/`, plus per-slug loop for the other 14 archives
- Smoke step: `rclone size r2-realufo:realufo` + `rclone lsd r2-realufo:realufo`

From `.planning/decisions/r2-setup.md`:
- Bucket: `realufo` (account `f1868a071996e836eae6da2b65f37929`)
- Custom domain: `https://assets.realufo.org`
- URL contract: `https://assets.realufo.org/pdfs/<slug>/<basename>` + `https://assets.realufo.org/videos/<slug>/<basename>`
- 4-active corpus to seed: ~116 wargov PDFs (`bundles/Release_1/`) + ~60 wargov videos (`bundles/videos/`) + aaro/nasa/nara PDFs/videos under `<slug>/bundles/` and `<slug>/videos/` where tracked

From CLAUDE.md §11 (don'ts that bind this plan):
- No force-push to main
- No history rewrite
- GH operations attribute to `hectorchanht`, NOT `frankchanflow` (memory file override)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator gate — confirm Phase 4 push state + gh auth identity</name>
  <read_first>
    - .planning/phases/05-scrape-automation/05-CONTEXT.md §D-21 + §D-22
    - User memory: `~/.claude/projects/-Users-laichan-code-war-gov-ufo-release/memory/MEMORY.md` (hectorchanht is the GH identity)
    - Current `git log --oneline -10` output (executor should run this and include in checkpoint message)
  </read_first>
  <what-built>
    Pre-execution gate. Before this plan modifies any workflow, the operator must confirm two prerequisites are met OR explicitly defer them:

    1. **Phase 4 push:** All Phase 4 commits (11 plans + scope-pivot + UAT + Pagefind + close — see `git log` since Phase 3 close) are pushed to `hectorchanht/gov-ufo-archive` `main`. Run `git status` and `git log origin/main..HEAD --oneline` locally; if the second command returns rows, Phase 4 commits are still local-only and Hector must push first.
    2. **gh auth identity:** `gh auth status` reports `Logged in to github.com account hectorchanht` (NOT `frankchanflow`). Per the project memory file, ALL `gh` operations for realufo attribute to `hectorchanht`.

    Per the orchestrator brief, BOTH gates are already satisfied (Hector pushed; `gh auth = hectorchanht`). This checkpoint exists to document that state in `05-01-SUMMARY.md` for audit trail.
  </what-built>
  <how-to-verify>
    1. Run `git log origin/main..HEAD --oneline` — expected: 0 rows (Phase 4 commits already on remote per orchestrator brief).
    2. Run `gh auth status` — expected: "Logged in to github.com account hectorchanht".
    3. Run `gh workflow list --repo hectorchanht/gov-ufo-archive` — expected: includes `quality-gates.yml` (Phase 2) and one of `r2-sync.yml` (Phase 4) or absence (will be added by this plan).
    4. If any check fails, report exact `git log`/`gh auth status`/`gh workflow list` output and STOP — operator must reconcile before resuming.
  </how-to-verify>
  <acceptance_criteria>
    - `git log origin/main..HEAD --oneline` returns 0 rows OR operator explicitly approves proceeding with a divergent state.
    - `gh auth status` shows `hectorchanht` as the active account.
    - The exact verification command outputs are captured in the plan's working notes for inclusion in `05-01-SUMMARY.md`.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the captured outputs, or describe the reconciliation needed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Rewrite r2-sync.yml — add repository_dispatch trigger + preserve full_sync path</name>
  <read_first>
    - Current `.github/workflows/r2-sync.yml` (entire file, top to bottom — preserve every comment block; this is the audit trail)
    - `.planning/decisions/r2-setup.md` §URL contract + §GitHub Actions secrets + §First-run bulk migration method
    - `.planning/phases/05-scrape-automation/05-CONTEXT.md` D-14 (rewrite directive) + D-16 (repository_dispatch event type `scrape-promote`)
  </read_first>
  <action>
    Rewrite `.github/workflows/r2-sync.yml` in place. Per D-14, the rewrite SUPERSEDES the Phase 4 version — preserve the rclone install + config + sync logic verbatim, but EXTEND the `on:` block.

    Changes to make:
    1. Workflow `name:` stays `R2 binary sync`. Update the top docblock to note "Phase 5 Plan 05-01 rewrite — adds repository_dispatch trigger for Plan 05-05 ingest pipeline; preserves Phase 4 Plan 04-02 rclone S3-compat sync logic."
    2. `on:` block additions (preserve existing `push.paths` + `workflow_dispatch.inputs.full_sync`):
       - Add `repository_dispatch:` with `types: [scrape-promote]` (the event type Plan 05-04 Worker will dispatch).
       - Add `workflow_dispatch.inputs.archive` (string, optional — for ad-hoc per-archive bulk re-seed; defaults to empty = all archives).
       - Add `workflow_dispatch.inputs.asset_keys` (string, optional — JSON array of staging object keys for incremental promote; ignored when `full_sync=true`).
    3. The diff-detection step (Step 4 in current file) gains a third branch: when `github.event_name == 'repository_dispatch'`, set `files=DISPATCH` (treats as incremental — sync is driven by the rclone `--checksum` flag, not the diff list). Document this in the step's comment block.
    4. The sync step (Step 5) keeps its existing per-archive scaffold but adds an early-return when `github.event.client_payload.archive` is set: in that case ONLY sync that one archive's `pdfs/<slug>/` and `videos/<slug>/` prefixes. (Plan 05-05's per-archive promote shrinks each invocation to a single archive — large bulk runs stay under the 30-min `timeout-minutes` cap.)
    5. The smoke step (Step 6) stays unchanged.
    6. NO changes to: secret names, the rclone install path (`rclone.org/install.sh | sudo bash` — T-04-SC blessed), the `concurrency` block, or `timeout-minutes: 30`.

    Do NOT inline any code in this action — modify the existing YAML file using the Edit tool to add the `repository_dispatch` event + new inputs + per-archive early-return guard. All other blocks remain byte-identical to the Phase 4 version.

    Per D-08 invariant: the `release-upload.py` helper lives in Plan 05-05 (NOT here) — r2-sync.yml stays R2-only. Do not add GH Release upload steps to this workflow.
  </action>
  <verify>
    <automated>yamllint .github/workflows/r2-sync.yml 2>&1 | (! grep -E '^(error|.* error)')</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/r2-sync.yml` parses as valid YAML (yamllint exits 0 or only emits warnings, no errors).
    - `grep -c 'repository_dispatch' .github/workflows/r2-sync.yml` returns ≥ 1.
    - `grep -c 'scrape-promote' .github/workflows/r2-sync.yml` returns ≥ 1.
    - `grep -c 'rclone.org/install.sh' .github/workflows/r2-sync.yml` returns ≥ 1 (preserved upstream install path).
    - `grep -c 'cancel-in-progress: false' .github/workflows/r2-sync.yml` returns ≥ 1 (T-04-07 preserved).
    - `grep -c 'CLOUDFLARE_R2_ACCESS_KEY' .github/workflows/r2-sync.yml` returns ≥ 1 (secret name unchanged).
    - The workflow_dispatch block lists three inputs: `full_sync` (boolean), `archive` (string), `asset_keys` (string).
  </acceptance_criteria>
  <done>r2-sync.yml accepts `repository_dispatch` events of type `scrape-promote` AND preserves the Phase 4 push + workflow_dispatch full_sync path; per-archive narrow-scope sync is honoured when client_payload includes `archive`.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator runs bulk-seed workflow_dispatch + verifies 3 R2 URLs</name>
  <read_first>
    - `.planning/decisions/r2-setup.md` §First-run bulk migration method
    - `.planning/phases/04-full-migration-search-offline-performance/04-UAT.md` §1 (R2 bulk migration runbook — PATH A is what this task executes)
  </read_first>
  <what-built>
    Task 2 landed the rewritten `r2-sync.yml`. This checkpoint asks the operator (Hector) to commit + push that file to `hectorchanht/gov-ufo-archive` main, then trigger the bulk-seed manually and spot-check the result. The Worker cron in Plans 05-03/04 has no canonical R2 path to promote into until this seed completes.
  </what-built>
  <how-to-verify>
    1. Operator commits `r2-sync.yml` and pushes:
       - `git add .github/workflows/r2-sync.yml`
       - `git commit -m "feat(05-01): rewrite r2-sync.yml — repository_dispatch + per-archive scope"`
       - `git push origin main`
    2. Operator triggers the bulk seed:
       - `gh workflow run r2-sync.yml -f full_sync=true --repo hectorchanht/gov-ufo-archive`
       - Wait for completion (~5–10 min per `r2-setup.md` §First-run bulk migration method).
       - `gh run watch --repo hectorchanht/gov-ufo-archive` until exit 0.
    3. Spot-check 3 R2 URLs return HTTP/2 200 (operator runs locally):
       - `curl -sI -H "Origin: https://realufo.org" https://assets.realufo.org/pdfs/wargov/<actual-filename>.pdf` — pick any filename listed in `bundles/Release_1/`.
       - `curl -sI -H "Origin: https://realufo.org" https://assets.realufo.org/videos/wargov/<actual-filename>.mp4` — pick any filename in `bundles/videos/`.
       - `curl -sI -H "Origin: https://realufo.org" https://assets.realufo.org/pdfs/aaro/<actual-filename>.pdf` OR equivalent under `aaro/bundles/` (if `aaro/bundles/` is empty per current scope, substitute a NASA or NARA PDF).
       - Each MUST return `HTTP/2 200` with `access-control-allow-origin: https://realufo.org` echoed.
    4. If any URL returns 404, capture the exact filename + folder layout (the rclone sync targets in the new r2-sync.yml may need a fix) and report back.
  </how-to-verify>
  <acceptance_criteria>
    - `gh run list --workflow=r2-sync.yml --repo hectorchanht/gov-ufo-archive --limit 1` shows the dispatched run with `status: completed` and `conclusion: success`.
    - All 3 spot-check curls return `HTTP/2 200` with `access-control-allow-origin: https://realufo.org` echoed.
    - The exact filenames + curl outputs are captured for inclusion in `05-01-SUMMARY.md`.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the 3 curl outputs, or report a 404 + the failing path.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local-dev → GitHub remote | Operator (Hector) push event; gates the entire phase landing on remote |
| GitHub Actions runner → R2 S3-compat endpoint | rclone uploads scraped binaries to R2 using long-lived S3 keys |
| Future Worker → GitHub repository_dispatch | Plan 05-04 Worker will fire scrape-promote events; this plan opens the trigger surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Spoofing | `repository_dispatch` event_type `scrape-promote` | mitigate | Event source is the Worker authenticated via GH App token (Plan 05-04 scope); r2-sync.yml has no auth surface beyond GH's own dispatch ACL. Document that ONLY repo-admin tokens can fire `repository_dispatch` per GH REST API spec — accept the existing GH ACL. |
| T-05-01-02 | Tampering | rclone install via `rclone.org/install.sh \| sudo bash` | accept | Already an accepted Phase 4 risk (T-04-SC); upstream-blessed path per rclone.org. Pinning would require quarterly bumps. If supply-chain incident lands, response is a pinned commit-sha install — out of Phase 5 scope. |
| T-05-01-03 | Information Disclosure | `CLOUDFLARE_R2_ACCESS_KEY`/`SECRET_KEY` logging | mitigate | Preserved from Phase 4: secrets are interpolated into rclone.conf via `env:` block (never echoed), `umask 077` + `chmod 600` on the config file, cleanup step removes config on `always()`. |
| T-05-01-04 | Denial of Service | Concurrency cancellation interrupting bulk seed | mitigate | Preserved `concurrency.cancel-in-progress: false` from Phase 4 (T-04-07). A second push that lands during bulk sync queues behind it — never aborts mid-upload. |
| T-05-01-05 | Elevation of Privilege | `permissions: contents: read` scope on r2-sync.yml | mitigate | Workflow keeps `permissions: contents: read` (no write needed — R2 writes go through CF secrets, not GH token). Refuse to add `contents: write` even though `repository_dispatch` payload could carry hostile data; this workflow never commits back. |
| T-05-01-SC | Tampering | npm/pip/cargo installs | mitigate | Workflow installs rclone only (no npm/pip/cargo). Action checked, no `[ASSUMED]`/`[SUS]` packages. T-04-SC carries forward for rclone. |
</threat_model>

<verification>
- yamllint on `.github/workflows/r2-sync.yml` reports zero errors.
- `gh workflow list --repo hectorchanht/gov-ufo-archive | grep -c r2-sync` returns 1 after operator push.
- `gh workflow run r2-sync.yml -f full_sync=true --repo hectorchanht/gov-ufo-archive` followed by `gh run watch` exits 0.
- 3 spot-check curls return HTTP/2 200 with CORS echoed.
</verification>

<success_criteria>
- Operator gate satisfied: Phase 4 push state confirmed; gh auth = hectorchanht.
- r2-sync.yml rewritten to accept `repository_dispatch` type `scrape-promote` + preserve workflow_dispatch full_sync path + per-archive narrow-scope sync.
- Bulk-seed workflow_dispatch run completes successfully on remote.
- R2 bucket `realufo` now contains the 4-active corpus at canonical `pdfs/<slug>/` + `videos/<slug>/` paths (smoke-verified via 3 curls returning 200).
- `05-01-SUMMARY.md` captures operator-gate outputs, the diff against the Phase 4 r2-sync.yml, the dispatched run URL, and the 3 spot-check curl outputs.
</success_criteria>

<output>
Create `.planning/phases/05-scrape-automation/05-01-SUMMARY.md` when done — must include: operator-gate verification outputs (Task 1), the YAML diff added in Task 2 (event triggers + inputs only — no full file dump), Task 3 dispatched-run URL + the 3 curl outputs, and an explicit "Wave 1 ready" line for Plan 05-02 (which runs in parallel, no dependency) and "Wave 3 unblocked" for Plan 05-05 (depends on 05-01 per orchestrator brief).
</output>

---
phase: 05-scrape-automation
plan: "07"
type: execute
wave: 5
depends_on: ["05-06"]
files_modified:
  - .planning/phases/05-scrape-automation/05-VERIFICATION.md
  - .planning/phases/05-scrape-automation/05-07-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [SCRP-01, SCRP-02, SCRP-03, SCRP-04, SCRP-05, SCRP-06, SCRP-07, SCRP-08, SCRP-09, SCRP-10]
must_haves:
  truths:
    - "End-to-end smoke verifies: Worker scheduled() OR manual /run fetch → lane execution → KV fingerprint write → repository_dispatch → ingest.yml runs → release-upload.py executes → r2-promote.sh runs → R2 canonical layout updated"
    - "All 10 SCRP requirements (SCRP-01..10) have explicit verification entries in 05-VERIFICATION.md citing the artifact (file/log/curl) that proves it"
    - "STATE.md updated: Phase 5 status moves from `Not started` to `Complete` once Plan 05-07 commits; progress counter increments (5 of 6 phases complete)"
    - "ROADMAP.md Phase 5 plan list checkboxes flipped to `[x]`"
    - "Plan 05-07 SUMMARY documents the smoke run-id, the captured Worker tail, the GH ingest.yml run URL, the resulting R2 canonical object + GH Release tag, and confirms 0 verify-python-retired.sh regressions"
  artifacts:
    - path: ".planning/phases/05-scrape-automation/05-VERIFICATION.md"
      provides: "Per-SCRP-requirement verification matrix with evidence"
      contains: "| SCRP-01 |"
    - path: ".planning/phases/05-scrape-automation/05-07-SUMMARY.md"
      provides: "Phase 5 close summary documenting E2E smoke + roadmap update"
      contains: "Phase 5 close"
  key_links:
    - from: ".planning/phases/05-scrape-automation/05-VERIFICATION.md"
      to: ".planning/ROADMAP.md §Phase 5 success criteria"
      via: "each VERIFICATION row maps a SCRP-NN requirement to specific evidence"
      pattern: "SCRP-"
    - from: ".planning/STATE.md"
      to: "ROADMAP.md Phase 5 status"
      via: "operator commits the status flip in this plan's git commit"
      pattern: "Complete"
---

<objective>
Run the end-to-end smoke that exercises the full producer→consumer loop built by Plans 05-01..06, document the result in `05-VERIFICATION.md`, flip ROADMAP + STATE to mark Phase 5 complete.

Purpose: Phase 5 close gate. Without this plan, Phase 5 stays "Not started" in STATE.md even though all artifacts exist. The smoke is the difference between scaffolded and shipping.

Output: VERIFICATION.md matrix + SUMMARY documenting smoke evidence + STATE.md/ROADMAP.md flipped.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scrape-automation/05-CONTEXT.md
@.planning/phases/05-scrape-automation/05-01-SUMMARY.md
@.planning/phases/05-scrape-automation/05-02-SUMMARY.md
@.planning/phases/05-scrape-automation/05-03-SUMMARY.md
@.planning/phases/05-scrape-automation/05-04-SUMMARY.md
@.planning/phases/05-scrape-automation/05-05-SUMMARY.md
@.planning/phases/05-scrape-automation/05-06-SUMMARY.md

<interfaces>
SCRP requirement matrix to verify (10 total, all 4 active archives in scope per D-10):

| ID | Plan(s) that close it | Evidence type |
|----|-----------------------|---------------|
| SCRP-01 | 05-03 + 05-04 | Worker scheduled() smoke shows 4 lane executions + KV fingerprint writes |
| SCRP-02 | 05-02 + 05-04 + 05-05 | Workers cron handles non-blocked sources; scrape-fallback.yml handles blocked. Spike ADR + lane outcome JSON + fallback workflow run URL |
| SCRP-03 | 05-04 + 05-05 | R2 staging objects exist + ingest.yml r2-promote.sh moves them to canonical |
| SCRP-04 | 05-04 + 05-05 | repository_dispatch event lands in GH audit log + ingest.yml run triggered |
| SCRP-05 | 05-05 | release-upload.py SHA idempotency: second consecutive ingest skips with `SKIP reason=sha-match` log |
| SCRP-06 | 05-05 | At least one per-archive-quarter release tag exists (e.g. `aaro-pdfs-2026q2`) — `gh release list` confirms |
| SCRP-07 | 05-03 | Two concurrent /run invocations: first 200 + lane outcomes, second 423 (lock held) |
| SCRP-08 | 05-06 | `grep -E "pip install curl_cffi.*\|\| true" .github/workflows/scrape.yml` returns 0 lines |
| SCRP-09 | 05-01 + 05-04 + 05-06 | `gh workflow run scrape.yml` runs successfully (manual dispatch). Worker /run fetch path also works (Plan 05-04) |
| SCRP-10 | 05-05 | release-upload.py R2_OVERFLOW log emission on a >2 GB synthetic asset (smoke creates a 2.1 GB sparse file locally + invokes release-upload.py in dry-run mode) |

The smoke combines several manual operator steps + automated checks. Coordinate via a single smoke session (~30 min of operator time).

Phase 5 close gates per ROADMAP §Phase 5 success criteria #1..#5:
1. Worker cron with KV fingerprints runs on schedule; per-source lanes partitioned; KV cron-lock prevents races → SCRP-01 + SCRP-07
2. Hybrid Akamai strategy; `|| true` removed from curl_cffi install → SCRP-02 + SCRP-08
3. release-upload.py with SHA idempotency + delete-then-upload + serialised concurrency + per-archive-quarter tags → SCRP-05 + SCRP-06
4. repository_dispatch → ingest pipeline → manifest deltas → SCRP-04
5. Manual gh workflow run scrape.yml works + assets >2 GB → R2 overflow → SCRP-09 + SCRP-10
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run the full E2E smoke (Worker → dispatch → ingest → R2 + GH Release)</name>
  <read_first>
    - .planning/phases/05-scrape-automation/05-01-SUMMARY.md (R2 bulk seed completed; canonical layout exists)
    - .planning/phases/05-scrape-automation/05-04-SUMMARY.md (Worker URL + manual trigger secret)
    - .planning/phases/05-scrape-automation/05-05-SUMMARY.md (ingest.yml deployed to remote main)
    - .planning/phases/05-scrape-automation/05-06-SUMMARY.md (SCRP-08 fix landed)
  </read_first>
  <what-built>
    Operator-driven E2E smoke. Goal: prove the Phase 5 pipeline works end-to-end on at least ONE active archive that is NOT in AKAMAI_BLOCKED_SOURCES. (If all 4 are blocked per the spike result, run the smoke through the scrape-fallback.yml path instead.)
  </what-built>
  <how-to-verify>
    Operator runs the following sequence, capturing outputs for inclusion in 05-VERIFICATION.md:

    **Stage A — Pre-smoke baseline (1 min):**
    1. `bash scripts/verify-python-retired.sh` → expect `OK` exit 0. Capture stdout.
    2. `gh release list --repo hectorchanht/gov-ufo-archive --limit 20` → capture current release tags (baseline for SCRP-06 comparison).
    3. `wrangler r2 object list realufo --prefix staging/` (from operator's wrangler-authenticated machine) → capture current staging contents.

    **Stage B — Trigger smoke via Worker manual /run (5 min):**
    4. `curl -s -H 'X-Manual-Trigger: <secret>' https://realufo-scrape-cron.<account>.workers.dev/run | jq .` → capture lane outcomes JSON.
    5. `pnpm exec wrangler tail --name realufo-scrape-cron --format json` (in another terminal, kept running during Stage B). Capture the tail JSON for the smoke run.
    6. Expected: at least one lane returns `outcome=changed` OR `outcome=akamai-fallback`. If ALL 4 return `unchanged`, force-clear ONE fingerprint to simulate change: `wrangler kv key delete --namespace-id <SCRAPE_STATE-id> fingerprint:aaro:<urlhash>` then re-run /run.

    **Stage C — Verify GH ingest.yml triggered (5 min):**
    7. `gh run list --workflow=ingest.yml --repo hectorchanht/gov-ufo-archive --limit 5` → expect a new run with `event: repository_dispatch`. Capture the run id + URL.
    8. `gh run watch <run-id> --repo hectorchanht/gov-ufo-archive` → wait for completion. Capture exit status.
    9. `gh run view <run-id> --log --repo hectorchanht/gov-ufo-archive | grep -E "UPLOADED|SKIP|R2_OVERFLOW"` → capture release-upload.py log lines.

    **Stage D — Verify R2 canonical + GH Release (5 min):**
    10. `wrangler r2 object list realufo --prefix pdfs/aaro/` (or whichever archive was 'changed') → verify a new object exists at canonical path. Compare against Stage A baseline.
    11. `wrangler r2 object list realufo --prefix staging/` → verify the staging object was DELETED (r2-promote.sh cleanup).
    12. `gh release list --repo hectorchanht/gov-ufo-archive --limit 5` → compare against Stage A baseline. Expect a NEW per-archive-quarter tag (e.g. `aaro-pdfs-2026q2`) if Stage C ran release-upload.py.
    13. `gh release view <new-tag> --repo hectorchanht/gov-ufo-archive --json assets --jq '.assets[].name'` → confirm the asset(s) are listed.

    **Stage E — SHA idempotency (SCRP-05) (3 min):**
    14. Re-fire the same dispatch by re-running the manual /run trigger (or `gh workflow run ingest.yml --repo hectorchanht/gov-ufo-archive -f archive=aaro -f asset_keys='["pdfs/aaro/landing-page.html"]' -f scraped_at=<same-ISO-as-before>`).
    15. `gh run view <new-run-id> --log | grep -E "SKIP reason=sha-match"` → expect at least one SKIP line per re-uploaded file.

    **Stage F — Cron-lock (SCRP-07) (1 min):**
    16. Two consecutive curls to /run within 1 second (use `&` or run from two terminals): `curl -s -H 'X-Manual-Trigger: <secret>' https://realufo-scrape-cron.<account>.workers.dev/run & curl -s -H 'X-Manual-Trigger: <secret>' https://realufo-scrape-cron.<account>.workers.dev/run`. Capture both responses; expect ONE 200 + ONE 423 (or similar lock-held shape).

    **Stage G — Workflow_dispatch manual trigger (SCRP-09) (3 min):**
    17. `gh workflow run scrape.yml --repo hectorchanht/gov-ufo-archive` → expect the workflow to start (it will fail on deleted build-*.py per python-build-retired ADR; that's EXPECTED and DOCUMENTED — what's being verified here is that the manual trigger PATH still works, not that the legacy workflow body succeeds).
    18. `gh run list --workflow=scrape.yml --limit 1` → confirm the run is listed with `event: workflow_dispatch`.

    **Stage H — R2 overflow synthetic test (SCRP-10) (3 min):**
    19. Operator creates a 2.1 GB sparse file locally: `dd if=/dev/zero of=/tmp/big.bin bs=1M count=0 seek=2150` (instant; sparse).
    20. `python3 scripts/release-upload.py --slug nara --asset-type pdfs --scraped-at 2026-05-28T12:00:00Z --files /tmp/big.bin` → capture stdout. Expect `R2_OVERFLOW slug=nara file=big.bin size=2254857728` log line + the file is NOT uploaded to a GH Release.
    21. `gh release view nara-pdfs-2026q2 --json assets --jq '.assets[].name'` → confirm `big.bin` is NOT in the asset list.
    22. `rm /tmp/big.bin`.

    Operator captures all outputs in a single text block and pastes back for inclusion in 05-VERIFICATION.md (Task 2).
  </how-to-verify>
  <acceptance_criteria>
    - All 8 stages run; ≥ 1 stage per SCRP requirement produces evidence captured for the VERIFICATION matrix.
    - Stage C ingest.yml run completes successfully (exit 0).
    - Stage D shows at least one NEW R2 canonical object that did NOT exist in Stage A baseline.
    - Stage D shows the staging object was removed by r2-promote.sh.
    - Stage D shows at least one NEW GH Release tag with per-archive-quarter format.
    - Stage E shows at least one `SKIP reason=sha-match` log line (SHA idempotency).
    - Stage F shows lock contention (one 200 + one 423/503).
    - Stage G workflow_dispatch invocation succeeds at the GitHub API level (run gets listed).
    - Stage H release-upload.py emits the R2_OVERFLOW log line + skips the GH Release upload.
    - All outputs are captured for the VERIFICATION matrix.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the full captured outputs from all 8 stages (or note any stage that failed + the next-step plan).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write .planning/phases/05-scrape-automation/05-VERIFICATION.md with per-SCRP evidence matrix</name>
  <read_first>
    - Captured smoke outputs from Task 1 (operator pasted into resume signal)
    - .planning/ROADMAP.md §Phase 5 success criteria (1..5)
    - .planning/REQUIREMENTS.md lines 74-83 (SCRP-01..10 verbatim text)
    - Existing VERIFICATION.md templates from prior phases (if present): `.planning/phases/04-full-migration-search-offline-performance/04-UAT.md` may serve as style reference
  </read_first>
  <action>
    Create `.planning/phases/05-scrape-automation/05-VERIFICATION.md`. Use Markdown.

    Required sections:

    1. `# Phase 5 Verification — Scrape Automation`
    2. Frontmatter-ish header (NOT YAML; just labeled rows): Phase, Smoke date, Smoke operator, Worker URL, ingest.yml run URL.
    3. `## SCRP Requirement Coverage Matrix` — a markdown table with columns: `ID | Requirement (short) | Closed by Plan(s) | Evidence | Status`.
       - One row per SCRP-01 through SCRP-10.
       - `Evidence` column: cite the specific log line, curl output, gh CLI command, or file path that proves the requirement. Be precise — e.g., "Stage C run id 12345678; `release-upload.py` log line: `UPLOADED slug=aaro file=landing-page.html tag=aaro-pdfs-2026q2 sha=abc...`".
       - `Status` column: `✓ Closed` or (if any gap) `△ Gap: <description>`.
    4. `## ROADMAP §Phase 5 Success Criteria` — for each of the 5 success criteria from ROADMAP.md §Phase 5, copy the criterion verbatim and add a check + the evidence-line citation.
    5. `## Smoke Run Outputs` — verbatim paste of operator-captured outputs from Task 1, organised by Stage A..H.
    6. `## Known Gaps / Follow-ups`:
       - List any SCRP requirement that landed with a workaround instead of a clean close (e.g., curl_cffi unpinned per Plan 05-05 follow-up).
       - List the Plan 05-02 throwaway KV namespace cleanup (`AKAMAI_SPIKE`) as still-pending operator action.
       - List the next-Monday production cron observation as deferred (the smoke proved the manual path; the schedule-driven path is provable only after a real Monday).
    7. `## Sign-off` — single line: `Phase 5 — Scrape Automation: CLOSED. Operator: hectorchanht. Date: <YYYY-MM-DD>.`

    No fenced code blocks except where the raw smoke outputs are pasted in §Smoke Run Outputs (those are intentional verbatim captures, NOT generated code).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-scrape-automation/05-VERIFICATION.md && grep -c "SCRP-01" .planning/phases/05-scrape-automation/05-VERIFICATION.md && grep -c "SCRP-02" .planning/phases/05-scrape-automation/05-VERIFICATION.md && grep -c "SCRP-03" .planning/phases/05-scrape-automation/05-VERIFICATION.md && grep -c "SCRP-04" .planning/phases/05-scrape-automation/05-VERIFICATION.md && grep -c "SCRP-05" .planning/phases/05-scrape-automation/05-VERIFICATION.md && grep -c "SCRP-06" .planning/phases/05-scrape-automation/05-VERIFICATION.md && grep -c "SCRP-07" .planning/phases/05-scrape-automation/05-VERIFICATION.md && grep -c "SCRP-08" .planning/phases/05-scrape-automation/05-VERIFICATION.md && grep -c "SCRP-09" .planning/phases/05-scrape-automation/05-VERIFICATION.md && grep -c "SCRP-10" .planning/phases/05-scrape-automation/05-VERIFICATION.md && grep -c "Phase 5 — Scrape Automation: CLOSED" .planning/phases/05-scrape-automation/05-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 05-VERIFICATION.md exists with all 7 required sections.
    - SCRP requirement coverage matrix has exactly 10 rows (SCRP-01..10).
    - Each row has an Evidence citation (NOT empty).
    - Each row's Status is `✓ Closed` OR documented as `△ Gap`.
    - The 5 ROADMAP success criteria are all listed and cross-referenced.
    - Smoke outputs section contains the captured logs/curls from Task 1 verbatim.
    - Sign-off line is present.
  </acceptance_criteria>
  <done>Phase 5 verification matrix is complete and audit-ready. Roadmap/STATE updates can now proceed.</done>
</task>

<task type="auto">
  <name>Task 3: Update .planning/STATE.md + .planning/ROADMAP.md to mark Phase 5 complete</name>
  <read_first>
    - .planning/STATE.md (entire — preserve unrelated content)
    - .planning/ROADMAP.md §Phase 5 + §Progress (entire)
    - .planning/phases/05-scrape-automation/05-VERIFICATION.md (just-written from Task 2)
  </read_first>
  <action>
    Two surgical edits using the Edit tool.

    Edit 1 — .planning/ROADMAP.md:
    a. In the top-of-file Phases checklist (lines ~14–19), change `- [ ] **Phase 5: Scrape Automation**` to `- [x] **Phase 5: Scrape Automation**`.
    b. In the §Phase 5 §Plans block, update the count + flip plan checkboxes to `[x]` for 05-01..05-07. (Currently the Plans block reads `**Plans**: TBD` — replace with `**Plans**: 7 plans` and add the full plan list below it, mirroring the format of Phases 1–4 in the same file.)
    c. In the §Progress table near the bottom, change the Phase 5 row from `0/0 | Not started | -` to `7/7 | Complete | <YYYY-MM-DD>` (today's date — 2026-05-28 per the operator's clock).

    Plan list to add under §Phase 5:
    ```
    Plans:
    **Wave 1**

    - [x] 05-01-PLAN.md — R2 bulk seed (operator gate + rewrite r2-sync.yml + initial bulk-sync trigger)
    - [x] 05-02-PLAN.md — Akamai spike Worker + akamai-spike.md ADR

    **Wave 2** *(blocked on Wave 1 completion)*

    - [x] 05-03-PLAN.md — Workers cron skeleton + wrangler.toml + KV namespace + bindings

    **Wave 3** *(blocked on Wave 2 completion)*

    - [x] 05-04-PLAN.md — Worker scrape lanes for 4 active archives + KV fingerprint diff
    - [x] 05-05-PLAN.md — GH Action ingest pipeline (release-upload.py + r2-promote.sh + scrape-fallback.yml)

    **Wave 4** *(blocked on Wave 3 completion)*

    - [x] 05-06-PLAN.md — SCRP-08 fix + verify-python-retired.sh extension

    **Wave 5** *(blocked on Wave 4 completion)*

    - [x] 05-07-PLAN.md — E2E smoke + Phase 5 close
    ```

    Edit 2 — .planning/STATE.md:
    a. Increment `gsd_state_version`-level fields:
       - `progress.completed_phases`: 3 → 4. (Phase 5 close moves from `completed_phases=3` to `completed_phases=4`. NOTE: as of the gitStatus snapshot, STATE shows `completed_phases: 3`, `completed_plans: 19`. After Plans 05-01..05-07 land = +7 plans, so `completed_plans` goes from 19 to 26. Verify the current count by reading the file before editing.)
       - `progress.completed_plans`: 19 → 26 (adjust based on the actual current count after intermediate plan completions).
       - `progress.percent`: recompute = round(completed_phases / total_phases × 100) — if total_phases=6, completed_phases=4: 67%.
       - `last_updated`: today's ISO timestamp.
       - `status`: stays `executing` (Phase 6 is next).
    b. Update the §Current Position block:
       - `Phase: 05 (scrape-automation) — COMPLETE`
       - `Plan: 7 of 7`
       - `**Status:** Phase 5 closed. Phase 6 (Hosting & Cutover) is the next phase.`
       - `**Progress:** 4 / 6 phases complete`
       - Update the progress bar visualisation accordingly.
    c. Append to §Accumulated Context §Key Decisions table any cross-Phase decisions worth surfacing (e.g., AKAMAI_BLOCKED_SOURCES from Plan 05-02 ADR).
    d. Append to §Session Continuity §Last Action:
       - "Phase 5 (Scrape Automation) closed via Plan 05-07 E2E smoke on <YYYY-MM-DD>. SCRP-01..10 all verified. ROADMAP.md updated, plans 05-01..07 marked [x]."
    e. §Next Action:
       - "`/gsd:plan-phase 6` to decompose Phase 6 (Hosting & Cutover) into executable plans. Phase 6 success criteria (per ROADMAP.md): preview deploy stable ≥ 7 days, DNS swap to CF Pages, 7-day monitor window, archive legacy GH Pages workflow, CLAUDE.md updated end-to-end."

    Constraints:
    - No other sections of STATE.md or ROADMAP.md change.
    - Preserve frontmatter on both files.
    - Use the Edit tool (NOT Write) — surgical edits, not rewrites.
  </action>
  <verify>
    <automated>grep -c "\\[x\\] \\*\\*Phase 5:" .planning/ROADMAP.md && grep -c "05-01-PLAN.md" .planning/ROADMAP.md && grep -c "05-07-PLAN.md" .planning/ROADMAP.md && grep -c "completed_phases: 4" .planning/STATE.md && grep -c "Phase 5 closed" .planning/STATE.md && grep -c "5. Scrape Automation" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phases checklist shows `[x]` for Phase 5.
    - ROADMAP.md §Phase 5 §Plans now lists 7 plans, all marked `[x]`.
    - ROADMAP.md §Progress table row for Phase 5 reads `7/7 | Complete | <today>`.
    - STATE.md frontmatter `progress.completed_phases` = 4, `progress.completed_plans` increased by 7 from the prior value, `progress.percent` recomputed.
    - STATE.md §Current Position reflects Phase 5 closed + Phase 6 as next.
    - STATE.md §Session Continuity §Last Action + §Next Action both updated.
    - No unrelated sections modified.
  </acceptance_criteria>
  <done>Phase 5 is officially closed in the planning system. `/gsd:next` will now recommend Phase 6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator local shell → Cloudflare wrangler + gh CLI | Smoke executes via authenticated CLI sessions |
| ingest.yml + scrape.yml on GH Actions | Reads repo secrets; smoke triggers verify auth surface still works |
| 2.1 GB sparse file local → release-upload.py | Synthetic test for R2 overflow; never uploaded anywhere |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering | Synthetic 2.1 GB sparse file could be confused for a real asset | mitigate | Test file written to `/tmp/big.bin` (NOT under repo); release-upload.py emits `R2_OVERFLOW` log + skips upload; operator deletes file after Stage H. No persistence. |
| T-05-07-02 | Information Disclosure | VERIFICATION.md contains raw operator-captured outputs (Worker URLs, run ids, KV namespace ids) | accept | These are not secrets per se — operator's CF/GH ACLs gate access. KV namespace id is opaque; Worker URL is `.workers.dev`-routable (manual-trigger secret still gates the /run endpoint). |
| T-05-07-03 | DoS | Stage F two-concurrent /run could trigger CF rate-limit | accept | Two requests within 1s is well under CF's per-IP rate limits. If the operator's IP is rate-limited from CF dashboard activity, smoke retries after 60s. |
| T-05-07-SC | Tampering | No new package installs by this plan | accept | Plan 05-07 only writes Markdown + edits STATE/ROADMAP. No new dependencies. |
</threat_model>

<verification>
- Task 1 E2E smoke runs to completion across all 8 stages; outputs captured.
- 05-VERIFICATION.md exists with all 10 SCRP rows + 5 ROADMAP success criteria + sign-off line.
- STATE.md + ROADMAP.md grep checks pass — Phase 5 status flipped to Complete.
- `bash scripts/verify-python-retired.sh` exits 0 with the extended Phase 5 invariants (regression guard against Worker scaffold removal).
</verification>

<success_criteria>
- E2E smoke proves all 10 SCRP requirements are met (or documents the gap explicitly).
- 05-VERIFICATION.md is audit-ready.
- STATE.md + ROADMAP.md reflect Phase 5 = Complete.
- 05-07-SUMMARY.md documents the close + the operator hand-off for Phase 6.
- Follow-up items captured: throwaway AKAMAI_SPIKE KV namespace cleanup, curl_cffi pinning status, next-Monday production cron observation.
</success_criteria>

<output>
Create `.planning/phases/05-scrape-automation/05-07-SUMMARY.md` documenting:
- Smoke run date + operator + run id references
- Per-SCRP closure status (matrix copy from VERIFICATION.md)
- Roadmap + STATE update diff (which lines changed)
- Follow-up items list
- "Phase 5 — Scrape Automation: CLOSED. Next: /gsd:plan-phase 6 (Hosting & Cutover)." closing line.
</output>

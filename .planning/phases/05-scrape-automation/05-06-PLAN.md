---
phase: 05-scrape-automation
plan: "06"
type: execute
wave: 4
depends_on: ["05-05"]
files_modified:
  - .github/workflows/scrape.yml
  - scripts/verify-python-retired.sh
  - .planning/phases/05-scrape-automation/05-06-SUMMARY.md
autonomous: false
requirements: [SCRP-08]
must_haves:
  truths:
    - "ALL occurrences of `|| true` are removed from the `pip install curl_cffi` line in `.github/workflows/scrape.yml` — SCRP-08 closed"
    - "The legacy scrape.yml continues to function for the dormant 11-archive scrape lanes (it currently references deleted `build-*.py` per python-build-retired ADR; this plan does NOT fix the dormant build paths — they stay broken on purpose per ADR §Negative/accepted risk; only the curl_cffi `|| true` is removed)"
    - "`scripts/verify-python-retired.sh` is updated to ADDITIONALLY assert: (a) the SCRP-08 invariant holds (no `|| true` after `pip install curl_cffi` anywhere in `.github/workflows/`), AND (b) the Phase 5 Worker scaffolding under `workers/scrape-cron/` + `workers/akamai-spike/` is present (Plan 05-03/04 outputs preserved)"
    - "Operator can still `gh workflow run scrape.yml` for ad-hoc back-fill (SCRP-09) — workflow_dispatch path preserved verbatim"
  artifacts:
    - path: ".github/workflows/scrape.yml"
      provides: "Legacy scrape workflow with SCRP-08 fix; cron line preserved; workflow_dispatch preserved"
      contains: "pip install curl_cffi"
    - path: "scripts/verify-python-retired.sh"
      provides: "Extended invariant guard covering SCRP-08 + Phase 5 Worker scaffolding"
      contains: "curl_cffi"
  key_links:
    - from: ".github/workflows/scrape.yml"
      to: "Plan 05-04 production Worker (realufo-scrape-cron)"
      via: "The new Worker now owns the 4-active scrape lanes; scrape.yml retains its dormant-only role per CONTEXT.md D-11"
      pattern: "scrape.yml"
    - from: "scripts/verify-python-retired.sh"
      to: ".github/workflows/scrape.yml SCRP-08 grep"
      via: "verify script greps for curl_cffi || true pattern; fails if found"
      pattern: "curl_cffi.*\\|\\| true"
---

<objective>
Two precise edits:

1. Remove every `|| true` masking `pip install curl_cffi` failures in `.github/workflows/scrape.yml` (SCRP-08).
2. Extend `scripts/verify-python-retired.sh` to enforce the SCRP-08 invariant AND verify the Phase 5 Worker scaffolding is present (regression guard against accidental Worker deletion).

Purpose: Closes SCRP-08 (the last open SCRP requirement before Plan 05-07's E2E smoke). The legacy scrape.yml's `|| true` was identified in CONTEXT.md as a bug — `pip install` failures silently swallowed, making CI green on broken installs. Plan 05-05's new `scrape-fallback.yml` was born without this anti-pattern; this plan retrofits the legacy file.

Output: scrape.yml without `|| true` on curl_cffi installs + verify-python-retired.sh extended.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-scrape-automation/05-CONTEXT.md
@.planning/decisions/python-build-retired.md
@.planning/phases/05-scrape-automation/05-05-SUMMARY.md
@.github/workflows/scrape.yml
@scripts/verify-python-retired.sh

<interfaces>
From CONTEXT.md §D-18:
- SCRP-08: remove all `|| true` from curl_cffi install steps. Phase 5 scrape.yml rewrite explicitly fails on dependency-install errors instead of silently swallowing.

From the current `.github/workflows/scrape.yml`:
- Line that needs fixing: `run: pip install --upgrade pip && pip install curl_cffi || true`. The `|| true` covers the WHOLE command — a `pip install` failure (network, version mismatch, package compromised) currently still passes the step.
- Surrounding context (line 26): `name: Install dependencies (curl_cffi for Akamai-fronted hosts)`.
- The legacy scrape.yml has many OTHER `|| true` invocations (most subsequent steps tolerate failures by design — those are FINE per python-build-retired ADR §Negative/accepted risk because the dormant 11-archive build scripts are intentionally broken and tolerated). ONLY the dependency-install `|| true` is the SCRP-08 target.

From python-build-retired ADR §Negative/accepted risk (line 79):
- "scrape.yml is presently broken in main: it references already-deleted `build-{aaro,nasa,nara}.py` (deleted in Plans 04-15..04-17). It runs only on the weekly Monday cron + `workflow_dispatch`; nobody depends on its output until Phase 5 reactivates scraping."
- This plan does NOT fix that brokenness. Plan 05-04 took over the 4-active scrape lanes via Workers cron; scrape.yml's reference to deleted builders is now wholly stale for the active set. The dormant 11-archive lanes inside scrape.yml are kept-broken on purpose (CONTEXT.md D-11).

From CONTEXT.md §D-19 (SCRP-09 preservation):
- Manual `gh workflow run scrape.yml` still works for ad-hoc / back-fill. The cron line + workflow_dispatch on/off block must be preserved verbatim.

From scripts/verify-python-retired.sh:
- Existing invariant guard from Phase 4 Plan 04-20. Asserts `build-wargov.py`, `build-details.py`, `sync-*.py`, `parse-*.py`, etc. are deleted.
- Extends naturally for Phase 5 with two additional assertions: (a) SCRP-08 grep, (b) Worker scaffolding present.

From Plan 05-05 §curl_cffi pinning follow-up:
- Optional: pin `curl_cffi==<version>` rather than unpinned. Only act on this if RESEARCH.md §Package Legitimacy Audit identifies a specific version. Otherwise leave unpinned (track as future-Phase-5 tightening).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove `|| true` from pip install curl_cffi in .github/workflows/scrape.yml (SCRP-08 fix)</name>
  <read_first>
    - .github/workflows/scrape.yml (entire file — only line 27 changes; preserve every other line verbatim per D-19 SCRP-09 preservation)
    - .planning/phases/05-scrape-automation/05-CONTEXT.md §D-18 + §D-19
    - .planning/decisions/python-build-retired.md §Negative/accepted risk
  </read_first>
  <action>
    Use the Edit tool to make a single surgical change to .github/workflows/scrape.yml.

    The current line 27 is:
    `        run: pip install --upgrade pip && pip install curl_cffi || true`

    Replace with:
    `        run: pip install --upgrade pip && pip install curl_cffi`

    Two-part fix in one line: the trailing `|| true` is removed.

    Do NOT touch any other `|| true` in the file — those mask deliberate-broken-tolerance for the dormant 11-archive builders per python-build-retired ADR. Only the dependency-install line is the SCRP-08 target.

    Also update the `name:` of the step (line 26) to make the intent explicit:
    Current: `name: Install dependencies (curl_cffi for Akamai-fronted hosts)`
    Replace with: `name: Install dependencies (curl_cffi for Akamai-fronted hosts — SCRP-08: hard-fail on install error)`

    No other edits.

    Optional (operator discretion — do NOT do this unless Plan 05-05 §curl_cffi pinning follow-up explicitly directed it): pin `curl_cffi==<version>`. Default: leave unpinned and document the pinning option in 05-06-SUMMARY.md.
  </action>
  <verify>
    <automated>yamllint .github/workflows/scrape.yml 2>&1 | (! grep -E '^(error|.* error)') && (! grep -E "pip install curl_cffi.*\\|\\| true" .github/workflows/scrape.yml) && grep -c "SCRP-08" .github/workflows/scrape.yml && grep -c "pip install curl_cffi" .github/workflows/scrape.yml && grep -c "0 6 \* \* 1" .github/workflows/scrape.yml && grep -c "workflow_dispatch" .github/workflows/scrape.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/scrape.yml still parses as YAML (yamllint exits 0 or only emits warnings, no errors).
    - `grep -E "pip install curl_cffi.*\\|\\| true" .github/workflows/scrape.yml` returns 0 lines (SCRP-08 invariant).
    - `grep "SCRP-08" .github/workflows/scrape.yml` returns ≥ 1 line (step name updated for traceability).
    - The cron line `0 6 * * 1` is preserved (D-02 / D-19).
    - The `workflow_dispatch:` trigger block is preserved (SCRP-09).
    - Other `|| true` occurrences in the file (on script-invocation lines) are UNCHANGED (intentional tolerance per python-build-retired ADR).
  </acceptance_criteria>
  <done>SCRP-08 closed at the file level. CI will now hard-fail on curl_cffi install errors.</done>
</task>

<task type="auto">
  <name>Task 2: Extend scripts/verify-python-retired.sh with SCRP-08 + Worker scaffold invariants</name>
  <read_first>
    - scripts/verify-python-retired.sh (current Phase 4 close output — entire file)
    - .planning/phases/05-scrape-automation/05-CONTEXT.md §D-18 (SCRP-08) + §Code Insights §Reusable Assets (spider.py preserved)
    - workers/scrape-cron/wrangler.toml (Plan 05-03 — confirm path; this script will assert presence)
    - workers/akamai-spike/wrangler.toml (Plan 05-02 — same)
    - .planning/decisions/akamai-spike.md (Plan 05-02 ADR — same)
  </read_first>
  <action>
    Edit scripts/verify-python-retired.sh in place. Use Edit tool (NOT Write — preserve the Phase 4 invariant logic verbatim).

    Additions to make, in order (append at the BOTTOM of the file, before the final `echo "verify-python-retired.sh: OK..."` line):

    Addition 1 — SCRP-08 invariant (Phase 5 Plan 05-06):
    A new check section with a clear header comment:
    `# Phase 5 Plan 05-06 — SCRP-08 invariant: pip install curl_cffi must NEVER be followed by || true.`

    Use `grep -rE "pip install curl_cffi.*\\|\\| true" .github/workflows/` (exclude this script itself — but it's in scripts/, not .github/workflows/, so no exclusion needed). If grep matches, append the offending filename(s) to a fail message and set `fail=1`.

    Addition 2 — Worker scaffold invariant (Phase 5 Plans 05-02 + 05-03):
    A new check section with a clear header comment:
    `# Phase 5 Plans 05-02 + 05-03 — Worker scaffolding must be present.`

    Required-present files (parallel structure to the existing must_be_absent array):
    ```
    must_be_present_workers=(
      workers/scrape-cron/wrangler.toml
      workers/scrape-cron/src/index.ts
      workers/akamai-spike/wrangler.toml
      .planning/decisions/akamai-spike.md
    )
    ```

    Loop over the array; for each path, `if [ ! -e "$f" ]; then echo "FAIL: $f missing — Plan 05-02/03 Worker scaffolding regression" >&2; fail=1; fi`.

    The final `echo "verify-python-retired.sh: OK..."` line must also be updated to acknowledge the new invariants:
    Current: `echo "verify-python-retired.sh: OK — Python build retirement invariant holds (spider.py + scrape.yml builders + copy-legacy-archives.sh preserved by design)"`
    Replace with: `echo "verify-python-retired.sh: OK — Phase 4 + Phase 5 invariants hold (spider.py + scrape.yml builders + copy-legacy-archives.sh preserved; SCRP-08 invariant; Worker scaffold present)"`

    No fenced code blocks in this action — the executor adds the new bash blocks using the structural pattern above. The script must still be `set -euo pipefail` compliant.

    File total length should stay ≤ 160 lines after additions.
  </action>
  <verify>
    <automated>bash -n scripts/verify-python-retired.sh && bash scripts/verify-python-retired.sh && grep -c "SCRP-08" scripts/verify-python-retired.sh && grep -c "workers/scrape-cron" scripts/verify-python-retired.sh && grep -c "workers/akamai-spike" scripts/verify-python-retired.sh && grep -c "akamai-spike.md" scripts/verify-python-retired.sh</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-python-retired.sh passes `bash -n` syntax check.
    - Running `bash scripts/verify-python-retired.sh` exits 0 (current state: all invariants hold). NOTE: if Plans 05-02 + 05-03 are not yet executed, this script will FAIL until those workers exist — this is the intended ordering (05-06 depends on 05-05, which depends on 05-04, which depends on 05-03). The verify script doubles as the "did all prior Phase 5 plans execute correctly" check.
    - `grep "SCRP-08" scripts/verify-python-retired.sh` returns ≥ 1 line.
    - `grep "workers/scrape-cron" scripts/verify-python-retired.sh` returns ≥ 1 line.
    - `grep "workers/akamai-spike" scripts/verify-python-retired.sh` returns ≥ 1 line.
    - `grep "akamai-spike.md" scripts/verify-python-retired.sh` returns ≥ 1 line.
    - The existing must_be_absent block from Phase 4 is unchanged (Phase 4 close invariants preserved).
    - Final "OK" message references both Phase 4 + Phase 5 invariants.
  </acceptance_criteria>
  <done>verify-python-retired.sh now guards Phase 5 Worker scaffolding + SCRP-08 in addition to Phase 4 deletions. Re-run before any commit touching scripts/, .github/workflows/, or workers/ catches regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GH Actions runner → pip / curl_cffi install | Upstream pip + PyPI surface; previously failure-masked by `|| true` |
| Local dev shell → verify-python-retired.sh | Pre-commit invariant guard; no auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | A future PR re-introduces `\|\| true` after `pip install curl_cffi` | mitigate | verify-python-retired.sh greps for the pattern on every invocation. Run pre-commit. (Wire into CI as a future follow-up if regressions appear.) |
| T-05-06-02 | DoS | A genuine curl_cffi install failure now hard-fails the workflow instead of silently passing | accept | Intentional — the silent-pass was the bug. If a transient pip failure hits, operator re-runs the workflow via `gh workflow run scrape.yml` (SCRP-09 preserved). If a sustained pip failure hits, that's a real issue that warrants operator attention. |
| T-05-06-03 | Spoofing | A compromised PyPI mirror could ship a malicious `curl_cffi` | mitigate | This is a meta-Phase concern. Plan 05-05 §curl_cffi pinning follow-up notes the option to pin a specific version. Currently unpinned (matches Phase 4 posture). If the package-legitimacy-audit table in RESEARCH.md flags `curl_cffi` as `[ASSUMED]` or `[SUS]`, Plan 05-06 must pin — see Plan 05-05 SUMMARY. |
| T-05-06-SC | Tampering | No new package installs introduced by this plan | accept | Plan 05-06 only edits YAML + bash. No new dependencies. |
</threat_model>

<verification>
- scrape.yml passes yamllint.
- `grep -E "pip install curl_cffi.*\\|\\| true" .github/workflows/scrape.yml` returns 0 lines.
- scrape.yml's cron + workflow_dispatch are preserved (SCRP-09).
- verify-python-retired.sh passes `bash -n` and exits 0 on the current tree.
- All Phase 4 invariants from the existing script are preserved.
</verification>

<success_criteria>
- SCRP-08 closed.
- verify-python-retired.sh extended for Phase 5 — runs clean.
- 05-06-SUMMARY.md captures the surgical diff (single-line change in scrape.yml + bottom-of-file additions in verify-python-retired.sh) + curl_cffi pinning follow-up status (pinned vs unpinned + rationale).
- "Wave 5 unblocked: Plan 05-07 (E2E smoke + Phase 5 close) may proceed."
</success_criteria>

<output>
Create `.planning/phases/05-scrape-automation/05-06-SUMMARY.md` documenting:
- The 1-line scrape.yml diff (before/after the `pip install curl_cffi` line) — captured exactly
- The verify-python-retired.sh additions (showing the new check blocks)
- The curl_cffi pinning decision (pinned vs unpinned + rationale referencing Plan 05-05 follow-up)
- "Wave 5 unblocked: Plan 05-07 may proceed" hand-off line.
</output>

---
phase: 05-scrape-automation
plan: "04"
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - workers/scrape-cron/src/index.ts
  - workers/scrape-cron/src/lanes.ts
  - workers/scrape-cron/src/fingerprint.ts
  - workers/scrape-cron/src/dispatch.ts
  - workers/scrape-cron/src/types.ts
  - workers/scrape-cron/wrangler.toml
  - .planning/phases/05-scrape-automation/05-04-SUMMARY.md
autonomous: false
requirements: [SCRP-01, SCRP-04]
must_haves:
  truths:
    - "Each of the 4 active archives (wargov, aaro, nasa, nara) has a dedicated scrape lane function that fetches its source landing page from the Worker"
    - "Each scrape lane records an ETag fingerprint in KV under `fingerprint:<slug>:<source-url-key>`; ETag mismatch (or first run with no prior fingerprint) treats the source as changed"
    - "Sources in `AKAMAI_BLOCKED_SOURCES` (locked in Plan 05-02 ADR) are skipped on the Worker; instead the Worker emits a `repository_dispatch` event of type `scrape-akamai-fallback` so the GH Actions runner (Plan 05-05) handles them"
    - "On change detection (ETag diff), the Worker writes the new content body to R2 staging at `staging/<asset-type>/<slug>/<basename>` and fires a `repository_dispatch` event of type `scrape-promote` with payload `{ archive, asset_keys: string[] }` for Plan 05-05's ingest pipeline"
    - "Per-archive failures are isolated: a wargov fetch throw does not prevent aaro/nasa/nara lanes from running. Each lane is wrapped in its own try/catch with a structured `console.error` log; the Worker scheduled() handler returns successfully even when 1+ lanes fail"
  artifacts:
    - path: "workers/scrape-cron/src/lanes.ts"
      provides: "Per-archive scrape lane functions for wargov, aaro, nasa, nara"
      min_lines: 80
    - path: "workers/scrape-cron/src/fingerprint.ts"
      provides: "KV ETag/content-hash fingerprint read+write helpers"
      contains: "readFingerprint"
    - path: "workers/scrape-cron/src/dispatch.ts"
      provides: "GitHub repository_dispatch event sender"
      contains: "sendRepositoryDispatch"
    - path: "workers/scrape-cron/src/types.ts"
      provides: "Shared TypeScript types: ScrapeLane, FingerprintRecord, DispatchPayload"
      contains: "interface ScrapeLane"
  key_links:
    - from: "workers/scrape-cron/src/index.ts scheduled()"
      to: "workers/scrape-cron/src/lanes.ts (LANES array)"
      via: "ctx.waitUntil(Promise.allSettled(LANES.map(lane => runLane(lane, env, runId))))"
      pattern: "Promise.allSettled"
    - from: "workers/scrape-cron/src/lanes.ts"
      to: "workers/scrape-cron/src/fingerprint.ts"
      via: "readFingerprint / writeFingerprint helpers per lane"
      pattern: "readFingerprint|writeFingerprint"
    - from: "workers/scrape-cron/src/lanes.ts"
      to: "GitHub repository_dispatch event `scrape-promote`"
      via: "src/dispatch.ts sendRepositoryDispatch helper using GH_DISPATCH_TOKEN secret"
      pattern: "scrape-promote"
    - from: "AKAMAI_BLOCKED_SOURCES (transcribed from .planning/decisions/akamai-spike.md)"
      to: "workers/scrape-cron/src/lanes.ts constant"
      via: "verbatim copy of the AKAMAI_BLOCKED_SOURCES array from the ADR"
      pattern: "AKAMAI_BLOCKED_SOURCES"
---

<objective>
Fill in the no-op `scrape()` from Plan 05-03 with real per-archive lanes. Each lane fetches its source landing page, computes an ETag fingerprint, diffs against the prior KV-stored fingerprint, and on change writes to R2 staging + fires `repository_dispatch` events. Akamai-blocked sources (per `.planning/decisions/akamai-spike.md`) fall through to a different `repository_dispatch` event type for the GH Actions fallback runner.

Purpose: Closes SCRP-01 (Worker cron skeleton invokes per-source scrape lanes; KV stores per-source fingerprints) and SCRP-04 (`repository_dispatch` payload from Worker → GitHub Action triggers ingest pipeline). Sets up the producer side of the contract that Plan 05-05 implements on the consumer side.

Output: Production-ready scrape-cron Worker with 4 isolated lanes, KV fingerprinting, and outbound `repository_dispatch` events.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-scrape-automation/05-CONTEXT.md
@.planning/decisions/akamai-spike.md
@.planning/phases/05-scrape-automation/05-03-SUMMARY.md
@workers/scrape-cron/src/index.ts
@workers/scrape-cron/src/lock.ts
@scripts/dl-aaro.sh
@scripts/dl-nasa.sh
@scripts/dl-nara.sh

<interfaces>
From CONTEXT.md §D-01 + §D-10:
- 4 active archives in lane order: wargov, aaro, nasa, nara. NEVER add geipan/uk/brazil/chile/argentina/canada/italy/nz/peru/spain/uruguay — dormant per D-11.

Per-archive source URLs (sourced from CLAUDE.md §2 table + existing `scripts/dl-<slug>.sh` orchestrators):
- wargov: `https://www.war.gov/UFO/`
- aaro: `https://www.aaro.mil/Cases/` (CONTEXT.md D-03 reference)
- nasa: `https://science.nasa.gov/uap/`
- nara: `https://catalog.archives.gov/` (Project Blue Book sub-catalogue is `https://catalog.archives.gov/search?q=Project+Blue+Book&limit=10` — use the root for fingerprinting; full asset enumeration is GH-Actions-fallback territory)

NOTE: wargov fingerprinting is best-effort only because the wargov binary corpus is sourced from `uap-release001.csv` (CLAUDE.md §11 untouchable), NOT from scraping the war.gov page. The wargov lane STILL runs (records ETag for the source landing page so operator can see when war.gov updates) but does NOT trigger binary fetches/R2 writes. Document this asymmetry in `lanes.ts` comments.

From CONTEXT.md §Integration Points:
- repository_dispatch event types this plan emits:
  - `scrape-promote` — fired on detected ETag change with a payload of asset keys staged in R2 (Plan 05-05 ingest)
  - `scrape-akamai-fallback` — fired for Akamai-blocked sources (Plan 05-05 fallback runner picks up)
- Payload shape (locked here for Plan 05-05): `{ archive: string, asset_keys: string[], scraped_at: string, run_id: string, reason: 'etag-changed' | 'akamai-blocked' | 'manual-resync' }`

From .planning/decisions/akamai-spike.md:
- Plan 05-04 imports `AKAMAI_BLOCKED_SOURCES` verbatim from the ADR.
- Transcribe the array literal as a `const` in `workers/scrape-cron/src/lanes.ts`. Add a comment with the source reference: `// AKAMAI_BLOCKED_SOURCES — locked in .planning/decisions/akamai-spike.md §Blocked sources`.

GH `repository_dispatch` REST API:
- Endpoint: `POST https://api.github.com/repos/hectorchanht/gov-ufo-archive/dispatches`
- Headers: `Accept: application/vnd.github+json`, `Authorization: Bearer <GH_DISPATCH_TOKEN>`, `X-GitHub-Api-Version: 2022-11-28`, `User-Agent: realufo-scrape-cron`
- Body: `{ event_type: 'scrape-promote' | 'scrape-akamai-fallback', client_payload: <DispatchPayload> }`
- Required token scope: `contents: write` AND `actions: write` for the target repo. Use a fine-grained PAT scoped to `hectorchanht/gov-ufo-archive` only, stored via `wrangler secret put GH_DISPATCH_TOKEN`.

KV fingerprint key shape:
- `fingerprint:<slug>:<urlhash>` — urlhash = first 16 chars of SHA-256 hex of the source URL (so multiple source URLs per archive don't collide).
- Stored value (JSON): `{ etag: string | null, lastModified: string | null, contentLength: number | null, fetchedAt: string }`.
- TTL: NONE (intentional — these persist across runs to drive diff detection).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shared types + dispatch helper + fingerprint helper</name>
  <read_first>
    - workers/scrape-cron/src/lock.ts (existing Env interface — extend in types.ts)
    - .planning/phases/05-scrape-automation/05-CONTEXT.md §Integration Points
    - .planning/decisions/akamai-spike.md §Blocked sources (the constant transcribed below)
    - GH repository_dispatch docs: https://docs.github.com/en/rest/repos/repos#create-a-repository-dispatch-event
  </read_first>
  <action>
    Create three new TypeScript modules under workers/scrape-cron/src/.

    File 1 — src/types.ts (≤ 60 lines):
    Export interfaces and types used across lanes/dispatch/fingerprint/index:
    - `Env` (extends Plan 05-03's): adds `REALUFO: R2Bucket`, `GH_DISPATCH_TOKEN: string`, `MANUAL_TRIGGER_SECRET: string`. Keep `SCRAPE_STATE: KVNamespace` from 05-03.
    - `FingerprintRecord` — shape declared in <interfaces> above (etag, lastModified, contentLength, fetchedAt).
    - `DispatchPayload` — shape declared in <interfaces> above (archive, asset_keys, scraped_at, run_id, reason).
    - `DispatchEventType` — `'scrape-promote' | 'scrape-akamai-fallback'`.
    - `ScrapeLane` — `{ slug: 'wargov' | 'aaro' | 'nasa' | 'nara'; sourceUrl: string; assetType: 'pdfs' | 'videos' | 'landing-only' }`. The `landing-only` variant means "fingerprint only; no R2 write" (used for wargov per the asymmetry note).

    File 2 — src/fingerprint.ts (≤ 80 lines):
    Export two async functions:
    - `readFingerprint(env: Env, slug: string, sourceUrl: string): Promise<FingerprintRecord | null>`
      Computes the urlhash (first 16 chars of SHA-256 hex of `sourceUrl` via `crypto.subtle.digest('SHA-256', new TextEncoder().encode(sourceUrl))`).
      Reads `fingerprint:<slug>:<urlhash>` from KV via `env.SCRAPE_STATE.get(..., 'json')`. Returns the parsed value or null.
    - `writeFingerprint(env: Env, slug: string, sourceUrl: string, record: FingerprintRecord): Promise<void>`
      Same urlhash; writes JSON to KV with no TTL (persistent across runs).
    Helper `urlHash(url: string): Promise<string>` may be exported for tests/dispatch consumers.

    File 3 — src/dispatch.ts (≤ 80 lines):
    Export:
    - `sendRepositoryDispatch(env: Env, eventType: DispatchEventType, payload: DispatchPayload): Promise<void>` — POSTs to `https://api.github.com/repos/hectorchanht/gov-ufo-archive/dispatches` with the body shape and headers documented in <interfaces>. On non-204 response, `console.error` with status + response text and THROW (caller's try/catch in `lanes.ts` decides whether lane fails completely or marks soft-failed).
    - Internal constant `REPO = 'hectorchanht/gov-ufo-archive'` (D-21 operator identity; never `frankchanflow`).

    No fenced code blocks in this action — the executor uses the typed signatures and constraints above as the contract.

    Strict TypeScript; no `any`. Reference `@cloudflare/workers-types` for KVNamespace + R2Bucket + R2PutOptions.
  </action>
  <verify>
    <automated>test -f workers/scrape-cron/src/types.ts && test -f workers/scrape-cron/src/fingerprint.ts && test -f workers/scrape-cron/src/dispatch.ts && grep -c "sendRepositoryDispatch" workers/scrape-cron/src/dispatch.ts && grep -c "readFingerprint" workers/scrape-cron/src/fingerprint.ts && grep -c "DispatchPayload" workers/scrape-cron/src/types.ts && grep -c "hectorchanht/gov-ufo-archive" workers/scrape-cron/src/dispatch.ts</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist with the exported signatures listed.
    - dispatch.ts hard-codes `hectorchanht/gov-ufo-archive` as the dispatch target (NOT `frankchanflow/*`; per memory file).
    - types.ts ScrapeLane.slug union is exactly `'wargov' | 'aaro' | 'nasa' | 'nara'` — no other slugs (D-10).
    - fingerprint.ts uses `crypto.subtle.digest('SHA-256', ...)` for url-hashing (no string-based hash).
    - No `any` anywhere across the three files.
  </acceptance_criteria>
  <done>Shared types + helpers ready for the lane orchestrator in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Implement per-archive scrape lanes in workers/scrape-cron/src/lanes.ts</name>
  <read_first>
    - workers/scrape-cron/src/types.ts + fingerprint.ts + dispatch.ts (just-written from Task 1)
    - .planning/decisions/akamai-spike.md §Blocked sources (transcribe AKAMAI_BLOCKED_SOURCES verbatim)
    - scripts/dl-aaro.sh + scripts/dl-nasa.sh + scripts/dl-nara.sh (read each in full — confirms the source-page URLs already used by the GH Actions runner; do NOT change those URLs, mirror them in the Worker lanes)
    - .planning/phases/05-scrape-automation/05-CONTEXT.md §D-01 + §D-10
  </read_first>
  <action>
    Create workers/scrape-cron/src/lanes.ts. Targets ≤ 200 lines.

    Exports:

    1. `AKAMAI_BLOCKED_SOURCES` constant — verbatim transcription from .planning/decisions/akamai-spike.md §Blocked sources. Executor: open the ADR, copy the slug array literal exactly. Add a top-of-file comment: `// AKAMAI_BLOCKED_SOURCES — locked in .planning/decisions/akamai-spike.md §Blocked sources. Do NOT edit here; re-run Plan 05-02 spike + update ADR if Akamai posture changes.`

    2. `LANES` constant — readonly array of `ScrapeLane` with one entry per active slug. Order: wargov, aaro, nasa, nara.
       - wargov: `{ slug: 'wargov', sourceUrl: 'https://www.war.gov/UFO/', assetType: 'landing-only' }` — wargov is fingerprint-only (CSV-driven binary corpus per CLAUDE.md §11; the lane is for operator visibility into war.gov edits, NOT for binary scraping).
       - aaro: `{ slug: 'aaro', sourceUrl: 'https://www.aaro.mil/Cases/', assetType: 'pdfs' }` — pdfs because aaro publishes case PDFs.
       - nasa: `{ slug: 'nasa', sourceUrl: 'https://science.nasa.gov/uap/', assetType: 'pdfs' }`
       - nara: `{ slug: 'nara', sourceUrl: 'https://catalog.archives.gov/', assetType: 'pdfs' }`

    3. `async function runLane(lane: ScrapeLane, env: Env, runId: string): Promise<{ slug: string; outcome: 'unchanged' | 'changed' | 'akamai-fallback' | 'error'; detail?: string }>` — wraps a single lane's fetch + fingerprint + dispatch flow. Behaviour:
       a. If `AKAMAI_BLOCKED_SOURCES.includes(lane.slug)`: call `sendRepositoryDispatch(env, 'scrape-akamai-fallback', { archive: lane.slug, asset_keys: [], scraped_at: <ISO now>, run_id: runId, reason: 'akamai-blocked' })`. Return `{ slug, outcome: 'akamai-fallback' }`. Do NOT fetch from the Worker — the spike said the IP is blocked.
       b. Otherwise: `fetch(lane.sourceUrl, { method: 'GET', headers: { 'User-Agent': USER_AGENT, Accept: 'text/html,*/*' }, redirect: 'follow', cf: { cacheTtl: 0 } })` — declare `const USER_AGENT` at the top of the file matching the spike's realistic Chrome UA (so production traffic patterns match what the spike measured).
       c. Read response ETag, Last-Modified, Content-Length headers.
       d. `readFingerprint(env, lane.slug, lane.sourceUrl)` — get prior record.
       e. Compare: if prior is null OR `prior.etag !== currentEtag`: treated as changed.
          - For `assetType === 'landing-only'` (wargov): write the new fingerprint but do NOT write to R2, do NOT fire `scrape-promote`. Log `[runId] wargov landing-page changed — no-op (CSV-driven)`. Return outcome `'changed'` with detail `'landing-only — operator-visibility log'`.
          - For `assetType !== 'landing-only'`: write the response body to R2 at `staging/<assetType>/<slug>/landing-page.html` via `env.REALUFO.put(key, await response.clone().arrayBuffer(), { httpMetadata: { contentType: response.headers.get('content-type') || 'text/html' } })`. NOTE: this plan writes ONLY the landing page; per-asset enumeration (extracting PDF URLs from the landing page) is a Plan 05-05 / future-iteration concern. Then `writeFingerprint(env, lane.slug, lane.sourceUrl, newRecord)`. Then `sendRepositoryDispatch(env, 'scrape-promote', { archive: lane.slug, asset_keys: [`staging/${lane.assetType}/${lane.slug}/landing-page.html`], scraped_at: <ISO>, run_id: runId, reason: 'etag-changed' })`. Return outcome `'changed'`.
       f. If prior exists AND ETags match: write nothing, dispatch nothing, just log `[runId] <slug> unchanged (etag match)`. Return outcome `'unchanged'`.
       g. On any throw (fetch failure, KV failure, R2 failure, dispatch failure): `console.error('[runId] <slug> lane failed:', err)`. Return `{ slug, outcome: 'error', detail: <err.message> }`. Caller does NOT throw — lane failures are isolated.

    4. `async function runAllLanes(env: Env, runId: string): Promise<Array<{ slug: string; outcome: string; detail?: string }>>` — calls `Promise.allSettled` on `LANES.map(lane => runLane(lane, env, runId))`, unwraps settled values (rejections shouldn't happen because runLane catches), returns the array.

    Constraints:
    - Strict TS; no `any`.
    - No fenced code blocks in this action.
    - Each lane try/catch isolates failures — Promise.allSettled is belt-and-suspenders; the inner try/catch is the actual isolation.
    - The `USER_AGENT` constant matches the Plan 05-02 spike Worker's UA exactly (production traffic patterns match measurement).
  </action>
  <verify>
    <automated>test -f workers/scrape-cron/src/lanes.ts && grep -c "AKAMAI_BLOCKED_SOURCES" workers/scrape-cron/src/lanes.ts && grep -c "runLane" workers/scrape-cron/src/lanes.ts && grep -c "runAllLanes" workers/scrape-cron/src/lanes.ts && grep -c "Promise.allSettled" workers/scrape-cron/src/lanes.ts && grep -c "scrape-akamai-fallback" workers/scrape-cron/src/lanes.ts && grep -c "scrape-promote" workers/scrape-cron/src/lanes.ts && grep -c "'wargov'" workers/scrape-cron/src/lanes.ts && grep -c "'aaro'" workers/scrape-cron/src/lanes.ts && grep -c "'nasa'" workers/scrape-cron/src/lanes.ts && grep -c "'nara'" workers/scrape-cron/src/lanes.ts</automated>
  </verify>
  <acceptance_criteria>
    - workers/scrape-cron/src/lanes.ts exists, ≤ 220 lines.
    - LANES array has exactly 4 entries (wargov, aaro, nasa, nara) — no extra slugs.
    - `runLane` catches all inner throws; never propagates.
    - `runAllLanes` uses `Promise.allSettled`.
    - Both dispatch event types (`scrape-promote`, `scrape-akamai-fallback`) are emitted from runLane based on the AKAMAI_BLOCKED_SOURCES check.
    - wargov lane is `landing-only` (writes fingerprint, no R2, no scrape-promote dispatch).
    - The USER_AGENT constant matches Plan 05-02 spike Worker's UA verbatim.
  </acceptance_criteria>
  <done>Four scrape lanes implemented with isolated failure semantics; KV fingerprint diff drives R2 writes + dispatch events.</done>
</task>

<task type="auto">
  <name>Task 3: Wire runAllLanes into index.ts scheduled() + fetch() handlers (replacing the no-op scrape)</name>
  <read_first>
    - workers/scrape-cron/src/index.ts (Plan 05-03 output — no-op scrape() helper)
    - workers/scrape-cron/src/lanes.ts (just-written from Task 2)
    - workers/scrape-cron/src/types.ts (just-written from Task 1 — Env now has REALUFO + GH_DISPATCH_TOKEN)
  </read_first>
  <action>
    Edit workers/scrape-cron/src/index.ts (preserve Plan 05-03 lock + manual-trigger structure):

    1. Remove the no-op `scrape(env, runId)` helper. Import `runAllLanes` from `./lanes`.

    2. Inside the `scheduled()` handler's `ctx.waitUntil` try block, REPLACE the no-op-scrape call with: call `runAllLanes(env, runId)`. Capture the returned array of lane outcomes. After release, `console.log` a final summary line: `[<runId>] lanes complete: <n-changed> changed, <n-unchanged> unchanged, <n-akamai-fallback> akamai-fallback, <n-error> errors`.

    3. Inside the `fetch()` handler's `/run` path, same replacement: call `runAllLanes(env, runId)`. Return the outcomes array in the JSON response body so the operator can inspect lane states without tailing logs.

    4. Update the imports to include the new types/helpers. Update the local `Env` type alias to import from `./types` (single source of truth now) — remove any duplicate Env declaration from Plan 05-03.

    5. wrangler.toml updates:
       - Add a comment near the `[[kv_namespaces]]` block: `# GH_DISPATCH_TOKEN secret set via 'wrangler secret put GH_DISPATCH_TOKEN' — fine-grained PAT for hectorchanht/gov-ufo-archive (contents:write + actions:write).`
       - Do NOT add a `[vars]` entry for GH_DISPATCH_TOKEN — it MUST be a secret, set by operator in Task 4.

    Constraints:
    - File stays ≤ 140 lines after the edit.
    - All Plan 05-03 lock acquire/release semantics preserved.
    - No fenced code blocks in this action.
  </action>
  <verify>
    <automated>grep -c "runAllLanes" workers/scrape-cron/src/index.ts && grep -c "scheduled" workers/scrape-cron/src/index.ts && grep -c "acquireLock" workers/scrape-cron/src/index.ts && grep -c "releaseLock" workers/scrape-cron/src/index.ts && grep -c "from './types'" workers/scrape-cron/src/index.ts && (! grep -q "no-op" workers/scrape-cron/src/index.ts || echo "WARN: no-op references should be gone")</automated>
  </verify>
  <acceptance_criteria>
    - index.ts imports `runAllLanes` from `./lanes`.
    - The no-op scrape() helper is removed (no references to "no-op" in index.ts).
    - scheduled() and fetch() both call runAllLanes; both still call acquireLock + releaseLock in the same order.
    - Env interface is imported from `./types` (not redeclared inline).
    - wrangler.toml gains the GH_DISPATCH_TOKEN secret-setup comment.
  </acceptance_criteria>
  <done>The Worker is feature-complete for SCRP-01 + SCRP-04 from the producer side. Plan 05-05 closes the loop on the consumer side.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Operator sets GH_DISPATCH_TOKEN secret + redeploys + smoke-tests lane execution</name>
  <read_first>
    - workers/scrape-cron/wrangler.toml (Task 3 output with GH_DISPATCH_TOKEN comment)
    - .planning/phases/05-scrape-automation/05-03-SUMMARY.md (KV namespace id + MANUAL_TRIGGER_SECRET context)
    - GitHub fine-grained PAT docs: https://github.com/settings/personal-access-tokens (operator creates one)
  </read_first>
  <what-built>
    The Worker is now feature-complete on the producer side. Operator creates a GitHub fine-grained PAT scoped to `hectorchanht/gov-ufo-archive` with `contents: write` + `actions: write`, sets it as a Worker secret, redeploys, and runs an end-to-end smoke. The smoke fires real `repository_dispatch` events to the GH repo — Plan 05-05 has not yet built the consumer side, so the events will appear in `gh api repos/hectorchanht/gov-ufo-archive/dispatches` history but trigger nothing (Plan 05-05 wires the receiver workflow).
  </what-built>
  <how-to-verify>
    Operator steps (from `workers/scrape-cron/` directory):

    1. Create fine-grained PAT in GitHub:
       - Browser → https://github.com/settings/personal-access-tokens → "Generate new token"
       - Resource owner: `hectorchanht`
       - Repository access: "Only select repositories" → `hectorchanht/gov-ufo-archive`
       - Repository permissions: `Contents: Read and write`, `Actions: Read and write`, `Metadata: Read-only` (auto-added)
       - Expiration: 90 days (calendar reminder for rotation)
       - Generate → copy the `github_pat_...` value into clipboard.

    2. Set the Worker secret:
       - `pnpm exec wrangler secret put GH_DISPATCH_TOKEN`
       - Paste the PAT when prompted.
       - Wrangler confirms secret bound to `realufo-scrape-cron`.

    3. Redeploy: `pnpm exec wrangler deploy`. Confirm success.

    4. Smoke-test via manual fetch trigger:
       - `curl -s -H 'X-Manual-Trigger: <MANUAL_TRIGGER_SECRET>' https://realufo-scrape-cron.<account>.workers.dev/run | jq .`
       - Expected JSON body: array of 4 lane outcomes. Reasonable shape (depending on Akamai spike result):
         - wargov: `{ slug: 'wargov', outcome: 'changed' | 'unchanged', detail?: ... }` (first run = 'changed' because no prior fingerprint; second run = 'unchanged'). If wargov is in AKAMAI_BLOCKED_SOURCES per the spike, outcome will be `akamai-fallback`.
         - aaro/nasa/nara: same pattern; first run produces 'changed' + R2 staging write + repository_dispatch event fired. If blocked: 'akamai-fallback'.

    5. Verify repository_dispatch events landed in the GH repo:
       - `gh api repos/hectorchanht/gov-ufo-archive/dispatches --jq '.[0]'` — NOTE: this endpoint may not list dispatches directly; `gh api` may need to inspect the `actions/runs` or the audit log. Alternative verification: open the GH repo → Actions tab → look for `repository_dispatch` events listed (they appear even if no workflow yet listens). Capture screenshot or event payload.
       - Per-event content_type can also be verified via `gh api -X GET repos/hectorchanht/gov-ufo-archive/actions/runs --jq '.workflow_runs[]|select(.event=="repository_dispatch")|{id,event,name,head_commit}' | head -20` — though no workflow is listening yet (Plan 05-05 task).

    6. Verify R2 staging objects exist:
       - From operator local machine: `wrangler r2 object get realufo --pipe staging/pdfs/aaro/landing-page.html | head -20` (if aaro lane was 'changed' and not akamai-fallback). Expect HTML content of aaro.mil/Cases/.
       - For each non-akamai-fallback lane that returned 'changed', verify the corresponding `staging/<assetType>/<slug>/landing-page.html` object exists in R2.

    7. Run the smoke a SECOND time within 5 minutes:
       - `curl -s -H 'X-Manual-Trigger: <secret>' https://realufo-scrape-cron.<account>.workers.dev/run | jq .`
       - Expected: lanes that returned 'changed' on first run now return 'unchanged' (ETag matched). This verifies the fingerprint persistence path.
       - Note: if Workers KV propagation lag affects the read, second run may still show 'changed' — re-run after 60s. Document this in 05-04-SUMMARY.md as known eventual-consistency posture.

    8. Verify cron-lock behaviour:
       - Run two `/run` curls back-to-back within 1 second. Expected: first returns 200 with lane outcomes; second returns 423 (or 503 — depends on implementation choice in Plan 05-03 fetch handler) with "lock held" message.

    Operator reports: PAT secret bound, deploy succeeded, smoke 1 lane outcomes, smoke 2 idempotency verification, repository_dispatch landing confirmation, R2 object presence.
  </how-to-verify>
  <acceptance_criteria>
    - `wrangler secret list` (against realufo-scrape-cron) shows BOTH `MANUAL_TRIGGER_SECRET` and `GH_DISPATCH_TOKEN`.
    - Manual /run smoke returns 4 lane outcomes; at least one is `'changed'` OR `'akamai-fallback'` on first invocation.
    - At least one R2 object exists at `staging/<assetType>/<slug>/landing-page.html` (for any non-akamai-fallback lane).
    - At least one `repository_dispatch` event is visible in the GH repo audit log (either Actions tab or `gh api`).
    - Second consecutive smoke shows `unchanged` for lanes that were `changed` on the first run (fingerprint diff is working).
    - Lock-held smoke (two requests within 1s) returns the second one with 423/503.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the captured lane-outcome JSON from smoke 1, smoke 2 outcomes, and a confirmation that ≥ 1 repository_dispatch event landed in the GH audit log.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → upstream .gov/.mil origins | Production traffic on every cron; subject to Akamai WAF posture from Plan 05-02 spike |
| Worker → GitHub REST API | `repository_dispatch` events authenticated by `GH_DISPATCH_TOKEN` PAT |
| Worker → KV `SCRAPE_STATE` | Fingerprint persistence + cron-lock; first-party CF binding |
| Worker → R2 `realufo` | Writes to `staging/` prefix; first-party CF binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Spoofing | A leaked `GH_DISPATCH_TOKEN` could fire arbitrary repository_dispatch events into hectorchanht/gov-ufo-archive | mitigate | PAT is fine-grained (single-repo scope) + minimal permissions (contents+actions write); stored as Worker secret (not env var, not source). Rotation: re-run `wrangler secret put GH_DISPATCH_TOKEN`. 90-day expiration with calendar reminder. |
| T-05-04-02 | Tampering | A compromised Worker could write malicious staging objects to R2 | mitigate | Plan 05-05 ingest pipeline validates staging objects (SHA check, content-type) before promoting to canonical R2 path + GH Releases. Plan 05-04 alone has no write to canonical R2 — staging-only. |
| T-05-04-03 | Information Disclosure | upstream site response bodies (HTML) cached in R2 staging | accept | These are public landing pages; no PII. R2 staging objects are private (no custom domain — accessible only via S3 keys + GH Actions runner) until promoted to canonical. |
| T-05-04-04 | Denial of Service | An Akamai-blocked source could flood the GH Actions runner with repository_dispatch events | mitigate | Plan 05-04 dispatches `scrape-akamai-fallback` ONLY once per cron run per source — cron is weekly, max 4 events/week of this type. GH Actions free-tier minutes well above this. |
| T-05-04-05 | Repudiation | No audit trail of which Worker invocation made which R2 write | mitigate | Each lane logs `runId` (crypto.randomUUID()) and R2 object metadata includes a `httpMetadata.cacheControl` field containing `run-id=<runId>`. `wrangler tail` captures the runId; CF Logs retain for 7 days. |
| T-05-04-06 | Elevation of Privilege | PAT scope creep (operator accidentally grants more than `contents+actions:write`) | mitigate | PAT creation runbook in Task 4 explicitly enumerates the 3 allowed permissions. Plan 05-07 smoke includes a PAT-permission audit step. |
| T-05-04-07 | Denial of Service | Unbounded Worker fetch latency on flaky source could exhaust scheduled() CPU budget | mitigate | Each fetch has `cf: { cacheTtl: 0 }` (no edge cache for upstream — we want fresh) but no per-request timeout. Plan 05-04 adds `AbortSignal.timeout(10_000)` to each lane fetch (10s per lane × 4 lanes = 40s worst case, well under Workers Paid 50ms-billable-CPU envelope per invocation but within the 30s wall-time per scheduled() invocation). |
| T-05-04-SC | Tampering | No new npm packages added (lock.ts/types.ts/etc. are stdlib + workers-types only) | accept | Inherits Plan 05-03 T-05-03-SC posture. No new third-party packages. |
</threat_model>

<verification>
- All 5 src files exist (types, fingerprint, dispatch, lanes, index) and reference each other via correct relative imports.
- Each grep gate from Tasks 1–3 returns the expected counts.
- Task 4 smoke produces 4 lane outcomes; at least one `repository_dispatch` event lands in the GH audit log.
- R2 staging contains ≥ 1 landing-page.html object per non-akamai-fallback lane.
- Second smoke shows `unchanged` for previously `changed` lanes (fingerprint persistence verified).
</verification>

<success_criteria>
- Worker runs all 4 lanes on Monday 06:00 UTC (verified via local --test-scheduled in Plan 05-03 Task 4 smoke; production verification deferred to Plan 05-07).
- Lane failures are isolated via per-lane try/catch + Promise.allSettled.
- ETag fingerprint diff drives R2 staging writes + scrape-promote dispatches.
- Akamai-blocked sources emit scrape-akamai-fallback dispatches instead.
- 05-04-SUMMARY.md captures lane outcomes from Task 4 smoke + R2 staging URLs verified + PAT expiration date for rotation reminder.
</success_criteria>

<output>
Create `.planning/phases/05-scrape-automation/05-04-SUMMARY.md` documenting:
- The 4 lane definitions (slug + sourceUrl + assetType)
- The AKAMAI_BLOCKED_SOURCES list as transcribed from Plan 05-02 ADR
- Task 4 smoke 1 lane-outcome JSON
- Task 4 smoke 2 idempotency verification
- R2 staging object URLs verified
- GH PAT expiration date (operator records for rotation)
- "Wave 3 partially unblocked: Plan 05-05 (ingest pipeline) may now build the consumer side of these dispatches. Plan 05-05 also depends on 05-01 (r2-sync.yml seeded) per orchestrator brief."
</output>

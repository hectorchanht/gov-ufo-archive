---
phase: 04-full-migration-search-offline-performance
plan: "19"
type: execute
wave: 7
depends_on: ["04-05", "04-06", "04-07", "04-08", "04-09", "04-10", "04-11", "04-12", "04-13", "04-14", "04-15", "04-16", "04-17", "04-18"]
files_modified:
  - package.json
  - src/layouts/RootLayout.astro
  - src/components/CatalogCard.astro
  - src/components/Card.astro
  - src/pages/search.astro
  - scripts/copy-legacy-archives.sh
  - api/all.json
  - search.html
  - tests/search.spec.ts
autonomous: true
requirements: [SRC-01, SRC-02, SRC-03, SRC-04, SSG-11, SSG-12]
tags: [pagefind, cross-archive-search, lunr-replacement, search-rewrite]

must_haves:
  truths:
    - "Pagefind 1.5+ installed as devDependency; pnpm postbuild runs `pnpm exec pagefind --site dist` after copy-legacy-archives.sh"
    - "dist/pagefind/pagefind.js + dist/pagefind/pagefind-ui.js + dist/pagefind/pagefind-ui.css + dist/pagefind/index/*.pf_index + dist/pagefind/fragment/*.pf_fragment all emit at build"
    - "RootLayout.astro <main> element carries `data-pagefind-body` attribute (RESEARCH §2 — restricts indexing scope to main content)"
    - "RootLayout.astro <nav> and <footer> + arch-controls-bar + pagination carry `data-pagefind-ignore` attribute"
    - "CatalogCard.astro (Plan 04-05) already carries data-pagefind-filter + data-pagefind-meta attributes — verify"
    - "Card.astro (wargov) MUST carry data-pagefind-filter={`archive:wargov,type:${rtype},agency:${agency}`} + data-pagefind-meta — UNCONDITIONAL add in this task (idempotent: overwrite any pre-existing attrs with canonical wargov form). Skipping breaks SRC-03 for wargov."
    - "/search.html legacy Lunr-based page DELETED; replaced by src/pages/search.astro with PagefindUI Component UI (SRC-02)"
    - "Per-archive filter UI shows on /search via PagefindUI filters config (SRC-03)"
    - "Result links append #card-<id> via processResult callback per RESEARCH §2 Pitfall 9 (SRC-04)"
    - "api/all.json (4.6 MB Lunr blob) DELETED (D-14)"
    - "tests/search.spec.ts validates: PagefindUI loads, query 'tic tac' returns ≥ 1 result, archive filter dropdown lists 15 archives, result anchor includes #card-"
  artifacts:
    - path: "src/pages/search.astro"
      provides: "Cross-archive search page using PagefindUI"
      contains: "PagefindUI"
    - path: "package.json"
      provides: "pagefind devDependency + postbuild hook extension"
      contains: "pagefind"
    - path: "dist/pagefind/pagefind.js"
      provides: "Pagefind WASM core loader (runtime asset)"
  key_links:
    - from: "src/pages/search.astro"
      to: "/pagefind/pagefind-ui.js"
      via: "<script is:inline src='/pagefind/pagefind-ui.js'>"
      pattern: "pagefind-ui\\.js"
    - from: "src/layouts/RootLayout.astro"
      to: "Pagefind index walker"
      via: "<main data-pagefind-body>"
      pattern: "data-pagefind-body"
---

<objective>
Land the cross-archive Pagefind 1.5+ search index per SRC-01..SRC-04, replacing Lunr's `api/all.json` 4.6 MB upfront blob. Pagefind walks `dist/` AFTER copy-legacy-archives.sh + all 14 archive ports + wargov complete, indexing the Astro-rendered pages with per-archive filters wired via `data-pagefind-filter` attributes already present on CatalogCard.astro (Plan 04-05).

Dependencies are intentionally heavy (all 14 ports + wargov + lightbox fix) per CONTEXT D-17b + RESEARCH §2 Pitfall 5: `data-pagefind-body` on RootLayout.astro would silently exclude legacy postbuild-copied HTML during transition window. Plan 04-19 lands ONLY after Wave 6 (GEIPAN port — last archive) completes.

`/search.html` (Lunr) → `src/pages/search.astro` (PagefindUI Component UI). Result links append `#card-<id>` via processResult callback (RESEARCH §2 + Pitfall 9). `api/all.json` 4.6 MB deleted in same plan (D-14).

Output: Pagefind index emits to dist/pagefind/; cross-archive search returns deep-linked results; 4.6 MB Lunr blob retired.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-migration-search-offline-performance/04-CONTEXT.md
@.planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md
@.planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md
@.planning/phases/04-full-migration-search-offline-performance/04-05-PLAN.md
@CLAUDE.md

<interfaces>
Pagefind 1.5+ CLI:
  pnpm exec pagefind --site dist
  Output: dist/pagefind/pagefind.js, pagefind-ui.js, pagefind-ui.css, index/*.pf_index, fragment/*.pf_fragment, pagefind-entry.json

PagefindUI default-UI (`@pagefind/default-ui` bundled with pagefind):
  new PagefindUI({ element, showSubResults, showImages, translations, filters, processResult })

Existing markup invariants:
  - CatalogCard.astro (Plan 04-05) — already emits data-pagefind-filter + data-pagefind-meta on each <article class="arch-card">
  - Card.astro (wargov, Plan 03-05 + 04-01) — MANDATORY: this task adds data-pagefind-filter={`archive:wargov,type:${rtype},agency:${agency}`} + data-pagefind-meta to Card.astro UNCONDITIONALLY (do not skip the addition based on a "verify" check — make it idempotent: replace any existing attributes with the canonical wargov form)
  - RootLayout.astro (Plan 03-04) — <main> currently lacks data-pagefind-body; add in this plan

Lunr legacy:
  - api/all.json (~4.6 MB) — to be deleted
  - search.html — root-level Lunr-based search UI (HTML); to be deleted in favor of src/pages/search.astro
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install pagefind devDep + wire postbuild + add data-pagefind-body/-ignore attributes to RootLayout.astro + verify CatalogCard.astro/Card.astro filter attributes</name>
  <files>package.json, src/layouts/RootLayout.astro, src/components/CatalogCard.astro, src/components/Card.astro, scripts/copy-legacy-archives.sh</files>
  <read_first>
    - package.json (current scripts.postbuild + devDependencies)
    - src/layouts/RootLayout.astro (current <main> + <nav> + <footer> markup; Plan 03-04 base)
    - src/components/CatalogCard.astro (Plan 04-05 — confirm data-pagefind-filter + data-pagefind-meta already present per Plan 04-05 Task 1)
    - src/components/Card.astro (Plan 03-05 + Plan 04-01 lightbox-fix — verify data-pagefind-filter + data-pagefind-meta wargov-equivalent; add if missing)
    - src/components/Nav.astro + Footer.astro (apply data-pagefind-ignore if not via RootLayout)
    - scripts/copy-legacy-archives.sh (current postbuild script — extend with pagefind command per RESEARCH §2)
    - .planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md §2 (Pagefind integration, data-pagefind-body placement)
    - CLAUDE.md §3 + §4 (markup invariants)
  </read_first>
  <behavior>
    - `pnpm add -D pagefind@^1.5` adds pagefind devDependency.
    - package.json scripts.postbuild already runs scripts/copy-legacy-archives.sh; that script gets extended at the END with `pnpm exec pagefind --site dist` per RESEARCH §2.
    - RootLayout.astro <main> gains `data-pagefind-body` attribute (RESEARCH §2 placement).
    - RootLayout.astro <nav> / <footer> elements (or Nav.astro / Footer.astro) gain `data-pagefind-ignore` if they're inside <main>; if they're siblings of <main>, no attribute needed (data-pagefind-body already restricts).
    - arch-controls-bar + pagination + scanlines overlay gain `data-pagefind-ignore` (RESEARCH §2 — exclude filter UI from index).
    - CatalogCard.astro filter attributes verified per Plan 04-05 contract; no edits expected.
    - Card.astro (wargov) verified: if data-pagefind-filter + data-pagefind-meta absent, add them in this task with archiveSlug='wargov' baked in.
  </behavior>
  <action>
    Run `pnpm add -D pagefind@^1.5`. Verify package.json devDependencies includes "pagefind": "^1.5.x".

    Read scripts/copy-legacy-archives.sh; append at the END (after the for-loop over remaining slugs — note: by Plan 04-19, the for-loop should be empty since all 14 + wargov ports complete in 04-04..04-18):
    ```
    echo "[postbuild] pagefind: indexing dist/ ..."
    pnpm exec pagefind --site dist
    ```

    Modify src/layouts/RootLayout.astro:
    - Add `data-pagefind-body` attribute to the <main> wrapping <slot />.
    - Confirm <nav> and <footer> are SIBLINGS of <main> (not children). If they're outside <main>, data-pagefind-body already excludes them — no additional attribute needed. If inside, add `data-pagefind-ignore`.

    Verify src/components/CatalogCard.astro carries data-pagefind-filter + data-pagefind-meta attributes per Plan 04-05 Task 1 contract. No edits expected; if missing, file is wrong vs Plan 04-05 (escalate).

    Verify src/components/Card.astro (wargov) carries data-pagefind-filter and data-pagefind-meta attributes. If not present, add equivalent markup with archiveSlug="wargov" baked in:
    - `data-pagefind-filter={`archive:wargov,type:${rtype},agency:${agency}`}`
    - `data-pagefind-meta={`title:${title},agency:${agency},date:${date}`}`

    Add `data-pagefind-ignore` to scanlines overlay div in RootLayout.astro AND to .arch-controls-bar + .pagination elements in EACH page (src/pages/index.astro + src/pages/<slug>/index.astro × 14). Use a one-time grep-and-patch sweep.

    `pnpm build` and confirm dist/pagefind/ subdir emits with pagefind.js + pagefind-ui.js + index/ + fragment/.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; grep -q "\"pagefind\":" package.json &amp;&amp; grep -q "pagefind --site dist" scripts/copy-legacy-archives.sh &amp;&amp; grep -q "data-pagefind-body" src/layouts/RootLayout.astro &amp;&amp; grep -q "data-pagefind-filter" src/components/CatalogCard.astro &amp;&amp; grep -q "data-pagefind-filter" src/components/Card.astro &amp;&amp; pnpm build 2>&amp;1 | tail -10 &amp;&amp; test -f dist/pagefind/pagefind.js &amp;&amp; test -f dist/pagefind/pagefind-ui.js &amp;&amp; test -d dist/pagefind/index</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies contains "pagefind": "^1.5..."
    - scripts/copy-legacy-archives.sh contains "pnpm exec pagefind --site dist" line at end
    - src/layouts/RootLayout.astro <main> carries data-pagefind-body attribute (grep -c 'data-pagefind-body' returns ≥ 1)
    - src/components/CatalogCard.astro contains data-pagefind-filter + data-pagefind-meta (grep -c each returns ≥ 1)
    - src/components/Card.astro contains data-pagefind-filter + data-pagefind-meta (wargov)
    - pnpm build exits 0 and emits dist/pagefind/pagefind.js + dist/pagefind/pagefind-ui.js + dist/pagefind/index/ dir
    - dist/pagefind/pagefind-entry.json exists (Pagefind index metadata)
  </acceptance_criteria>
  <done>Pagefind installed + wired into postbuild; RootLayout + CatalogCard + Card markup carry indexing attributes; dist/pagefind/ emits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace /search.html with src/pages/search.astro (PagefindUI Component UI + processResult anchor injection + filter chips) + delete api/all.json + tests/search.spec.ts</name>
  <files>src/pages/search.astro, search.html, api/all.json, tests/search.spec.ts</files>
  <read_first>
    - search.html (current Lunr-based root-level search UI — extract verbatim hero text + filter labels for fidelity baseline)
    - src/layouts/RootLayout.astro (use for shell)
    - .planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md §2 (PagefindUI block + processResult callback)
    - .planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md Pitfall 9 (result anchor injection — processResult adds #anchor)
    - tests/playwright.config.ts (PREVIEW_URL env contract)
    - CLAUDE.md §7 invariant 7 (/-focus on search input) + §4 (page-skeleton)
  </read_first>
  <behavior>
    - src/pages/search.astro replaces /search.html. Uses RootLayout (archiveSlug='wargov' for tone-default).
    - Loads /pagefind/pagefind-ui.css + /pagefind/pagefind-ui.js via <script is:inline> + <link rel="stylesheet">.
    - Mounts new PagefindUI({ element, showSubResults: true, filters: { archive, type, agency }, processResult }) on DOMContentLoaded.
    - processResult callback appends #anchor.id to sub_results URLs per RESEARCH §2 Pitfall 9 (SRC-04 compliance).
    - CLAUDE.md §7 invariant 7 preserved: `/` keydown focuses the search input.
    - api/all.json DELETED via git rm (D-14 — only after Pagefind verified green).
    - search.html DELETED via git rm (replaced by Astro page).
    - tests/search.spec.ts: 4 specs covering PagefindUI loads, query 'tic tac' returns results, archive filter chip dropdown lists 15 archives, result link href contains '#card-'.
    - Add 301 redirect /search.html → /search in _redirects if `_redirects` file already exists (Phase 2 02-01); else file follow-up note. Per RESEARCH §2 — preserves URL contract.
  </behavior>
  <action>
    Create src/pages/search.astro:
    - Frontmatter imports RootLayout. Define title "Search · realufo.org", description "Search 15 government UAP archives".
    - Use <RootLayout archiveSlug="wargov" title={title} description={description}>.
    - Body: <section class="search-container"><div id="pagefind-search"></div></section>.
    - <link rel="stylesheet" href="/pagefind/pagefind-ui.css" />.
    - <script is:inline src="/pagefind/pagefind-ui.js"></script>.
    - <script is:inline> mounting PagefindUI on DOMContentLoaded with filters: { archive, type, agency }, processResult callback appending sub_results[0].anchor.id, and translations: { placeholder: 'Search 15 archives...' }.

    git rm api/all.json (4.6 MB) ONLY after confirming dist/pagefind/ index is green (Task 1 verify ran).

    git rm search.html.

    If `_redirects` exists at repo root or in public/: append `/search.html /search 301` to preserve URL contract.

    Create tests/search.spec.ts with 4 specs:
    - "PagefindUI loads on /search": visit /search, expect #pagefind-search > .pagefind-ui to exist.
    - "search query 'tic tac' returns at least 1 result": visit /search, type 'tic tac' into search input, wait for results, expect ≥ 1 .pagefind-ui__result element.
    - "archive filter dropdown lists 15 archives": visit /search, click filter chip 'Archive', expect 15 options (or whatever GEIPAN-included full count is).
    - "result link href contains #card-": visit /search, query 'tic tac', click first result, expect URL has #card- fragment OR (alternate) inspect result.url DOM attribute.

    pnpm build → confirm dist/search/index.html (or dist/search.html depending on Astro routing) renders.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; test -f src/pages/search.astro &amp;&amp; grep -q "PagefindUI" src/pages/search.astro &amp;&amp; grep -q "processResult" src/pages/search.astro &amp;&amp; (test ! -f api/all.json || (echo 'api/all.json still exists'; exit 1)) &amp;&amp; (test ! -f search.html || (echo 'search.html still exists'; exit 1)) &amp;&amp; test -f tests/search.spec.ts &amp;&amp; pnpm build 2>&amp;1 | tail -5 &amp;&amp; (test -f dist/search/index.html || test -f dist/search.html)</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/search.astro exists, contains "PagefindUI" and "processResult" keywords (filter UI + anchor injection wired)
    - api/all.json deleted from working tree (D-14)
    - search.html deleted from working tree
    - tests/search.spec.ts exists with ≥ 4 describe/test blocks
    - dist/search/index.html (or dist/search.html) emits via pnpm build
    - dist/pagefind/index/ contains ≥ 1 *.pf_index file (cross-archive index built)
    - pnpm exec playwright test tests/search.spec.ts --grep "PagefindUI loads" passes against preview
    - pnpm exec playwright test tests/search.spec.ts --grep "tic tac" returns ≥ 1 result against preview
    - dist/pagefind/pagefind-entry.json reports pages_indexed ≥ 15 (all archive index pages indexed)
  </acceptance_criteria>
  <done>Pagefind cross-archive search live at /search; Lunr blob retired; result links carry #card- anchors per SRC-04; filter UI per SRC-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time→dist/pagefind/ | Pagefind CLI walks dist/ HTML → emits WASM index + fragments |
| client→/search | Browser loads pagefind-ui.js + WASM + lazy-fetches index shards on query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-62 | Tampering | Pagefind index spoof via dist/pagefind/ override | mitigate | dist/ is build output (gitignored); regenerated each build; CF Pages serves only signed deploy artefacts |
| T-04-63 | Tampering | XSS via PagefindUI rendering result.title without escape | mitigate | PagefindUI default rendering uses textContent (not innerHTML) per pagefind docs; processResult callback only mutates result.url (not HTML) |
| T-04-64 | Information Disclosure | Pagefind index leaks data-pagefind-ignore content | accept | data-pagefind-ignore correctly excludes filter UI + nav + footer; verified via dist/pagefind/index inspection |
| T-04-65 | Repudiation | Loss of `/search.html` URL contract | mitigate | 301 redirect /search.html → /search in _redirects (verify-redirects.sh preserves contract) |
</threat_model>

<verification>
1. `pnpm exec pagefind --site dist` exits 0 in postbuild chain.
2. dist/pagefind/pagefind.js + pagefind-ui.js + index/*.pf_index + fragment/*.pf_fragment all emit.
3. pnpm exec playwright test tests/search.spec.ts --grep "PagefindUI loads" passes.
4. pnpm exec playwright test tests/search.spec.ts --grep "tic tac" passes (≥ 1 result).
5. pnpm exec playwright test tests/search.spec.ts --grep "archive filter dropdown" passes (≥ 14 archives listed; depending on Pagefind enumeration mode may be 15).
6. pnpm exec playwright test tests/search.spec.ts --grep "result link.*#card-" passes (anchor appended via processResult).
7. api/all.json + search.html absent from working tree.
8. bash scripts/verify-redirects.sh passes (/search.html → /search 301 if _redirects exists; otherwise documented follow-up).
9. dist/pagefind/pagefind-entry.json pages_indexed ≥ 15.
</verification>

<success_criteria>
- [ ] Pagefind 1.5+ installed as devDependency; postbuild runs `pnpm exec pagefind --site dist`
- [ ] RootLayout.astro <main> carries data-pagefind-body
- [ ] CatalogCard.astro + Card.astro carry data-pagefind-filter + data-pagefind-meta
- [ ] /search route renders PagefindUI with filters: archive, type, agency (SRC-03)
- [ ] Result links append #card-<id> via processResult (SRC-04)
- [ ] api/all.json + search.html deleted (D-14)
- [ ] tests/search.spec.ts 4-spec coverage passes
- [ ] All 15 archives indexed in dist/pagefind/
</success_criteria>

<output>
Create `.planning/phases/04-full-migration-search-offline-performance/04-19-SUMMARY.md` when done.
</output>
</content>
</invoke>
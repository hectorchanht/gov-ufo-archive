---
phase: 04-full-migration-search-offline-performance
plan: "05"
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-04"]
files_modified:
  - src/components/CatalogCard.astro
  - src/pages/nz/index.astro
  - src/styles/nz.css
  - scripts/normalize-nz.py
  - data/nz.json
  - scripts/copy-legacy-archives.sh
  - scripts/sync-nav.py
  - scripts/sync-footer.py
  - tests/fidelity-samples.json
  - tests/visual-baselines/nz-360.png
  - tests/visual-baselines/nz-768.png
  - tests/visual-baselines/nz-1024.png
  - tests/visual-baselines/nz-1440.png
autonomous: true
requirements: [SSG-06, SSG-08, SSG-09, SSG-10, SSG-11, SSG-12, PERF-04]
tags: [archive-port, nz, first-port, catalog-card-establishment, small-static]

must_haves:
  truths:
    - "Visiting /nz/ renders NZ Defence Force archive from Astro Content Collections (catalogEnvelopeSchema)"
    - "Per-archive tone-colour --caution: #5b8def applied via RootLayout TONE map"
    - "NZ page weighs ≤ 500 KB HTML+inline per SSG-07"
    - "PAGE_SIZE = 20 cards/page (Plan 04-04 contract inherited)"
    - "JS-disabled view renders meaningful card content (server-side render, no hydration per D-23)"
    - "Lightbox + hamburger + arrow-keys + /?q= persistence all work per CLAUDE.md §7 invariants"
    - "Card URLs point to assets.realufo.org via _archive_common.rewrite_to_r2"
    - "scripts/build-nz.py is DELETED; nz/ legacy HTML dropped from copy-legacy-archives.sh"
    - "tone-colour Playwright spec passes for NZ archive"
  artifacts:
    - path: "src/components/CatalogCard.astro"
      provides: "Reusable CatalogCard for all 14 catalog-shape archives — catalogAssetSchema input, lightbox-fix attributes, Pagefind data-* hooks"
      min_lines: 80
    - path: "src/pages/nz/index.astro"
      provides: "NZ archive page using RootLayout + CatalogCard + pagination handler"
      contains: "archiveSlug=\"nz\""
    - path: "scripts/normalize-nz.py"
      provides: "Stdlib-only normalizer reading nz/.cache or nz/index.html source → data/nz.json (catalogEnvelopeSchema)"
      min_lines: 50
    - path: "data/nz.json"
      provides: "Schema-valid catalog envelope for NZ Defence Force"
      contains: "schemaVersion"
    - path: "src/styles/nz.css"
      provides: "NZ-archive-specific layout extensions (optional; min if no per-archive layout differs)"
  key_links:
    - from: "src/pages/nz/index.astro"
      to: "data/nz.json (Content Collection 'nz')"
      via: "getEntry('nz', 'v1')"
      pattern: "getEntry\\('nz'"
    - from: "scripts/normalize-nz.py"
      to: "scripts/_archive_common.py rewrite_to_r2"
      via: "from _archive_common import rewrite_to_r2, slugify"
      pattern: "rewrite_to_r2"
---

<objective>
Port the NZ Defence Force archive (smallest static archive — D-08 Wave 1 tier) to Astro. This is the FIRST per-archive port plan, so it establishes `src/components/CatalogCard.astro` (reused by all 13 subsequent port plans) per 04-RESEARCH.md §8 "Card.astro generalisation" + 04-PATTERNS.md §3.

Template: Plan 03-05 wargov pattern. NZ uses `catalogEnvelopeSchema` (Plan 03-02 already wired in src/content.config.ts line ~167 for the 14 catalog archives). Normalizer (`scripts/normalize-nz.py`) reads the existing nz/ source (likely nz/index.html or nz/.cache/* — choose whichever has parseable structure) and emits `data/nz.json` matching the schema.

Per-archive Python retirement (D-09): delete `scripts/build-nz.py`, drop `nz` from `scripts/copy-legacy-archives.sh` slug array, drop `nz` from `scripts/sync-nav.py` and `scripts/sync-footer.py` policed-archives lists (if applicable).

Output: NZ archive on Astro + CatalogCard.astro (reused by Wave 4+5 ports) + normalize-nz.py + data/nz.json + per-archive CSS + visual + fidelity baselines.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-migration-search-offline-performance/04-CONTEXT.md
@.planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md
@.planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md
@.planning/phases/03-ssg-foundation/03-CONTEXT.md
@.planning/phases/03-ssg-foundation/03-06-SUMMARY.md
@CLAUDE.md

<interfaces>
src/content.config.ts (Plan 03-02):
  catalogAssetSchema.strict() — unknown fields fail build. Keys: t, ti, de, ag, cat, date, region, l, u, s?, th
  catalogEnvelopeSchema — { v1: { schemaVersion: 1, slug, assets: [...], stats: { total, local_total, pdf_total, catalog_total } } }
  Collection 'nz' loader: file('data/nz.json'), schema: catalogEnvelopeSchema

src/layouts/RootLayout.astro (Plan 03-04):
  TONE map already contains nz: '#5b8def' per CLAUDE.md §3.1; archiveSlug prop drives the CSS variable injection.

src/components/Card.astro (post Plan 04-01):
  wargov-only. NOT reused for catalog archives. CatalogCard.astro is the catalog-shape sibling.

scripts/_archive_common.py (Plan 04-02):
  rewrite_to_r2(local_path, slug, asset_type='pdfs'|'videos') -> str
  slugify(text) -> str  (byte-exact port)

scripts/normalize-csv.py (Plan 03-03 + 04-01 + 04-02 patches):
  Deterministic JSON serialiser pattern (sort_keys=True, ensure_ascii=False, indent=2, trailing \\n).
  Source-untouchable guard via git diff --quiet.
  PUBLIC_DATA_DIR mirror to public/data/.

NZ source data (existing):
  nz/index.html (current legacy HTML built by scripts/build-nz.py)
  nz/.cache/*.json (if exists — likely scraper output)
  nz/pdfs/ (committed PDF files if any)
  nz/assets/images/ (thumbnails)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/components/CatalogCard.astro — reusable for all 14 catalog archives</name>
  <files>src/components/CatalogCard.astro</files>
  <read_first>
    - src/components/Card.astro (Plan 03-05 + 04-01 — full file; CatalogCard is its catalog-shape sibling)
    - src/content.config.ts (catalogAssetSchema — exact field names + types)
    - .planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md §8 "Card.astro generalisation" (hybrid recommendation)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §3 (CatalogCard.astro markup template)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §4 (Card.astro lightbox-fix patches — apply equivalent to CatalogCard)
    - CLAUDE.md §4.3 (action button matrix — Open/Download/Source ↗/DVIDS ↗)
  </read_first>
  <behavior>
    - CatalogCard.astro takes interface Props { asset: CatalogAsset; idx: number; slug: string; archiveSlug: string }.
    - Markup mirrors Card.astro structure: <article class="arch-card" id={`card-${slug}`} data-id data-row-id data-action="open" data-type data-agency data-date data-pagefind-filter data-pagefind-meta>.
    - btn-open: href="#", data-action, data-row-id, data-url, data-local (lightbox-fix patch A inherited).
    - btn-download: href={asset.l || asset.u}, data-url, data-local, download.
    - btn-source: href={asset.s}, target="_blank", rel="noopener". Shown only when asset.s present.
    - data-pagefind-filter syntax `archive:<archiveSlug>,type:<t>,agency:<ag>` (Plan 04-19 will index using these).
    - data-pagefind-meta syntax `title:<ti>,agency:<ag>,date:<date>`.
    - rowId = `r${String(idx+1).padStart(3,'0')}` (matches normalize-csv.py and normalize-<slug>.py convention).
    - No fenced code blocks emitted; Astro auto-escapes all `{expr}` per Plan 03-05 D-21..D-23.
  </behavior>
  <action>
    Create `src/components/CatalogCard.astro` with frontmatter declaring CatalogAsset interface (matching catalogAssetSchema fields: t, ti, de, ag, cat, date, region, l, u, s?, th) and Props { asset: CatalogAsset; idx: number; slug: string; archiveSlug: string }.

    Frontmatter computes:
    - rowId = `r${String(idx+1).padStart(3,'0')}`
    - Local copies of fields with defaults (asset.ti ?? '', asset.de ?? '', asset.l ?? '', etc.) for stable templating.

    Template body (mirror Card.astro structure with catalog-shape fields and apply lightbox-fix patch A):
    - <article class="arch-card" id={`card-${slug}`} data-id={rowId} data-row-id={rowId} data-action="open" data-type={asset.t} data-agency={asset.ag} data-date={asset.date} data-pagefind-filter={`archive:${archiveSlug},type:${asset.t},agency:${asset.ag}`} data-pagefind-meta={`title:${asset.ti},agency:${asset.ag},date:${asset.date}`}>
    - {asset.th && <img loading="lazy" src={asset.th} data-fallback={asset.u} alt={asset.ti} onerror="this.onerror=null;this.src=this.dataset.fallback||''" />}
    - <h3 class="card-title">{asset.ti}</h3>
    - {asset.de && <p class="card-desc">{asset.de}</p>}
    - <dl class="card-meta"><dt>Agency</dt><dd>{asset.ag}</dd><dt>Date</dt><dd>{asset.date}</dd><dt>Type</dt><dd>{asset.t}</dd></dl>
    - <div class="card-actions"> with:
       - {asset.u && <a href="#" class="btn-open" data-action="open" data-row-id={rowId} data-url={asset.u} data-local={asset.l ?? ''}>Open</a>}
       - {asset.u && <a href={asset.l || asset.u} class="btn-download" data-url={asset.u} data-local={asset.l ?? ''} download>Download</a>}
       - {asset.s && <a href={asset.s} class="btn-source" target="_blank" rel="noopener">Source ↗</a>}
    - </article>

    Do NOT add HeroCarousel or per-archive imports — that's the page's job. Do NOT touch the existing Card.astro (wargov-specific).

    Astro check + pnpm build to validate.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; test -f src/components/CatalogCard.astro &amp;&amp; grep -q "data-row-id" src/components/CatalogCard.astro &amp;&amp; grep -q "data-pagefind-filter" src/components/CatalogCard.astro &amp;&amp; grep -q "data-pagefind-meta" src/components/CatalogCard.astro &amp;&amp; grep -q "data-url" src/components/CatalogCard.astro &amp;&amp; grep -q "data-local" src/components/CatalogCard.astro &amp;&amp; pnpm exec astro check 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/CatalogCard.astro exists with file length ≥ 60 lines
    - grep -c 'data-row-id' returns ≥ 2 (article + btn-open)
    - grep -c 'data-url' returns ≥ 2 (btn-open + btn-download)
    - grep -c 'data-local' returns ≥ 2
    - grep -c 'data-pagefind-filter' returns ≥ 1
    - grep -c 'data-pagefind-meta' returns ≥ 1
    - grep -c 'btn-source' returns ≥ 1
    - pnpm exec astro check exits 0
  </acceptance_criteria>
  <done>CatalogCard.astro created with lightbox-fix attributes + Pagefind data-* hooks; astro check green; reusable by next 13 plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write scripts/normalize-nz.py + emit data/nz.json + delete scripts/build-nz.py</name>
  <files>scripts/normalize-nz.py, data/nz.json, public/data/nz.json, scripts/build-nz.py</files>
  <read_first>
    - scripts/normalize-csv.py (Plan 03-03 + 04-01 + 04-02 patches — header docstring + stdlib imports + REPO constant + deterministic JSON write + source-untouchable guard + public/data mirror)
    - scripts/build-nz.py (current Python build script — extract data structure / source path)
    - nz/ directory (list files; identify source — nz/index.html or nz/.cache/parsed.json)
    - src/content.config.ts (catalogAssetSchema — exact strict() field set)
    - .planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md §8 "Per-archive normalizer approaches" + Table row for NZ
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §2 (normalize-<slug>.py template) + Pattern B + Pattern G + Pattern H + Pattern I
    - scripts/_archive_common.py (Plan 04-02 — rewrite_to_r2, slugify)
    - CLAUDE.md §11 (source data untouchability)
  </read_first>
  <behavior>
    - scripts/normalize-nz.py is stdlib-only Python 3 (argparse, html, json, re, subprocess, sys, pathlib). Imports rewrite_to_r2 and slugify from _archive_common.
    - Reads NZ source (from nz/.cache/*.json if present; otherwise parses nz/index.html via Python's html.parser stdlib) WITHOUT writing to it.
    - Emits data/nz.json + public/data/nz.json via deterministic JSON write (sort_keys=True, ensure_ascii=False, indent=2, trailing newline).
    - Envelope shape: { "v1": { "schemaVersion": 1, "slug": "nz", "assets": [...], "stats": { "total": N, "local_total": M, "pdf_total": P, "catalog_total": C } } }.
    - Each asset has exactly the catalogAssetSchema.strict() keys: t, ti, de, ag, cat, date, region, l, u (R2 URL via rewrite_to_r2), s, th.
    - Every PDF/video URL goes through rewrite_to_r2(local_path, 'nz', 'pdfs'|'videos').
    - Post-write guard: subprocess.run(['git','-C',REPO,'diff','--quiet','--','nz/.cache/']) returncode == 0 (source untouched).
    - scripts/build-nz.py is DELETED (D-09 per-archive Python retirement).
  </behavior>
  <action>
    Inspect nz/ directory contents to determine source data location. If `nz/.cache/*.json` exists, parse that. Otherwise parse the current `nz/index.html` extracting the embedded `<script id="arch-data" type="application/json">` payload (this is the JSON manifest per CLAUDE.md §4 / scripts/validate-manifests.py convention).

    Create scripts/normalize-nz.py modelled on scripts/normalize-csv.py (Plan 03-03):
    - Header docstring per 04-PATTERNS.md §2 (adapt source path to nz/.cache or nz/index.html).
    - Stdlib imports + sys.path.insert + `from _archive_common import rewrite_to_r2, slugify`.
    - Module constants REPO, OUT_PRIMARY = REPO/'data'/'nz.json', PUBLIC_DATA_DIR = REPO/'public'/'data'.
    - Parse function `parse_source() -> list[dict]` returning raw NZ records from the chosen source.
    - Map function `to_catalog_asset(rec: dict) -> dict` returning a catalogAssetSchema-valid dict with keys exactly: t, ti, de, ag, cat, date, region, l, u, s, th. Apply rewrite_to_r2 on local PDF/video paths.
    - `_build_envelope(assets, total, local, pdf, catalog) -> dict` returning the v1 envelope shape.
    - `_serialise(data) -> str` and `_write_json(path, data)` per pattern.
    - `_assert_source_unchanged()` function checking `git diff --quiet -- nz/.cache/` (or nz/index.html, whichever is the source).
    - main(): parse, map, compute stats (total = len(assets); local_total = count where l != ''; pdf_total = count where t in {'PDF','DOC'}; catalog_total = total - pdf_total - count(t in {'VID','IMG','AUDIO','CASE'})), build envelope, write primary + public mirror, call _assert_source_unchanged.

    DELETE scripts/build-nz.py via `git rm scripts/build-nz.py`.

    Run `python3 scripts/normalize-nz.py` and confirm:
    - data/nz.json + public/data/nz.json exist
    - JSON parses; pnpm build succeeds (Zod validation green)
    - No git diff in nz/ source files

    Do NOT inline fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; python3 scripts/normalize-nz.py 2>&amp;1 | tail -10 &amp;&amp; test -f data/nz.json &amp;&amp; test -f public/data/nz.json &amp;&amp; node -e "const j=require('./data/nz.json'); if(!j.v1) process.exit(1); if(j.v1.slug!=='nz') process.exit(2); if(!Array.isArray(j.v1.assets)) process.exit(3); console.log('total='+j.v1.stats.total)" &amp;&amp; (test ! -f scripts/build-nz.py || (echo 'build-nz.py still exists'; exit 1)) &amp;&amp; pnpm build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - python3 scripts/normalize-nz.py exits 0
    - data/nz.json and public/data/nz.json exist with valid catalogEnvelopeSchema JSON
    - jq '.v1.slug' data/nz.json returns "nz"
    - jq '.v1.assets | length' returns >= 1 (some assets present even for small archive)
    - jq '.v1.stats.total' equals .v1.assets | length
    - scripts/build-nz.py NO LONGER EXISTS
    - git diff --quiet -- nz/ exits 0 (source untouched per CLAUDE.md §11)
    - Each asset object has exactly the keys t, ti, de, ag, cat, date, region, l, u, s, th (catalogAssetSchema.strict)
    - Every non-empty asset.u where t='PDF' starts with 'https://assets.realufo.org/pdfs/nz/'; every non-empty asset.u where t='VID' starts with 'https://assets.realufo.org/videos/nz/'
    - pnpm build exits 0 (Zod schema validation passes)
  </acceptance_criteria>
  <done>normalize-nz.py emits valid catalogEnvelope; build-nz.py deleted; Zod build passes; source untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create src/pages/nz/index.astro + src/styles/nz.css + drop nz from postbuild + sync scripts</name>
  <files>src/pages/nz/index.astro, src/styles/nz.css, scripts/copy-legacy-archives.sh, scripts/sync-nav.py, scripts/sync-footer.py, nz/index.html, tests/fidelity-samples.json, tests/visual-baselines/nz-360.png, tests/visual-baselines/nz-768.png, tests/visual-baselines/nz-1024.png, tests/visual-baselines/nz-1440.png</files>
  <read_first>
    - src/pages/index.astro (Plan 03-05 + 04-01 + 04-04 — wargov template; copy the page-skeleton + pagination handler + lightbox)
    - src/components/CatalogCard.astro (Task 1 of this plan)
    - src/styles/wargov.css (template for per-archive CSS — keep minimal if NZ has no layout differences)
    - scripts/copy-legacy-archives.sh (Plan 03-06 — current ARCHIVES list to drop 'nz' from)
    - scripts/sync-nav.py (current policed-archives list; check if 'nz' is policed)
    - scripts/sync-footer.py (current policed-archives list; check if 'nz' is policed)
    - tests/fidelity-samples.json (current 105 samples — Phase 4 adds NZ samples for hero-lede + license-footer + 2 card titles)
    - tests/visual-baselines/README.md (operator-conscious recapture per D-17)
    - .planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md §8 "Per-archive page route" + "Tone-colour fix" + 04-PATTERNS.md §1 + Pattern A
    - CLAUDE.md §3.1 (NZ tone: --caution #5b8def) + §9 (NZ public-domain attribution — keep verbatim from existing nz/index.html footer)
  </read_first>
  <behavior>
    - src/pages/nz/index.astro imports RootLayout, CatalogCard, Lightbox, '../../styles/nz.css'. Calls `getEntry('nz', 'v1')`. Throws on missing entry with a message hinting `pnpm prebuild`.
    - RootLayout invoked with archiveSlug="nz", title/description/totalRecords/lastUpdated props.
    - Server-renders all assets into `<div class="arch-grid" id="nz-grid">` via CatalogCard map.
    - Includes the ?page=N pagination handler from Plan 04-04 (adapted: grid id='nz-grid', no shards manifest since small archive — all cards fit in one page or render statically).
    - Includes Lightbox component + lightbox handlers (or relies on RootLayout if Lightbox is registered there — verify Plan 03-04/03-05 wiring).
    - Note: if NZ has < 20 cards, the pagination nav can be hidden via CSS or the handler short-circuits gracefully (renderPaginationNav with total=1 emits no nav).
    - scripts/copy-legacy-archives.sh: 'nz' removed from ARCHIVES array; NZ no longer copied to dist/nz/.
    - scripts/sync-nav.py + sync-footer.py: 'nz' dropped from policed-archives list (if present).
    - nz/index.html DELETED via git rm (legacy HTML retired since Astro now serves /nz/).
    - tests/fidelity-samples.json gains 5 NZ samples (hero-lede, license-footer, 1-2 card titles + 1 agency string).
    - tests/visual-baselines/nz-{360,768,1024,1440}.png recaptured against new preview via scripts/capture-baselines.py (operator-conscious per D-17 — documented in SUMMARY).
  </behavior>
  <action>
    Create src/pages/nz/index.astro:
    - Frontmatter: imports RootLayout (`../../layouts/RootLayout.astro`), CatalogCard (`../../components/CatalogCard.astro`), Lightbox (`../../components/Lightbox.astro`), '../../styles/nz.css'. Call `const entry = await getEntry('nz','v1')`; throw if missing.
    - Destructure `const { assets, stats } = entry.data;`.
    - Slugify helper inline (byte-exact copy from src/pages/index.astro Plan 03-05 lines 51-55).
    - Compute title/description from CLAUDE.md §2 ("NZ Defence Force") + nz/index.html hero lede (verbatim per §9).
    - <RootLayout archiveSlug="nz" title={title} description={description} totalRecords={stats.total} lastUpdated="2026-05-27">.
    - Hero section (mirror wargov hero structure without HeroCarousel — NZ unlikely to have ≥ 4 hero images per 04-RESEARCH.md §8 "HeroCarousel inclusion").
    - <section class="archive"> with stats-grid, arch-controls-bar (search/sort/filter — copy from wargov), <div class="arch-grid" id="nz-grid"> mapping assets to CatalogCard, <nav class="pagination" id="nz-pagination" data-pagefind-ignore>.
    - <Lightbox /> component invocation.
    - <script is:inline> block containing the Plan 04-04 pagination handler adapted for grid id='nz-grid' + pagination id='nz-pagination'. NZ likely has few assets — if < 20, handler should still render a single-page nav (no prev/next, no current span needed).
    - <script id="nz-shards"> manifest if shards needed (NZ likely doesn't shard — embed all cards in initial HTML).
    - refreshLbList walks `#nz-grid .arch-card` per Pitfall #6.

    Create src/styles/nz.css with minimum scope: include any NZ-archive-specific layout that legacy nz/index.html had, OR keep file as a placeholder importing global.css patterns. If nothing differs from global.css, write a one-line comment header only.

    Modify scripts/copy-legacy-archives.sh:
    - Locate the `for slug in aaro nasa nara geipan uk brazil chile argentina canada italy nz peru spain uruguay; do` line (line ~53 per 04-PATTERNS.md §10).
    - Remove `nz` from that list. New line: `for slug in aaro nasa nara geipan uk brazil chile argentina canada italy peru spain uruguay; do`.

    Modify scripts/sync-nav.py and scripts/sync-footer.py:
    - Locate the ARCHIVES/SLUGS/POLICED list constant.
    - Remove 'nz' from it. (If 'nz' is not in the list, no-op — no edit needed.)

    Delete nz/index.html via `git rm nz/index.html` (legacy HTML no longer served by postbuild copy).

    Recapture visual baselines per D-17 operator-conscious procedure:
    - After deploy (or local pnpm build + preview), run `python3 scripts/capture-baselines.py --archive nz --base-url <preview-url>` to emit tests/visual-baselines/nz-{360,768,1024,1440}.png.

    Add NZ fidelity samples to tests/fidelity-samples.json:
    - 5 entries with slug='nz', selector + expected_text pairs covering: hero-lede paragraph (verbatim from old nz/index.html), license-footer paragraph (public-domain attribution per CLAUDE.md §9), 2 card-title strings, 1 agency string.

    Document the operator-conscious recapture rationale + the 5 fidelity samples in the SUMMARY (Output section will produce 04-05-SUMMARY.md after execution).

    pnpm build to confirm Astro emits dist/nz/index.html. Run quality-gates locally where possible (visual-regression --grep nz, tone-colours --grep nz, fidelity --grep nz).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; test -f src/pages/nz/index.astro &amp;&amp; grep -q "archiveSlug=\"nz\"" src/pages/nz/index.astro &amp;&amp; grep -q "getEntry('nz'" src/pages/nz/index.astro &amp;&amp; (! grep -E '\\bnz\\b' scripts/copy-legacy-archives.sh) &amp;&amp; (test ! -f nz/index.html || (echo 'nz/index.html still exists'; exit 1)) &amp;&amp; pnpm build 2>&amp;1 | tail -10 &amp;&amp; test -f dist/nz/index.html &amp;&amp; grep -q "card-" dist/nz/index.html</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/nz/index.astro exists, references RootLayout + CatalogCard + Lightbox + nz.css; uses getEntry('nz','v1')
    - src/styles/nz.css exists (may be near-empty if no overrides needed)
    - grep -E '\\bnz\\b' scripts/copy-legacy-archives.sh returns 0 matches (slug dropped from postbuild copy)
    - grep '\\bnz\\b' scripts/sync-nav.py and scripts/sync-footer.py return 0 (slug dropped from policed lists, if applicable)
    - nz/index.html no longer exists in the working tree (git rm'd)
    - pnpm build exits 0 and emits dist/nz/index.html
    - dist/nz/index.html contains at least 1 `.arch-card` element
    - tests/visual-baselines/nz-{360,768,1024,1440}.png exist (recaptured)
    - tests/fidelity-samples.json contains at least 5 entries with `"slug": "nz"`
    - tone-colours.spec.ts grep nz reports green (--caution computed to #5b8def from RootLayout TONE map)
  </acceptance_criteria>
  <done>NZ archive ported on Astro; postbuild + sync scripts shrunk by 'nz'; legacy nz/index.html removed; baselines + fidelity updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time→data/nz.json | normalize-nz.py emits schema-validated JSON consumed by Astro at build |
| client→/nz/ HTML | Browser renders Astro-emitted page; same trust posture as wargov |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Tampering | NZ scraper output → normalizer input | mitigate | source-untouchable guard via git diff --quiet -- nz/ (CLAUDE.md §11); Zod schema.strict() at build catches unexpected keys |
| T-04-20 | Information Disclosure | data-local exposes local repo paths | accept | Phase 4 R2 migration replaces with R2 URLs; local paths are public knowledge |
| T-04-21 | Tampering | Card markup XSS via injected text fields | mitigate | Astro auto-escapes `{expr}` per Plan 03-05 D-21..D-23; CatalogCard.astro uses only `{expr}` interpolation; no set:html for user data |
| T-04-22 | Repudiation | Loss of legacy nz/index.html content fidelity | mitigate | tests/fidelity-samples.json gains 5 NZ samples; verify-fidelity.py byte-checks; operator-conscious baseline recapture per D-17 |
</threat_model>

<verification>
1. python3 scripts/normalize-nz.py emits data/nz.json + public/data/nz.json without writing to nz/ source.
2. pnpm build exits 0 and emits dist/nz/index.html containing rendered cards (no client hydration; cards visible with JS off).
3. pnpm exec playwright test tests/visual-regression.spec.ts --grep nz passes (against recaptured baselines).
4. pnpm exec playwright test tests/tone-colours.spec.ts --grep nz passes (--caution #5b8def).
5. pnpm exec playwright test tests/js-off.spec.ts --grep nz passes.
6. python3 scripts/verify-fidelity.py --grep nz passes (5 new samples byte-equivalent).
7. git diff --quiet -- nz/ returns 0 (source untouched).
8. scripts/build-nz.py + nz/index.html absent from working tree (deleted).
9. scripts/copy-legacy-archives.sh no longer iterates over 'nz'.
</verification>

<success_criteria>
- [ ] src/components/CatalogCard.astro shipped (reusable by 13 remaining port plans)
- [ ] scripts/normalize-nz.py emits valid catalogEnvelope at data/nz.json
- [ ] src/pages/nz/index.astro renders from Astro with archiveSlug="nz" tone-colour applied
- [ ] PAGE_SIZE=20 pagination handler ported from Plan 04-04 (or graceful single-page if < 20 cards)
- [ ] CLAUDE.md §7 invariants preserved (lightbox, hamburger, /-focus, ?q= persistence)
- [ ] scripts/build-nz.py + nz/index.html deleted; 'nz' dropped from copy-legacy + sync-nav + sync-footer policing
- [ ] Visual + fidelity + tone-colour + js-off + Lighthouse soft gates green for NZ
- [ ] Source data (nz/.cache/*, nz/index.html if it was the source — only legacy HTML deleted, scraped cache untouched) byte-equivalent
- [ ] R2 URLs flow through `_archive_common.rewrite_to_r2` (zero `bundles/` or relative paths in card.url fields)
</success_criteria>

<output>
Create `.planning/phases/04-full-migration-search-offline-performance/04-05-SUMMARY.md` when done.
</output>

---
phase: 04-full-migration-search-offline-performance
plan: "17"
type: execute
wave: 5
depends_on: ["04-05"]
files_modified:
  - src/pages/aaro/index.astro
  - src/styles/aaro.css
  - scripts/normalize-aaro.py
  - data/aaro.json
  - public/data/aaro.json
  - data/aaro-shard-1.json
  - scripts/copy-legacy-archives.sh
  - scripts/sync-nav.py
  - scripts/sync-footer.py
  - tests/fidelity-samples.json
  - tests/visual-baselines/aaro-360.png
  - tests/visual-baselines/aaro-768.png
  - tests/visual-baselines/aaro-1024.png
  - tests/visual-baselines/aaro-1440.png
  - aaro/index.html
  - scripts/build-aaro.py
  - scripts/parse-aaro.py
  - scripts/extract-evidence.py
autonomous: true
requirements: [SSG-06, SSG-08, SSG-09, SSG-10, SSG-11, SSG-12, PERF-04]
tags: [archive-port, aaro, large-catalog, parse-aaro-retirement]

must_haves:
  truths:
    - "Visiting /aaro/ renders AARO (All-domain Anomaly Resolution Office) archive from Astro"
    - "Per-archive tone --caution #4a9eff applied via RootLayout TONE map (AARO blue)"
    - "Page ≤ 500 KB; JS-off shows cards; lightbox + hamburger preserved per CLAUDE.md §7"
    - "Sharding triggers (AARO is largest non-GEIPAN archive — many cases + videos + DVIDS links)"
    - "scripts/build-aaro.py + scripts/parse-aaro.py + scripts/extract-evidence.py all DELETED"
    - "'aaro' dropped from postbuild + sync scripts"
    - "Card URLs flow through assets.realufo.org for PDFs/videos; DVIDS deep-links preserved verbatim for VID t='VID' with a.dvidsId per CLAUDE.md §4.3"
    - "License footer carries '17 U.S.C. § 105' verbatim (USA public-domain)"
  artifacts:
    - path: "src/pages/aaro/index.astro"
      contains: "archiveSlug=\"aaro\""
    - path: "scripts/normalize-aaro.py"
      min_lines: 80
    - path: "data/aaro.json"
      contains: "schemaVersion"
  key_links:
    - from: "src/pages/aaro/index.astro"
      to: "data/aaro.json"
      via: "getEntry('aaro','v1')"
      pattern: "getEntry\\('aaro'"
---

<objective>
Port the AARO (All-domain Anomaly Resolution Office) archive (D-08 Wave 2 medium catalog tier — largest non-GEIPAN) to Astro using the CatalogCard.astro template from Plan 04-05. Per-archive Python retirement (D-09 + RESEARCH §10): delete scripts/build-aaro.py + scripts/parse-aaro.py + scripts/extract-evidence.py (AARO-specific cross-cutting scripts) + drop slug from copy-legacy-archives.sh + sync-nav.py + sync-footer.py.

AARO has aaro/.cache/ scraped data + many long-form HTML pages (aaro/belgian-wave.html, aaro/cash-landrum.html, aaro/coyne.html, etc. — all preserved out of Phase 4 scope) + aaro/pdfs/ + aaro/videos/. AARO catalogue mixes Document/Video/Imagery/Catalog types; DVIDS IDs on videos drive 'DVIDS ↗' button per CLAUDE.md §4.3.

Output: AARO archive on Astro + normalize-aaro.py + data/aaro.json + shards + 3 AARO-specific Python scripts retired + baselines + 5 fidelity samples.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-migration-search-offline-performance/04-CONTEXT.md
@.planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md
@.planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md
@.planning/phases/04-full-migration-search-offline-performance/04-05-PLAN.md
@.planning/phases/04-full-migration-search-offline-performance/04-06-PLAN.md
@.planning/phases/04-full-migration-search-offline-performance/04-11-PLAN.md
@CLAUDE.md

<interfaces>
Identical interface set to Plan 04-11 (Brazil medium-catalog template). Differences:
- archiveSlug = "aaro"
- tone --caution = #4a9eff (CLAUDE.md §3.1 — AARO blue)
- License footer = "17 U.S.C. § 105" (USA)
- Source: aaro/.cache/*.json (scripts/parse-aaro.py output — port the parse logic into normalize-aaro.py since parse-aaro.py is being retired)
- Output: data/aaro.json + data/aaro-shard-N.json (sharding triggers — large catalogue)
- Multiple long-form HTML pages (aaro/<case-slug>.html × many) preserved — Phase 4 only retires aaro/index.html
- t='VID' cards may include `a.dvidsId` field for 'DVIDS ↗' button per CLAUDE.md §4.3 — extend catalogAssetSchema OR thread dvidsId through standard 'u' field with type-prefix; consult CatalogCard.astro contract from Plan 04-05
- 3 Python scripts to retire (not just 1): scripts/build-aaro.py + scripts/parse-aaro.py + scripts/extract-evidence.py
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write scripts/normalize-aaro.py (absorb parse-aaro.py + extract-evidence.py logic) + emit data/aaro.json + shards + delete 3 AARO Python scripts</name>
  <files>scripts/normalize-aaro.py, data/aaro.json, public/data/aaro.json, data/aaro-shard-1.json, scripts/build-aaro.py, scripts/parse-aaro.py, scripts/extract-evidence.py</files>
  <read_first>
    - scripts/normalize-brazil.py (Plan 04-11 — medium-catalog template with sharding)
    - scripts/build-aaro.py (current Python — orchestration logic)
    - scripts/parse-aaro.py (AARO-only HTML parser → cache structure; absorb into normalize-aaro.py since parse-aaro.py is retiring per RESEARCH §10)
    - scripts/extract-evidence.py (AARO-only evidence-map builder; absorb relevant logic into normalize-aaro.py)
    - aaro/.cache/ contents (parsed JSON from parse-aaro.py)
    - scripts/_archive_common.py
    - src/content.config.ts (catalogAssetSchema.strict — verify dvidsId allowed or threaded via existing fields)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §2 + Pattern B + Pattern G
    - CLAUDE.md §4.3 (DVIDS ↗ button — VID type with a.dvidsId)
  </read_first>
  <behavior>
    - Stdlib-only Python 3; deterministic JSON; source-untouchable guard via git diff --quiet -- aaro/.cache/.
    - Absorbs parse-aaro.py + extract-evidence.py logic that's still needed (HTML parsing → record extraction); discards Python build-side rendering.
    - Emits catalogEnvelope at data/aaro.json with slug='aaro'.
    - Each asset matches catalogAssetSchema.strict() (t, ti, de, ag, cat, date, region, l, u, s, th).
    - rewrite_to_r2 with archive_slug='aaro' for PDF + video assets.
    - For t='VID' with DVIDS metadata: thread dvidsId through asset.s (DVIDS page URL) since catalogAssetSchema.strict does not have a dvidsId field; verify CatalogCard.astro renders DVIDS ↗ button when asset.s matches dvidshub.net pattern (if not, document follow-up in SUMMARY rather than mutating shared CatalogCard.astro).
    - SHARD_SIZE = 50; shards trigger.
    - scripts/build-aaro.py + scripts/parse-aaro.py + scripts/extract-evidence.py all deleted via git rm.
  </behavior>
  <action>
    Clone scripts/normalize-brazil.py → scripts/normalize-aaro.py. Replace 'brazil' with 'aaro' in OUT_PRIMARY, slug field, rewrite_to_r2 arg, _assert_source_unchanged path.

    Inspect aaro/.cache/ contents + scripts/parse-aaro.py + scripts/extract-evidence.py. Identify the AARO-specific parsing logic that produces records (HTML scraping, evidence-map joining). Port the data-extraction-only portions into normalize-aaro.py's parse_source() function. Discard Python rendering (Astro CatalogCard replaces).

    Update to_catalog_asset(rec) for AARO record shape → catalogAssetSchema. For DVIDS videos: route dvidsId through asset.s as the dvidshub.net page URL; CatalogCard.astro renders Source ↗ which dispatches to DVIDS deep-link.

    Run python3 scripts/normalize-aaro.py; confirm data/aaro.json + shards.

    git rm scripts/build-aaro.py scripts/parse-aaro.py scripts/extract-evidence.py.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; python3 scripts/normalize-aaro.py 2>&amp;1 | tail -10 &amp;&amp; test -f data/aaro.json &amp;&amp; node -e "const j=require('./data/aaro.json'); if(j.v1.slug!=='aaro') process.exit(1); console.log('total='+j.v1.stats.total+' shards='+(j.v1.shards?.length||0))" &amp;&amp; (test ! -f scripts/build-aaro.py) &amp;&amp; (test ! -f scripts/parse-aaro.py) &amp;&amp; (test ! -f scripts/extract-evidence.py) &amp;&amp; git diff --quiet -- aaro/.cache/; echo source_diff=$? &amp;&amp; pnpm build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - data/aaro.json + public/data/aaro.json valid; slug='aaro'; stats.total ≥ 1
    - Each asset matches catalogAssetSchema.strict()
    - asset.u for t='PDF' starts with 'https://assets.realufo.org/pdfs/aaro/'
    - asset.u for t='VID' starts with 'https://assets.realufo.org/videos/aaro/' (when local) OR DVIDS URL for remote-only
    - asset.s for DVIDS videos starts with 'https://www.dvidshub.net/'
    - Shards emit
    - scripts/build-aaro.py + scripts/parse-aaro.py + scripts/extract-evidence.py all removed
    - git diff --quiet -- aaro/.cache/ returns 0
    - pnpm build exits 0
  </acceptance_criteria>
  <done>normalize-aaro.py emits valid catalogEnvelope absorbing parse-aaro + extract-evidence logic; 3 Python scripts retired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/pages/aaro/index.astro + aaro.css + drop slug from postbuild + recapture baselines + fidelity</name>
  <files>src/pages/aaro/index.astro, src/styles/aaro.css, scripts/copy-legacy-archives.sh, scripts/sync-nav.py, scripts/sync-footer.py, aaro/index.html, tests/fidelity-samples.json, tests/visual-baselines/aaro-360.png, tests/visual-baselines/aaro-768.png, tests/visual-baselines/aaro-1024.png, tests/visual-baselines/aaro-1440.png</files>
  <read_first>
    - src/pages/brazil/index.astro (Plan 04-11 — clone target with sharding)
    - src/styles/brazil.css (template)
    - scripts/copy-legacy-archives.sh (drop 'aaro')
    - aaro/index.html (extract hero-lede + license-footer for fidelity samples)
    - tests/fidelity-samples.json (add 5 aaro samples)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §1 + Pattern A
    - CLAUDE.md §3.1 (aaro tone #4a9eff) + §9 (USA "17 U.S.C. § 105") + §4.3 (DVIDS ↗ button)
  </read_first>
  <behavior>
    - src/pages/aaro/index.astro renders from getEntry('aaro','v1') via RootLayout + CatalogCard + Lightbox + pagination handler.
    - archiveSlug='aaro' drives --caution #4a9eff.
    - HeroCarousel: ON if AARO has ≥ 4 representative photos (per RESEARCH §8 A2 — likely yes for AARO); else OFF. Default OFF — operator can re-enable in follow-up.
    - Footer carries "17 U.S.C. § 105" verbatim.
    - 'aaro' dropped from postbuild + sync scripts.
    - aaro/index.html deleted; ALL aaro/<case-slug>.html long-form pages PRESERVED.
    - Sharded: shards manifest script + pagination handler adapted.
    - Visual baselines recaptured per D-17.
    - 5 fidelity samples added.
  </behavior>
  <action>
    Clone src/pages/brazil/index.astro → src/pages/aaro/index.astro:
    - Replace 'brazil' with 'aaro' in archiveSlug, getEntry, grid id, pagination id, CSS path, shards-manifest id.
    - Update title/description from CLAUDE.md §2 ("AARO — All-domain Anomaly Resolution Office") + aaro/index.html hero lede verbatim.

    Clone src/styles/brazil.css → src/styles/aaro.css.

    Modify scripts/copy-legacy-archives.sh: remove 'aaro' from ARCHIVES iteration.

    Modify scripts/sync-nav.py + scripts/sync-footer.py: drop 'aaro' if policed.

    git rm aaro/index.html. PRESERVE all aaro/<case-slug>.html long-form pages (belgian-wave, cash-landrum, coyne, and others).

    Recapture visual baselines: python3 scripts/capture-baselines.py --archive aaro --base-url <preview-url>.

    Add 5 fidelity samples: hero-lede, license-footer "17 U.S.C. § 105", 2 card titles, 1 agency. Optionally add 1 sample verifying DVIDS deep-link is present in a video card's Source ↗ href.

    pnpm build → confirm dist/aaro/index.html.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; test -f src/pages/aaro/index.astro &amp;&amp; grep -q "archiveSlug=\"aaro\"" src/pages/aaro/index.astro &amp;&amp; (! grep -v '^#' scripts/copy-legacy-archives.sh | grep -E '\baaro\b') &amp;&amp; (test ! -f aaro/index.html) &amp;&amp; pnpm build 2>&amp;1 | tail -5 &amp;&amp; test -f dist/aaro/index.html &amp;&amp; grep -q "arch-card" dist/aaro/index.html</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/aaro/index.astro renders via getEntry('aaro','v1')
    - scripts/copy-legacy-archives.sh no longer iterates 'aaro'
    - aaro/index.html deleted; aaro/<case-slug>.html long-form pages preserved (verify via `ls aaro/*.html | wc -l` returns ≥ 3)
    - dist/aaro/index.html exists with rendered cards
    - tests/visual-baselines/aaro-{360,768,1024,1440}.png exist
    - tests/fidelity-samples.json contains ≥ 5 entries with slug:"aaro"
    - License footer in dist/aaro/index.html contains "17 U.S.C. § 105" verbatim
    - tone-colours.spec.ts --grep aaro reports green (#4a9eff)
    - dist/aaro/index.html contains shard manifest if total > 50
  </acceptance_criteria>
  <done>AARO archive on Astro; --caution #4a9eff; license verbatim; 3 Python scripts retired; baselines recaptured; long-form HTML pages preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time→data/aaro.json | normalize-aaro.py absorbs parse-aaro + extract-evidence; emits schema-validated JSON + shards |
| client→/aaro/ HTML | Browser renders Astro page; shards lazy-fetched |
| client→DVIDS deep-link | Source ↗ button on video cards opens dvidshub.net in new tab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-54 | Tampering | AARO scraper cache mutation | mitigate | source-untouchable guard via git diff --quiet -- aaro/.cache/; Zod strict() |
| T-04-55 | Tampering | XSS via injected text from scraped AARO HTML | mitigate | Astro `{expr}` auto-escape on CatalogCard; no set:html |
| T-04-56 | Repudiation | Loss of DVIDS deep-link contract for video cards | mitigate | Source ↗ button href verified per CLAUDE.md §4.3; fidelity sample for sample video card includes DVIDS URL |
| T-04-57 | Repudiation | Loss of "17 U.S.C. § 105" attribution | mitigate | 5 fidelity samples include license-footer verbatim |
</threat_model>

<verification>
1. python3 scripts/normalize-aaro.py exits 0; data/aaro.json valid; source untouched.
2. pnpm build → dist/aaro/index.html with rendered cards + shards in dist/data/.
3. pnpm exec playwright test tests/visual-regression.spec.ts --grep aaro passes.
4. pnpm exec playwright test tests/tone-colours.spec.ts --grep aaro passes (#4a9eff).
5. pnpm exec playwright test tests/js-off.spec.ts --grep aaro passes.
6. python3 scripts/verify-fidelity.py --grep aaro passes.
7. scripts/build-aaro.py + scripts/parse-aaro.py + scripts/extract-evidence.py + aaro/index.html all absent.
8. scripts/copy-legacy-archives.sh no longer iterates 'aaro'.
9. DVIDS ↗ deep-links: spot-check 1 video card in dist/aaro/index.html href contains dvidshub.net.
</verification>

<success_criteria>
- [ ] AARO archive renders from Astro at /aaro/
- [ ] --caution #4a9eff applied
- [ ] License footer "17 U.S.C. § 105" verbatim
- [ ] Shards emitted (large AARO catalogue)
- [ ] 3 Python scripts deleted: scripts/build-aaro.py + scripts/parse-aaro.py + scripts/extract-evidence.py
- [ ] aaro/index.html deleted; aaro/<case-slug>.html long-form pages preserved
- [ ] 'aaro' dropped from copy-legacy-archives.sh + sync-nav.py + sync-footer.py
- [ ] Visual + fidelity + tone-colour + js-off + Lighthouse soft gates green
- [ ] R2 URLs for PDF/video assets; DVIDS URLs for video Source ↗ buttons
</success_criteria>

<output>
Create `.planning/phases/04-full-migration-search-offline-performance/04-17-SUMMARY.md` when done.
</output>
</content>
</invoke>
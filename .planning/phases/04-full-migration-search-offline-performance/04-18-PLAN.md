---
phase: 04-full-migration-search-offline-performance
plan: "18"
type: execute
wave: 6
depends_on: ["04-05"]
files_modified:
  - src/pages/geipan/index.astro
  - src/styles/geipan.css
  - scripts/normalize-geipan.py
  - data/geipan.json
  - public/data/geipan.json
  - data/geipan-shard-1.json
  - scripts/copy-legacy-archives.sh
  - scripts/sync-nav.py
  - scripts/sync-footer.py
  - tests/fidelity-samples.json
  - tests/visual-baselines/geipan-360.png
  - tests/visual-baselines/geipan-768.png
  - tests/visual-baselines/geipan-1024.png
  - tests/visual-baselines/geipan-1440.png
  - tests/lighthouse-baselines/geipan.json
  - geipan/index.html
  - scripts/build-geipan.py
autonomous: true
requirements: [SSG-06, SSG-07, SSG-08, SSG-09, SSG-10, SSG-11, SSG-12, PERF-01, PERF-02, PERF-03, PERF-04]
tags: [archive-port, geipan, perf-01-driver, large-catalog-3000-cases, tone-colour-fix, lcp-budget]

must_haves:
  truths:
    - "Visiting /geipan/ renders France GEIPAN (CNES) archive from Astro"
    - "Per-archive tone --caution #0055a4 applied via RootLayout TONE map (D-10 tone-colour bug fix — legacy geipan HTML had drift; retiring legacy resolves)"
    - "Page HTML+inline ≤ 500 KB (was 3.3 MB; PERF-01 reduction is the headline win — SSG-07 compliance)"
    - "LCP mobile + 4× CPU throttle ≤ 2500 ms (PERF-01)"
    - "≤ 3 chunks lazy-fetched per page-view (PERF-02 — initial 20 cards inline + max 2 shards fetched given 50/shard SHARD_SIZE)"
    - "First paint contains server-rendered cards (PERF-03 — no client hydration; D-23 invariant)"
    - "JS-off shows first 20 cards inline; lightbox + hamburger preserved per CLAUDE.md §7"
    - "Sharding triggers heavily (GEIPAN ~3000 cases → ~60 shards of 50 each)"
    - "scripts/build-geipan.py deleted; 'geipan' dropped from postbuild + sync scripts"
    - "Card URLs flow through assets.realufo.org"
    - "License footer carries 'Loi n° 78-753' verbatim (France Information Publique)"
    - "tests/lighthouse-baselines/geipan.json frozen as Wave 2 regression baseline (PERF-04 watch)"
  artifacts:
    - path: "src/pages/geipan/index.astro"
      contains: "archiveSlug=\"geipan\""
    - path: "scripts/normalize-geipan.py"
      min_lines: 80
    - path: "data/geipan.json"
      contains: "schemaVersion"
    - path: "data/geipan-shard-1.json"
      provides: "First 50-case shard (Plan 03-03 D-10 LOCKED scheme; expect ~60 shards total)"
  key_links:
    - from: "src/pages/geipan/index.astro"
      to: "data/geipan.json"
      via: "getEntry('geipan','v1')"
      pattern: "getEntry\\('geipan'"
    - from: "src/pages/geipan/index.astro"
      to: "Plan 04-04 ?page=N handler"
      via: "client-side windowing over all 3000 cards (sharded fetch)"
      pattern: "PAGE_SIZE"
---

<objective>
Port the France GEIPAN (CNES) archive (D-08 Wave 3 PERF-tied standalone) to Astro using the CatalogCard.astro template from Plan 04-05. This plan IS the PERF-01/PERF-02/PERF-03 driver per RESEARCH §9 — GEIPAN's 3.3 MB inline JSON drops to ≤ 500 KB HTML+inline via 50/shard sharding + 20/page client-side windowing inherited from Plan 04-04.

**Tone-colour bug fix bundled per D-10:** legacy geipan/index.html had drift on `--caution` (should be French blue #0055a4); deleting legacy + RootLayout TONE map resolves.

Per-archive Python retirement (D-09): delete scripts/build-geipan.py + drop slug from copy-legacy-archives.sh + sync-nav.py + sync-footer.py.

GEIPAN has geipan/.cache/ + geipan/cases.json (3.25 MB raw scraped data, source untouchable) + geipan/pdfs/. ~3000 cases → ~60 shards of 50 each (PERF-02 math: page-view = initial inline 20 cards + max 2 shards = 3 chunks).

Output: GEIPAN archive on Astro + normalize-geipan.py (heavily sharded) + data/geipan.json + ~60 shards + tone-colour fix + frozen Lighthouse baseline + baselines + 5 fidelity samples + LCP ≤ 2.5s confirmed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-migration-search-offline-performance/04-CONTEXT.md
@.planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md
@.planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md
@.planning/phases/04-full-migration-search-offline-performance/04-05-PLAN.md
@.planning/phases/04-full-migration-search-offline-performance/04-06-PLAN.md
@.planning/phases/04-full-migration-search-offline-performance/04-11-PLAN.md
@CLAUDE.md

<interfaces>
Identical interface set to Plan 04-11 (Brazil medium-catalog template with sharding) PLUS PERF-01 emphasis. Differences:
- archiveSlug = "geipan"
- tone --caution = #0055a4 (CLAUDE.md §3.1 French blue); RootLayout TONE map already correct
- License footer = "Loi n° 78-753" (France Information Publique)
- Source: geipan/.cache/ + geipan/cases.json (~3.25 MB scraped catalogue, untouchable per CLAUDE.md §11)
- Output: data/geipan.json (small envelope with first 50 inline + shards manifest of ~60 entries) + data/geipan-shard-1..N.json
- PERF-01: initial HTML ≤ 500 KB; LCP mobile + 4× CPU ≤ 2500 ms
- PERF-02: max 3 chunks loaded per page-view (initial HTML + max 2 shards at boundary pages)
- PERF-03: first paint includes 20 server-rendered cards
- PERF-04: lighthouse-baselines/geipan.json frozen as regression baseline
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write scripts/normalize-geipan.py with sharding + emit data/geipan.json + ~60 shards + delete scripts/build-geipan.py</name>
  <files>scripts/normalize-geipan.py, data/geipan.json, public/data/geipan.json, data/geipan-shard-1.json, scripts/build-geipan.py</files>
  <read_first>
    - scripts/normalize-brazil.py (Plan 04-11 — medium-catalog sharding template)
    - scripts/normalize-csv.py (Plan 03-03 — full sharding implementation reference)
    - scripts/build-geipan.py (current Python — extract GEIPAN cases.json record shape)
    - geipan/.cache/ + geipan/cases.json (raw scraped data)
    - scripts/_archive_common.py
    - src/content.config.ts (catalogAssetSchema.strict)
    - .planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md §9 (PERF-01 GEIPAN strategy)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §2 + Pattern B + Pattern G
  </read_first>
  <behavior>
    - Stdlib-only Python 3; deterministic JSON; source-untouchable guard via git diff --quiet -- geipan/cases.json AND geipan/.cache/.
    - Reads geipan/cases.json (~3000 records) → emits catalogEnvelope at data/geipan.json containing ONLY the first 50 cases inline (rows) + shards manifest [{file, count}, ...] for the remaining ~2950.
    - Each asset matches catalogAssetSchema.strict() (t, ti, de, ag, cat, date, region, l, u, s, th).
    - rewrite_to_r2 with archive_slug='geipan' for any local PDF/video paths.
    - SHARD_SIZE = 50; ~60 shards data/geipan-shard-1.json .. data/geipan-shard-60.json each with {"shard": N, "assets": [...]}.
    - envelope.assets contains first 50 (page 1+2 fully covered server-side, supporting PERF-03 first-paint).
    - envelope.shards is an array of ~60 manifest entries.
    - scripts/build-geipan.py deleted via git rm.
  </behavior>
  <action>
    Clone scripts/normalize-brazil.py → scripts/normalize-geipan.py. Replace 'brazil' with 'geipan' in OUT_PRIMARY, slug field, rewrite_to_r2 arg, _assert_source_unchanged path (cover both geipan/cases.json AND geipan/.cache/).

    Inspect geipan/cases.json structure. Update parse_source() to read this monolithic file. Update to_catalog_asset(rec) for GEIPAN case shape (typically: case ID, date, locality/region, status classification A/B/C/D, description) → catalogAssetSchema.

    Verify sharding logic produces ~60 shards (3000 / 50 = 60). Envelope payload ≤ ~30 KB for first 50 records + ~6 KB shards manifest = ~36 KB → well under 500 KB target.

    Run python3 scripts/normalize-geipan.py; confirm data/geipan.json + ~60 shard files. Check envelope size:
    - `wc -c data/geipan.json` should report < 50 KB.
    - `ls data/geipan-shard-*.json | wc -l` should report ~60.

    git rm scripts/build-geipan.py.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; python3 scripts/normalize-geipan.py 2>&amp;1 | tail -10 &amp;&amp; test -f data/geipan.json &amp;&amp; node -e "const j=require('./data/geipan.json'); if(j.v1.slug!=='geipan') process.exit(1); const total=j.v1.stats.total, shards=j.v1.shards?.length||0; console.log('total='+total+' shards='+shards+' envelope_assets='+j.v1.assets.length); if(total < 100) process.exit(2); if(j.v1.assets.length > 60) process.exit(3); if(shards < 10) process.exit(4);" &amp;&amp; ls data/geipan-shard-*.json | wc -l &amp;&amp; (test ! -f scripts/build-geipan.py) &amp;&amp; git diff --quiet -- geipan/cases.json; echo cases_diff=$? &amp;&amp; git diff --quiet -- geipan/.cache/; echo cache_diff=$? &amp;&amp; pnpm build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - data/geipan.json + public/data/geipan.json valid; slug='geipan'; stats.total ≥ 100 (likely ~3000)
    - envelope.assets contains first 50 records inline (NOT all 3000)
    - envelope.shards manifest contains ≥ 10 shard entries (likely ~60)
    - data/geipan-shard-N.json files exist matching shards manifest count
    - wc -c data/geipan.json reports < 50 KB
    - Each asset matches catalogAssetSchema.strict()
    - scripts/build-geipan.py removed
    - git diff --quiet -- geipan/cases.json AND geipan/.cache/ both return 0
    - pnpm build exits 0
  </acceptance_criteria>
  <done>normalize-geipan.py shards ~3000 cases into ~60 shards; envelope < 50 KB; build-geipan.py removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/pages/geipan/index.astro + geipan.css + drop slug from postbuild + recapture baselines + fidelity + tone-colour validation + freeze Lighthouse baseline (PERF-01/02/03/04 enforcement)</name>
  <files>src/pages/geipan/index.astro, src/styles/geipan.css, scripts/copy-legacy-archives.sh, scripts/sync-nav.py, scripts/sync-footer.py, geipan/index.html, tests/fidelity-samples.json, tests/visual-baselines/geipan-360.png, tests/visual-baselines/geipan-768.png, tests/visual-baselines/geipan-1024.png, tests/visual-baselines/geipan-1440.png, tests/lighthouse-baselines/geipan.json</files>
  <read_first>
    - src/pages/brazil/index.astro (Plan 04-11 — clone target with sharding)
    - src/pages/index.astro (Plan 04-04 — pagination handler reference: shards manifest fetch + 20-card window + pushState + popstate + hash anchor cross-page)
    - src/styles/brazil.css (template)
    - scripts/copy-legacy-archives.sh (drop 'geipan')
    - geipan/index.html (extract hero-lede + license-footer for fidelity samples)
    - tests/fidelity-samples.json (add 5 geipan samples)
    - tests/tone-colours-fixture.json (verify geipan = "#0055a4")
    - tests/lighthouse-baselines/ (frozen baseline directory; create if absent)
    - .planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md §9 (PERF-01 strategy)
    - CLAUDE.md §3.1 (geipan tone #0055a4 French blue) + §9 (France "Loi n° 78-753")
  </read_first>
  <behavior>
    - src/pages/geipan/index.astro renders from getEntry('geipan','v1') via RootLayout + CatalogCard + Lightbox + Plan 04-04 pagination handler.
    - archiveSlug='geipan' drives --caution #0055a4 from RootLayout TONE map.
    - HeroCarousel: OFF (no representative imagery for GEIPAN per RESEARCH §8 A2).
    - Footer carries "Loi n° 78-753" verbatim.
    - 'geipan' dropped from postbuild + sync scripts.
    - geipan/index.html DELETED. geipan/cases.json + geipan/.cache/ PRESERVED (source data).
    - Heavily sharded: shards manifest script + Plan 04-04 pagination handler adapted for geipan-grid + geipan-pagination ids; window over ~3000 cards via 20-card pages; client lazy-fetches shards on demand for pages > 1 (PERF-02 ≤ 3 chunks per page-view = initial HTML with 20 cards + max 2 shards at boundary pages).
    - Server-renders first 20 cards inline (PERF-03 first-paint contains cards).
    - Visual baselines recaptured per D-17.
    - 5 fidelity samples added.
    - **Lighthouse baseline frozen:** capture tests/lighthouse-baselines/geipan.json against preview after pnpm build + deploy via `npx lhci collect --collect.url=<preview>/geipan/ --collect.settings.preset=mobile --collect.settings.throttlingMethod=devtools` → save the report JSON.
    - **PERF-01 validation:** initial HTML+inline weight < 500 KB; LCP mobile @ 4× CPU ≤ 2500 ms verified in Lighthouse report.
    - **PERF-02 validation:** open DevTools network panel on preview, navigate /geipan/?page=10 → confirm ≤ 3 JSON shard fetches.
    - **Tone-colour fixture:** validate geipan = "#0055a4"; update if buggy.
  </behavior>
  <action>
    Clone src/pages/brazil/index.astro → src/pages/geipan/index.astro:
    - Replace 'brazil' with 'geipan' in archiveSlug, getEntry, grid id, pagination id, CSS path, shards-manifest id.
    - Update title/description from CLAUDE.md §2 ("France GEIPAN — CNES") + geipan/index.html hero lede verbatim.
    - HeroCarousel OFF.

    Clone src/styles/brazil.css → src/styles/geipan.css.

    Modify scripts/copy-legacy-archives.sh: remove 'geipan' from ARCHIVES iteration.

    Modify scripts/sync-nav.py + scripts/sync-footer.py: drop 'geipan' if policed.

    git rm geipan/index.html. PRESERVE geipan/cases.json + geipan/.cache/ (source data per CLAUDE.md §11).

    **Tone-colour fixture validation:** Read tests/tone-colours-fixture.json; verify geipan = "#0055a4". If buggy, update per D-10.

    Recapture visual baselines: python3 scripts/capture-baselines.py --archive geipan --base-url <preview-url>.

    Add 5 fidelity samples: hero-lede, license-footer "Loi n° 78-753", 2 card titles, 1 agency/case-ID.

    **Freeze Lighthouse baseline:** After preview deploy succeeds, run:
    - `npx lhci collect --collect.url=<preview-url>/geipan/ --collect.settings.preset=mobile --collect.settings.throttlingMethod=devtools`
    - Save the report JSON to tests/lighthouse-baselines/geipan.json
    - Commit baseline as PERF-04 regression watchpoint (future GEIPAN changes lighthouse-diff against this).

    **PERF-01/02 sanity check (documented in 04-18-SUMMARY):**
    - `wc -c dist/geipan/index.html` → confirm < 500 KB.
    - Lighthouse mobile + 4× CPU: LCP < 2500 ms reported.
    - DevTools network panel on /geipan/?page=10: confirm ≤ 3 JSON shard requests (initial + 2 shards at boundary).

    pnpm build → confirm dist/geipan/index.html.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; test -f src/pages/geipan/index.astro &amp;&amp; grep -q "archiveSlug=\"geipan\"" src/pages/geipan/index.astro &amp;&amp; (! grep -v '^#' scripts/copy-legacy-archives.sh | grep -E '\bgeipan\b') &amp;&amp; (test ! -f geipan/index.html) &amp;&amp; test -f geipan/cases.json &amp;&amp; pnpm build 2>&amp;1 | tail -5 &amp;&amp; test -f dist/geipan/index.html &amp;&amp; grep -q "arch-card" dist/geipan/index.html &amp;&amp; node -e "const s=require('fs').statSync('dist/geipan/index.html').size; console.log('size_bytes='+s); if (s > 512000) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/geipan/index.astro renders via getEntry('geipan','v1')
    - scripts/copy-legacy-archives.sh no longer iterates 'geipan'
    - geipan/index.html deleted; geipan/cases.json + geipan/.cache/ preserved
    - dist/geipan/index.html exists with ≥ 1 `.arch-card` rendered server-side
    - wc -c dist/geipan/index.html reports < 500 KB (≤ 512000 bytes)
    - tests/visual-baselines/geipan-{360,768,1024,1440}.png exist
    - tests/fidelity-samples.json contains ≥ 5 entries with slug:"geipan"
    - tests/lighthouse-baselines/geipan.json exists with mobile + 4× CPU LCP ≤ 2500 ms (PERF-01)
    - License footer in dist/geipan/index.html contains "Loi n° 78-753" verbatim
    - tone-colours.spec.ts --grep geipan reports green (--caution #0055a4 — D-10 bug fix confirmed)
    - dist/geipan/index.html contains shard manifest `<script id="geipan-shards">` with ≥ 10 entries
    - DevTools network capture for /geipan/?page=10: ≤ 3 JSON requests (PERF-02 verified)
  </acceptance_criteria>
  <done>GEIPAN archive on Astro; --caution #0055a4 (tone bug fixed); HTML ≤ 500 KB (3.3 MB → < 500 KB reduction); LCP ≤ 2500 ms; PERF-01/02/03/04 all satisfied; license verbatim; Python retired; baselines + Lighthouse baseline frozen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time→data/geipan.json + ~60 shards | normalize-geipan.py emits schema-validated JSON; cases.json source untouchable |
| client→/geipan/ HTML | Browser renders Astro page; lazy-fetches shards per page-view (max 2 + initial = 3 chunks) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-58 | Tampering | GEIPAN cases.json mutation | mitigate | source-untouchable guard via git diff --quiet -- geipan/cases.json AND geipan/.cache/; Zod strict() |
| T-04-59 | Tampering | XSS via French text in case descriptions | mitigate | Astro `{expr}` auto-escape; no set:html |
| T-04-60 | Repudiation | Loss of "Loi n° 78-753" attribution OR tone-colour drift | mitigate | 5 fidelity samples; tone-colours fixture locks #0055a4 |
| T-04-61 | Denial of Service | 3000 cards lazy-loaded all upfront → blow PERF budget | mitigate | Sharding ~60 × 50 + 20-card client window + max 2 shards lazy-fetched per page-view (PERF-02); LCP enforced via Lighthouse baseline |
</threat_model>

<verification>
1. python3 scripts/normalize-geipan.py exits 0; data/geipan.json valid; envelope < 50 KB; ~60 shards emitted; source untouched.
2. pnpm build → dist/geipan/index.html ≤ 500 KB with first 20 cards server-rendered.
3. pnpm exec playwright test tests/visual-regression.spec.ts --grep geipan passes.
4. pnpm exec playwright test tests/tone-colours.spec.ts --grep geipan passes (#0055a4 — D-10 bug fix).
5. pnpm exec playwright test tests/js-off.spec.ts --grep geipan passes (PERF-03 first-paint cards present).
6. python3 scripts/verify-fidelity.py --grep geipan passes.
7. Lighthouse mobile + 4× CPU: LCP ≤ 2500 ms (PERF-01); transfer ≤ 500 KB (SSG-07).
8. DevTools network panel /geipan/?page=10: ≤ 3 JSON requests (PERF-02).
9. tests/lighthouse-baselines/geipan.json frozen.
10. scripts/build-geipan.py + geipan/index.html absent.
11. scripts/copy-legacy-archives.sh no longer iterates 'geipan'.
</verification>

<success_criteria>
- [ ] GEIPAN archive renders from Astro at /geipan/
- [ ] --caution #0055a4 applied (D-10 tone-colour bug resolved)
- [ ] HTML ≤ 500 KB (was 3.3 MB — PERF-01 reduction)
- [ ] LCP mobile + 4× CPU ≤ 2500 ms (PERF-01)
- [ ] ≤ 3 chunks per page-view (PERF-02)
- [ ] First paint contains cards (PERF-03)
- [ ] License footer "Loi n° 78-753" verbatim
- [ ] ~60 shards emitted (3000 cases / 50)
- [ ] scripts/build-geipan.py + geipan/index.html deleted; geipan/cases.json preserved
- [ ] 'geipan' dropped from copy-legacy-archives.sh + sync-nav.py + sync-footer.py
- [ ] tests/lighthouse-baselines/geipan.json frozen (PERF-04 watchpoint)
- [ ] R2 URLs in card.url fields
</success_criteria>

<output>
Create `.planning/phases/04-full-migration-search-offline-performance/04-18-SUMMARY.md` when done.
</output>
</content>
</invoke>
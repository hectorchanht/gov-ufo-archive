---
phase: 04-full-migration-search-offline-performance
plan: "08"
type: execute
wave: 4
depends_on: ["04-02", "04-05"]
files_modified:
  - src/pages/spain/index.astro
  - src/styles/spain.css
  - scripts/normalize-spain.py
  - data/spain.json
  - public/data/spain.json
  - scripts/copy-legacy-archives.sh
  - scripts/sync-nav.py
  - scripts/sync-footer.py
  - tests/fidelity-samples.json
  - tests/visual-baselines/spain-360.png
  - tests/visual-baselines/spain-768.png
  - tests/visual-baselines/spain-1024.png
  - tests/visual-baselines/spain-1440.png
  - spain/index.html
  - scripts/build-spain.py
autonomous: true
requirements: [SSG-06, SSG-08, SSG-09, SSG-10, SSG-11, SSG-12, PERF-04]
tags: [archive-port, spain, ejercito-del-aire, small-static]

must_haves:
  truths:
    - "Visiting /spain/ renders Spain Ejército del Aire archive from Astro Content Collections"
    - "Per-archive tone --caution #f4c542 applied via RootLayout TONE map"
    - "Page weighs ≤ 500 KB HTML+inline (SSG-07); JS-off shows cards (D-23 server-side render)"
    - "Lightbox + hamburger + /-focus + ?q= persistence preserved per CLAUDE.md §7"
    - "scripts/build-spain.py deleted; 'spain' dropped from postbuild + sync scripts"
    - "Card URLs flow through assets.realufo.org via rewrite_to_r2"
    - "License footer carries 'Ley 19/2013' verbatim (CLAUDE.md §9 Spain attribution)"
  artifacts:
    - path: "src/pages/spain/index.astro"
      contains: "archiveSlug=\"spain\""
    - path: "scripts/normalize-spain.py"
      min_lines: 50
    - path: "data/spain.json"
      contains: "schemaVersion"
  key_links:
    - from: "src/pages/spain/index.astro"
      to: "data/spain.json"
      via: "getEntry('spain','v1')"
      pattern: "getEntry\\('spain'"
---

<objective>
Port the Spain Ejército del Aire archive (D-08 Wave 1 small/static tier) to Astro using the CatalogCard.astro template established in Plan 04-05. Per-archive Python retirement (D-09): delete scripts/build-spain.py, drop 'spain' from copy-legacy-archives.sh + sync-nav.py + sync-footer.py.

Mirrors Plan 04-06 Uruguay port template. Only slug, tone-colour (#f4c542 per CLAUDE.md §3.1), license attribution, and per-archive content differ.

Output: Spain archive on Astro + normalize-spain.py + data/spain.json + baselines + 5 fidelity samples.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-migration-search-offline-performance/04-CONTEXT.md
@.planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md
@.planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md
@.planning/phases/04-full-migration-search-offline-performance/04-05-PLAN.md
@.planning/phases/04-full-migration-search-offline-performance/04-06-PLAN.md
@CLAUDE.md

<interfaces>
Identical interface set to Plan 04-06 (Uruguay template). Differences:
- archiveSlug = "spain"
- tone --caution = #f4c542 (CLAUDE.md §3.1)
- License footer = "Ley 19/2013" (Spain Transparencia)
- Source data: spain/.cache (if exists) or spain/index.html embedded <script id="arch-data">
- Output: data/spain.json
- Spain has spain/index.html (61 KB), spain/manises.html, spain/story.html — only spain/index.html is the archive listing; manises.html + story.html are long-form pages (Phase 4 leaves them alone; covered by Phase 5 if at all)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write scripts/normalize-spain.py + emit data/spain.json + delete scripts/build-spain.py</name>
  <files>scripts/normalize-spain.py, data/spain.json, public/data/spain.json, scripts/build-spain.py</files>
  <read_first>
    - scripts/normalize-uruguay.py (Plan 04-06 — closest template; clone + slug-swap)
    - scripts/normalize-nz.py (Plan 04-05 — original CatalogCard-aware template)
    - scripts/build-spain.py (current Python build — extract source path + record shape)
    - spain/ directory contents (find source: spain/.cache/*.json if present, else parse embedded arch-data JSON in spain/index.html)
    - scripts/_archive_common.py (rewrite_to_r2 + slugify)
    - src/content.config.ts (catalogAssetSchema.strict)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §2 + Pattern B
    - CLAUDE.md §11 (source untouchability)
  </read_first>
  <behavior>
    - Stdlib-only Python 3 (argparse, html, html.parser, json, re, subprocess, sys, pathlib); imports rewrite_to_r2 + slugify from _archive_common.
    - Reads Spain source WITHOUT writing to it. Source discovery: if spain/.cache/*.json exists, read it; else extract `<script id="arch-data" type="application/json">` payload from spain/index.html.
    - Emits data/spain.json + public/data/spain.json via deterministic JSON write (sort_keys=True, ensure_ascii=False, indent=2, trailing newline).
    - Envelope shape: { "v1": { "schemaVersion": 1, "slug": "spain", "assets": [...], "stats": { total, local_total, pdf_total, catalog_total } } }.
    - Each asset has exactly catalogAssetSchema.strict() keys: t, ti, de, ag, cat, date, region, l, u, s, th.
    - rewrite_to_r2(local_path, 'spain', 'pdfs'|'videos') applied to every PDF/video URL.
    - Source-untouchable guard via `subprocess.run(['git','-C',REPO,'diff','--quiet','--','spain/.cache/'])` returncode == 0 (or `spain/index.html` if that is the source).
    - scripts/build-spain.py DELETED via `git rm`.
  </behavior>
  <action>
    Clone scripts/normalize-uruguay.py (Plan 04-06) → scripts/normalize-spain.py:
    - Update header docstring source path reference (point at spain/.cache or spain/index.html as discovered).
    - Replace 'uruguay' literal with 'spain' in OUT_PRIMARY, slug field of envelope, archive_slug arg to rewrite_to_r2, and _assert_source_unchanged path.
    - Update parse_source() to read Spain-specific record shape. Inspect spain/.cache/ or extract from spain/index.html embedded arch-data JSON.
    - Update to_catalog_asset(rec) field mapping for Spain record keys → catalogAssetSchema (t, ti, de, ag, cat, date, region, l, u, s, th). Apply rewrite_to_r2 on every asset.l → asset.u construction.
    - Run python3 scripts/normalize-spain.py; confirm data/spain.json + public/data/spain.json exist and parse.
    - git rm scripts/build-spain.py.

    Do NOT inline code in this action — clone the proven Plan 04-06 normalizer and adapt mechanically.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; python3 scripts/normalize-spain.py 2>&amp;1 | tail -10 &amp;&amp; test -f data/spain.json &amp;&amp; test -f public/data/spain.json &amp;&amp; node -e "const j=require('./data/spain.json'); if(j.v1.slug!=='spain') process.exit(1); if(!Array.isArray(j.v1.assets)) process.exit(2); console.log('total='+j.v1.stats.total)" &amp;&amp; (test ! -f scripts/build-spain.py || (echo 'build-spain.py exists'; exit 1)) &amp;&amp; pnpm build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - data/spain.json + public/data/spain.json valid catalogEnvelopeSchema JSON; slug='spain'; stats.total ≥ 1
    - Each asset object has exactly catalogAssetSchema.strict() keys (t, ti, de, ag, cat, date, region, l, u, s, th)
    - Every non-empty asset.u with t='PDF' starts with 'https://assets.realufo.org/pdfs/spain/'
    - Every non-empty asset.u with t='VID' starts with 'https://assets.realufo.org/videos/spain/'
    - scripts/build-spain.py NO LONGER EXISTS in working tree
    - git diff --quiet -- spain/.cache/ (or spain/index.html if that was the source) returns 0
    - pnpm build exits 0 (Zod schema validation passes)
  </acceptance_criteria>
  <done>normalize-spain.py ships valid catalog data; build-spain.py removed; Zod schema accepts; source untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/pages/spain/index.astro + spain.css + drop slug from postbuild + recapture baselines + fidelity</name>
  <files>src/pages/spain/index.astro, src/styles/spain.css, scripts/copy-legacy-archives.sh, scripts/sync-nav.py, scripts/sync-footer.py, spain/index.html, tests/fidelity-samples.json, tests/visual-baselines/spain-360.png, tests/visual-baselines/spain-768.png, tests/visual-baselines/spain-1024.png, tests/visual-baselines/spain-1440.png</files>
  <read_first>
    - src/pages/uruguay/index.astro (Plan 04-06 — clone target)
    - src/pages/nz/index.astro (Plan 04-05 — original template)
    - src/styles/uruguay.css (template)
    - scripts/copy-legacy-archives.sh (drop 'spain' from ARCHIVES iteration)
    - scripts/sync-nav.py + scripts/sync-footer.py (drop 'spain' if policed)
    - spain/index.html (extract hero-lede + license-footer for fidelity samples — verbatim per CLAUDE.md §9)
    - tests/fidelity-samples.json (add 5 spain samples)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §1 + Pattern A
    - CLAUDE.md §3.1 (spain tone #f4c542) + §9 (Spain "Ley 19/2013" Transparencia attribution)
  </read_first>
  <behavior>
    - src/pages/spain/index.astro renders Spain archive from getEntry('spain','v1') via RootLayout + CatalogCard + Lightbox + pagination handler.
    - archiveSlug='spain' drives --caution: #f4c542 from RootLayout TONE map.
    - HeroCarousel: OFF (Spain small archive — no ≥4 hero images per RESEARCH §8).
    - Footer carries verbatim "Ley 19/2013" Spain Transparencia attribution per CLAUDE.md §9.
    - scripts/copy-legacy-archives.sh: 'spain' removed from ARCHIVES iteration list.
    - scripts/sync-nav.py + sync-footer.py: 'spain' dropped from policed lists (if present).
    - spain/index.html DELETED (legacy HTML retired). spain/manises.html + spain/story.html UNTOUCHED (long-form pages not covered by Phase 4 scope; only the archive listing index.html retires).
    - tests/visual-baselines/spain-{360,768,1024,1440}.png recaptured per D-17 operator-conscious procedure.
    - tests/fidelity-samples.json gains 5 spain samples (hero-lede, license-footer "Ley 19/2013", 2 card titles, 1 agency).
  </behavior>
  <action>
    Clone src/pages/uruguay/index.astro → src/pages/spain/index.astro:
    - Replace 'uruguay' literal with 'spain' in archiveSlug, getEntry argument, grid id, pagination id, CSS import path.
    - Update title/description from CLAUDE.md §2 ("Spain Ejército del Aire") + spain/index.html hero lede (verbatim per CLAUDE.md §9 — extract the actual hero `<h1>` + `.hero-sub` text).
    - HeroCarousel inclusion OFF.

    Clone src/styles/uruguay.css → src/styles/spain.css (minimal; comment-only header if no per-archive layout differs).

    Modify scripts/copy-legacy-archives.sh: remove 'spain' from the `for slug in ...` array.

    Modify scripts/sync-nav.py + scripts/sync-footer.py: drop 'spain' from policed-archives lists if present.

    git rm spain/index.html (do NOT delete spain/manises.html or spain/story.html — out of Phase 4 scope per CONTEXT D-11 spirit + RESEARCH §10 retirement schedule).

    Recapture visual baselines via D-17 procedure: python3 scripts/capture-baselines.py --archive spain --base-url <preview-url> AFTER pnpm build + preview.

    Add 5 spain fidelity samples to tests/fidelity-samples.json: hero-lede selector + verbatim text, license-footer selector + "Ley 19/2013" verbatim text, 2 card-title strings, 1 agency string.

    pnpm build → confirm dist/spain/index.html emits with rendered cards.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; test -f src/pages/spain/index.astro &amp;&amp; grep -q "archiveSlug=\"spain\"" src/pages/spain/index.astro &amp;&amp; grep -q "getEntry('spain'" src/pages/spain/index.astro &amp;&amp; (! grep -v '^#' scripts/copy-legacy-archives.sh | grep -E '\bspain\b') &amp;&amp; (test ! -f spain/index.html || (echo 'spain/index.html exists'; exit 1)) &amp;&amp; pnpm build 2>&amp;1 | tail -5 &amp;&amp; test -f dist/spain/index.html &amp;&amp; grep -q "arch-card" dist/spain/index.html</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/spain/index.astro renders via getEntry('spain','v1'); archiveSlug='spain'; imports RootLayout + CatalogCard + Lightbox + spain.css
    - src/styles/spain.css exists
    - grep -v '^#' scripts/copy-legacy-archives.sh | grep -E '\bspain\b' returns 0 matches (slug dropped)
    - spain/index.html deleted; spain/manises.html + spain/story.html STILL EXIST (preserved)
    - dist/spain/index.html exists post-build with ≥ 1 `.arch-card`
    - tests/visual-baselines/spain-{360,768,1024,1440}.png exist
    - tests/fidelity-samples.json contains ≥ 5 entries with `"slug": "spain"`
    - License footer in dist/spain/index.html contains "Ley 19/2013" verbatim
    - tone-colours.spec.ts --grep spain reports green (--caution #f4c542)
  </acceptance_criteria>
  <done>Spain archive on Astro; --caution #f4c542; license verbatim; Python retired; baselines recaptured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time→data/spain.json | normalize-spain.py emits schema-validated JSON consumed by Astro |
| client→/spain/ HTML | Browser renders Astro-emitted page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-26 | Tampering | Spain scraper output mutation | mitigate | source-untouchable guard via git diff --quiet -- spain/; Zod schema.strict() at build catches unknown keys |
| T-04-27 | Tampering | XSS via injected text fields | mitigate | Astro `{expr}` auto-escape on CatalogCard; no set:html for user data |
| T-04-28 | Repudiation | Loss of Spain license-footer verbatim | mitigate | 5 fidelity samples (verify-fidelity.py gate) including "Ley 19/2013" exact string |
</threat_model>

<verification>
1. python3 scripts/normalize-spain.py exits 0; data/spain.json valid; source untouched.
2. pnpm build → dist/spain/index.html with rendered cards.
3. pnpm exec playwright test tests/visual-regression.spec.ts --grep spain passes against recaptured baselines.
4. pnpm exec playwright test tests/tone-colours.spec.ts --grep spain passes (#f4c542).
5. pnpm exec playwright test tests/js-off.spec.ts --grep spain passes.
6. python3 scripts/verify-fidelity.py --grep spain passes (5 samples byte-equivalent).
7. scripts/build-spain.py + spain/index.html absent from working tree.
8. scripts/copy-legacy-archives.sh no longer iterates 'spain'.
</verification>

<success_criteria>
- [ ] Spain archive renders from Astro at /spain/
- [ ] --caution #f4c542 applied via RootLayout TONE map
- [ ] License footer carries "Ley 19/2013" verbatim
- [ ] scripts/build-spain.py + spain/index.html deleted
- [ ] 'spain' dropped from copy-legacy-archives.sh + sync-nav.py + sync-footer.py
- [ ] Visual + fidelity + tone-colour + js-off + Lighthouse soft gates green for spain
- [ ] R2 URLs in card.url fields (no bundles/ paths)
</success_criteria>

<output>
Create `.planning/phases/04-full-migration-search-offline-performance/04-08-SUMMARY.md` when done.
</output>
</content>
</invoke>
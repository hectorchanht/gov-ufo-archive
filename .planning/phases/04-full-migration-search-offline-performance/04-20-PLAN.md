---
phase: 04-full-migration-search-offline-performance
plan: "20"
type: execute
wave: 8
depends_on: ["04-19"]
files_modified:
  - .lighthouserc.cf.json
  - package.json
  - scripts/copy-legacy-archives.sh
  - scripts/sync-nav.py
  - scripts/sync-footer.py
  - scripts/build-details.py
  - scripts/spider.py
  - scripts/verify-python-retired.sh
  - .github/workflows/nav-sync.yml
  - .github/workflows/footer-sync.yml
  - .github/workflows/html-validate.yml
autonomous: true
requirements: [PERF-04, SSG-10]
tags: [phase-close, lighthouse-hard-flip, python-cleanup, postbuild-retirement, ci-drift-gate-removal]

must_haves:
  truths:
    - ".lighthouserc.cf.json: categories:performance + lcp-element + resource-summary:total:size all flipped from `warn` → `error` (D-40)"
    - "All scripts/build-*.py archive build scripts deleted (covered in prior plans; this plan verifies + cleans stragglers like scripts/build-details.py)"
    - "scripts/sync-nav.py + scripts/sync-footer.py + scripts/copy-legacy-archives.sh deleted (no slugs remain; Nav.astro + Footer.astro are sole source per RESEARCH §10)"
    - "scripts/parse-aaro.py + scripts/extract-evidence.py confirmed deleted (Plan 04-17)"
    - "scripts/spider.py STATUS — per RESEARCH §10, this is Phase 5 SCRP scope. Keep, do not delete in Phase 4. Document in SUMMARY."
    - "package.json scripts.postbuild reduced to ONLY `pnpm exec pagefind --site dist` (no more legacy HTML copy step — all 14 archives + wargov + GEIPAN now on Astro)"
    - "CI drift workflows .github/workflows/nav-sync.yml + footer-sync.yml + (html-validate.yml if dup with quality-gates.yml) removed"
    - "scripts/verify-python-retired.sh shell script asserts no scripts/build-*.py, sync-*.py, parse-aaro.py, extract-evidence.py, copy-legacy-archives.sh"
    - "Full quality-gates.yml matrix green against final preview: visual + fidelity + tone-colours (15/15) + js-off (15/15) + lighthouse (HARD now) + redirects + lightbox + search + sw + r2-urls + pagination"
    - "Phase 4 SUMMARY documents: all 23 requirement IDs satisfied; Python build legacy fully retired (except spider.py reserved for Phase 5)"
  artifacts:
    - path: ".lighthouserc.cf.json"
      provides: "Lighthouse budgets HARD-enforced post Phase 4 close"
      contains: "error"
    - path: "scripts/verify-python-retired.sh"
      provides: "Single-pass shell script asserting Python build retirement complete"
      min_lines: 20
  key_links:
    - from: ".lighthouserc.cf.json"
      to: "quality-gates.yml lighthouse job"
      via: "assertion levels error vs warn"
      pattern: "\"error\""
    - from: "scripts/verify-python-retired.sh"
      to: "scripts/ directory state"
      via: "ls + test pattern"
      pattern: "ls scripts/build-\\*\\.py"
---

<objective>
Close Phase 4: flip Lighthouse SOFT → HARD (`warn` → `error`) per D-40 + 02-08 phase4-close-toggle, delete the cross-cutting Python scripts that lost their consumers (sync-nav.py, sync-footer.py, copy-legacy-archives.sh, build-details.py), shrink package.json scripts.postbuild to ONLY Pagefind (no more legacy HTML copy step — all 15 archives are now Astro), retire CI drift gates that policed sync-nav/sync-footer drift, and run the full quality-gates.yml suite green against final preview.

Per RESEARCH §10:
- scripts/spider.py STAYS (Phase 5 SCRP scope — scrape automation rewrites it).
- scripts/dl-<slug>.sh STAY (D-11; download scripts not touched).
- scripts/build-details.py: covers AARO + case-detail long-form pages; if all long-form pages are still legacy HTML (preserved out of Phase 4 scope), this script remains needed. Verify by checking if any plan 04-17 (AARO) re-runs it; if NOT consumed by postbuild, delete; else preserve and document.

Output: Phase 4 closed; Lighthouse HARD; Python build retired; postbuild = Pagefind only; CI drift gates dropped; full quality-gates green; 04-20-SUMMARY records Phase 4 sign-off.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-migration-search-offline-performance/04-CONTEXT.md
@.planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md
@.planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md
@CLAUDE.md

<interfaces>
.lighthouserc.cf.json current state (Phase 2 02-08 toggle: warn-level):
  assertions:
    "categories:performance": ["warn", { "minScore": 0.85 }]
    "lcp-element": ["warn", { "maxNumericValue": 2500 }]
    "resource-summary:total:size": ["warn", { "maxNumericValue": 512000 }]
  Phase 4 close flips all 3 to "error".

package.json scripts (current shape):
  "build": "astro build"
  "postbuild": "bash scripts/copy-legacy-archives.sh"
  Phase 4 close: postbuild → "pnpm exec pagefind --site dist" (copy-legacy retires since no slugs left).

CI workflows policed by Phase 4 close:
  - .github/workflows/quality-gates.yml — STAY (run final HARD pass)
  - .github/workflows/nav-sync.yml — REMOVE (sync-nav.py retired; Nav.astro is sole source)
  - .github/workflows/footer-sync.yml — REMOVE (sync-footer.py retired)
  - .github/workflows/html-validate.yml — REMOVE only if duplicated by quality-gates.yml
  - .github/workflows/r2-sync.yml (Plan 04-02) — STAY (R2 sync remains needed)
  - .github/workflows/deploy-cf-pages.yml or equivalent — STAY

Python script inventory expected at start of Plan 04-20:
  scripts/build-aaro.py — DELETED (Plan 04-17)
  scripts/build-brazil.py — DELETED (Plan 04-11)
  scripts/build-canada.py — DELETED (Plan 04-13)
  scripts/build-chile.py — DELETED (Plan 04-12)
  scripts/build-geipan.py — DELETED (Plan 04-18)
  scripts/build-italy.py — DELETED (Plan 04-10)
  scripts/build-nara.py — DELETED (Plan 04-15)
  scripts/build-nasa.py — DELETED (Plan 04-16)
  scripts/build-nz.py — DELETED (Plan 04-05)
  scripts/build-peru.py — DELETED (Plan 04-07)
  scripts/build-spain.py — DELETED (Plan 04-08)
  scripts/build-uk.py — DELETED (Plan 04-14)
  scripts/build-uruguay.py — DELETED (Plan 04-06)
  scripts/build-wargov.py — DELETED (Phase 3 Plan 03-05)
  scripts/build-argentina.py — DELETED (Plan 04-09)
  scripts/parse-aaro.py — DELETED (Plan 04-17)
  scripts/extract-evidence.py — DELETED (Plan 04-17)
  scripts/build-details.py — STATUS UNCLEAR; verify and decide
  scripts/spider.py — KEEP (Phase 5 SCRP)
  scripts/dl-*.sh — KEEP (D-11)
  scripts/build-api.py + scripts/build-cases.py + scripts/build-feeds.py + scripts/build-geo.py + scripts/build-og.py + scripts/build-pages-index.py + scripts/build-redirects.py + scripts/build-stories.py + scripts/build-sw.py — STATUS UNCLEAR for each; verify consumer chain
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all 14 archive ports + Pagefind ✅, audit remaining Python scripts (delete orphaned, preserve still-consumed), shrink package.json postbuild, retire copy-legacy + sync-nav + sync-footer + drift CI gates</name>
  <files>package.json, scripts/copy-legacy-archives.sh, scripts/sync-nav.py, scripts/sync-footer.py, scripts/build-details.py, scripts/build-api.py, scripts/build-cases.py, scripts/build-feeds.py, scripts/build-geo.py, scripts/build-og.py, scripts/build-pages-index.py, scripts/build-redirects.py, scripts/build-stories.py, scripts/build-sw.py, scripts/verify-python-retired.sh, .github/workflows/nav-sync.yml, .github/workflows/footer-sync.yml, .github/workflows/html-validate.yml</files>
  <read_first>
    - package.json (current scripts.postbuild)
    - scripts/copy-legacy-archives.sh (current state — should iterate over EMPTY slug list after 04-05..04-18 complete + Pagefind line at end from Plan 04-19)
    - scripts/sync-nav.py + scripts/sync-footer.py (current state — verify policed-archives list is empty)
    - scripts/build-details.py + scripts/build-api.py + scripts/build-cases.py + scripts/build-feeds.py + scripts/build-geo.py + scripts/build-og.py + scripts/build-pages-index.py + scripts/build-redirects.py + scripts/build-stories.py + scripts/build-sw.py (each: check whether package.json or any GH workflow invokes them; if NO consumer, delete; else preserve and document)
    - .github/workflows/ directory (list all yml files; identify nav-sync.yml, footer-sync.yml, html-validate.yml; verify they exist before attempting deletion)
    - .planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md §10 (retirement schedule)
    - CLAUDE.md §6 (build & sync rules — postbuild contract)
  </read_first>
  <behavior>
    - **Audit Python build scripts:** for each scripts/build-*.py (excluding ones known to be already retired in prior plans), grep package.json + .github/workflows/ for consumer reference. If NO consumer, git rm. If consumer exists, preserve and document in SUMMARY.
    - **scripts/build-details.py:** likely covers AARO + per-archive long-form pages (CLAUDE.md §5). If long-form HTML pages (e.g. aaro/belgian-wave.html, brazil/operacao-prato.html) are still committed and the script regenerates them, KEEP. If no longer consumed, delete.
    - **scripts/build-sw.py:** likely Phase 1 kill-switch SW builder; Plan 04-03 replaced via @vite-pwa/astro. Verify and delete if orphaned.
    - **scripts/copy-legacy-archives.sh:** by end of Wave 6 (Plan 04-18), the for-slug-in array is empty. Plan 04-19 appended Pagefind line. Now: delete the for-loop entirely; move Pagefind invocation directly into package.json scripts.postbuild as `pnpm exec pagefind --site dist`. Then git rm scripts/copy-legacy-archives.sh.
    - **scripts/sync-nav.py + scripts/sync-footer.py:** all 15 slugs have been removed from their policed lists across Plan 04-05..04-18. The scripts now no-op. Delete via git rm.
    - **CI drift gates:** if .github/workflows/nav-sync.yml + footer-sync.yml exist, git rm. If .github/workflows/html-validate.yml is duplicated by quality-gates.yml job (check for html-validate matrix entry in quality-gates.yml), git rm. Else preserve.
    - **Create scripts/verify-python-retired.sh:** shell script asserting:
      - ls scripts/build-*.py returns no `build-<archive>.py` matches (whitelist: build-api.py, build-cases.py, etc. that survive must be explicit)
      - ls scripts/sync-*.py returns empty
      - ls scripts/parse-aaro.py returns nonexistent
      - ls scripts/extract-evidence.py returns nonexistent
      - ls scripts/copy-legacy-archives.sh returns nonexistent
      - Exit code 0 if all checks pass, 1 if any survives unaccounted.
    - **package.json scripts.postbuild:** replace `bash scripts/copy-legacy-archives.sh` with `pnpm exec pagefind --site dist`.
  </behavior>
  <action>
    Read package.json scripts.postbuild current value.

    Verify scripts/copy-legacy-archives.sh has empty for-loop body (all 15 slugs removed in Plans 04-05..04-18) and Pagefind line at end (Plan 04-19). If not in expected state, document in SUMMARY and reconcile.

    For each scripts/build-{details,api,cases,feeds,geo,og,pages-index,redirects,stories,sw}.py:
    - `grep -l "build-<NAME>.py" package.json .github/workflows/*.yml` → if matches, preserve; else `git rm scripts/build-<NAME>.py`.

    git rm scripts/sync-nav.py.
    git rm scripts/sync-footer.py.

    Modify package.json scripts.postbuild: change from `bash scripts/copy-legacy-archives.sh` to `pnpm exec pagefind --site dist`.

    git rm scripts/copy-legacy-archives.sh.

    Check .github/workflows/ for nav-sync.yml + footer-sync.yml + html-validate.yml:
    - test -f .github/workflows/nav-sync.yml && git rm .github/workflows/nav-sync.yml
    - test -f .github/workflows/footer-sync.yml && git rm .github/workflows/footer-sync.yml
    - If html-validate.yml exists and quality-gates.yml has an html-validate job: git rm; else preserve.

    Create scripts/verify-python-retired.sh:
    - Shebang `#!/usr/bin/env bash` + `set -euo pipefail`.
    - Whitelist of retained scripts: spider.py + dl-*.sh + any build-*.py that grep proved still consumed.
    - Iterate scripts/build-*.py; for each match NOT in whitelist, print error + exit 1.
    - Check absence of: scripts/sync-nav.py, scripts/sync-footer.py, scripts/parse-aaro.py, scripts/extract-evidence.py, scripts/copy-legacy-archives.sh.
    - Exit 0 if all assertions pass.
    - chmod +x scripts/verify-python-retired.sh.

    Run bash scripts/verify-python-retired.sh → expect exit 0.

    Run pnpm build → confirm dist/pagefind/ still emits via the slimmer postbuild.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; grep -q "pnpm exec pagefind --site dist" package.json &amp;&amp; (! grep -q "copy-legacy-archives.sh" package.json) &amp;&amp; (test ! -f scripts/copy-legacy-archives.sh) &amp;&amp; (test ! -f scripts/sync-nav.py) &amp;&amp; (test ! -f scripts/sync-footer.py) &amp;&amp; (test ! -f scripts/parse-aaro.py) &amp;&amp; (test ! -f scripts/extract-evidence.py) &amp;&amp; test -f scripts/verify-python-retired.sh &amp;&amp; bash scripts/verify-python-retired.sh &amp;&amp; pnpm build 2>&amp;1 | tail -5 &amp;&amp; test -f dist/pagefind/pagefind.js</automated>
  </verify>
  <acceptance_criteria>
    - package.json scripts.postbuild equals `pnpm exec pagefind --site dist` (or equivalent — does NOT call copy-legacy-archives.sh)
    - scripts/copy-legacy-archives.sh deleted
    - scripts/sync-nav.py + scripts/sync-footer.py deleted
    - scripts/parse-aaro.py + scripts/extract-evidence.py deleted (Plan 04-17 prerequisite; double-check)
    - scripts/verify-python-retired.sh exists, executable, exits 0
    - All scripts/build-*.py for archive slugs (aaro, brazil, canada, chile, geipan, italy, nara, nasa, nz, peru, spain, uk, uruguay, argentina, wargov) deleted; orphaned cross-cutting build-*.py also deleted
    - .github/workflows/nav-sync.yml + footer-sync.yml deleted (if existed)
    - .github/workflows/html-validate.yml deleted if duplicated by quality-gates.yml; else preserved with rationale
    - pnpm build exits 0; dist/pagefind/ still emits (Pagefind unchanged behavior under slimmer postbuild)
    - scripts/spider.py STILL EXISTS (Phase 5 SCRP scope per RESEARCH §10)
    - scripts/dl-*.sh STILL EXIST (D-11)
  </acceptance_criteria>
  <done>Postbuild reduced to Pagefind only; cross-cutting Python build scripts retired; CI drift gates removed; verify-python-retired.sh enforces invariant; spider.py + dl scripts preserved for Phase 5.</done>
</task>

<task type="auto">
  <name>Task 2: Flip Lighthouse SOFT → HARD in .lighthouserc.cf.json + run full quality-gates.yml against final preview + Phase 4 sign-off</name>
  <files>.lighthouserc.cf.json</files>
  <read_first>
    - .lighthouserc.cf.json (current state — Phase 2 02-08 set to `warn`)
    - .github/workflows/quality-gates.yml (full 7-job matrix + Phase 4 NEW: lightbox + search + sw + r2-urls + pagination specs)
    - .planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md §9 Lighthouse HARD-flip checklist
    - .planning/phases/02-foundation/02-08-PLAN.md (Phase 4 close toggle reference if exists)
    - .planning/ROADMAP.md (Phase 4 success criteria — D-27 budgets LCP ≤ 2500ms, transfer ≤ 500KB)
  </read_first>
  <behavior>
    - .lighthouserc.cf.json assertions flip from `warn` to `error` on all three:
      - "categories:performance": ["error", { "minScore": 0.85 }]
      - "lcp-element": ["error", { "maxNumericValue": 2500 }]
      - "resource-summary:total:size": ["error", { "maxNumericValue": 512000 }]
    - Full quality-gates.yml matrix run against final preview-url (post 04-20-Task-1 build):
      - preflight job
      - visual-regression (15 archives × 4 viewports = 60 baselines)
      - fidelity (175 samples after Phase 4: original 105 + ~70 new from per-archive ports)
      - tone-colours (15/15 green — including 4 D-10 bug fixes: brazil/chile/uk/geipan)
      - js-off (15/15 server-rendered)
      - lighthouse (NOW HARD — fails on regression)
      - redirects (URL contract preserved)
      - lightbox.spec.ts (Plan 04-01)
      - search.spec.ts (Plan 04-19)
      - sw.spec.ts (Plan 04-03)
      - r2-urls.spec.ts (Plan 04-02)
      - pagination.spec.ts (Plan 04-04)
    - All jobs MUST pass green before Phase 4 close. Any HARD failure (LCP > 2500 ms, transfer > 500 KB, etc.) blocks Phase 4 close — file a follow-up plan instead of merging.
    - 04-20-SUMMARY documents: 23 requirement IDs satisfied per the table in `<requirements>` section below.
  </behavior>
  <action>
    Read .lighthouserc.cf.json. Locate the three assertion lines for categories:performance + lcp-element + resource-summary:total:size. Change the first array element from "warn" to "error" on each.

    Run pnpm build && bash scripts/verify-python-retired.sh; confirm both exit 0.

    Trigger quality-gates.yml against final preview (operator-driven if workflow_dispatch or push). Observe all jobs report green:
    - visual-regression: 15 archives × 4 viewports green
    - fidelity: full sample list green
    - tone-colours: 15/15 green
    - js-off: 15/15 green
    - lighthouse: HARD assertions all green (LCP < 2500 ms, transfer < 500 KB, performance score ≥ 0.85)
    - redirects: green
    - Phase 4 NEW specs: lightbox + search + sw + r2-urls + pagination all green

    If any HARD assertion fails (LCP regression, transfer bloat, etc.): DO NOT merge. Document the failure in 04-20-SUMMARY and file a follow-up gap-closure plan (per `/gsd:plan-phase --gaps` workflow). Phase 4 stays open until all HARD assertions pass.

    On green: 04-20-SUMMARY documents Phase 4 sign-off per requirement-ID table.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; node -e "const r=require('./.lighthouserc.cf.json'); const a=r.ci?.assert?.assertions||r.assertions||{}; const checks=['categories:performance','lcp-element','resource-summary:total:size']; for(const k of checks){const v=a[k]||a[k.replace(':','-')]; if(!Array.isArray(v)||v[0]!=='error'){console.error('FAIL:',k,'is',v?v[0]:'missing'); process.exit(1);}} console.log('all lighthouse assertions HARD')" &amp;&amp; pnpm build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .lighthouserc.cf.json: categories:performance assertion level = "error"
    - .lighthouserc.cf.json: lcp-element assertion level = "error"
    - .lighthouserc.cf.json: resource-summary:total:size assertion level = "error"
    - Maximum LCP value still 2500 ms per D-40 + ROADMAP Phase 4 SC#2
    - Maximum transfer-size still 512000 bytes (~500 KB)
    - Performance score minScore still 0.85
    - pnpm build still exits 0
    - bash scripts/verify-python-retired.sh exits 0
    - quality-gates.yml against final preview ALL JOBS green (operator-confirmed in SUMMARY)
    - 04-20-SUMMARY records full requirement-ID coverage table: SSG-06..12, SRC-01..05, SW-01..07, PERF-01..04 each mapped to landing plan + verification status
  </acceptance_criteria>
  <done>Phase 4 closed: Lighthouse HARD-enforced; full quality-gates green against final preview; 23 requirement IDs all satisfied; Phase 4 SUMMARY signs off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI HARD assertion enforcement | .lighthouserc.cf.json `error` level blocks merges on regression |
| build artefact integrity | dist/pagefind/ + dist/<slug>/index.html × 15 must remain consistent across deploys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-66 | Repudiation | Performance regression slips through SOFT gate after Phase 4 close | mitigate | HARD-flip on LCP + transfer + perf-score; future regressions block merge |
| T-04-67 | Tampering | Unmonitored Python script revival after Phase 5 (SCRP) reuses spider.py | accept | spider.py reserved for Phase 5 by RESEARCH §10; verify-python-retired.sh whitelists it explicitly |
| T-04-68 | Denial of Service | CI drift gates removed → silent nav/footer regression | mitigate | Nav.astro + Footer.astro are sole source; any drift surfaces as failed visual-regression OR fidelity gate against frozen samples |
</threat_model>

<verification>
1. .lighthouserc.cf.json: all 3 assertions flipped to "error".
2. scripts/verify-python-retired.sh: exits 0 (no orphaned build/sync/parse scripts).
3. package.json scripts.postbuild equals `pnpm exec pagefind --site dist` (no copy-legacy-archives.sh).
4. dist/pagefind/ still emits after slimmer postbuild.
5. Full quality-gates.yml against final preview: ALL JOBS green (15/15 archives, 15/15 tone-colours, 15/15 js-off, Lighthouse HARD assertions all pass).
6. 04-20-SUMMARY documents: 23 requirement IDs from Phase 4 each mapped to the plan that landed it + verification status.
7. .github/workflows/nav-sync.yml + footer-sync.yml (if existed) deleted.
8. scripts/spider.py + scripts/dl-*.sh preserved (Phase 5 SCRP).
</verification>

<success_criteria>
- [ ] .lighthouserc.cf.json: categories:performance, lcp-element, resource-summary:total:size all `error` (HARD)
- [ ] scripts/copy-legacy-archives.sh + sync-nav.py + sync-footer.py deleted
- [ ] All scripts/build-<slug>.py archive scripts deleted (verified by scripts/verify-python-retired.sh)
- [ ] scripts/build-details.py + cross-cutting unused build-*.py scripts deleted if orphaned
- [ ] scripts/spider.py + scripts/dl-*.sh preserved (Phase 5 + D-11)
- [ ] package.json scripts.postbuild reduced to Pagefind only
- [ ] CI drift workflows nav-sync.yml + footer-sync.yml removed; html-validate.yml removed if duplicate
- [ ] Full quality-gates.yml suite green against final preview (visual + fidelity + tone-colours 15/15 + js-off 15/15 + lighthouse HARD + redirects + lightbox + search + sw + r2-urls + pagination)
- [ ] PERF-04 satisfied: NZ/Uruguay/Peru lighthouse baselines preserved; no regression vs lighthouse-baselines/<slug>.json
- [ ] 04-20-SUMMARY records: 23 requirement IDs satisfied across Plans 04-01..04-20
- [ ] Phase 4 sign-off recorded; STATE.md updated by execute-phase
</success_criteria>

<output>
Create `.planning/phases/04-full-migration-search-offline-performance/04-20-SUMMARY.md` when done. SUMMARY MUST include a requirement-coverage table mapping all 23 Phase 4 requirement IDs (SSG-06..12, SRC-01..05, SW-01..07, PERF-01..04) → landing plan ID + green-verification status.
</output>
</content>
</invoke>
---
phase: 04-full-migration-search-offline-performance
plan: "04"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/pages/index.astro
  - tests/pagination.spec.ts
autonomous: true
requirements: [SSG-09]
tags: [pagination, wargov, query-param, client-side-windowing, retroactive-repaging]

must_haves:
  truths:
    - "Visiting `/` shows exactly 20 wargov cards (was 50 in Phase 3)"
    - "Visiting `/?page=2` shows wargov cards 21..40; URL bar shows `?page=2`"
    - "Footer is reachable on every page without scrolling through hundreds of cards"
    - "Clicking a pagination link updates `history.pushState` and re-renders without full nav"
    - "Browser back/forward (popstate) restores previous `?page=N` correctly"
    - "Visiting `/#card-<id>` (no page param) auto-resolves to the page containing that anchor and scrolls to it"
    - "Lightbox prev/next navigates across page boundaries (lbList contains ALL cards, not just visible)"
    - "All 222 cards remain in initial HTML + shard JSON (server-side render unchanged per D-32)"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "IntersectionObserver replaced with ?page=N client-side windowing handler; adds <nav class='pagination' id='wargov-pagination'>"
      contains: "URLSearchParams"
    - path: "tests/pagination.spec.ts"
      provides: "Playwright spec: ?page=N renders correct window, footer reachable, popstate works, cross-page anchor resolves, lightbox crosses pages"
      min_lines: 80
  key_links:
    - from: "src/pages/index.astro pagination script"
      to: "src/scripts/invariants.ts openAt (Plan 04-01)"
      via: "lbList walks ALL .arch-card regardless of display:none (Pitfall #6)"
      pattern: "querySelectorAll\\('.arch-card'\\)"
---

<objective>
Replace the Phase 3 IntersectionObserver lazy-load on the wargov home page (`src/pages/index.astro` lines 302-351) with the `?page=N` client-side pagination handler per 04-RESEARCH.md §6 + D-27..D-33. PAGE_SIZE drops from 50 (Phase 3 wargov) to 20 (Phase 4 D-27). Footer becomes reachable on every page.

The IntersectionObserver currently materialises ALL 222 cards into the grid as the user scrolls; without pagination control, the user must scroll through all 222 cards to reach the footer. New behaviour: all cards still get materialised upfront via `Promise.all` over shards (D-33), but `display:none` toggles hide cards outside the current 20-card window. Lightbox prev/next still sees all cards (Pitfall #6 — walk querySelectorAll regardless of display).

Per 04-RESEARCH.md §6: Astro paginate() is path-based only; query-string pagination requires client-side over server-rendered shards.

This plan establishes the pagination contract that all 14 archive ports (Wave 3+) inherit. Depends on Plan 04-01 lightbox-fix because the pagination handler must coexist with `__lbList` reading `dataset.url` / `dataset.local` not href.

Output: index.astro IO-block replaced with pagination handler + nav markup + pagination spec.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-migration-search-offline-performance/04-CONTEXT.md
@.planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md
@.planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md
@.planning/phases/03-ssg-foundation/03-CONTEXT.md
@CLAUDE.md

<interfaces>
Current state of src/pages/index.astro (post Plan 04-01 patches):

Lines 272-280: server-rendered card grid (Astro map over rows.slice(0,50))
Lines 282-301: script id="wargov-shards" type="application/json" manifest of remaining shards
Lines 302-351: IntersectionObserver block — REPLACED in this plan
Lines 353-450: lightbox-related handlers (preserved verbatim from Plan 04-01)
Lines 451-475: refreshLbList — Plan 04-01 already updated to read dataset.url/dataset.local; THIS plan updates it again to walk ALL cards (querySelectorAll('.arch-card') not getCards()) per Pitfall #6

D-32: shards stay at 50 cards/shard server-side render unit (NO change to scripts/normalize-csv.py — only client-side display logic changes).
D-33: IntersectionObserver REMOVED; replaced by fetch-all-shards-on-load + display:none windowing.
URL contract (D-29): paths unchanged (still `/`); query string out of URL-CONTRACT.txt scope.
D-10 LOCKED: shard cards are pre-rendered HTML strings; runtime ONLY calls insertAdjacentHTML.

Pagination nav markup (NEW — inside <section class="archive">):
  <nav class="pagination" id="wargov-pagination" data-pagefind-ignore aria-label="Pagination"></nav>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace IntersectionObserver block with ?page=N pagination handler + nav markup</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (lines 272-475 — grid markup, shards manifest, IO block to delete, refreshLbList)
    - .planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md §6 (80-line skeleton + hash handling + Pitfall #6)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §6 (index.astro repaging patch summary)
    - .planning/phases/04-full-migration-search-offline-performance/04-CONTEXT.md D-27..D-33
    - src/scripts/invariants.ts (post-04-01 state — openAt accepts data-row-id; navLb iterates over lbList)
  </read_first>
  <behavior>
    - On page load, JS reads `URLSearchParams.get('page') || '1'`, clamps to [1, totalPages].
    - All shards fetched in parallel via Promise.all over manifest. Cards inserted via grid.insertAdjacentHTML('beforeend', card.html) per D-10 LOCKED.
    - After all cards materialised, cards.forEach toggles display:none to show only the 20-card window.
    - Pagination nav renders prev / page-numbers / next links with ?page=N hrefs; clicking calls preventDefault + history.pushState + renderPage; popstate listener re-renders.
    - refreshLbList walks grid.querySelectorAll('.arch-card') (ALL cards including display:none) per Pitfall #6.
    - Hash handling: if location.hash on load and target card hidden, JS finds card index, computes targetPage, renders that page, scrollIntoView.
    - window.scrollTo top on page change (except hash navigation).
  </behavior>
  <action>
    In src/pages/index.astro:
    - Add pagination nav markup `<nav class="pagination" id="wargov-pagination" data-pagefind-ignore aria-label="Pagination"></nav>` inside `<section class="archive">` AFTER the `<div class="arch-grid" id="wargov-grid">` block.
    - Locate and DELETE the existing IntersectionObserver block (search anchors: `new IntersectionObserver`, lines roughly 302-351). The shards manifest `<script id="wargov-shards">` is PRESERVED.
    - Insert the new `<script is:inline>` IIFE per 04-RESEARCH.md §6 skeleton. Required contents:
      - Constants: PAGE_SIZE = 20 (D-27); grid = getElementById('wargov-grid'); manifestEl = getElementById('wargov-shards'); paginationNav = getElementById('wargov-pagination'). Early return if any null.
      - Parse manifest JSON via try/catch; on parse failure return without crashing.
      - allCardsLoaded = Promise.all(manifest.map(s => fetch('/' + s.file).then(r => r.json()).catch(() => ({ cards: [] })))).
      - readPage(): returns Math.max(1, parseInt(new URLSearchParams(location.search).get('page')||'1',10) || 1).
      - async renderPage(pageNum): if !grid.dataset.fullyLoaded, await allCardsLoaded, iterate shards and call grid.insertAdjacentHTML('beforeend', card.html) for each (D-10 LOCKED), set grid.dataset.fullyLoaded='1'; collect cards = Array.from(grid.querySelectorAll('.arch-card')); totalPages = Math.ceil(cards.length/PAGE_SIZE); clamp pageNum = Math.min(Math.max(1,pageNum), totalPages); start = (pageNum-1)*PAGE_SIZE; end = start+PAGE_SIZE; cards.forEach((c,i) => c.style.display = (i>=start && i<end) ? '' : 'none'); call renderPaginationNav(pageNum, totalPages); window.scrollTo({top:0, behavior:'instant'}).
      - renderPaginationNav(current, total): builds innerHTML with prev anchor (when current>1), page-buttons 1..total (current uses span.pg-current aria-current="page"; others are anchors with href ?page=N), next anchor (when current<total).
      - Click delegate on document, scoped to closest('#wargov-pagination a'): preventDefault, parse anchor.href, history.pushState({}, '', url), renderPage(parseInt(url.searchParams.get('page')||'1',10)).
      - popstate listener: renderPage(readPage()).
      - Hash handling per RESEARCH §6 inside renderPage end: if location.hash and !document.querySelector(location.hash+':not([style*="none"])'), find target via document.querySelector(location.hash), compute idx = cards.indexOf(target), targetPage = Math.floor(idx/PAGE_SIZE)+1, if targetPage !== pageNum re-render then target.scrollIntoView({block:'start'}).
      - Initial call: renderPage(readPage()).
    - Update refreshLbList (from Plan 04-01) per Pitfall #6: change `getCards()` to `grid.querySelectorAll('.arch-card')` so lbList walks ALL cards including display:none. Preserve dataset reads (openA.dataset.url, openA.dataset.local, c.dataset.rowId).
    Reference 04-RESEARCH.md §6 for the full 80-line skeleton.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; pnpm exec astro check 2>&amp;1 | tail -10 &amp;&amp; grep -q "wargov-pagination" src/pages/index.astro &amp;&amp; grep -qE "PAGE_SIZE *= *20" src/pages/index.astro &amp;&amp; grep -q "URLSearchParams" src/pages/index.astro &amp;&amp; grep -q "popstate" src/pages/index.astro &amp;&amp; (! grep -q "new IntersectionObserver" src/pages/index.astro) &amp;&amp; pnpm build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'wargov-pagination' src/pages/index.astro returns >= 1
    - grep -qE 'PAGE_SIZE *= *20' src/pages/index.astro matches
    - grep -c 'URLSearchParams' src/pages/index.astro returns >= 1
    - grep -c 'popstate' src/pages/index.astro returns >= 1
    - grep -c 'new IntersectionObserver' src/pages/index.astro returns 0 (IO block deleted)
    - grep -c "querySelectorAll\\('.arch-card'\\)" src/pages/index.astro returns >= 1 (refreshLbList Pitfall #6 fix)
    - pnpm exec astro check exits 0
    - pnpm build exits 0
    - dist/index.html still contains 50 server-rendered .arch-card elements (server-side render unchanged per D-32)
  </acceptance_criteria>
  <done>IntersectionObserver removed; ?page=N pagination handler in place; refreshLbList walks ALL cards; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests/pagination.spec.ts covering ?page=N, popstate, footer, hash, lightbox-cross-page</name>
  <files>tests/pagination.spec.ts</files>
  <read_first>
    - tests/playwright.config.ts (PREVIEW_URL env, baseURL)
    - tests/visual-regression.spec.ts (Playwright structure)
    - .planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md §6 (behavior spec) + Pitfall #6 (lightbox cross-page)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §14 + No Analog Found table
    - tests/lightbox.spec.ts (Plan 04-01 — pagination test 6 currently skipped pending this plan; un-skip path)
  </read_first>
  <behavior>
    - Spec runs against PREVIEW_URL env var.
    - Tests cover: (1) `/` shows exactly 20 visible cards; (2) `?page=2` shows cards 21..40; (3) footer is in viewport without scrolling 1500px; (4) clicking next link updates URL via pushState; (5) browser back restores prior page; (6) `/#card-<id>` resolves to correct page; (7) lightbox open → arrow right at end of visible page advances across page boundary (lbList sees hidden cards).
  </behavior>
  <action>
    Create tests/pagination.spec.ts modelled on tests/visual-regression.spec.ts. test.describe('Pagination'). Seven tests:
    - "page 1 shows exactly 20 visible cards": goto '/'; await grid loaded (wait for grid.dataset.fullyLoaded); count `.arch-card:visible` (Playwright pseudo-state or filter where style display != none) and expect 20.
    - "?page=2 shows cards 21..40": goto '/?page=2'; await loaded; assert the first visible card's data-row-id is 'r021' (or assert visible card count 20 and first visible matches row 21).
    - "footer reachable without scrolling past ~1500 px": goto '/?page=1'; await; evaluate document.querySelector('footer').getBoundingClientRect().top; expect < 2000 (in mobile viewport 375x667).
    - "pagination link click updates URL via pushState (no full reload)": goto '/'; listen for navigationrequest; click pagination anchor `?page=2`; expect URL bar shows ?page=2; expect NO navigation event (pushState only); expect first visible card now r021.
    - "popstate restores previous page": goto '/'; click `?page=3` anchor; await; click browser-back via page.goBack(); expect URL shows '/' (no page param OR ?page=1); expect first visible card r001.
    - "hash link resolves to correct page": goto '/#card-r050' (assuming r050 exists on page 3 given PAGE_SIZE=20); await render; assert URL still contains the hash; assert card r050 is visible (display !== none); assert ?page=3 visible in URL (pushState'd by hash handler).
    - "lightbox arrow advances across page boundary": goto '/?page=1'; click open on r020 (last on page 1); await lightbox open; press ArrowRight; expect lightbox title matches r021 title (lbList includes hidden cards per Pitfall #6); press Escape.
    Do NOT inline fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; pnpm exec playwright test tests/pagination.spec.ts --list 2>&amp;1 | tail -15 &amp;&amp; grep -c "test('" tests/pagination.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/pagination.spec.ts exists
    - pnpm exec playwright test tests/pagination.spec.ts --list returns >= 7 test cases
    - grep -c "test('" tests/pagination.spec.ts returns >= 7
    - File references substrings: URLSearchParams or '?page=2', popstate or goBack, pushState, getBoundingClientRect, card-r0
    - Against a built preview, all 7 tests pass
  </acceptance_criteria>
  <done>tests/pagination.spec.ts ships with 7 tests; covers URL, popstate, footer, hash, lightbox-cross-page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→DOM | Pagination handler reads URL search params; mutates display style on cards |
| client→shard JSON | fetch(/data/wargov-shard-N.json) returns pre-rendered HTML strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Tampering | Untrusted page param parsing | mitigate | parseInt + Math.max(1, ...) + Math.min(totalPages, ...) clamp prevents out-of-bounds; non-numeric input falls back to 1 |
| T-04-16 | Tampering | Untrusted JSON shard data | mitigate | Shards emitted by build-time scripts/normalize-csv.py; runtime ONLY calls insertAdjacentHTML on card.html (D-10 LOCKED — pre-rendered, html.escaped at build per Plan 03-03) |
| T-04-17 | Spoofing | Hash injection via location.hash | accept | location.hash is read-only used as querySelector(hash) and indexOf lookup; no eval, no innerHTML; standard browser-controlled string |
| T-04-18 | Denial of Service | Promise.all(shards) blocks first paint on slow networks | mitigate | First 50 cards are server-rendered (display:none on cards 21-50 still works because element exists); Promise.all only adds 51-222 which are off-screen on page 1; SW (Plan 04-03) SWR-caches shards on subsequent visits |
</threat_model>

<verification>
1. pnpm build exits 0; dist/index.html contains nav#wargov-pagination element and PAGE_SIZE constant.
2. Manual smoke: visit /, /?page=2, /?page=3 — each shows 20 cards; URL bar updates; footer reachable.
3. Manual smoke: `/#card-r050` resolves to ?page=3 and scrolls to that card.
4. pnpm exec playwright test tests/pagination.spec.ts passes (7 tests).
5. pnpm exec playwright test tests/lightbox.spec.ts — un-skip the test 6 placeholder for pagination smoke; assert it passes.
6. Visual regression (against recaptured wargov baselines from Plan 03-06): pixel diff under threshold; page-1 view shows the new pagination nav at bottom of grid.
</verification>

<success_criteria>
- [ ] PAGE_SIZE=20 windowing replaces Phase 3 IntersectionObserver lazy-load (D-27, D-33)
- [ ] URL contract uses ?page=N query string (D-28, D-29 — paths unchanged, URL-CONTRACT.txt unaffected)
- [ ] Footer reachable on every page (D-31)
- [ ] Browser back/forward, hash links, and lightbox prev/next all work cross-page (Pitfall #6)
- [ ] Server-side render unit unchanged (50 cards in initial HTML per D-32)
- [ ] tests/pagination.spec.ts ships with ≥ 7 tests
- [ ] tests/lightbox.spec.ts test 6 (pagination smoke) un-skipped and passing
- [ ] Visual regression baselines for wargov still match (or are recaptured operator-consciously per D-17 if pagination nav visibly differs)
</success_criteria>

<output>
Create `.planning/phases/04-full-migration-search-offline-performance/04-04-SUMMARY.md` when done.
</output>

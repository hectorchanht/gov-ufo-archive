---
phase: 04-full-migration-search-offline-performance
plan: "06"
type: execute
wave: 4
depends_on: ["04-05"]
files_modified:
  - src/pages/uruguay/index.astro
  - src/styles/uruguay.css
  - scripts/normalize-uruguay.py
  - data/uruguay.json
  - scripts/copy-legacy-archives.sh
  - scripts/sync-nav.py
  - scripts/sync-footer.py
  - tests/fidelity-samples.json
  - tests/visual-baselines/uruguay-360.png
  - tests/visual-baselines/uruguay-768.png
  - tests/visual-baselines/uruguay-1024.png
  - tests/visual-baselines/uruguay-1440.png
  - uruguay/index.html
  - scripts/build-uruguay.py
autonomous: true
requirements: [SSG-06, SSG-08, SSG-09, SSG-10, SSG-11, SSG-12, PERF-04]
tags: [archive-port, uruguay, cridovni, small-static]

must_haves:
  truths:
    - "Visiting /uruguay/ renders Uruguay CRIDOVNI archive from Astro"
    - "Per-archive tone --caution #3ba0d8 applied"
    - "Page weighs ≤ 500 KB; JS-off render shows cards; lightbox + hamburger work"
    - "scripts/build-uruguay.py deleted; uruguay slug dropped from postbuild + sync scripts"
    - "Card URLs route through assets.realufo.org"
  artifacts:
    - path: "src/pages/uruguay/index.astro"
      contains: "archiveSlug=\"uruguay\""
    - path: "scripts/normalize-uruguay.py"
      min_lines: 50
    - path: "data/uruguay.json"
      contains: "schemaVersion"
  key_links:
    - from: "src/pages/uruguay/index.astro"
      to: "data/uruguay.json"
      via: "getEntry('uruguay','v1')"
      pattern: "getEntry\\('uruguay'"
---

<objective>
Port the Uruguay CRIDOVNI archive (D-08 Wave 1 small/static tier) to Astro using the CatalogCard.astro template established in Plan 04-05. Per-archive Python retirement: delete scripts/build-uruguay.py, drop slug from copy-legacy-archives.sh and sync-nav/footer.py.

Uses the same per-archive port pattern established in Plan 04-05 — only the slug, tone-colour (#3ba0d8 per CLAUDE.md §3.1), and per-archive content differ.

Output: Uruguay archive on Astro + normalize-uruguay.py + data/uruguay.json + baselines + fidelity samples.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-migration-search-offline-performance/04-CONTEXT.md
@.planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md
@.planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md
@.planning/phases/04-full-migration-search-offline-performance/04-05-PLAN.md
@CLAUDE.md

<interfaces>
Identical interface set to Plan 04-05 (NZ template). Differences:
- archiveSlug = "uruguay"
- tone --caution = #3ba0d8 (CLAUDE.md §3.1)
- License footer = "Ley nº 18.381" (Uruguay public-domain attribution per CLAUDE.md §9)
- Source data: uruguay/.cache or uruguay/index.html
- Output: data/uruguay.json
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write scripts/normalize-uruguay.py + emit data/uruguay.json + delete scripts/build-uruguay.py</name>
  <files>scripts/normalize-uruguay.py, data/uruguay.json, public/data/uruguay.json, scripts/build-uruguay.py</files>
  <read_first>
    - scripts/normalize-nz.py (Plan 04-05 — template)
    - scripts/build-uruguay.py (current Python build — source path discovery)
    - uruguay/ directory contents (find source — uruguay/.cache/*.json OR uruguay/index.html embedded JSON)
    - scripts/_archive_common.py (Plan 04-02 — rewrite_to_r2 + slugify)
    - src/content.config.ts (catalogAssetSchema.strict)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §2 + Pattern B
    - CLAUDE.md §11
  </read_first>
  <behavior>
    - Mirrors scripts/normalize-nz.py structure: stdlib-only, deterministic JSON write, source-untouchable guard, public/data mirror.
    - Emits catalogEnvelope at data/uruguay.json with slug="uruguay", asset keys exactly { t, ti, de, ag, cat, date, region, l, u, s, th }.
    - rewrite_to_r2 applied to every PDF/video URL with archive_slug='uruguay'.
    - scripts/build-uruguay.py deleted via git rm.
    - git diff --quiet -- uruguay/ returns 0 after normalize run.
  </behavior>
  <action>
    Adapt scripts/normalize-nz.py (Plan 04-05 Task 2) to Uruguay:
    - Copy normalize-nz.py to scripts/normalize-uruguay.py.
    - Update header docstring source path reference.
    - Change OUT_PRIMARY to REPO/'data'/'uruguay.json'.
    - Update _assert_source_unchanged path arg to 'uruguay/' (or 'uruguay/.cache/').
    - Update slug literal in _build_envelope to 'uruguay'.
    - Update parse_source() to read Uruguay-specific source (likely the embedded `<script id="arch-data">` JSON in uruguay/index.html or uruguay/.cache/parsed.json — discover via reading uruguay/).
    - Update to_catalog_asset(rec) to map Uruguay record shape into catalogAssetSchema. Uruguay may use short keys (ti/de/ag) already per .planning/codebase/CONVENTIONS.md §"Manifest record keys" — pass through with rewrite_to_r2 on local PDF paths.
    - Run python3 scripts/normalize-uruguay.py to emit data/uruguay.json + public/data/uruguay.json.
    - git rm scripts/build-uruguay.py.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; python3 scripts/normalize-uruguay.py 2>&amp;1 | tail -10 &amp;&amp; test -f data/uruguay.json &amp;&amp; node -e "const j=require('./data/uruguay.json'); if(j.v1.slug!=='uruguay') process.exit(1); console.log('total='+j.v1.stats.total)" &amp;&amp; (test ! -f scripts/build-uruguay.py || (echo 'build-uruguay.py exists'; exit 1)) &amp;&amp; git diff --quiet -- uruguay/.cache/ 2>/dev/null; echo source_diff=$?</automated>
  </verify>
  <acceptance_criteria>
    - data/uruguay.json + public/data/uruguay.json valid; slug='uruguay'; stats.total ≥ 1
    - Each asset keys match catalogAssetSchema.strict() (t, ti, de, ag, cat, date, region, l, u, s, th)
    - asset.u for type 'PDF' starts with 'https://assets.realufo.org/pdfs/uruguay/' (when local file exists)
    - scripts/build-uruguay.py removed
    - git diff --quiet -- uruguay/.cache/ returns 0 (or no diff)
    - pnpm build exits 0
  </acceptance_criteria>
  <done>normalize-uruguay.py ships valid catalog data; build-uruguay.py removed; Zod schema accepts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/pages/uruguay/index.astro + uruguay.css + drop slug from postbuild + recapture baselines + fidelity</name>
  <files>src/pages/uruguay/index.astro, src/styles/uruguay.css, scripts/copy-legacy-archives.sh, scripts/sync-nav.py, scripts/sync-footer.py, uruguay/index.html, tests/fidelity-samples.json, tests/visual-baselines/uruguay-360.png, tests/visual-baselines/uruguay-768.png, tests/visual-baselines/uruguay-1024.png, tests/visual-baselines/uruguay-1440.png</files>
  <read_first>
    - src/pages/nz/index.astro (Plan 04-05 — template to clone with slug swap)
    - src/styles/nz.css (template)
    - scripts/copy-legacy-archives.sh (drop 'uruguay' from slug array)
    - tests/fidelity-samples.json (add 5 uruguay samples)
    - uruguay/index.html (extract hero-lede + license-footer for fidelity samples — verbatim per CLAUDE.md §9)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §1 + Pattern A
    - CLAUDE.md §3.1 (uruguay tone #3ba0d8) + §9 (Uruguay Ley 18.381 attribution)
  </read_first>
  <behavior>
    - src/pages/uruguay/index.astro renders Uruguay archive from getEntry('uruguay','v1') via RootLayout + CatalogCard + Lightbox + pagination handler.
    - archiveSlug='uruguay' drives --caution: #3ba0d8 from RootLayout TONE map.
    - Footer carries verbatim Uruguay public-domain attribution "Ley nº 18.381".
    - Postbuild copy + sync scripts drop 'uruguay' from their lists.
    - Legacy uruguay/index.html deleted.
    - Visual baselines (4 viewports) recaptured per D-17 operator-conscious procedure.
    - 5 fidelity samples added for uruguay (hero-lede, license-footer, 2 card titles, 1 agency).
  </behavior>
  <action>
    Clone src/pages/nz/index.astro to src/pages/uruguay/index.astro:
    - Update all 'nz' references to 'uruguay' (archiveSlug, getEntry argument, grid id, pagination id, CSS import path).
    - Update title/description from CLAUDE.md §2 ("Uruguay CRIDOVNI") + uruguay/index.html hero lede verbatim.
    - Update HeroCarousel inclusion to OFF (Uruguay small archive — no carousel).

    Clone src/styles/nz.css to src/styles/uruguay.css — keep minimal.

    Modify scripts/copy-legacy-archives.sh: remove 'uruguay' from ARCHIVES iteration list.

    Modify scripts/sync-nav.py + sync-footer.py: drop 'uruguay' from policed lists if present.

    git rm uruguay/index.html.

    Recapture visual baselines: python3 scripts/capture-baselines.py --archive uruguay --base-url <preview-url> (after pnpm build + preview).

    Add 5 uruguay fidelity samples to tests/fidelity-samples.json: hero-lede selector + text, footer license selector + "Ley nº 18.381" text, 2 card-title strings, 1 agency.

    pnpm build to confirm dist/uruguay/index.html emits.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; test -f src/pages/uruguay/index.astro &amp;&amp; grep -q "archiveSlug=\"uruguay\"" src/pages/uruguay/index.astro &amp;&amp; (! grep -E '\\buruguay\\b' scripts/copy-legacy-archives.sh) &amp;&amp; (test ! -f uruguay/index.html) &amp;&amp; pnpm build 2>&amp;1 | tail -5 &amp;&amp; test -f dist/uruguay/index.html</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/uruguay/index.astro renders via getEntry('uruguay','v1')
    - scripts/copy-legacy-archives.sh no longer iterates 'uruguay'
    - uruguay/index.html deleted (git rm'd)
    - dist/uruguay/index.html exists post-build with rendered cards
    - tests/visual-baselines/uruguay-{360,768,1024,1440}.png exist
    - tests/fidelity-samples.json contains ≥ 5 entries with slug:"uruguay"
    - tone-colours.spec.ts --grep uruguay reports green
    - License footer contains "Ley nº 18.381" string verbatim (CLAUDE.md §9)
  </acceptance_criteria>
  <done>Uruguay archive on Astro; --caution #3ba0d8; license verbatim; Python retired; baselines recaptured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time→data/uruguay.json | normalize-uruguay.py emits schema-validated JSON consumed by Astro |
| client→/uruguay/ HTML | Browser renders Astro-emitted page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-23 | Tampering | Uruguay scraper output mutation | mitigate | source-untouchable guard via git diff --quiet -- uruguay/; Zod schema.strict() at build |
| T-04-24 | Tampering | XSS via injected text fields | mitigate | Astro `{expr}` auto-escape on CatalogCard; no set:html |
| T-04-25 | Repudiation | Loss of Uruguay license-footer verbatim | mitigate | 5 fidelity samples (verify-fidelity.py gate) including "Ley nº 18.381" exact string |
</threat_model>

<verification>
1. python3 scripts/normalize-uruguay.py exits 0; data/uruguay.json valid; source untouched.
2. pnpm build → dist/uruguay/index.html with rendered cards.
3. pnpm exec playwright test tests/visual-regression.spec.ts --grep uruguay passes (recaptured baselines).
4. pnpm exec playwright test tests/tone-colours.spec.ts --grep uruguay passes (#3ba0d8).
5. pnpm exec playwright test tests/js-off.spec.ts --grep uruguay passes.
6. python3 scripts/verify-fidelity.py --grep uruguay passes (5 samples byte-equivalent).
7. scripts/build-uruguay.py + uruguay/index.html absent.
8. scripts/copy-legacy-archives.sh no longer iterates 'uruguay'.
</verification>

<success_criteria>
- [ ] Uruguay archive renders from Astro at /uruguay/
- [ ] --caution #3ba0d8 applied via RootLayout TONE map
- [ ] License footer carries "Ley nº 18.381" verbatim
- [ ] scripts/build-uruguay.py + uruguay/index.html deleted
- [ ] 'uruguay' dropped from copy-legacy-archives.sh + sync-nav.py + sync-footer.py
- [ ] Visual + fidelity + tone-colour + js-off + Lighthouse soft gates green for uruguay
- [ ] R2 URLs in card.url fields (no bundles/ paths)
</success_criteria>

<output>
Create `.planning/phases/04-full-migration-search-offline-performance/04-06-SUMMARY.md` when done.
</output>

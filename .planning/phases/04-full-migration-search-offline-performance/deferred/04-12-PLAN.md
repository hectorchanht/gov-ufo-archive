---
phase: 04-full-migration-search-offline-performance
plan: "12"
type: execute
wave: 5
depends_on: ["04-05"]
files_modified:
  - src/pages/chile/index.astro
  - src/styles/chile.css
  - scripts/normalize-chile.py
  - data/chile.json
  - public/data/chile.json
  - data/chile-shard-1.json
  - scripts/copy-legacy-archives.sh
  - scripts/sync-nav.py
  - scripts/sync-footer.py
  - tests/fidelity-samples.json
  - tests/visual-baselines/chile-360.png
  - tests/visual-baselines/chile-768.png
  - tests/visual-baselines/chile-1024.png
  - tests/visual-baselines/chile-1440.png
  - chile/index.html
  - scripts/build-chile.py
autonomous: true
requirements: [SSG-06, SSG-08, SSG-09, SSG-10, SSG-11, SSG-12, PERF-04]
tags: [archive-port, chile, sefaa, medium-catalog, tone-colour-fix]

must_haves:
  truths:
    - "Visiting /chile/ renders Chile CEFAA / SEFAA archive from Astro"
    - "Per-archive tone --caution #d52b1e applied via RootLayout TONE map (D-10 tone-colour bug fix — legacy chile HTML had drift; retiring legacy resolves)"
    - "Page ≤ 500 KB; JS-off shows cards; lightbox + hamburger preserved per CLAUDE.md §7"
    - "Sharding if asset count > 50 (Plan 03-03 D-10 LOCKED)"
    - "scripts/build-chile.py deleted; 'chile' dropped from postbuild + sync scripts"
    - "Card URLs flow through assets.realufo.org"
    - "License footer carries 'Ley nº 20.285' verbatim (Chile Acceso a la Información Pública)"
  artifacts:
    - path: "src/pages/chile/index.astro"
      contains: "archiveSlug=\"chile\""
    - path: "scripts/normalize-chile.py"
      min_lines: 60
    - path: "data/chile.json"
      contains: "schemaVersion"
  key_links:
    - from: "src/pages/chile/index.astro"
      to: "data/chile.json"
      via: "getEntry('chile','v1')"
      pattern: "getEntry\\('chile'"
---

<objective>
Port the Chile CEFAA / SEFAA (DGAC) archive (D-08 Wave 2 medium catalog tier) to Astro using the CatalogCard.astro template from Plan 04-05. **Tone-colour bug fix bundled per D-10:** legacy chile/index.html had drift on `--caution`; deleting legacy + serving via RootLayout TONE map (#d52b1e) resolves. Per-archive Python retirement (D-09): delete scripts/build-chile.py + drop slug from copy-legacy-archives.sh + sync-nav.py + sync-footer.py.

Chile has `.cache/` scraped data (chile/.cache/) + chile/el-bosque.html long-form (preserved out of Phase 4 scope) + chile/pdfs/ directory.

Output: Chile archive on Astro + normalize-chile.py + data/chile.json + shards if > 50 + tone-colour fix via legacy retirement + baselines + 5 fidelity samples.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-migration-search-offline-performance/04-CONTEXT.md
@.planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md
@.planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md
@.planning/phases/04-full-migration-search-offline-performance/04-05-PLAN.md
@.planning/phases/04-full-migration-search-offline-performance/04-06-PLAN.md
@.planning/phases/04-full-migration-search-offline-performance/04-11-PLAN.md
@CLAUDE.md

<interfaces>
Identical interface set to Plan 04-11 (Brazil — medium-catalog template). Differences:
- archiveSlug = "chile"
- tone --caution = #d52b1e (CLAUDE.md §3.1 — Chilean red); RootLayout TONE map already correct
- License footer = "Ley nº 20.285" (Chile)
- Source: chile/.cache/*.json (scraped)
- Output: data/chile.json + data/chile-shard-N.json if > 50
- chile/el-bosque.html preserved (long-form)
- chile/index.html size 100 KB suggests larger asset count → sharding likely triggers
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write scripts/normalize-chile.py + emit data/chile.json (+ shards if > 50) + delete scripts/build-chile.py</name>
  <files>scripts/normalize-chile.py, data/chile.json, public/data/chile.json, data/chile-shard-1.json, scripts/build-chile.py</files>
  <read_first>
    - scripts/normalize-brazil.py (Plan 04-11 — closest medium-catalog template with sharding)
    - scripts/normalize-uruguay.py (Plan 04-06 — base small-archive template)
    - scripts/normalize-csv.py (sharding logic)
    - scripts/build-chile.py (current Python — extract record shape)
    - chile/.cache/ contents (scraped JSON files)
    - scripts/_archive_common.py
    - src/content.config.ts (catalogAssetSchema.strict + shards field)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §2 + Pattern B + Pattern G
    - CLAUDE.md §11
  </read_first>
  <behavior>
    - Mirrors Plan 04-11 Brazil normalizer with sharding.
    - Stdlib-only; deterministic JSON; source-untouchable guard via git diff --quiet -- chile/.cache/.
    - Emits catalogEnvelope at data/chile.json with slug='chile'.
    - Each asset matches catalogAssetSchema.strict() (t, ti, de, ag, cat, date, region, l, u, s, th).
    - rewrite_to_r2 with archive_slug='chile'.
    - SHARD_SIZE = 50; if total > 50, shards data/chile-shard-N.json + envelope.shards manifest.
    - scripts/build-chile.py deleted via git rm.
  </behavior>
  <action>
    Clone scripts/normalize-brazil.py → scripts/normalize-chile.py. Replace 'brazil' with 'chile' in OUT_PRIMARY, slug field, rewrite_to_r2 arg, _assert_source_unchanged path.

    Inspect chile/.cache/ contents to discover record shape; update parse_source() + to_catalog_asset(rec) for Chile fields → catalogAssetSchema.

    Run python3 scripts/normalize-chile.py; confirm outputs.

    git rm scripts/build-chile.py.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; python3 scripts/normalize-chile.py 2>&amp;1 | tail -10 &amp;&amp; test -f data/chile.json &amp;&amp; node -e "const j=require('./data/chile.json'); if(j.v1.slug!=='chile') process.exit(1); console.log('total='+j.v1.stats.total+' shards='+(j.v1.shards?.length||0))" &amp;&amp; (test ! -f scripts/build-chile.py) &amp;&amp; git diff --quiet -- chile/.cache/; echo source_diff=$? &amp;&amp; pnpm build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - data/chile.json + public/data/chile.json valid; slug='chile'; stats.total ≥ 1
    - Each asset matches catalogAssetSchema.strict()
    - asset.u for t='PDF' starts with 'https://assets.realufo.org/pdfs/chile/'
    - If total > 50: data/chile-shard-N.json files exist; envelope.shards enumerates
    - scripts/build-chile.py removed
    - git diff --quiet -- chile/.cache/ returns 0
    - pnpm build exits 0
  </acceptance_criteria>
  <done>normalize-chile.py emits valid catalogEnvelope; build-chile.py removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/pages/chile/index.astro + chile.css + drop slug from postbuild + recapture baselines + fidelity + tone-colour validation</name>
  <files>src/pages/chile/index.astro, src/styles/chile.css, scripts/copy-legacy-archives.sh, scripts/sync-nav.py, scripts/sync-footer.py, chile/index.html, tests/fidelity-samples.json, tests/visual-baselines/chile-360.png, tests/visual-baselines/chile-768.png, tests/visual-baselines/chile-1024.png, tests/visual-baselines/chile-1440.png</files>
  <read_first>
    - src/pages/brazil/index.astro (Plan 04-11 — clone target; medium-catalog with sharding)
    - src/styles/brazil.css (template)
    - scripts/copy-legacy-archives.sh (drop 'chile')
    - chile/index.html (extract hero-lede + license-footer for fidelity samples)
    - tests/fidelity-samples.json (add 5 chile samples)
    - tests/tone-colours-fixture.json (verify chile entry = "#d52b1e"; if buggy legacy value, update)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §1 + Pattern A
    - CLAUDE.md §3.1 (chile tone #d52b1e) + §9 (Chile "Ley nº 20.285" attribution)
  </read_first>
  <behavior>
    - src/pages/chile/index.astro renders from getEntry('chile','v1') via RootLayout + CatalogCard + Lightbox + pagination handler.
    - archiveSlug='chile' drives --caution #d52b1e from RootLayout TONE map.
    - HeroCarousel: OFF.
    - Footer carries "Ley nº 20.285" verbatim.
    - 'chile' dropped from postbuild + sync scripts.
    - chile/index.html deleted; chile/el-bosque.html PRESERVED.
    - If sharded: include shards manifest script + pagination handler adapted for chile-grid + chile-pagination ids.
    - Visual baselines (4 viewports) recaptured per D-17.
    - 5 fidelity samples added.
    - Tone-colour fixture validates chile = "#d52b1e".
  </behavior>
  <action>
    Clone src/pages/brazil/index.astro → src/pages/chile/index.astro:
    - Replace 'brazil' with 'chile' in archiveSlug, getEntry, grid id, pagination id, CSS path, shards-manifest id.
    - Update title/description from CLAUDE.md §2 ("Chile CEFAA / SEFAA") + chile/index.html hero lede verbatim.

    Clone src/styles/brazil.css → src/styles/chile.css.

    Modify scripts/copy-legacy-archives.sh: remove 'chile' from ARCHIVES iteration.

    Modify scripts/sync-nav.py + scripts/sync-footer.py: drop 'chile' if policed.

    git rm chile/index.html (preserve chile/el-bosque.html).

    **Tone-colour fixture validation:** Read tests/tone-colours-fixture.json; verify chile = "#d52b1e". If buggy legacy value present, update to "#d52b1e" per CLAUDE.md §3.1 (D-10).

    Recapture visual baselines: python3 scripts/capture-baselines.py --archive chile --base-url <preview-url>.

    Add 5 fidelity samples: hero-lede, license-footer "Ley nº 20.285", 2 card titles, 1 agency.

    pnpm build → confirm dist/chile/index.html.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; test -f src/pages/chile/index.astro &amp;&amp; grep -q "archiveSlug=\"chile\"" src/pages/chile/index.astro &amp;&amp; (! grep -v '^#' scripts/copy-legacy-archives.sh | grep -E '\bchile\b') &amp;&amp; (test ! -f chile/index.html) &amp;&amp; pnpm build 2>&amp;1 | tail -5 &amp;&amp; test -f dist/chile/index.html &amp;&amp; grep -q "arch-card" dist/chile/index.html</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/chile/index.astro renders via getEntry('chile','v1')
    - scripts/copy-legacy-archives.sh no longer iterates 'chile'
    - chile/index.html deleted; chile/el-bosque.html preserved
    - dist/chile/index.html exists with rendered cards
    - tests/visual-baselines/chile-{360,768,1024,1440}.png exist
    - tests/fidelity-samples.json contains ≥ 5 entries with slug:"chile"
    - License footer in dist/chile/index.html contains "Ley nº 20.285" verbatim
    - tone-colours.spec.ts --grep chile reports green (--caution #d52b1e — D-10 bug fixed)
    - If sharded: shards manifest in dist/chile/index.html
  </acceptance_criteria>
  <done>Chile archive on Astro; --caution #d52b1e (tone bug fixed); license verbatim; Python retired; baselines recaptured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time→data/chile.json | normalize-chile.py emits schema-validated JSON + shards |
| client→/chile/ HTML | Browser renders Astro page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-39 | Tampering | Chile scraper output mutation | mitigate | source-untouchable guard via git diff --quiet -- chile/.cache/; Zod strict() |
| T-04-40 | Tampering | XSS via injected text | mitigate | Astro `{expr}` auto-escape |
| T-04-41 | Repudiation | Loss of Chile license OR tone-colour drift | mitigate | 5 fidelity samples; tone-colours fixture locks #d52b1e |
</threat_model>

<verification>
1. python3 scripts/normalize-chile.py exits 0; data/chile.json valid; source untouched.
2. pnpm build → dist/chile/index.html with rendered cards; shards if > 50.
3. pnpm exec playwright test tests/visual-regression.spec.ts --grep chile passes.
4. pnpm exec playwright test tests/tone-colours.spec.ts --grep chile passes (#d52b1e — D-10 bug fix).
5. pnpm exec playwright test tests/js-off.spec.ts --grep chile passes.
6. python3 scripts/verify-fidelity.py --grep chile passes.
7. scripts/build-chile.py + chile/index.html absent.
8. scripts/copy-legacy-archives.sh no longer iterates 'chile'.
</verification>

<success_criteria>
- [ ] Chile archive renders from Astro at /chile/
- [ ] --caution #d52b1e applied (D-10 tone-colour bug resolved)
- [ ] License footer "Ley nº 20.285" verbatim
- [ ] Shards emitted if asset count > 50
- [ ] scripts/build-chile.py + chile/index.html deleted
- [ ] 'chile' dropped from copy-legacy-archives.sh + sync-nav.py + sync-footer.py
- [ ] Visual + fidelity + tone-colour + js-off + Lighthouse soft gates green
- [ ] R2 URLs in card.url fields
</success_criteria>

<output>
Create `.planning/phases/04-full-migration-search-offline-performance/04-12-SUMMARY.md` when done.
</output>
</content>
</invoke>
---
phase: 04-full-migration-search-offline-performance
plan: "11"
type: execute
wave: 5
depends_on: ["04-05"]
files_modified:
  - src/pages/brazil/index.astro
  - src/styles/brazil.css
  - scripts/normalize-brazil.py
  - data/brazil.json
  - public/data/brazil.json
  - data/brazil-shard-1.json
  - scripts/copy-legacy-archives.sh
  - scripts/sync-nav.py
  - scripts/sync-footer.py
  - tests/fidelity-samples.json
  - tests/visual-baselines/brazil-360.png
  - tests/visual-baselines/brazil-768.png
  - tests/visual-baselines/brazil-1024.png
  - tests/visual-baselines/brazil-1440.png
  - brazil/index.html
  - scripts/build-brazil.py
autonomous: true
requirements: [SSG-06, SSG-08, SSG-09, SSG-10, SSG-11, SSG-12, PERF-04]
tags: [archive-port, brazil, fab, medium-catalog, tone-colour-fix]

must_haves:
  truths:
    - "Visiting /brazil/ renders Brazil FAB (Operação Prato) archive from Astro"
    - "Per-archive tone --caution #009c3b applied via RootLayout TONE map (D-10 tone-colour bug fix — legacy HTML had wrong value; retiring legacy resolves)"
    - "Page ≤ 500 KB; JS-off shows cards; lightbox + hamburger preserved per CLAUDE.md §7"
    - "Sharding if asset count > 50 (Plan 03-03 D-10 LOCKED pattern; 50-asset shard unit)"
    - "scripts/build-brazil.py deleted; 'brazil' dropped from postbuild + sync scripts"
    - "Card URLs flow through assets.realufo.org"
    - "License footer carries 'Lei nº 12.527/2011' verbatim (Brazil LAI)"
  artifacts:
    - path: "src/pages/brazil/index.astro"
      contains: "archiveSlug=\"brazil\""
    - path: "scripts/normalize-brazil.py"
      min_lines: 60
    - path: "data/brazil.json"
      contains: "schemaVersion"
  key_links:
    - from: "src/pages/brazil/index.astro"
      to: "data/brazil.json"
      via: "getEntry('brazil','v1')"
      pattern: "getEntry\\('brazil'"
---

<objective>
Port the Brazil FAB / Operação Prato archive (D-08 Wave 2 medium catalog tier) to Astro using the CatalogCard.astro template from Plan 04-05. **Tone-colour bug fix bundled per D-10:** the legacy brazil/index.html had drift on `--caution`; deleting the legacy HTML + serving via RootLayout TONE map (#009c3b) resolves automatically. Per-archive Python retirement (D-09): delete scripts/build-brazil.py + drop slug from copy-legacy-archives.sh + sync-nav.py + sync-footer.py.

Brazil has `.cache/` directory (scraped data) — normalizer reads from there. Operação Prato long-form page (brazil/operacao-prato.html) preserved out of Phase 4 scope.

Output: Brazil archive on Astro + normalize-brazil.py + data/brazil.json + shards if needed + tone-colour fix via legacy retirement + baselines + 5 fidelity samples.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-migration-search-offline-performance/04-CONTEXT.md
@.planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md
@.planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md
@.planning/phases/04-full-migration-search-offline-performance/04-05-PLAN.md
@.planning/phases/04-full-migration-search-offline-performance/04-06-PLAN.md
@CLAUDE.md

<interfaces>
Identical interface set to Plan 04-06 (Uruguay template). Differences:
- archiveSlug = "brazil"
- tone --caution = #009c3b (CLAUDE.md §3.1) — RootLayout TONE map already correct per Phase 3 audit; legacy HTML drift resolved by retirement
- License footer = "Lei nº 12.527/2011" (Brazil LAI)
- Source: brazil/.cache/*.json (scraped data)
- Output: data/brazil.json + optional data/brazil-shard-N.json (if count > 50)
- brazil/operacao-prato.html preserved (long-form; out of Phase 4 scope)
- Card count likely 30-100 (catalogue-shape; sharding triggers if > 50 per Plan 03-03 D-10)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write scripts/normalize-brazil.py + emit data/brazil.json (+ shards if > 50) + delete scripts/build-brazil.py</name>
  <files>scripts/normalize-brazil.py, data/brazil.json, public/data/brazil.json, data/brazil-shard-1.json, scripts/build-brazil.py</files>
  <read_first>
    - scripts/normalize-uruguay.py (Plan 04-06 — base template)
    - scripts/normalize-csv.py (Plan 03-03 + 04-04 patches — SHARDING logic; clone for medium catalogs > 50 assets)
    - scripts/build-brazil.py (current Python build — extract source path + record shape)
    - brazil/.cache/ contents (scraped JSON files — discover record shape)
    - scripts/_archive_common.py (rewrite_to_r2 + slugify)
    - src/content.config.ts (catalogAssetSchema.strict + catalogEnvelopeSchema shards field)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §2 + Pattern B + Pattern G
    - CLAUDE.md §11
  </read_first>
  <behavior>
    - Stdlib-only Python 3; deterministic JSON; source-untouchable guard via git diff --quiet -- brazil/.cache/.
    - Emits catalogEnvelope at data/brazil.json with slug='brazil'.
    - Each asset matches catalogAssetSchema.strict() (t, ti, de, ag, cat, date, region, l, u, s, th).
    - rewrite_to_r2 with archive_slug='brazil'.
    - SHARDING: if len(assets) > 50, emit shards data/brazil-shard-N.json (50 per shard) + brazil.json envelope contains rows[0..49] inline + shards[] manifest. Mirrors Plan 03-03 wargov pattern (D-10 LOCKED).
    - Server-rendered HTML strings per shard NOT needed here (catalog archives use CatalogCard.astro which renders client-rendered from JSON — Plan 04-05 established this contract); shards contain raw asset objects only, NOT pre-rendered HTML.
    - scripts/build-brazil.py deleted via git rm.
  </behavior>
  <action>
    Clone scripts/normalize-uruguay.py → scripts/normalize-brazil.py. Replace 'uruguay' with 'brazil' in OUT_PRIMARY, slug field, rewrite_to_r2 arg, _assert_source_unchanged path (point at brazil/.cache/).

    Inspect brazil/.cache/ contents to discover record shape. Update parse_source() to read scraped JSON files. Update to_catalog_asset(rec) for Brazil record keys → catalogAssetSchema (t, ti, de, ag, cat, date, region, l, u, s, th).

    Add sharding logic from scripts/normalize-csv.py:
    - SHARD_SIZE = 50
    - If len(assets) > SHARD_SIZE: split into shards; envelope.assets keeps first SHARD_SIZE; subsequent shards written to data/brazil-shard-N.json with { "shard": N, "assets": [...] }; envelope gains shards: [ {file: "data/brazil-shard-1.json", count: 50}, ... ] manifest.
    - If len(assets) ≤ SHARD_SIZE: all assets inline in envelope; no shards emitted.

    Run python3 scripts/normalize-brazil.py; confirm data/brazil.json + public/data/brazil.json. If shards emitted, confirm data/brazil-shard-N.json files exist and parse.

    git rm scripts/build-brazil.py.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; python3 scripts/normalize-brazil.py 2>&amp;1 | tail -10 &amp;&amp; test -f data/brazil.json &amp;&amp; test -f public/data/brazil.json &amp;&amp; node -e "const j=require('./data/brazil.json'); if(j.v1.slug!=='brazil') process.exit(1); console.log('total='+j.v1.stats.total+' shards='+(j.v1.shards?.length||0))" &amp;&amp; (test ! -f scripts/build-brazil.py || (echo 'build-brazil.py exists'; exit 1)) &amp;&amp; git diff --quiet -- brazil/.cache/ ; echo source_diff=$? &amp;&amp; pnpm build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - data/brazil.json + public/data/brazil.json valid; slug='brazil'; stats.total ≥ 1
    - Each asset matches catalogAssetSchema.strict()
    - asset.u for t='PDF' starts with 'https://assets.realufo.org/pdfs/brazil/'
    - If total > 50: data/brazil-shard-1.json exists; envelope.shards manifest enumerates them
    - scripts/build-brazil.py removed
    - git diff --quiet -- brazil/.cache/ returns 0
    - pnpm build exits 0 (Zod validation passes)
  </acceptance_criteria>
  <done>normalize-brazil.py emits valid catalogEnvelope (sharded if > 50); build-brazil.py removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/pages/brazil/index.astro + brazil.css + drop slug from postbuild + recapture baselines + fidelity + tone-colour validation</name>
  <files>src/pages/brazil/index.astro, src/styles/brazil.css, scripts/copy-legacy-archives.sh, scripts/sync-nav.py, scripts/sync-footer.py, brazil/index.html, tests/fidelity-samples.json, tests/visual-baselines/brazil-360.png, tests/visual-baselines/brazil-768.png, tests/visual-baselines/brazil-1024.png, tests/visual-baselines/brazil-1440.png</files>
  <read_first>
    - src/pages/uruguay/index.astro (Plan 04-06 — clone target)
    - src/pages/index.astro (Plan 04-04 — sharded pagination handler if Brazil has > 50 assets)
    - src/styles/uruguay.css (template)
    - scripts/copy-legacy-archives.sh (drop 'brazil')
    - brazil/index.html (extract hero-lede + license-footer for fidelity samples)
    - tests/fidelity-samples.json (add 5 brazil samples)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §1 + Pattern A
    - CLAUDE.md §3.1 (brazil tone #009c3b — verify RootLayout TONE map line for 'brazil' matches) + §9 (Brazil "Lei nº 12.527/2011" LAI)
  </read_first>
  <behavior>
    - src/pages/brazil/index.astro renders from getEntry('brazil','v1') via RootLayout + CatalogCard + Lightbox + pagination handler.
    - archiveSlug='brazil' drives --caution #009c3b (Brazilian green) from RootLayout TONE map.
    - HeroCarousel: OFF (Brazil medium catalog; no carousel content available).
    - Footer carries "Lei nº 12.527/2011" verbatim.
    - 'brazil' dropped from postbuild + sync scripts.
    - brazil/index.html deleted; brazil/operacao-prato.html PRESERVED.
    - If sharded (> 50 assets): include shards manifest script + Plan 04-04 pagination handler adapted for brazil-grid + brazil-pagination ids.
    - Visual baselines (4 viewports) recaptured per D-17.
    - 5 fidelity samples added.
    - **Tone-colour validation:** tests/tone-colours-fixture.json should already have brazil:#009c3b; if it points at the buggy legacy value, update fixture to canonical hex per CLAUDE.md §3.1.
  </behavior>
  <action>
    Clone src/pages/uruguay/index.astro → src/pages/brazil/index.astro:
    - Replace 'uruguay' with 'brazil' in archiveSlug, getEntry, grid id, pagination id, CSS path.
    - Update title/description from CLAUDE.md §2 ("Brazil FAB / Operação Prato") + brazil/index.html hero lede verbatim.
    - HeroCarousel OFF.
    - If Brazil normalizer emitted shards (Task 1 envelope.shards present), include shard manifest `<script id="brazil-shards">` + pagination handler from Plan 04-04 (adapted ids).

    Clone src/styles/uruguay.css → src/styles/brazil.css.

    Modify scripts/copy-legacy-archives.sh: remove 'brazil' from ARCHIVES iteration.

    Modify scripts/sync-nav.py + scripts/sync-footer.py: drop 'brazil' if policed.

    git rm brazil/index.html (preserve brazil/operacao-prato.html).

    **Tone-colour fixture validation:** Read tests/tone-colours-fixture.json (if exists) and verify brazil entry equals "#009c3b". If fixture file uses the buggy legacy value, update to "#009c3b" per CLAUDE.md §3.1 (D-10 LOCKED).

    Recapture visual baselines: python3 scripts/capture-baselines.py --archive brazil --base-url <preview-url>.

    Add 5 fidelity samples: hero-lede, license-footer "Lei nº 12.527/2011", 2 card titles, 1 agency.

    pnpm build → confirm dist/brazil/index.html.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; test -f src/pages/brazil/index.astro &amp;&amp; grep -q "archiveSlug=\"brazil\"" src/pages/brazil/index.astro &amp;&amp; (! grep -v '^#' scripts/copy-legacy-archives.sh | grep -E '\bbrazil\b') &amp;&amp; (test ! -f brazil/index.html) &amp;&amp; pnpm build 2>&amp;1 | tail -5 &amp;&amp; test -f dist/brazil/index.html &amp;&amp; grep -q "arch-card" dist/brazil/index.html</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/brazil/index.astro renders via getEntry('brazil','v1')
    - scripts/copy-legacy-archives.sh no longer iterates 'brazil'
    - brazil/index.html deleted; brazil/operacao-prato.html preserved
    - dist/brazil/index.html exists with rendered cards
    - tests/visual-baselines/brazil-{360,768,1024,1440}.png exist
    - tests/fidelity-samples.json contains ≥ 5 entries with slug:"brazil"
    - License footer in dist/brazil/index.html contains "Lei nº 12.527/2011" verbatim
    - tone-colours.spec.ts --grep brazil reports green (--caution computes to #009c3b — tone-colour bug fixed by legacy retirement)
    - If sharded: dist/brazil/index.html contains `<script id="brazil-shards">` with manifest; pagination handler wired for brazil-grid id
  </acceptance_criteria>
  <done>Brazil archive on Astro; --caution #009c3b (tone bug fixed); license verbatim; Python retired; baselines recaptured; sharded if > 50.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time→data/brazil.json | normalize-brazil.py emits schema-validated JSON + shards |
| client→/brazil/ HTML | Browser renders Astro page; shards fetched lazily |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-35 | Tampering | Brazil scraper output mutation | mitigate | source-untouchable guard via git diff --quiet -- brazil/.cache/; Zod strict() |
| T-04-36 | Tampering | XSS via injected text | mitigate | Astro `{expr}` auto-escape on CatalogCard |
| T-04-37 | Repudiation | Loss of Brazil license-footer verbatim OR tone-colour drift | mitigate | 5 fidelity samples; tone-colours fixture validation locks #009c3b |
| T-04-38 | Information Disclosure | Shard JSON exposes asset metadata | accept | All assets already public per CLAUDE.md §9 (verbatim from official source) |
</threat_model>

<verification>
1. python3 scripts/normalize-brazil.py exits 0; data/brazil.json valid; source untouched.
2. pnpm build → dist/brazil/index.html with rendered cards; shards in dist/data/ if > 50.
3. pnpm exec playwright test tests/visual-regression.spec.ts --grep brazil passes.
4. pnpm exec playwright test tests/tone-colours.spec.ts --grep brazil passes (#009c3b — bug fix confirmed).
5. pnpm exec playwright test tests/js-off.spec.ts --grep brazil passes (first 50 cards inline).
6. python3 scripts/verify-fidelity.py --grep brazil passes.
7. scripts/build-brazil.py + brazil/index.html absent.
8. scripts/copy-legacy-archives.sh no longer iterates 'brazil'.
</verification>

<success_criteria>
- [ ] Brazil archive renders from Astro at /brazil/
- [ ] --caution #009c3b applied (D-10 tone-colour bug resolved by legacy retirement)
- [ ] License footer "Lei nº 12.527/2011" verbatim
- [ ] Shards emitted if asset count > 50
- [ ] scripts/build-brazil.py + brazil/index.html deleted
- [ ] 'brazil' dropped from copy-legacy-archives.sh + sync-nav.py + sync-footer.py
- [ ] Visual + fidelity + tone-colour + js-off + Lighthouse soft gates green
- [ ] R2 URLs in card.url fields
</success_criteria>

<output>
Create `.planning/phases/04-full-migration-search-offline-performance/04-11-SUMMARY.md` when done.
</output>
</content>
</invoke>
---
phase: 04-full-migration-search-offline-performance
plan: "16"
type: execute
wave: 5
depends_on: ["04-05"]
files_modified:
  - src/pages/nasa/index.astro
  - src/styles/nasa.css
  - scripts/normalize-nasa.py
  - data/nasa.json
  - public/data/nasa.json
  - scripts/copy-legacy-archives.sh
  - scripts/sync-nav.py
  - scripts/sync-footer.py
  - tests/fidelity-samples.json
  - tests/visual-baselines/nasa-360.png
  - tests/visual-baselines/nasa-768.png
  - tests/visual-baselines/nasa-1024.png
  - tests/visual-baselines/nasa-1440.png
  - nasa/index.html
  - scripts/build-nasa.py
autonomous: true
requirements: [SSG-06, SSG-08, SSG-09, SSG-10, SSG-11, SSG-12, PERF-04]
tags: [archive-port, nasa, uap-independent-study, medium-catalog]

must_haves:
  truths:
    - "Visiting /nasa/ renders NASA UAP Independent Study Team archive from Astro"
    - "Per-archive tone --caution #fc3d21 applied via RootLayout TONE map (NASA red)"
    - "Page ≤ 500 KB; JS-off shows cards; lightbox + hamburger preserved per CLAUDE.md §7"
    - "scripts/build-nasa.py deleted; 'nasa' dropped from postbuild + sync scripts"
    - "Card URLs flow through assets.realufo.org"
    - "License footer carries '17 U.S.C. § 105' verbatim (USA public-domain)"
  artifacts:
    - path: "src/pages/nasa/index.astro"
      contains: "archiveSlug=\"nasa\""
    - path: "scripts/normalize-nasa.py"
      min_lines: 50
    - path: "data/nasa.json"
      contains: "schemaVersion"
  key_links:
    - from: "src/pages/nasa/index.astro"
      to: "data/nasa.json"
      via: "getEntry('nasa','v1')"
      pattern: "getEntry\\('nasa'"
---

<objective>
Port the NASA UAP Independent Study Team archive (D-08 Wave 2 medium catalog tier) to Astro using the CatalogCard.astro template from Plan 04-05. Per-archive Python retirement (D-09): delete scripts/build-nasa.py + drop slug from copy-legacy-archives.sh + sync-nav.py + sync-footer.py.

NASA archive has small hand-curated content (per RESEARCH §8 table — "small static list") + nasa/.cache + nasa/pdfs + nasa/story.html (preserved). Likely no sharding needed.

Output: NASA archive on Astro + normalize-nasa.py + data/nasa.json + baselines + 5 fidelity samples.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-migration-search-offline-performance/04-CONTEXT.md
@.planning/phases/04-full-migration-search-offline-performance/04-RESEARCH.md
@.planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md
@.planning/phases/04-full-migration-search-offline-performance/04-05-PLAN.md
@.planning/phases/04-full-migration-search-offline-performance/04-06-PLAN.md
@.planning/phases/04-full-migration-search-offline-performance/04-11-PLAN.md
@CLAUDE.md

<interfaces>
Identical interface set to Plan 04-06 (Uruguay small template — likely no shards) or Plan 04-11 (Brazil medium template — if sharding triggers). Differences:
- archiveSlug = "nasa"
- tone --caution = #fc3d21 (CLAUDE.md §3.1 — NASA red)
- License footer = "17 U.S.C. § 105"
- Source: nasa/.cache/*.json OR small hand-curated list extracted from nasa/index.html arch-data
- Output: data/nasa.json (sharding optional if total > 50)
- nasa/story.html preserved (long-form)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write scripts/normalize-nasa.py + emit data/nasa.json + delete scripts/build-nasa.py</name>
  <files>scripts/normalize-nasa.py, data/nasa.json, public/data/nasa.json, scripts/build-nasa.py</files>
  <read_first>
    - scripts/normalize-uruguay.py (Plan 04-06 — base template; NASA small per RESEARCH §8 table)
    - scripts/normalize-brazil.py (Plan 04-11 — sharding logic if total > 50)
    - scripts/build-nasa.py (current Python — extract NASA record shape)
    - nasa/.cache/ contents + nasa/index.html embedded arch-data
    - scripts/_archive_common.py
    - src/content.config.ts (catalogAssetSchema.strict)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §2 + Pattern B
  </read_first>
  <behavior>
    - Stdlib-only Python 3; deterministic JSON; source-untouchable guard via git diff --quiet -- nasa/.cache/ (or nasa/index.html if source).
    - Emits catalogEnvelope at data/nasa.json with slug='nasa'.
    - Each asset matches catalogAssetSchema.strict() (t, ti, de, ag, cat, date, region, l, u, s, th).
    - rewrite_to_r2 with archive_slug='nasa'.
    - Sharding optional: if total > 50, shard; else inline all assets in envelope.
    - scripts/build-nasa.py deleted via git rm.
  </behavior>
  <action>
    Decide base template by inspecting NASA source size:
    - If asset count likely ≤ 50: clone scripts/normalize-uruguay.py → scripts/normalize-nasa.py (small template).
    - If > 50: clone scripts/normalize-brazil.py instead (sharded template).

    Replace 'uruguay' (or 'brazil') with 'nasa' in OUT_PRIMARY, slug field, rewrite_to_r2 arg, _assert_source_unchanged path.

    Inspect nasa/.cache/ contents (or nasa/index.html embedded arch-data JSON). Update parse_source() + to_catalog_asset(rec) for NASA record shape → catalogAssetSchema.

    Run python3 scripts/normalize-nasa.py; confirm outputs.

    git rm scripts/build-nasa.py.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; python3 scripts/normalize-nasa.py 2>&amp;1 | tail -10 &amp;&amp; test -f data/nasa.json &amp;&amp; node -e "const j=require('./data/nasa.json'); if(j.v1.slug!=='nasa') process.exit(1); console.log('total='+j.v1.stats.total)" &amp;&amp; (test ! -f scripts/build-nasa.py) &amp;&amp; pnpm build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - data/nasa.json + public/data/nasa.json valid; slug='nasa'; stats.total ≥ 1
    - Each asset matches catalogAssetSchema.strict()
    - asset.u for t='PDF' starts with 'https://assets.realufo.org/pdfs/nasa/'
    - scripts/build-nasa.py removed
    - pnpm build exits 0
  </acceptance_criteria>
  <done>normalize-nasa.py emits valid catalogEnvelope; build-nasa.py removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/pages/nasa/index.astro + nasa.css + drop slug from postbuild + recapture baselines + fidelity</name>
  <files>src/pages/nasa/index.astro, src/styles/nasa.css, scripts/copy-legacy-archives.sh, scripts/sync-nav.py, scripts/sync-footer.py, nasa/index.html, tests/fidelity-samples.json, tests/visual-baselines/nasa-360.png, tests/visual-baselines/nasa-768.png, tests/visual-baselines/nasa-1024.png, tests/visual-baselines/nasa-1440.png</files>
  <read_first>
    - src/pages/uruguay/index.astro (Plan 04-06 — if NASA small) OR src/pages/brazil/index.astro (Plan 04-11 — if NASA sharded)
    - src/styles/uruguay.css OR src/styles/brazil.css (template)
    - scripts/copy-legacy-archives.sh (drop 'nasa')
    - nasa/index.html (extract hero-lede + license-footer for fidelity samples)
    - tests/fidelity-samples.json (add 5 nasa samples)
    - .planning/phases/04-full-migration-search-offline-performance/04-PATTERNS.md §1 + Pattern A
    - CLAUDE.md §3.1 (nasa tone #fc3d21) + §9 (USA "17 U.S.C. § 105")
  </read_first>
  <behavior>
    - src/pages/nasa/index.astro renders from getEntry('nasa','v1') via RootLayout + CatalogCard + Lightbox + pagination handler.
    - archiveSlug='nasa' drives --caution #fc3d21.
    - HeroCarousel: ON if NASA UAP Independent Study supplies ≥ 4 representative photos per RESEARCH §8 A2; otherwise OFF. Default OFF — operator can re-enable in follow-up if photos exist.
    - Footer carries "17 U.S.C. § 105" verbatim.
    - 'nasa' dropped from postbuild + sync scripts.
    - nasa/index.html deleted; nasa/story.html PRESERVED.
    - Visual baselines recaptured per D-17.
    - 5 fidelity samples added.
  </behavior>
  <action>
    Choose clone target based on Task 1 normalizer choice (small → uruguay; sharded → brazil). Clone → src/pages/nasa/index.astro:
    - Replace 'uruguay' (or 'brazil') with 'nasa' in archiveSlug, getEntry, grid id, pagination id, CSS path, shards-manifest id if applicable.
    - Update title/description from CLAUDE.md §2 ("NASA UAP Independent Study Team") + nasa/index.html hero lede verbatim.
    - HeroCarousel OFF by default (revisit if NASA UAP report photos available).

    Clone corresponding CSS template → src/styles/nasa.css.

    Modify scripts/copy-legacy-archives.sh: remove 'nasa' from ARCHIVES iteration.

    Modify scripts/sync-nav.py + scripts/sync-footer.py: drop 'nasa' if policed.

    git rm nasa/index.html (preserve nasa/story.html).

    Recapture visual baselines: python3 scripts/capture-baselines.py --archive nasa --base-url <preview-url>.

    Add 5 fidelity samples: hero-lede, license-footer "17 U.S.C. § 105", 2 card titles, 1 agency.

    pnpm build → confirm dist/nasa/index.html.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/war-gov-ufo-release &amp;&amp; test -f src/pages/nasa/index.astro &amp;&amp; grep -q "archiveSlug=\"nasa\"" src/pages/nasa/index.astro &amp;&amp; (! grep -v '^#' scripts/copy-legacy-archives.sh | grep -E '\bnasa\b') &amp;&amp; (test ! -f nasa/index.html) &amp;&amp; pnpm build 2>&amp;1 | tail -5 &amp;&amp; test -f dist/nasa/index.html &amp;&amp; grep -q "arch-card" dist/nasa/index.html</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/nasa/index.astro renders via getEntry('nasa','v1')
    - scripts/copy-legacy-archives.sh no longer iterates 'nasa'
    - nasa/index.html deleted; nasa/story.html preserved
    - dist/nasa/index.html exists with rendered cards
    - tests/visual-baselines/nasa-{360,768,1024,1440}.png exist
    - tests/fidelity-samples.json contains ≥ 5 entries with slug:"nasa"
    - License footer in dist/nasa/index.html contains "17 U.S.C. § 105" verbatim
    - tone-colours.spec.ts --grep nasa reports green (#fc3d21)
  </acceptance_criteria>
  <done>NASA archive on Astro; --caution #fc3d21; license verbatim; Python retired; baselines recaptured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time→data/nasa.json | normalize-nasa.py emits schema-validated JSON |
| client→/nasa/ HTML | Browser renders Astro page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-51 | Tampering | NASA source mutation | mitigate | source-untouchable guard; Zod strict() |
| T-04-52 | Tampering | XSS via injected text | mitigate | Astro `{expr}` auto-escape |
| T-04-53 | Repudiation | Loss of "17 U.S.C. § 105" attribution | mitigate | 5 fidelity samples include license-footer verbatim |
</threat_model>

<verification>
1. python3 scripts/normalize-nasa.py exits 0; data/nasa.json valid; source untouched.
2. pnpm build → dist/nasa/index.html with rendered cards.
3. pnpm exec playwright test tests/visual-regression.spec.ts --grep nasa passes.
4. pnpm exec playwright test tests/tone-colours.spec.ts --grep nasa passes (#fc3d21).
5. pnpm exec playwright test tests/js-off.spec.ts --grep nasa passes.
6. python3 scripts/verify-fidelity.py --grep nasa passes.
7. scripts/build-nasa.py + nasa/index.html absent.
8. scripts/copy-legacy-archives.sh no longer iterates 'nasa'.
</verification>

<success_criteria>
- [ ] NASA archive renders from Astro at /nasa/
- [ ] --caution #fc3d21 applied
- [ ] License footer "17 U.S.C. § 105" verbatim
- [ ] scripts/build-nasa.py + nasa/index.html deleted
- [ ] 'nasa' dropped from copy-legacy-archives.sh + sync-nav.py + sync-footer.py
- [ ] Visual + fidelity + tone-colour + js-off + Lighthouse soft gates green
- [ ] R2 URLs in card.url fields
</success_criteria>

<output>
Create `.planning/phases/04-full-migration-search-offline-performance/04-16-SUMMARY.md` when done.
</output>
</content>
</invoke>